# 3.3 Mô hình hóa với dữ liệu có thể thay đổi

Chương 2 đã giới thiệu dữ liệu tổng hợp như một phương tiện để xây dựng các đối tượng tính toán có nhiều thành phần, nhằm mô phỏng những đối tượng thực sự có nhiều khía cạnh. Trong chương đó, ta đã tiếp cận _trừu tượng hóa dữ liệu_, theo đó các cấu trúc dữ liệu được đặc tả thông qua **bộ tạo** — tạo ra các đối tượng dữ liệu — và **bộ chọn** — truy cập các thành phần của chúng. Thế nhưng giờ đây ta nhận ra rằng chương 2 vẫn còn bỏ ngỏ một khía cạnh khác của dữ liệu.

Khi ta muốn mô phỏng các hệ thống gồm những đối tượng có _trạng thái thay đổi_, ta sẽ cần không chỉ xây dựng và chọn lọc dữ liệu tổng hợp, mà còn phải _sửa đổi_ chúng. Để mô hình hóa các đối tượng tổng hợp có trạng thái thay đổi, ta sẽ thiết kế các phần trừu tượng dữ liệu bao gồm, ngoài **bộ chọn** và **bộ tạo**, còn có các phép tính gọi là _bộ biến đổi_ — dùng để sửa đổi đối tượng dữ liệu.[^1] Ví dụ, mô hình hóa hệ thống ngân hàng đòi hỏi ta phải thay đổi số dư tài khoản.

Dữ liệu tổng hợp có thể thay đổi được có thể được tạo ra nhờ các _cặp_ như cách đã trình bày, nên ta sẽ bắt đầu bằng việc xem xét cách mở rộng biểu diễn cặp cơ bản để hỗ trợ cả thao tác biến đổi. Điều này sẽ giúp ta xây dựng nên những cấu trúc dữ liệu vượt ra ngoài các dãy và cây đơn thuần. Ta cũng sẽ khám phá những vấn đề nảy sinh khi đưa phép biến đổi vào trong các chương trình.

---

[^1]: Chương 3 sẽ cho ta thấy rằng việc giới thiệu phép biến đổi buộc ta phải từ bỏ khả năng sử dụng phép thay thế đơn giản làm mô hình tính toán, và chuyển sang _mô hình môi trường_ phức tạp hơn của chương 3.2.

---

[`data abstraction`](GLOSSARY.md#data-abstraction) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`pair`](GLOSSARY.md#pair) [`compound data`](GLOSSARY.md#compound-data)
