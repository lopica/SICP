## 2.2 Dữ liệu phân cấp và tính đóng

Như ta đã thấy, _cặp_ (pair) là loại "keo" nguyên thủy mà ta có thể dùng để xây dựng các đối tượng **dữ liệu** tổng hợp. Hình 2.2 cho thấy một cách biểu diễn chuẩn của một cặp — trong trường hợp này là cặp được tạo bởi `(cons 1 2)`. Trong cách biểu diễn này, được gọi là _ký hiệu hộp và con trỏ_ (box-and-pointer notation), mỗi đối tượng được thể hiện như một mũi tên trỏ vào một hộp. Hộp chứa một đối tượng nguyên thủy sẽ chứa biểu diễn của đối tượng đó — ví dụ, hộp của một số sẽ chứa chữ số. Hộp của một cặp thực ra là một hộp đôi: nửa trái chứa (con trỏ đến) `car` của cặp, còn nửa phải chứa `cdr`.

![Ký hiệu hộp và con trỏ cho cặp (cons 1 2)](./figures/figure20.png)

Ta đã thấy rằng `cons` không chỉ kết hợp được các số mà còn kết hợp được các cặp với nhau (điều này bạn đã tận dụng — hoặc lẽ ra phải tận dụng — khi làm bài tập 2.2 và 2.3). Vì vậy, _cặp_ cho ta một khối xây dựng phổ quát để tạo ra mọi loại cấu trúc **dữ liệu**.

Khả năng tạo ra các cặp mà các phần tử của nó cũng là cặp chính là bản chất của tầm quan trọng của cấu trúc danh sách như một công cụ biểu diễn. Ta gọi khả năng này là _tính đóng_ (closure property) của `cons`.[^1] Nói chung, một phép toán kết hợp các đối tượng **dữ liệu** thỏa mãn _tính đóng_ nếu kết quả của việc kết hợp các thứ bằng phép toán đó có thể tự thân được kết hợp tiếp bằng cùng phép toán đó. _Tính đóng_ là chìa khóa cho sức mạnh của mọi phương tiện kết hợp, vì nó cho phép ta tạo ra _cấu trúc phân cấp_ — những cấu trúc được tạo nên từ các bộ phận, mà bản thân các bộ phận đó lại được tạo nên từ các bộ phận nhỏ hơn, và cứ thế tiếp tục.

Ngay từ đầu chương 1, ta đã liên tục dùng tính đóng khi làm việc với **quy trình**, vì hầu hết mọi chương trình trừ những chương trình đơn giản nhất đều dựa vào việc các thành phần của một biểu thức kết hợp bản thân cũng có thể là những biểu thức kết hợp. Trong phần này, ta sẽ xem xét những hệ quả của tính đóng đối với **dữ liệu** tổng hợp. Ta sẽ tìm hiểu các kỹ thuật chuẩn để dùng các cặp biểu diễn các dãy và cây, và sẽ thấy một dạng ngôn ngữ đồ họa biểu diễn tính đóng một cách sinh động.[^2]

[^1]: Khái niệm "tính đóng" ở đây xuất phát từ đại số trừu tượng, nơi một tập hợp các phần tử được gọi là _đóng_ dưới một phép toán nếu áp dụng phép toán đó lên các phần tử trong tập hợp luôn cho ra kết quả cũng thuộc tập hợp đó. Cộng đồng Lisp cũng dùng từ "closure" theo một nghĩa hoàn toàn khác: để chỉ một kỹ thuật biểu diễn cho phép một **quy trình** truy cập các **biến** tự do trong phần thân định nghĩa của nó. Để tránh nhầm lẫn, trong cuốn sách này ta sẽ không dùng từ "closure" theo nghĩa thứ hai.

[^2]: Ý tưởng về ngôn ngữ đồ họa được trình bày trong mục 2.2.4, dựa trên Henderson (1982).

---

[`pair`](GLOSSARY.md#pair) [`closure`](GLOSSARY.md#closure) [`data`](GLOSSARY.md#data) [`compound data`](GLOSSARY.md#compound-data) [`list-structured data`](GLOSSARY.md#list-structured-data) [`procedure`](GLOSSARY.md#procedure)
