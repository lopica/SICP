# 1.1.8 Quy trình như những hộp đen trừu tượng

`sqrt` là ví dụ đầu tiên của chúng ta về một **tiến trình** được định nghĩa bởi một tập các **quy trình** cùng định nghĩa lẫn nhau. Hãy để ý rằng định nghĩa của `sqrt-iter` mang tính **đệ quy**; nghĩa là quy trình được định nghĩa thông qua chính nó. Ý tưởng có thể định nghĩa một quy trình dựa trên chính nó nghe có vẻ khó chịu: thoạt nhìn ta không rõ một định nghĩa "vòng vo" như vậy có ý nghĩa gì, lại càng khó tin nó mô tả được một tiến trình rõ ràng để máy tính thực hiện. Vấn đề này sẽ được bàn kỹ hơn ở mục 1.2. Nhưng trước hết, hãy cùng xem xét vài điểm quan trọng khác mà ví dụ `sqrt` minh họa.

Hãy quan sát rằng bài toán tính căn bậc hai tách ra một cách tự nhiên thành nhiều bài toán con: làm sao biết một phỏng đoán đã đủ tốt hay chưa, làm sao cải thiện một phỏng đoán, và cứ thế. Mỗi nhiệm vụ đó được hoàn thành bởi một quy trình riêng. Toàn bộ chương trình `sqrt` có thể được nhìn nhận như một _cụm quy trình_ (xem Hình 1.2) phản chiếu cách bài toán được phân rã thành các bài toán con.

```
          sqrt
            |
        sqrt-iter
         /       \
   good-enough?   improve
    /     \          \
 square   abs      average
```

Tầm quan trọng của chiến lược phân rã này không nằm ở chỗ đơn thuần chia chương trình thành các phần. Suy cho cùng, ta có thể lấy bất kỳ chương trình lớn nào rồi chia nó ra — mười dòng đầu, mười dòng tiếp theo, mười dòng tiếp nữa, và cứ thế. Điều cốt yếu hơn là mỗi quy trình hoàn thành một nhiệm vụ nhận diện được, để rồi có thể dùng nó như một **mô đun** khi định nghĩa các quy trình khác. Ví dụ, khi định nghĩa quy trình `good-enough?` dựa trên `square`, ta có thể coi quy trình `square` như một "hộp đen". Lúc đó ta không bận tâm quy trình ấy tính ra kết quả bằng cách nào, mà chỉ quan tâm đến việc nó tính ra bình phương. Các chi tiết về cách tính bình phương có thể được gác lại để xét sau. Thật vậy, đối với quy trình `good-enough?`, `square` không hẳn là một quy trình mà đúng hơn là một sự trừu tượng hóa của một quy trình, cái mà ta gọi là _sự trừu tượng quy trình_. Ở mức trừu tượng này, mọi quy trình tính ra bình phương đều tốt như nhau.

Như vậy, nếu chỉ xét tới giá trị mà chúng trả về, thì hai quy trình tính bình phương dưới đây phải được coi là không thể phân biệt. Mỗi cái nhận một **đối số** là số và cho ra giá trị là bình phương của số đó.[^1]

```lisp
(define (square x) (* x x))

(define (square x)
  (exp (double (log x))))

(define (double x) (+ x x))
```

Vậy một định nghĩa quy trình nên có khả năng giấu đi chi tiết. Người dùng một quy trình có thể không tự viết ra nó, mà nhận nó từ một lập trình viên khác như một hộp đen. Người dùng không cần biết quy trình được hiện thực ra sao thì mới dùng được nó.

## Tên cục bộ

Một chi tiết trong cách hiện thực một quy trình mà người dùng không cần bận tâm chính là lựa chọn của người viết về **tên** cho các **tham số hình thức** của quy trình. Do đó, hai quy trình sau phải là không thể phân biệt:

```lisp
(define (square x) (* x x))
(define (square y) (* y y))
```

Nguyên lý này — rằng ý nghĩa của một quy trình phải độc lập với tên các **tham số** mà tác giả dùng — thoạt nhìn có vẻ hiển nhiên, nhưng hệ quả của nó lại sâu sắc. Hệ quả đơn giản nhất là tên các tham số của một quy trình phải mang tính **cục bộ** trong phần thân của quy trình đó. Ví dụ, ta đã dùng `square` trong định nghĩa của `good-enough?` ở chương trình căn bậc hai:

```lisp
(define (good-enough? guess x)
  (< (abs (- (square guess) x)) 0.001))
```

Ý đồ của tác giả `good-enough?` là kiểm tra xem bình phương của đối số thứ nhất có nằm trong một sai số cho trước so với đối số thứ hai hay không. Ta thấy tác giả dùng tên `guess` để chỉ đối số thứ nhất và `x` để chỉ đối số thứ hai. Đối số truyền cho `square` chính là `guess`. Nếu tác giả của `square` dùng `x` (như ở trên) để chỉ đối số của nó, thì ta thấy `x` trong `good-enough?` phải là một `x` khác với `x` trong `square`. Việc chạy quy trình `square` không được phép làm thay đổi giá trị `x` mà `good-enough?` đang dùng, bởi `good-enough?` có thể vẫn cần đến giá trị `x` đó sau khi `square` đã tính xong.

Nếu các tham số không cục bộ trong phần thân của từng quy trình tương ứng, thì tham số `x` trong `square` có thể bị lẫn với tham số `x` trong `good-enough?`, và hành vi của `good-enough?` sẽ phụ thuộc vào việc ta dùng phiên bản `square` nào. Khi đó `square` sẽ không còn là cái hộp đen mà ta mong muốn.

Một tham số hình thức của một quy trình có một vai trò rất đặc biệt trong định nghĩa quy trình: nó mang tên gì không quan trọng. Một cái tên như vậy được gọi là _biến đóng_, và ta nói rằng định nghĩa quy trình _ràng buộc_ các tham số hình thức của nó. Ý nghĩa của một định nghĩa quy trình không thay đổi nếu một _biến đóng_ được đổi tên một cách nhất quán xuyên suốt định nghĩa.[^2] Còn nếu một biến không bị ràng buộc, ta nói nó _tự do_. Tập hợp các biểu thức mà trong đó một sự ràng buộc định nghĩa một cái tên được gọi là **phạm vi** của tên đó. Trong một định nghĩa quy trình, các _biến đóng_ được khai báo dưới dạng các tham số hình thức của quy trình có phạm vi chính là phần thân của quy trình.

Trong định nghĩa `good-enough?` ở trên, `guess` và `x` là _biến đóng_, còn `<`, `-`, `abs` và `square` là _biến tự do_. Ý nghĩa của `good-enough?` không phụ thuộc vào những cái tên ta chọn cho `guess` và `x`, miễn là chúng khác nhau và khác với `<`, `-`, `abs` và `square`. (Nếu ta đổi tên `guess` thành `abs`, ta sẽ tạo ra một lỗi do "bắt giữ" mất biến `abs`: nó sẽ chuyển từ _tự do_ thành _đóng_.) Tuy vậy, ý nghĩa của `good-enough?` lại _không_ độc lập với tên các **biến tự do** của nó. Nó chắc chắn phụ thuộc vào một thực tế (nằm ngoài định nghĩa này) rằng **ký hiệu** `abs` đặt tên cho một quy trình tính giá trị tuyệt đối của một số. `good-enough?` sẽ tính ra một hàm khác nếu ta thay `abs` bằng `cos` trong định nghĩa của nó.

## Các định nghĩa nội bộ và cấu trúc khối

Cho đến giờ ta mới có một kiểu cô lập tên: các tham số hình thức của một quy trình là cục bộ trong phần thân của quy trình đó. Chương trình căn bậc hai cho thấy thêm một cách nữa mà ta muốn kiểm soát việc dùng tên. Chương trình hiện tại gồm các quy trình tách rời nhau:

```lisp
(define (sqrt x)
  (sqrt-iter 1.0 x))

(define (sqrt-iter guess x)
  (if (good-enough? guess x)
      guess
      (sqrt-iter (improve guess x) x)))

(define (good-enough? guess x)
  (< (abs (- (square guess) x)) 0.001))

(define (improve guess x)
  (average guess (/ x guess)))
```

Vấn đề với chương trình này là: đối với người dùng `sqrt`, quy trình duy nhất đáng kể là `sqrt`. Các quy trình còn lại (`sqrt-iter`, `good-enough?` và `improve`) chỉ làm rối trí họ. Họ cũng không thể định nghĩa một quy trình nào khác mang tên `good-enough?` như một phần của một chương trình khác chạy chung với chương trình căn bậc hai, vì `sqrt` đang cần đến nó. Vấn đề này đặc biệt nghiêm trọng khi nhiều lập trình viên độc lập cùng xây dựng các hệ thống lớn. Chẳng hạn, khi xây một thư viện lớn các quy trình tính toán số học, nhiều hàm số được tính bằng phép xấp xỉ liên tiếp, nên rất có thể chúng đều có các quy trình phụ trợ mang tên `good-enough?` và `improve`. Ta muốn _cục bộ hóa_ các quy trình con này, giấu chúng vào bên trong `sqrt`, để `sqrt` có thể chung sống với các phép xấp xỉ liên tiếp khác, mỗi phép có một quy trình `good-enough?` riêng. Để làm được điều đó, ta cho phép một quy trình chứa các **định nghĩa nội bộ** chỉ có hiệu lực cục bộ trong quy trình ấy. Ví dụ, với bài toán căn bậc hai ta có thể viết:

```lisp
(define (sqrt x)
  (define (good-enough? guess x)
    (< (abs (- (square guess) x)) 0.001))
  (define (improve guess x)
    (average guess (/ x guess)))
  (define (sqrt-iter guess x)
    (if (good-enough? guess x)
        guess
        (sqrt-iter (improve guess x) x)))
  (sqrt-iter 1.0 x))
```

Kiểu lồng các định nghĩa như vậy, gọi là _cấu trúc khối_, về cơ bản chính là lời giải đúng cho bài toán đóng gói tên ở mức đơn giản nhất. Nhưng ở đây còn ẩn chứa một ý tưởng hay hơn. Ngoài việc đưa định nghĩa của các quy trình phụ trợ vào bên trong, ta còn có thể đơn giản hóa chúng. Vì `x` đã bị ràng buộc trong định nghĩa của `sqrt`, nên các quy trình `good-enough?`, `improve` và `sqrt-iter` — vốn được định nghĩa nội bộ bên trong `sqrt` — đều nằm trong phạm vi của `x`. Do đó không cần truyền `x` một cách tường minh vào từng quy trình này nữa. Thay vào đó, ta để `x` là một _biến tự do_ trong các định nghĩa nội bộ, như dưới đây. Khi đó `x` lấy giá trị từ đối số được truyền vào khi gọi quy trình bao ngoài là `sqrt`. Quy ước này được gọi là **phạm vi ngữ cảnh**.[^3]

```lisp
(define (sqrt x)
  (define (good-enough? guess)
    (< (abs (- (square guess) x)) 0.001))
  (define (improve guess)
    (average guess (/ x guess)))
  (define (sqrt-iter guess)
    (if (good-enough? guess)
        guess
        (sqrt-iter (improve guess))))
  (sqrt-iter 1.0))
```

Ta sẽ dùng _cấu trúc khối_ một cách rộng rãi để giúp chia các chương trình lớn thành những phần dễ xử lý hơn.[^4] Ý tưởng về _cấu trúc khối_ bắt nguồn từ ngôn ngữ lập trình Algol 60. Nó có mặt trong hầu hết các ngôn ngữ lập trình tiên tiến và là một công cụ quan trọng giúp tổ chức việc xây dựng các chương trình lớn.

[^1]: Thậm chí cũng không rõ trong hai quy trình này cái nào hiện thực hiệu quả hơn. Điều đó phụ thuộc vào phần cứng sẵn có. Có những cỗ máy mà trên đó cách hiện thực "hiển nhiên" lại là cách kém hiệu quả hơn. Hãy thử hình dung một cỗ máy có sẵn các bảng logarit và đối logarit (antilogarithm) đồ sộ được lưu trữ một cách rất hiệu quả.

[^2]: Khái niệm đổi tên một cách nhất quán thực ra rất tinh tế và khó định nghĩa một cách hình thức. Nhiều nhà logic học lừng danh đã từng phạm phải những sai lầm đáng xấu hổ ở chỗ này.

[^3]: **Phạm vi ngữ cảnh** quy định rằng các _biến tự do_ trong một quy trình được hiểu là tham chiếu đến các ràng buộc do những định nghĩa quy trình bao ngoài tạo ra; nghĩa là chúng được tra cứu trong **môi trường** nơi quy trình được định nghĩa. Ta sẽ thấy điều này hoạt động chi tiết ra sao ở chương 3, khi nghiên cứu về môi trường và hành vi cụ thể của trình phiên dịch.

[^4]: Các định nghĩa lồng bên trong phải nằm ở đầu phần thân quy trình. Ban quản lý không chịu trách nhiệm cho hậu quả của việc chạy những chương trình đan xen lẫn lộn giữa định nghĩa và sử dụng.

---

[`procedure`](GLOSSARY.md#procedure) [`argument`](GLOSSARY.md#argument) [`name`](GLOSSARY.md#name) [`formal parameter`](GLOSSARY.md#formal-parameter) [`parameter`](GLOSSARY.md#parameter) [`bound variable`](GLOSSARY.md#bound-variable) [`scope`](GLOSSARY.md#scope) [`free variable`](GLOSSARY.md#free-variable) [`internal definitions`](GLOSSARY.md#internal-definitions) [`block structure`](GLOSSARY.md#block-structure) [`lexical scoping`](GLOSSARY.md#lexical-scoping) [`environment`](GLOSSARY.md#environment)
