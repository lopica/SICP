# 3.3.2 Biểu diễn hàng đợi

_Hàng đợi_ là một dãy các phần tử trong đó phần tử được chèn vào ở một đầu (gọi là _đuôi_ của hàng đợi) và bị xóa ở đầu kia (gọi là _đầu_ của hàng đợi). Hình 3.18 minh họa một hàng đợi ban đầu rỗng, sau đó lần lượt chèn vào các phần tử `a` và `b`, rồi xóa `a`, tiếp tục chèn `c` và `d`, và cuối cùng xóa `b`. Vì phần tử nào vào trước thì ra trước, hàng đợi còn được gọi là _bộ đệm FIFO_ (first in, first out — vào trước ra trước).

![Hình 3.18: Các phép toán trên hàng đợi.](./figures/figure38.png)

Nhìn từ góc độ **trừu tượng hóa dữ liệu**, một hàng đợi được xác định qua các phép toán sau:

- **Bộ tạo:** `(make-queue)` — trả về một hàng đợi rỗng.
- **Bộ chọn:** `(empty-queue? <queue>)` — kiểm tra xem hàng đợi có rỗng không; `(front-queue <queue>)` — trả về phần tử ở đầu hàng đợi mà không sửa đổi hàng đợi. Nếu hàng đợi rỗng, báo lỗi.
- **Bộ biến đổi:** `(insert-queue! <queue> <item>)` — chèn phần tử vào đuôi hàng đợi và trả về hàng đợi đã được sửa đổi; `(delete-queue! <queue>)` — xóa phần tử ở đầu hàng đợi và trả về hàng đợi đã sửa đổi. Nếu hàng đợi rỗng khi gọi `delete-queue!`, báo lỗi.

## Biểu diễn hàng đợi bằng danh sách thông thường

Ta hoàn toàn có thể biểu diễn hàng đợi bằng một danh sách thông thường: đầu hàng đợi là `car` của danh sách, chèn một phần tử mới nghĩa là nối nó vào cuối danh sách, còn xóa phần tử ở đầu chỉ cần lấy `cdr` của danh sách. Tuy nhiên, cách biểu diễn này kém hiệu quả, vì để chèn một phần tử ta phải duyệt qua toàn bộ danh sách cho đến khi đến cuối — mất $\Theta(n)$ bước với hàng đợi có $n$ phần tử.

Điểm yếu của biểu diễn danh sách nằm ở chỗ ta phải duyệt để tìm đến cuối danh sách. Nguyên nhân là cách biểu diễn danh sách chuẩn — chuỗi các _cặp_ nối tiếp nhau — chỉ cho ta con trỏ tới đầu danh sách, chứ không có con trỏ sẵn tới cuối. Cách khắc phục đơn giản là biểu diễn hàng đợi như một danh sách kèm thêm một con trỏ bổ sung trỏ tới _cặp cuối cùng_ trong danh sách. Như vậy, khi chèn phần tử mới ta chỉ cần tra con trỏ đuôi mà không cần duyệt lại toàn bộ danh sách.

## Biểu diễn hai con trỏ

_Hàng đợi_ được biểu diễn là một cặp gồm hai con trỏ: `front-ptr` trỏ tới cặp đầu tiên của danh sách, và `rear-ptr` trỏ tới cặp cuối cùng. Hình 3.19 minh họa cấu trúc này.

![Hình 3.19: Cài đặt hàng đợi dưới dạng danh sách có con trỏ đầu và đuôi.](./figures/figure39.png)

Để truy cập và sửa đổi hai con trỏ này, ta định nghĩa các **quy trình** trợ giúp:

```lisp
(define (front-ptr queue) (car queue))
(define (rear-ptr queue) (cdr queue))
(define (set-front-ptr! queue item) (set-car! queue item))
(define (set-rear-ptr! queue item) (set-cdr! queue item))
```

Một hàng đợi rỗng khi `front-ptr` của nó là danh sách rỗng:

```lisp
(define (empty-queue? queue) (null? (front-ptr queue)))
```

**Bộ tạo** `make-queue` khởi tạo một cặp mà cả `car` lẫn `cdr` đều là danh sách rỗng:

```lisp
(define (make-queue) (cons '() '()))
```

**Bộ chọn** `front-queue` trả về phần tử ở đầu hàng đợi:

```lisp
(define (front-queue queue)
  (if (empty-queue? queue)
      (error "FRONT called with an empty queue" queue)
      (car (front-ptr queue))))
```

## Chèn và xóa phần tử

Để chèn một phần tử mới vào hàng đợi, ta tạo một cặp mới chứa phần tử đó. Nếu hàng đợi ban đầu rỗng, cả `front-ptr` lẫn `rear-ptr` đều được đặt trỏ tới cặp mới này. Còn nếu hàng đợi đã có phần tử, ta nối cặp mới vào sau cặp cuối cùng (bằng cách sửa `cdr` của cặp cuối) rồi cập nhật `rear-ptr` trỏ tới cặp mới. Hình 3.20 minh họa kết quả sau khi gọi `(insert-queue! q 'd)`.

![Hình 3.20: Kết quả của `(insert-queue! q 'd)` trên hàng đợi ở Hình 3.19.](./figures/figure40.png)

```lisp
(define (insert-queue! queue item)
  (let ((new-pair (cons item '())))
    (cond ((empty-queue? queue)
           (set-front-ptr! queue new-pair)
           (set-rear-ptr! queue new-pair)
           queue)
          (else (set-cdr! (rear-ptr queue) new-pair)
                (set-rear-ptr! queue new-pair)
                queue))))
```

Để xóa phần tử ở đầu hàng đợi, ta chỉ cần chuyển `front-ptr` sang cặp tiếp theo — tức là `(cdr (front-ptr queue))`.[^1] Hình 3.21 minh họa kết quả sau khi gọi `(delete-queue! q)` trên hàng đợi ở Hình 3.20.

![Hình 3.21: Kết quả của `(delete-queue! q)` trên hàng đợi ở Hình 3.20.](./figures/figure41.png)

```lisp
(define (delete-queue! queue)
  (cond ((empty-queue? queue)
         (error "DELETE! called with an empty queue" queue))
        (else (set-front-ptr! queue (cdr (front-ptr queue)))
              queue)))
```

---

[^1]: Giá trị trả về của `delete-queue!` có thể được bỏ qua, vì `delete-queue!` thường chỉ được gọi vì tác dụng phụ của nó.

---

[`queue`](GLOSSARY.md#queue) [`FIFO buffer`](GLOSSARY.md#FIFO) [`data abstraction`](GLOSSARY.md#data-abstraction) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`mutator`](GLOSSARY.md#mutator) [`pair`](GLOSSARY.md#pair) [`list`](GLOSSARY.md#list) [`procedure`](GLOSSARY.md#procedure)
