# 1.1.2. Đặt tên và môi trường
Một khía cạnh quan trọng của một ngôn ngữ lập trình là các cách nó hỗ trợ để 
đặt tên cho object tính toán. Chúng ta nói rằng tên phân biệt các biến, còn biến
có giá trị là object.
Với Schema, chúng ta đặt tên bằng cách dùng từ khóa `define`. Gõ:
```bash
(define size 2)
```
Nó sẽ trình phiên dịch liên hệ giá trị 2 với tên `size`. Khi đã có liên hệ này,
ta có thể lấy giá trị 2 bằng tên:
```bash
size
2

(* 5 size)
10
```

Đây là một số ví dụ khác của việc dùng `define`:
```bash
(define pi 3.14159)
(define radius 10)

(* pi (* radius radius))
314.159

(define circumference (* 2 pi radius))

circumference
62.8318
```

`define` là cách đơn giản nhất để tạo ra trừu tượng trong ngôn ngữ của chúng ta,
chúng ta có thể dùng nó để liên kết đến giá trị của các phép tính tổng hợp, như
là `circumference` ở trên. Nói chung, đối tượng tính toán có thể có cấu trúc 
phức tạp, và sẽ rất là bất tiện khi mà phải nhớ và viết lại chúng mỗi khi chúng
ta muốn dùng. Quả thực, các chương trình phức tạo được tạo ra bằng cách tạo ra, 
từng bước một, các đối tượng tính toán mà có độ phức tạp tăng dần. Trình phiên 
dịch khiếncho việc tạo cái chương trình kiểu step-by-step này thuận tiện vì mối
liên kết giữa tên với object có thể được tạo ra tăng dần và liên tục tương tác 
với nhau. Tính năng này hỗ trợ tốt cho việc phát triển dần chương trình và kiểm
thử dần chương trình.
Cần phải nói rõ ở đây là khả năng liên kết giá trị với ký hiệu và sau đó lấy lại
được giá trị đồng nghĩa với trình phiên dịch phải giữ một kiểu bộ nhớ nào đó mà
theo dõi các cặp tên - objects. Cái bộ nhớ đó được gọi là môi trường (nói đúng 
hơn thì là môi trường toàn cục, sau này sẽ còn nhiều loại môi trường nữa) 