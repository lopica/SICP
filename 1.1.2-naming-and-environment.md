# 1.1.2. Đặt tên và môi trường

Một khía cạnh quan trọng của một ngôn ngữ lập trình là các cách nó hỗ trợ để **đặt tên** cho đối tượng tính toán. Chúng ta nói rằng **tên** phân biệt các **biến**, còn **biến** là cái có giá trị là đối tượng đó. Với Schema, chúng ta đặt **tên** bằng cách dùng từ khóa `define`. Gõ:

```bash
(define size 2)
```

Nó sẽ báo trình phiên dịch liên hệ giá trị `2` với **tên** `size`.[^1] Khi đã có liên hệ này, ta có thể lấy giá trị `2` bằng **tên**:

```bash
size
2

(* 5 size)
10
```

Đây là một số ví dụ khác của việc dùng `define`:

```bash
(define pi 3.14159)
(define radius 10)

(* pi (* radius radius))
314.159

(define circumference (* 2 pi radius))

circumference
62.8318
```

`define` là cách đơn giản nhất để tạo ra sự trừu tượng trong ngôn ngữ của chúng ta, chúng ta có thể dùng nó để _liên kết_ đến giá trị của các _phép tính tổng hợp_, như là `circumference` ở trên. Nói chung, đối tượng tính toán có thể có cấu trúc phức tạp, và sẽ rất là bất tiện khi mà phải nhớ và viết lại chúng mỗi khi chúng ta muốn dùng. Quả thực, các **chương trình** phức tạo được tạo ra bằng cách tạo ra, từng bước một, các đối tượng tính toán mà có độ phức tạp tăng dần. **Trình phiên dịch** khiến cho việc tạo cái **chương trình** kiểu từng bước một này thuận tiện vì **mối liên kết** giữa **tên** với đối tượng đó có thể được thêm dần và liên tục thay đổi. Tính năng này hỗ trợ tốt cho việc phát triển và kiểm thử **chương trình**. Cần phải nói rõ ở đây là khả năng _liên kết_ giá trị với **ký hiệu** và sau đó lấy lại được giá trị đồng nghĩa với trình phiên dịch phải giữ một kiểu bộ nhớ nào đó mà theo dõi các cặp **tên** - đối tượng. Cái bộ nhớ đó được gọi là **môi trường** (nói đúng hơn thì là **môi trường toàn cục**, sau này sẽ còn nhiều loại môi trường nữa).[^2]

[^1]: Trong quyển sách này, chúng ta sẽ không hiển thị phản hồi của **trình phiên dịch** khi thực thi các định nghĩa, vì điều này phụ thuộc nhiều vào cách triển khai cụ thể.

[^2]: Chương 3 sẽ cho thấy khái niệm **môi trường** này là rất quan trọng, cả trong việc hiểu cách **trình phiên dịch** hoạt động lẫn trong việc hiện thực các **trình phiên dịch**.

---

`name` `variable` `environment` `interpreter` `program` `symbol` `global environment`
