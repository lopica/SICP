## 2.5 Hệ thống với các phép toán tổng quát

Trong mục trước, ta đã biết cách thiết kế hệ thống cho phép cùng một dữ liệu có nhiều cách biểu diễn khác nhau. Chìa khóa là dùng các quy trình giao diện tổng quát kết hợp với thẻ kiểu để nối code với từng cách biểu diễn. Giờ ta sẽ đẩy ý tưởng này đi xa hơn: xây dựng một hệ thống số học có thể hoạt động không chỉ tổng quát trên các cách biểu diễn khác nhau của cùng một loại số, mà còn tổng quát qua nhiều _loại_ số khác nhau.

Chúng ta đã có sẵn nhiều "gói" số học riêng lẻ: số học nguyên thủy (`+`, `-`, `*`, `/`), số học hữu tỉ (`add-rat`, `sub-rat`, v.v.) và số học số phức. Mục tiêu ở đây là dùng kỹ thuật **lập trình hướng dữ liệu** để tạo ra một gói số học thống nhất, gom tất cả các gói trên vào chung.

Hệ thống sẽ có nhiều tầng **hàng rào trừu tượng**. Nhìn từ phía người dùng, chỉ có một quy trình `add` duy nhất, hoạt động được với mọi loại số được cung cấp. Phía dưới, giao diện tổng quát đó cho phép các gói số-nguyên, số-hữu-tỉ và số-phức hoạt động cùng nhau. Bên trong mỗi gói còn có thêm một tầng phân nhánh nữa: chẳng hạn gói số phức lại được xây dựng từ hai gói biểu diễn chữ nhật và cực. Toàn bộ kiến trúc của hệ thống này được minh họa trong Hình 2.23.

![Hệ thống số học tổng quát với nhiều tầng trừu tượng](./figures/figure80.png)

---

[`data-directed`](GLOSSARY.md#data-directed) [`abstraction barrier`](GLOSSARY.md#abstraction-barrier) [`procedure`](GLOSSARY.md#procedure) [`data abstraction`](GLOSSARY.md#data-abstraction)
