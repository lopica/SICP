## 2.4 Nhiều cách biểu diễn cho dữ liệu trừu tượng

Cho đến đây ta đã biết cách dùng _trừu tượng hóa dữ liệu_ để tách phần sử dụng một đối tượng dữ liệu ra khỏi các chi tiết cài đặt của nó. Nhưng chương trình của ta vẫn đang giả định rằng **dữ liệu** chỉ có _một_ cách biểu diễn cụ thể ở tầng thấp. Trong thực tế, một đối tượng dữ liệu thường có thể được biểu diễn theo nhiều cách khác nhau, và đôi khi ta muốn tất cả các cách đó cùng tồn tại trong một hệ thống.

Hãy lấy ví dụ về số phức. Tùy theo bài toán cụ thể, đôi khi ta muốn dùng dạng _trực giao_ (biểu diễn bằng phần thực và phần ảo), đôi khi lại muốn dùng _dạng cực_ (biểu diễn bằng mô đun và argument). Phép cộng số phức thực hiện tự nhiên nhất trên dạng trực giao, trong khi phép nhân lại tiện lợi hơn trên dạng cực. Một hệ thống xử lý số phức trong thực tế có thể cần cả hai cách biểu diễn này cùng một lúc.

Bên cạnh vấn đề về nhiều cách biểu diễn, còn có một thực tế khác: các hệ thống lập trình lớn thường được phát triển bởi nhiều người trong thời gian dài, với những yêu cầu thay đổi theo thời gian. Trong một môi trường như vậy, việc yêu cầu mọi người phải thỏa thuận trước về cách biểu diễn dữ liệu là điều không thực tế. Điều ta thực sự muốn là có thể _thêm_ một cách biểu diễn mới vào hệ thống mà không phải thay đổi các module đã có.

Mục này sẽ trình bày cách xây dựng hệ thống có thể làm việc với nhiều cách biểu diễn khác nhau cho cùng một kiểu dữ liệu. Có hai kỹ thuật chủ chốt ở đây. Thứ nhất là dùng _thẻ kiểu_ (type tag) — thông tin tường minh đính kèm theo dữ liệu để nói dữ liệu đó đang được biểu diễn theo cách nào. Thứ hai là _lập trình hướng dữ liệu_ (data-directed programming) — một chiến lược cho phép xây dựng hệ thống theo kiểu cộng dần (additive) mà không cần thiết kế lại các module đã có mỗi khi thêm cách biểu diễn mới.

Ta sẽ minh họa những kỹ thuật này thông qua ví dụ về số phức. Bắt đầu từ hai lập trình viên — Ben Bitdiddle và Alyssa P. Hacker — mỗi người độc lập xây dựng cách biểu diễn số phức của riêng mình. Ben dùng dạng trực giao, Alyssa dùng dạng cực. Sau đó ta sẽ thấy cách tích hợp cả hai cách biểu diễn vào một hệ thống duy nhất, với các phép tính số học hoạt động đúng trên cả hai — mà không cần Ben hay Alyssa phải sửa lại code của nhau.

Kết quả cuối cùng là một hệ thống phân lớp theo chiều ngang lẫn chiều dọc: hàng rào trừu tượng nằm ngang tách các phép tính bậc cao ra khỏi phần cài đặt, còn các hàng rào theo chiều dọc ngăn cách các cách biểu diễn riêng biệt với nhau. Hình bên dưới minh họa cấu trúc này.

![Các hàng rào trừu tượng trong hệ thống số phức](./figures/figure70.png)

> **Lưu ý:** Bạn cần thêm `figures/figure70.png` — ảnh chụp từ hình 2.19 trong sách (Data-abstraction barriers in the complex-number system).

---

[`data abstraction`](GLOSSARY.md#data-abstraction) [`data`](GLOSSARY.md#data) [`data-directed programming`](GLOSSARY.md#data-directed) [`abstraction barrier`](GLOSSARY.md#abstraction-barrier) [`module`](GLOSSARY.md#module) [`type tag`](GLOSSARY.md#type-tag)
