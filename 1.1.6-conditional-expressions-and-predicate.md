# 1.1.6. Biểu thức có điều kiện và mệnh đề đúng sai
Khả năng sử dụng của các quy trình mà chúng ta có thể định nghĩa ở thời điểm hiện tại rất là hạn chế, bởi vì chúng ta không có cách nào để kiểm tra và dùng các phép biến đổi khác theo kết quả của bài kiểm tra. Ví dụ, chúng ta không thể định nghĩa một quy trình mà tính toán giá trị tuyệt đối của một số bằng cách kiểm tra xem số đó có dương, âm hoặc bằng 0 và có các hành vi khác nhau trong các trường hợp khác nhau tùy vào luật: $$
|x| =
\begin{cases} x & \text{if } x > 0 \\ 0 & \text{if } x = 0 \\ -x & \text{if } x < 0 \end{cases} $$

Cái chúng ta làm ở đây gọi là phân tích trường hợp, và có một trường hợp đặc biệt trong Lisp để thể hiện cái này. Nó gọi là `cond` (là viết tắt của từ conditional), và nó được dùng như sau:
```bash
(define (abs x)
  (cond ((> x 0) x)
        ((= x 0) 0)
        ((< x 0) (- x))))
```
Cách viết tổng quát của _biểu thức có điều kiện_ là
```bash
(cond (⟨p₁⟩ ⟨e₁⟩)
      (⟨p₂⟩ ⟨e₂⟩)
      …
      (⟨pₙ⟩ ⟨eₙ⟩))
```
bao gồm ký hiệu `cond` cùng với cặp ngoặc đơn của các biểu thức
```bash
(⟨p⟩ ⟨e⟩)
```
gọi là _nhánh điều kiện_. Biểu thức đầu tiên ở mỗi cặp là _mệnh đề điều kiện_. Nó là một biểu thức có giá trị được phiên dịch thành true hoặc false.[^1]

_Biểu thức có điều kiện_ được thực thi như này. _Mệnh đề điều kiện_ ⟨p1⟩ được thực thi trước. Nếu giá trị của nó là false, thì ⟨p2⟩ sẽ được thực thi tiếp, nếu giá trị ⟨p2⟩ cũng là false, ⟨p3⟩ sẽ được thực thi. **Tiến trình** này tiếp tục cho đến khi có _mệnh đề điều kiện_ có giá trị là true, khi đó trình phiên dịch trả về giá trị của biểu thức hệ quả tương ứng ⟨e⟩ của nhánh như là giá trị của biểu thức điều kiện. Nếu không có ⟨p⟩ nào có giá trị true, giá trị của `cond` sẽ là undefined.

Cụm từ _mệnh đề điều kiện_ được dùng cho quá trình mà trả về true hoặc false, hay cả các biểu thức mà thực thi thì ra true hoặc false. Giá trị tuyệt đối của quy trình `abs` sử dụng mệnh đề điều kiện nguồn `>`, `<` và `=`. Các mệnh đề này lấy 2 số làm đối số và kiểm tra xem số đầu tiên có lớn hơn, bé hơn hoặc bằng số thứ hai, trả về true hoặc false tương ứng.[^2]

Một các khác đề viết quy trình tính giá trị tuyệt đối là:
```bash
(define (abs x)
  (cond ((< x 0) (- x))
        (else x)))
```
cái này có thể được diễn giải là "nếu `x` bé hơn `0` thì trả về `-x`, nếu không thì trả về `x`". `else` là ký hiệu có thể dùng ở mệnh đề điều kiện cuối cùng trong nhánh của của `cond`. Nó giúp `cond` trả về giá trị của ⟨e⟩ tương ứng khi các _nhánh điều kiện_ trước đó đều đã bị vượt qua. Trong thực tế, bất kỳ biểu thức nào mà đều thực thi ra true có thể được dùng như là mệnh đề điều kiện.

Đây là một cách khác nữa đề viết quy trình tính gía trị tuyệt đối:
```bash
(define (abs x)
  (if (< x 0)
      (- x)
      x))
```
Cái này dùng trường hợp đặc biệt `if`, một loại giới hạn của điều kiện mà có thể dùng khi chỉ có chính xác 2 trường hợp trong phân tích trường hợp. Cấu trúc chung của một biểu thức `if` là
```bash
(if ⟨predicate⟩ ⟨consequent⟩ ⟨alternative⟩)
```
Để thực thi biểu thức `if`, trình phiên dịch bắt đầu bằng cách thực thi phần _mệnh đề điều kiện_ của biểu thức. Nếu giá trị của nó là true, trình phiên dịch sẽ thực thi phần hệ quả và trả về giá trị của nó. Nếu không thì nó thực thi ohần thay thế và trả về giá trị.[^3]

Ngoài các _mệnh đề điều kiện_ nguồn như là `<`, `>`, `=`, ta còn có các phép logic tổng hợp, cho phép chúng ta tạo các _mệnh đề điều kiện tổng hợp_. 3 cái được dùng thường xuyên nhất là:

- `(and ⟨e₁⟩ … ⟨eₙ⟩)`
Trình phiên dịch thực thi các biểu thức ⟨e⟩ từng cái một, theo thứ tự từ trái qua phải. Nếu có bất kỳ ⟨e⟩ nào thực thi trả ra false, giá trị của biểu thức `and` sẽ là false, và các ⟨e⟩ còn lại sẽ không được thực thi. Nếu tất cả ⟨e⟩ thực thi ra true, giá trị của biểu thức `and` sẽ là giá trị của cái cuối cùng.

- `(or ⟨e₁⟩ … ⟨eₙ⟩)`
Trình phiên dịch thực thi các biểu thức ⟨e⟩ từng cái một, theo thứ tự từ trái qua phải. Nếu có bất kỳ ⟨e⟩ nào thực thi trả ra true, giá trị đó sẽ là giá trị của `or` và các ⟨e⟩ còn lại sẽ không được thực thi. Nếu toàn bộ ⟨e⟩ thực thi ra false, giá trị của biểu thức `or` sẽ là false.

- `(not ⟨e⟩)`
Giá trị của biểu thức `not` là true nếu thực thi biểu thức ⟨e⟩ là false, và là false nếu ngược lại.

Để ý là `and` và `or` là các _trường hợp đặc biệt_, không phải các quy trình, bởi vì các biểu thức con đều không nhất thiết phải thực thi hết. `not` thì là quy trình bình thường.

Ví dụ về cách chúng được sử dụng, điều kiện mà số `x` trong khoảng `5 < x < 10` sẽ được biểu diễn là
```bash
(and (> x 5) (< x 10))
```
Hay một ví dụ khác, chúng ta có thể định nghĩa một _mệnh đề điều kiện_ để kiểm tra liệu một số có lớn hơn hoặc bằng số khác băng cách

```bash
(define (>= x y) 
  (or (> x y) (= x y)))
```
hoặc cách khác là
```bash
(define (>= x y) 
  (not (< x y)))
```

---

[^1]: "Được phiên dịch thành true hoặc false" có nghĩa như sau: trong Scheme, có hai giá trị đặc biệt được ký hiệu bởi các hằng số `#t` và `#f`. Khi **trình phiên dịch** kiểm tra giá trị của một _mệnh đề điều kiện_, nó coi `#f` là false. Mọi giá trị khác đều được coi là true (do đó, việc cung cấp `#t` là không cần thiết về mặt logic, nhưng tiện hơn). Trong quyển sách này chúng ta sẽ dùng tên `true` và `false`, tương ứng với giá trị `#t` và `#f`.

[^2]: `abs` cũng dùng toán tử "trừ" `-`, khi dùng với một **phần tử** duy nhất — như trong `(- x)` — có nghĩa là phủ định (lấy số đối).

[^3]: Một điểm khác nhỏ giữa `if` và `cond` là phần ⟨e⟩ của mỗi _nhánh điều kiện_ trong `cond` có thể là một chuỗi các **biểu thức**. Khi _mệnh đề điều kiện_ ⟨p⟩ tương ứng có giá trị true, các **biểu thức** ⟨e⟩ được thực thi lần lượt và giá trị của **biểu thức** cuối cùng được trả về làm giá trị của `cond`. Còn trong biểu thức `if`, ⟨consequent⟩ và ⟨alternative⟩ phải là các **biểu thức** đơn lẻ.

---

`conditional expression` `predicate` `compound predicate`