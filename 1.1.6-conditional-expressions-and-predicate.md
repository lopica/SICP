# 1.1.6. Biểu thức có điều kiện và mệnh đề đúng sai
Khả năng sử dụng của các quy trình mà chúng ta có thể định nghĩa ở thời điểm 
hiện tại rất là hạn chế, bởi vì chúng ta không có cách nào để kiểm tra và dùng
các phép biến đổi khác theo kết quả của bài kiểm tra. Ví dụ, chúng ta không thể định nghĩa một quy trình mà tính toán giá trị tuyệt đối của một số bằng cách 
kiểm tra xem số đó có dương, âm hoặc bằng 0 và có các hành vi khác nhau trong 
các trường hợp khác nhau tùy vào luật:
$$
|x| =
\begin{cases}
x & \text{if } x > 0 \\
0 & \text{if } x = 0 \\
-x & \text{if } x < 0
\end{cases}
$$

Cái chúng ta làm ở đây gọi là phân tích trường hợp, và có một trường hợp đặc 
biệt trong Lisp để thể hiện cái này. Nó gọi là `cond` (là viết tắt của từ 
conditional), và nó được dùng như sau:

```bash
(define (abs x)
  (cond ((> x 0) x)
        ((= x 0) 0)
        ((< x 0) (- x))))
```

Cách viết tổng quát của biểu thức có điều kiện là

```bash
(cond (⟨p₁⟩ ⟨e₁⟩)
      (⟨p₂⟩ ⟨e₂⟩)
      …
      (⟨pₙ⟩ ⟨eₙ⟩))
```

bao gồm ký hiệu `cond` cùng với cặp ngoặc đơn của các biểu thức

```bash
(⟨p⟩ ⟨e⟩)
```

gọi là nhánh điều kiện. Biểu thức đầu tiên ở mỗi cặp là mệnh đề điều kiện. Nó là
một biểu thức có giá trị được phiên dịch thành true hoặc false.

Biểu thức có điều kiện được thực thi như này. Mệnh đề điều kiện ⟨p1⟩ được thực
thi trước. Nếu giá trị của nó là false, thì ⟨p2⟩ sẽ được thực thi tiếp, nếu giá
trị ⟨p2⟩ cũng là false, ⟨p3⟩ sẽ được thực thi. Tiến trình này tiếp tực cho đến 
khi có mệnh đề điều kiện có giá trị là true, khi đó trình phiên dịch trả về giá 
trị của biểu thức hệ quả tương ứng ⟨e⟩ của nhánh như là giá trị của biểu thức
điều kiện. Nếu không có ⟨p⟩ nào có giá trị true, giá trị của `cond` sẽ là 
undefined.

Cụm từ mệnh đề điều kiện được dùng cho quá trình mà trả về true hoặc false, hay
cả các biểu thức mà thực thi thì ra true hoặc false. Giá trị tuyệt đối của quy
trình `abs` sử  dụng mệnh đề điều kiện nguồn >, < và =. Các mệnh đề này lấy 2 só
làm đối số và kiểm tra xem số đầu tiên có lớn hơn, bé hơn hoặc bằng số thứ hai, 
trả về true hoặc false tương ứng.

Một các khác đề viết quy trình tính giá trị tuyệt đối là:
```bash
(define (abs x)
  (cond ((< x 0) (- x))
        (else x)))
```

cái này có thể được diễn giải là "nếu x bé hơn 0 thì trả về -x, nếu không thì
trả về x". `else` là ký hiệu có thể dùng ở mệnh đề điều kiện cuối cùng trong nhánh của của `cond`. Nó giúp `cond` trả về giá trị là giá trị của ⟨e⟩ tương ứng
khi các nhánh trước đó đều đã bị vượt qua. Trong thực tế, bất kỳ biểu thức nào
mà đều thực thi ra true có thể được dùng như là mệnh đề điều kiện.

Đây là một cách khác nữa đề viết quy trình tính gía trị tuyệt đối:

```bash
(define (abs x)
  (if (< x 0)
      (- x)
      x))
```

Cái này dùng trường hợp đặc biệt `if`, một loại giới hạn của điều kiện mà có thể
dùng khi chỉ có chính xác 2 trường hợp trong phân tích trường hợp. Cấu trúc 
chung của một biểu thức `if` là 

```bash
(if ⟨predicate⟩ ⟨consequent⟩ ⟨alternative⟩)
```

Để thực thi biểu thức `if`, trình phiên dịch bắt đầu bằng cách thực thi phần 
mệnh đề điều kiện của biểu thức. Nếu giá trị của nó là true, trình phiên dịch sẽ
thực thi phần hệ quả và trả về giá trị của nó. Nếu không thì nó thực thi ohần thay thế và trả về giá trị.

Ngoài các mệnh đề điều kiện nguồn như là <, >, =, ta còn có các phép logic tổng
hợp, cho phpé chúng ta tạo các mệnh đề điều kiện tổng hợp. 3 cái được dùng 
thường xuyên nhất là:

- `(and ⟨e₁⟩ … ⟨eₙ⟩)`
  Trình phiên dịch thực thi các biểu thức ⟨e⟩ từng cái một, theo thứ tự từ trái
  qua phải. Nếu có bất kỳ ⟨e⟩ nào thực thi trả ra false, giá trị của biểu thức
  `and` sẽ là false, và các ⟨e⟩ còn lại sẽ không được thực thi. Nếu tất cả ⟨e⟩
  thực thi ra true, giá trị của biểu thức `and` sẽ là giá trị của cái cuối cùng.

- `(or ⟨e₁⟩ … ⟨eₙ⟩)`
  Trình phiên dịch thực thi các biểu thức ⟨e⟩ từng cái một, theo thứ tự từ trái
  qua phải. Nếu có bất kỳ ⟨e⟩ nào thực thi trả ra true, giá trị đó sẽ là giá trị
  của `or` và các ⟨e⟩ còn lại sẽ không được thực thi. Nếu toàn bộ ⟨e⟩ thực thi
  ra false, giá trị của biểu thức `or` sẽ là false.

- `(not ⟨e⟩)`
  Giá trị của biểu thức `not` là true nếu thực thi biểu thức ⟨e⟩ là false, và 
  là false nếu ngược lại.

Để ý là `and` và `or` là các trường hợp đặc biệt, không phải các quy trình, bởi
vì các biểu thức con đều không nhất thiết phải thực thi hết. `not` thì là quy
trình bình thường.

Ví dụ về cách chúng được sử dụng, điều kiện mà số x trong khoảng 5 < x < 10 sẽ
được biểu diễn là

```bash
(and (> x 5) (< x 10))
```

Hay một ví dụ khác, chúng ta có thể định nghĩa một mệnh đề điều kiện để kiểm tra
liệu một số có lớn hơn hoặc bằng số khác băng cách

```bash
(define (>= x y) 
  (or (> x y) (= x y)))
```

hoặc cách khác là 

```bash
(define (>= x y) 
  (not (< x y)))
```