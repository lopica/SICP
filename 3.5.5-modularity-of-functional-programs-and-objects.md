# 3.5.5 Tính mô đun của chương trình hàm và tính mô đun của đối tượng

Ở mục 3.1.2, ta đã lập luận rằng **phép gán** là một công cụ đắc lực để mô đun hóa chương trình, vì nó cho phép che giấu trạng thái bên trong của từng thành phần. **Dòng chảy** cũng giúp mô đun hóa, nhưng theo một cách khác: không cần đến **phép gán**. Hãy minh họa điều này qua ví dụ về `rand` và **phương pháp Monte Carlo**.

Trong mục đó, ta đã dùng **phép gán** để xây dựng `rand`:

```lisp
(define rand
  (let ((x random-init))
    (lambda ()
      (set! x (rand-update x))
      x)))
```

**Dòng chảy** cho phép ta đạt được hiệu quả tương tự mà không cần **phép gán**. Điểm mấu chốt là coi **dòng chảy** các số ngẫu nhiên như một dãy sinh ra bởi `rand-update` liên tiếp:

```lisp
(define random-numbers
  (cons-stream random-init
               (stream-map rand-update random-numbers)))
```

Từ đó ta xây dựng **dòng chảy** các cặp Cesàro dùng trong thí nghiệm Monte Carlo:

```lisp
(define cesaro-stream
  (map-successive-pairs
   (lambda (r1 r2) (= (gcd r1 r2) 1))
   random-numbers))

(define (map-successive-pairs f s)
  (cons-stream
   (f (stream-car s) (stream-car (stream-cdr s)))
   (map-successive-pairs f (stream-cdr (stream-cdr s)))))
```

`cesaro-stream` là **dòng chảy** các giá trị `true`/`false` — kết quả kiểm tra từng cặp. Tiếp theo, ta xây dựng quy trình `monte-carlo` dưới dạng **dòng chảy** xác suất hội tụ dần:

```lisp
(define (monte-carlo experiment-stream passed failed)
  (define (next passed failed)
    (cons-stream
     (/ passed (+ passed failed))
     (monte-carlo
      (stream-cdr experiment-stream) passed failed)))
  (if (stream-car experiment-stream)
      (next (+ passed 1) failed)
      (next passed (+ failed 1))))

(define pi
  (stream-map (lambda (p) (sqrt (/ 6 p)))
              (monte-carlo cesaro-stream 0 0)))
```

Phiên bản **dòng chảy** này có cùng tính mô đun: `cesaro-stream` và `monte-carlo` là những **quy trình** hoàn toàn tách biệt — `monte-carlo` không biết gì về thí nghiệm cụ thể đang được thực hiện. Hơn nữa, ta không cần **phép gán**: không có biến trạng thái nào bị thay đổi; toàn bộ lịch sử tính toán được mã hóa ngầm trong cấu trúc **dòng chảy**.

## Cái giá của tư duy theo thời gian

Tuy nhiên, **dòng chảy** có những hạn chế của riêng chúng. Khi ta cần mô hình hóa các đối tượng "chia sẻ trạng thái" và tương tác với nhau, tư duy **dòng chảy** trở nên khó xử.

Xét tài khoản ngân hàng đơn giản: theo lối tiếp cận đối tượng, ta có một đối tượng duy nhất với trạng thái mà nhiều giao dịch đồng thời có thể truy cập và thay đổi. Theo lối tiếp cận **dòng chảy**, ta cần trộn (_merge_) nhiều **dòng chảy** yêu cầu vào một **dòng chảy** duy nhất — nhưng việc trộn không xác định thứ tự là không đơn giản.

Ví dụ, nếu Peter và Paul cùng truy cập một tài khoản chung, ta cần trộn **dòng chảy** yêu cầu của Peter với **dòng chảy** yêu cầu của Paul thành một chuỗi duy nhất. Nhưng thứ tự trộn phản ánh thứ tự thực trong thời gian thực — và để mô hình hóa đúng điều đó, ta lại phải đưa ra những cơ chế kiểm soát **tính đồng thời** không khác gì những vấn đề ta đã gặp với **phép gán**.[^1]

## Hai thế giới quan

Vấn đề cơ bản ở đây là ở mức độ triết học. Có hai cách nhìn về thế giới trong lập trình:

- **Nhìn theo trạng thái:** Thế giới gồm các đối tượng riêng biệt, mỗi đối tượng mang _trạng thái_ thay đổi theo thời gian. Khi mô hình hóa thế giới này, ta dùng biến cục bộ và **phép gán**. Ưu điểm: trực quan, dễ đối chiếu với thực tại. Hạn chế: **tính đồng thời** trở nên phức tạp, và ta mất đi _tính trong suốt tham chiếu_ — cùng một biểu thức có thể cho giá trị khác nhau tuỳ thời điểm.

- **Nhìn theo dòng chảy:** Thế giới là tập hợp các quá trình biến đổi theo thời gian; ta mô tả mỗi quá trình bằng một **dòng chảy** không có trạng thái thay đổi. Ưu điểm: không cần **phép gán**, _tính trong suốt tham chiếu_ được bảo toàn, và các hàm hoàn toàn tự nhiên với tư duy toán học. Hạn chế: khi các quá trình chia sẻ và ảnh hưởng lẫn nhau theo cách phụ thuộc thứ tự thời gian, ta lại gặp khó khăn tương tự.

Ở một mức độ nào đó, cả hai quan điểm đều phản ánh cùng một thực tại từ những góc nhìn khác nhau. Theo vật lý lý thuyết, thế giới có thể được mô tả bằng hai ngôn ngữ tương đương: cơ học Hamilton (dựa trên trạng thái tức thời và sự biến đổi theo thời gian) và cơ học Lagrange (dựa trên toàn bộ lịch sử quỹ đạo). Sự dồi dào này cũng là đặc trưng của lập trình.

Điều đáng lo ngại là khi hai quan điểm va chạm — khi một đối tượng có _trạng thái_ chia sẻ với đối tượng khác, hoặc khi **dòng chảy** cần phản ánh thứ tự thực trong thế giới — ta buộc phải đối mặt với bản chất của thời gian trong tính toán.

Chúng ta chưa có cách giải quyết triệt để mâu thuẫn này trong khoa học máy tính. Ngay cả hiện nay, những vấn đề này vẫn còn ở biên giới của sự hiểu biết của chúng ta.[^2]

Bài tập 3.81 và Bài tập 3.82 sau đây tiếp tục khám phá cách áp dụng tư duy **dòng chảy** vào các bài toán đã từng cần đến **phép gán**.

---

[^1]: Vấn đề trộn **dòng chảy** trong môi trường **tính đồng thời** thực chất cũng ngang tầm phức tạp với vấn đề **tuần tự hóa** mà ta đã gặp ở mục 3.4. Đây không phải là vô tình: cả hai đều là cách tiếp cận vấn đề xác định thứ tự thời gian trong các tiến trình tương tác.

[^2]: Những căng thẳng triết học nêu ra ở đây không đặc biệt của khoa học máy tính. Trong vật lý cũng tồn tại mâu thuẫn tương tự khi chọn giữa "quan điểm hạt" (mỗi hạt có vị trí và vận tốc tại một thời điểm) và "quan điểm trường" (mô tả lịch sử toàn bộ quỹ đạo). Cơ học lượng tử đặt ra những khó khăn sâu hơn, khi khái niệm "đối tượng với trạng thái" và "dãy sự kiện" đều không hoàn toàn tương thích với thực tại. Dù sao, ta vẫn tiến về phía trước bằng cách lựa chọn mô hình phù hợp nhất với từng vấn đề cụ thể.

---

[`stream`](GLOSSARY.md#stream) [`assignment`](GLOSSARY.md#assignment) [`module`](GLOSSARY.md#module) [`functional-programming`](GLOSSARY.md#functional-programming) [`concurrency`](GLOSSARY.md#concurrency) [`concurrent`](GLOSSARY.md#concurrent) [`state-variables`](GLOSSARY.md#state-variables) [`referential-transparency`](GLOSSARY.md#referential-transparency) [`serialization`](GLOSSARY.md#serialization) [`monte-carlo-method`](GLOSSARY.md#monte-carlo-method) [`procedure`](GLOSSARY.md#procedure) [`delayed-evaluation`](GLOSSARY.md#delayed-evaluation)
