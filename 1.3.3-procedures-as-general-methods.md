# 1.3.3 Quy trình như là phương pháp tổng quát

Chúng ta đã giới thiệu quy trình tổng hợp (`sum`, `product`, …) như là những sự trừu tượng mô tả các phương pháp kết hợp số học. Trong phần này, ta đi xa hơn một bước: ta sẽ xem một số quy trình như là _phương pháp tổng quát_ — nghĩa là những quy trình không gắn với một hàm cụ thể nào mà thể hiện cả một chiến lược tính toán. Hai ví dụ kinh điển là: tìm nghiệm của phương trình bằng _phương pháp chia đôi_, và tìm điểm bất động của một hàm số.

## Tìm nghiệm của phương trình bằng phương pháp chia đôi

_Phương pháp chia đôi_ (half-interval method) là một kỹ thuật đơn giản để tìm nghiệm của phương trình $f(x) = 0$, trong đó $f$ là một hàm liên tục. Ý tưởng cơ bản: nếu ta có hai điểm $a$ và $b$ sao cho $f(a) < 0 < f(b)$, thì theo định lý giá trị trung gian $f$ phải có ít nhất một nghiệm nằm giữa $a$ và $b$. Ta tìm nghiệm bằng cách tính trung điểm $x = (a + b) / 2$ rồi so sánh dấu của $f(x)$:

- Nếu $f(x) > 0$, nghiệm nằm trong khoảng $[a, x]$.
- Nếu $f(x) < 0$, nghiệm nằm trong khoảng $[x, b]$.
- Nếu $f(x) = 0$, ta đã tìm được nghiệm chính xác.

Mỗi lần lặp, khoảng tìm kiếm giảm đi một nửa. Số bước cần thiết tăng theo $\Theta(\log(L/T))$, trong đó $L$ là độ dài khoảng ban đầu và $T$ là sai số chấp nhận được.[^1]

```lisp
(define (search f neg-point pos-point)
  (let ((midpoint (average neg-point pos-point)))
    (if (close-enough? neg-point pos-point)
        midpoint
        (let ((test-value (f midpoint)))
          (cond ((positive? test-value)
                 (search f neg-point midpoint))
                ((negative? test-value)
                 (search f midpoint pos-point))
                (else midpoint))))))
```

Ta dùng điều kiện "gần đủ" (`close-enough?`) sau:

```lisp
(define (close-enough? x y)
  (< (abs (- x y)) 0.001))
```

Quy trình `search` trên yêu cầu người dùng phải truyền đúng một điểm âm và một điểm dương. Để tiện hơn, ta viết thêm một quy trình bao ngoài tự kiểm tra dấu:

```lisp
(define (half-interval-method f a b)
  (let ((a-value (f a))
        (b-value (f b)))
    (cond ((and (negative? a-value) (positive? b-value))
           (search f a b))
          ((and (negative? b-value) (positive? a-value))
           (search f b a))
          (else
           (error "Values are not of opposite sign" a b)))))
```

Ví dụ, ta có thể dùng phương pháp này để xấp xỉ $\pi$ — đây là nghiệm của $\sin x = 0$ trong khoảng từ $2$ đến $4$:[^2]

```lisp
(half-interval-method sin 2.0 4.0)
3.14111328125
```

Hay tìm nghiệm của phương trình $x^3 - 2x - 3 = 0$ trong khoảng $1$ đến $2$:

```lisp
(half-interval-method
 (lambda (x) (- (* x x x) (* 2 x) 3))
 1.0
 2.0)
1.89306640625
```

## Tìm điểm bất động của hàm số

Số $x$ được gọi là _điểm bất động_ của hàm $f$ nếu $f(x) = x$. Với nhiều hàm $f$, ta có thể tìm điểm bất động bằng cách bắt đầu từ một giá trị ban đầu nào đó rồi liên tục áp dụng $f$:

$$x,\quad f(x),\quad f(f(x)),\quad f(f(f(x))),\quad \ldots$$

cho đến khi giá trị gần như không thay đổi nữa. Dựa trên ý tưởng đó, ta xây dựng quy trình `fixed-point`:

```lisp
(define tolerance 0.00001)

(define (fixed-point f first-guess)
  (define (close-enough? v1 v2)
    (< (abs (- v1 v2)) tolerance))
  (define (try guess)
    (let ((next (f guess)))
      (if (close-enough? guess next)
          next
          (try next))))
  (try first-guess))
```

Chẳng hạn, điểm bất động của hàm cosine được tìm bằng cách bắt đầu từ `1.0`:

```lisp
(fixed-point cos 1.0)
.7390822985224023
```

Hay tìm nghiệm của phương trình $y = \sin y + \cos y$:

```lisp
(fixed-point (lambda (y) (+ (sin y) (cos y))) 1.0)
1.2587315962971173
```

Tiến trình tìm điểm bất động gợi nhắc ta về cách tính căn bậc hai đã làm ở mục 1.1.7. Thật vậy, $\sqrt{x}$ là nghiệm của phương trình $y^2 = x$, tương đương với điểm bất động của ánh xạ $y \mapsto x/y$.[^3] Thử dùng cách này:

```lisp
(define (sqrt x)
  (fixed-point (lambda (y) (/ x y)) 1.0))
```

Tiếc thay, tìm kiếm điểm bất động theo cách này không hội tụ. Thử hình dung: giả sử giá trị thử đầu tiên là $y_1$, thì bước tiếp theo là $y_2 = x/y_1$, bước sau nữa là $y_3 = x/y_2 = y_1$. Kết quả là ta cứ dao động mãi giữa $y_1$ và $y_2$ mà không bao giờ hội tụ.

Cách khắc phục là ngăn chặn sự dao động này bằng cách không cho phép giá trị thử thay đổi quá nhiều. Vì nghiệm thực sự là điểm bất động của $y \mapsto x/y$, nên thay vì nhảy thẳng đến $x/y$, ta có thể dùng trung bình của $y$ và $x/y$:

```lisp
(define (sqrt x)
  (fixed-point (lambda (y) (average y (/ x y))) 1.0))
```

Cách thay đổi như thế này, tức là dùng trung bình giữa giá trị cũ và giá trị mới thay vì dùng thẳng giá trị mới, được gọi là _trung bình hóa_ (_average damping_). Kỹ thuật này thường giúp tăng tốc độ hội tụ khi tìm điểm bất động.[^4]

[^1]: Ta bỏ qua vấn đề thực thi tín hiệu lỗi khi cả hai điểm đều cùng dấu — quy trình `half-interval-method` ở đây chỉ gọi `error` và dừng lại.

[^2]: Sau vài lần lặp đầu tiên của `try`, khoảng tìm kiếm sẽ đủ nhỏ để kết quả làm tròn ra $\pi$.

[^3]: Ký hiệu $y \mapsto f(y)$ nghĩa là "hàm nhận đối số $y$ và trả về $f(y)$" — đây là cách viết toán học thông dụng cho lambda.

[^4]: Thực ra phiên bản `sqrt` ở mục 1.1.7 cũng chính là tìm điểm bất động với trung bình hóa.

---

[exercise 1.35](exercises/1.35.md) [exercise 1.36](exercises/1.36.md) [exercise 1.37](exercises/1.37.md) [exercise 1.38](exercises/1.38.md) [exercise 1.39](exercises/1.39.md) [`procedure`](GLOSSARY.md#procedure) [`process`](GLOSSARY.md#process) [`parameter`](GLOSSARY.md#parameter) [`environment`](GLOSSARY.md#environment) [`variable`](GLOSSARY.md#variable) [`iterative process`](GLOSSARY.md#iterative-process)
