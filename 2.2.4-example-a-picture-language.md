# 2.2.4 Ví dụ: Ngôn ngữ hình ảnh

Phần này trình bày một ngôn ngữ đơn giản để vẽ hình ảnh — và qua đó minh họa một cách rõ ràng sức mạnh của trừu tượng hóa dữ liệu và đóng gói. Ngôn ngữ này được thiết kế để làm dễ dàng việc thực nghiệm với các họa tiết lặp đi lặp lại kiểu như của Peter Henderson.[^1] Mặc dù ngôn ngữ chỉ gồm vài phép toán, nó vẫn đủ phong phú để tạo ra những hình ảnh phức tạp đến bất ngờ.

## Ngôn ngữ hình ảnh

Khi chúng ta mô tả một ngôn ngữ lập trình, nên chú ý đặc biệt đến những phương tiện kết hợp (means of combination) và phương tiện trừu tượng hóa (means of abstraction) mà ngôn ngữ cung cấp. Ở đây ta sẽ làm đúng như vậy với ngôn ngữ hình ảnh.

Ngôn ngữ này chỉ có một loại phần tử duy nhất, gọi là _họa sĩ_ (painter). Một _họa sĩ_ vẽ một hình ảnh đã được dịch chuyển và kéo giãn vào một _khung_ (frame) hình bình hành được chỉ định. Ví dụ, có một _họa sĩ_ nguồn tên là `wave` vẽ một hình dáng sơ giản như hình bên dưới. Hình dáng thực sự vẽ phụ thuộc vào _khung_ — tất cả bốn hình ảnh trong hình đều do cùng một _họa sĩ_ `wave` tạo ra, nhưng trên các _khung_ khác nhau.

![Họa sĩ wave được vẽ trong bốn khung khác nhau](./figures/figure7.png)

_Họa sĩ_ có thể phức tạp hơn `wave`, chẳng hạn `rogers` là một _họa sĩ_ vẽ ảnh của William Barton Rogers, người sáng lập MIT. Bốn hình ảnh trong ví dụ bên dưới đều dùng cùng một _họa sĩ_ nhưng trên các _khung_ khác nhau.

![Họa sĩ rogers được vẽ trong bốn khung](./figures/figure8.png)

Để kết hợp các hình ảnh, ta dùng các phép toán kết hợp _họa sĩ_. Ví dụ, phép toán `beside` nhận hai _họa sĩ_ và tạo ra một _họa sĩ_ mới vẽ hình của _họa sĩ_ đầu tiên ở nửa trái và hình của _họa sĩ_ thứ hai ở nửa phải của _khung_. Tương tự, `below` nhận hai _họa sĩ_ và tạo ra một _họa sĩ_ mới vẽ hình của _họa sĩ_ đầu tiên ở phần dưới và hình của _họa sĩ_ thứ hai ở phần trên của _khung_. Một số phép toán biến đổi đơn lẻ _họa sĩ_: `flip-vert` tạo ra một _họa sĩ_ vẽ hình ảnh lật ngược chiều dọc, và `flip-horiz` tạo ra một _họa sĩ_ vẽ hình ảnh lật ngược chiều ngang.

```lisp
(define wave2 (beside wave (flip-vert wave)))
(define wave4 (below wave2 wave2))
```

![wave2 và wave4](./figures/figure9.png)

Khi xây dựng _họa sĩ_ phức tạp theo cách này, ta đang khai thác thực tế rằng đây là ngôn ngữ đóng (_closed language_) — kết quả của phép kết hợp vẫn là một _họa sĩ_, vì vậy ta có thể tiếp tục kết hợp. Đây chính là _tính đóng_ (closure) đã đề cập khi nói về cặp `cons` ở đầu chương.

Ta có thể xây dựng những mẫu hình phức tạp hơn từ `wave` và `rogers`:

```lisp
(define (flipped-pairs painter)
  (let ((painter2 (beside painter (flip-vert painter))))
    (below painter2 painter2)))
```

Định nghĩa `wave4` ở trên là đặc biệt hóa của mẫu này:

```lisp
(define wave4 (flipped-pairs wave))
```

Ta cũng có thể định nghĩa các phép biến đổi đệ quy:

```lisp
(define (right-split painter n)
  (if (= n 0)
      painter
      (let ((smaller (right-split painter (- n 1))))
        (beside painter (below smaller smaller)))))
```

Ta có thể tạo ra các mẫu hình cân xứng bằng cách chia nhánh sang phải và sang trên:

```lisp
(define (corner-split painter n)
  (if (= n 0)
      painter
      (let ((up (up-split painter (- n 1)))
            (right (right-split painter (- n 1))))
        (let ((top-left (beside up up))
              (bottom-right (below right right))
              (corner (corner-split painter (- n 1))))
          (beside (below painter top-left)
                  (below bottom-right corner))))))
```

Bằng cách đặt bốn bản sao `corner-split` vào cùng nhau, ta có được mẫu hình gọi là `square-limit`:

```lisp
(define (square-limit painter n)
  (let ((quarter (corner-split painter n)))
    (let ((half (beside (flip-horiz quarter) quarter)))
      (below (flip-vert half) half))))
```

![right-split và corner-split của wave và rogers](./figures/figure10.png)

![square-limit của wave và rogers](./figures/figure11.png)

**Bài tập 2.44:** Định nghĩa **quy trình** `up-split` mà `corner-split` sử dụng. Nó tương tự `right-split`, chỉ khác là đổi vai trò của `below` và `beside`.

## Các quy trình bậc cao hơn

Sức mạnh thực sự của ngôn ngữ hình ảnh nằm ở chỗ ta có thể trừu tượng hóa các mẫu hình kết hợp. Có thể nhận thấy rằng `flipped-pairs` và `square-limit` đều dùng một mẫu chung: đặt bốn bản sao vào góc vuông. Ta có thể biểu diễn điều này như một _quy trình bậc cao_:

```lisp
(define (square-of-four tl tr bl br)
  (lambda (painter)
    (let ((top (beside (tl painter) (tr painter)))
          (bottom (beside (bl painter) (br painter))))
      (below bottom top))))
```

Khi đó `flipped-pairs` có thể định nghĩa lại dưới dạng `square-of-four`:[^2]

```lisp
(define (flipped-pairs painter)
  (let ((combine4 (square-of-four identity flip-vert
                                  identity flip-vert)))
    (combine4 painter)))
```

và `square-limit` là:

```lisp
(define (square-limit painter n)
  (let ((combine4 (square-of-four flip-horiz identity
                                  rotate180 flip-vert)))
    (combine4 (corner-split painter n))))
```

**Bài tập 2.45:** `right-split` và `up-split` có thể được biểu diễn như các trường hợp đặc biệt của một phép toán chia tách tổng quát. Định nghĩa một **quy trình** `split` với tính chất rằng việc đánh giá:

```lisp
(define right-split (split beside below))
(define up-split (split below beside))
```

sẽ tạo ra các **quy trình** với cùng hành vi như các **quy trình** đã được định nghĩa trước đó.

## Khung (Frames)

Trước khi có thể hiển thị các họa sĩ trên màn hình, ta cần nói về _khung_. Một _khung_ có thể được mô tả bởi ba vectơ: một vectơ gốc (origin) và hai vectơ cạnh (edge). Vectơ gốc xác định vị trí của góc dưới bên trái của _khung_ so với một điểm gốc tuyệt đối trong mặt phẳng, và các vectơ cạnh biểu diễn các cạnh liền kề với góc đó. Nếu các cạnh vuông góc với nhau thì _khung_ là hình chữ nhật. Nếu không, _khung_ là một hình bình hành tổng quát.

Hình bên dưới cho thấy một _khung_ và vectơ gốc, hai vectơ cạnh gắn với nó. Theo cách ký hiệu trong hình, vectơ từ gốc đến điểm $(x, y)$ trên _khung_ được tính bằng:

$$\text{Gốc} + x \cdot \text{Cạnh}_1 + y \cdot \text{Cạnh}_2$$

![Một khung được mô tả bởi ba vectơ](./figures/figure12.png)

Ta sẽ dùng tọa độ trong _khung_ đơn vị $[0, 1] \times [0, 1]$ để xác định hình ảnh. Với mỗi _khung_, ta gắn với nó một _ánh xạ tọa độ khung_ (frame coordinate map) chuyển đổi tọa độ trong _khung_ đơn vị sang tọa độ trong mặt phẳng. Nếu _khung_ được xác định bởi gốc $\mathbf{O}$, vectơ cạnh $\mathbf{e}_1$, và $\mathbf{e}_2$, thì ánh xạ tọa độ khung chuyển điểm $(x, y)$ thành:

$$\mathbf{O} + x \cdot \mathbf{e}_1 + y \cdot \mathbf{e}_2$$

Ví dụ, điểm $(0, 0)$ được ánh xạ đến gốc _khung_, $(1, 1)$ đến góc đối diện, và $(0.5, 0.5)$ đến tâm.

Ta có thể tạo ánh xạ tọa độ khung của một _khung_ với **quy trình** sau:

```lisp
(define (frame-coord-map frame)
  (lambda (v)
    (add-vect
     (origin-frame frame)
     (add-vect (scale-vect (xcor-vect v)
                           (edge1-frame frame))
               (scale-vect (ycor-vect v)
                           (edge2-frame frame))))))
```

Lưu ý rằng việc áp dụng `frame-coord-map` vào một _khung_ trả về một **quy trình** mà khi nhận vectơ, trả về vectơ. Nếu đối số là tọa độ _khung_ $(0, 0)$ thì trả về gốc của _khung_:

```lisp
((frame-coord-map a-frame) (make-vect 0 0))
; trả về: giống (origin-frame a-frame)
```

**Bài tập 2.46:** Một vectơ $v$ chạy từ gốc tọa độ đến một điểm có thể được biểu diễn như một cặp gồm tọa độ $x$ và tọa độ $y$. Cài đặt một _cách biểu diễn dữ liệu_ cho vectơ dưới dạng các cặp. Để làm điều đó, hãy viết hàm tạo `make-vect` và các **bộ chọn** tương ứng `xcor-vect` và `ycor-vect`. Dựa trên cách biểu diễn này, hãy cài đặt các **quy trình** `add-vect`, `sub-vect` và `scale-vect` thực hiện phép cộng vectơ, trừ vectơ, và nhân vô hướng:

$$\begin{aligned}
(x_1, y_1) + (x_2, y_2) &= (x_1 + x_2,\ y_1 + y_2) \\
(x_1, y_1) - (x_2, y_2) &= (x_1 - x_2,\ y_1 - y_2) \\
s \cdot (x, y) &= (sx,\ sy)
\end{aligned}$$

**Bài tập 2.47:** Dưới đây là hai _bộ tạo_ cho _khung_:

```lisp
(define (make-frame origin edge1 edge2)
  (list origin edge1 edge2))
```

```lisp
(define (make-frame origin edge1 edge2)
  (cons origin (cons edge1 edge2)))
```

Với mỗi cách biểu diễn, hãy cung cấp các **bộ chọn** thích hợp `origin-frame`, `edge1-frame` và `edge2-frame` để sử dụng với cách biểu diễn đó.

## Họa sĩ (Painters)

Một _họa sĩ_ được biểu diễn như một **quy trình** nhận một _khung_ làm đối số và vẽ một hình ảnh cụ thể được kéo giãn và dịch chuyển vừa khít trong _khung_ đó. Với cách biểu diễn này, chi tiết về cách vẽ của _họa sĩ_ là riêng tư — những điều duy nhất ta cần biết về _họa sĩ_ là nó có thể được áp dụng vào một _khung_ để tạo ra một hình ảnh.

Có thể tạo một _họa sĩ_ nguyên thủy từ một danh sách _đoạn thẳng_ (segments). _Họa sĩ_ này vẽ các đoạn thẳng được biến đổi theo ánh xạ tọa độ khung của _khung_ cho trước:

```lisp
(define (segments->painter segment-list)
  (lambda (frame)
    (for-each
     (lambda (segment)
       (draw-line
        ((frame-coord-map frame) (start-segment segment))
        ((frame-coord-map frame) (end-segment segment))))
     segment-list)))
```

Các đoạn thẳng được xác định bằng tọa độ trong _khung_ đơn vị. Ví dụ, `outline` — _họa sĩ_ vẽ đường viền của _khung_ chỉ định — là danh sách bốn đoạn thẳng nối bốn góc của _khung_ đơn vị:

```lisp
(segments->painter
 (list (make-segment (make-vect 0 0) (make-vect 1 0))
       (make-segment (make-vect 0 0) (make-vect 0 1))
       (make-segment (make-vect 1 0) (make-vect 1 1))
       (make-segment (make-vect 0 1) (make-vect 1 1))))
```

**Bài tập 2.48:** Một _đoạn thẳng_ có hướng trong mặt phẳng có thể được biểu diễn bằng một cặp vectơ — vectơ chạy từ gốc đến điểm đầu của đoạn, và vectơ chạy từ gốc đến điểm cuối của đoạn. Dùng cách biểu diễn vectơ từ Bài tập 2.46 để định nghĩa một cách biểu diễn _đoạn thẳng_ với hàm tạo `make-segment` và các **bộ chọn** `start-segment`, `end-segment`.

**Bài tập 2.49:** Dùng `segments->painter` để định nghĩa các _họa sĩ_ nguồn sau đây:

a. _Họa sĩ_ vẽ đường viền của _khung_ chỉ định.

b. _Họa sĩ_ vẽ đường chéo nối góc đối diện của _khung_.

c. _Họa sĩ_ vẽ hình thoi kết nối các điểm giữa của các cạnh _khung_.

d. _Họa sĩ_ `wave`.

## Biến đổi và kết hợp họa sĩ

Các phép biến đổi của _họa sĩ_ — chẳng hạn `flip-vert` hay `beside` — hoạt động bằng cách tạo ra một _họa sĩ_ mới gọi _họa sĩ_ ban đầu với một _khung_ đã được biến đổi. Vì vậy, ví dụ, `flip-vert` không cần biết _họa sĩ_ vẽ hình gì — nó chỉ cần biết cách lật ngược một _khung_: _họa sĩ_ lật ngược của `p` là `p` được vẽ trong _khung_ bị lật ngược.

Nói chung, ta có thể định nghĩa bất kỳ sự biến đổi nào từ một _khung_ thành một _khung_ khác và sử dụng nó để biến đổi _họa sĩ_. Ví dụ, để biểu diễn phép biến đổi `transform-painter`, ta chỉ định:

```lisp
(define (transform-painter painter origin corner1 corner2)
  (lambda (frame)
    (let ((m (frame-coord-map frame)))
      (let ((new-origin (m origin)))
        (painter
         (make-frame new-origin
                     (sub-vect (m corner1) new-origin)
                     (sub-vect (m corner2) new-origin)))))))
```

Khi áp dụng vào một _khung_, _họa sĩ_ vẽ trong _khung_ được xác định bởi các điểm được biến đổi của `origin`, `corner1` và `corner2`. Biểu tượng `flip-vert` có thể định nghĩa là:

```lisp
(define (flip-vert painter)
  (transform-painter painter
                     (make-vect 0.0 1.0)
                     (make-vect 1.0 1.0)
                     (make-vect 0.0 0.0)))
```

Tương tự, thu nhỏ một hình ảnh về góc trên bên phải của _khung_ là:

```lisp
(define (shrink-to-upper-right painter)
  (transform-painter painter
                     (make-vect 0.5 0.5)
                     (make-vect 1.0 0.5)
                     (make-vect 0.5 1.0)))
```

Xoay _họa sĩ_ ngược chiều kim đồng hồ 90 độ:

```lisp
(define (rotate90 painter)
  (transform-painter painter
                     (make-vect 1.0 0.0)
                     (make-vect 1.0 1.0)
                     (make-vect 0.0 0.0)))
```

Thu nhỏ hình ảnh về tâm _khung_:

```lisp
(define (squash-inwards painter)
  (transform-painter painter
                     (make-vect 0.0 0.0)
                     (make-vect 0.65 0.35)
                     (make-vect 0.35 0.65)))
```

Còn `beside` kết hợp hai _họa sĩ_ bằng cách chia _khung_ thành hai nửa:

```lisp
(define (beside painter1 painter2)
  (let ((split-point (make-vect 0.5 0.0)))
    (let ((paint-left
           (transform-painter painter1
                              (make-vect 0.0 0.0)
                              split-point
                              (make-vect 0.0 1.0)))
          (paint-right
           (transform-painter painter2
                              split-point
                              (make-vect 1.0 0.0)
                              (make-vect 0.5 1.0))))
      (lambda (frame)
        (paint-left frame)
        (paint-right frame)))))
```

Hãy để ý cách thức hoạt động của sơ đồ này. Mỗi _họa sĩ_ vẽ vào nửa _khung_ của nó bằng cách áp dụng vào _khung_ đã được thu nhỏ phù hợp.

**Bài tập 2.50:** Định nghĩa các phép biến đổi `flip-horiz` — lật _họa sĩ_ theo chiều ngang — và các phép xoay ngược chiều kim đồng hồ 180 độ và 270 độ.

**Bài tập 2.51:** Định nghĩa phép toán `below` cho các _họa sĩ_. `below` nhận hai _họa sĩ_ làm đối số. _Họa sĩ_ kết quả, khi nhận một _khung_, vẽ _họa sĩ_ đầu tiên ở phần dưới và _họa sĩ_ thứ hai ở phần trên của _khung_. Định nghĩa `below` theo hai cách — một lần trực tiếp (tương tự cách định nghĩa `beside`), và một lần bằng cách dùng `beside` kết hợp với các phép xoay thích hợp (từ Bài tập 2.50).

## Các mức của ngôn ngữ đối với thiết kế bền vững

Ngôn ngữ hình ảnh khai thác một số ý tưởng chủ chốt mà ta đã thảo luận trong những phần trước về **quy trình** và **dữ liệu**. Mức độ trừu tượng cơ bản nhất là _họa sĩ_ được cài đặt bằng cách biểu diễn trực tiếp các phần tử vẽ. Tiếp theo là mức _biến đổi họa sĩ_ và kết hợp. Ta đã thấy cách xây dựng ở mức này: `beside`, `below`, `flip-vert`, `flip-horiz`, `rotate90`, v.v. Cuối cùng là mức cao nhất mà ta dùng các **quy trình** như `square-of-four`, `right-split`, `corner-split`, `square-limit` để mô tả các mẫu hình phức tạp.

Điều đặc biệt là mỗi mức ngôn ngữ có _họa sĩ_ như phần tử thống nhất của mình. Sức mạnh của cách tiếp cận này chính là _tính đóng_ — kết quả của phép kết hợp họa sĩ vẫn là _họa sĩ_, do đó nó có thể tiếp tục kết hợp. Điều này cho phép ta xây dựng cấu trúc phức tạp bằng cách đặt các khối đơn giản lên nhau mà không cần thay đổi gì ở tầng dưới.

Mỗi mức ngôn ngữ cung cấp các _hàng rào trừu tượng_ riêng của nó. Khi tìm lỗi, ta có thể xác định chính xác lỗi nằm ở mức nào và không cần lo đến các mức khác. Điều này làm cho hệ thống dễ kiểm tra hơn nhiều.

Có một điều khác biệt quan trọng giữa ý tưởng ở đây và thiết kế thông thường: ở đây ta không chỉ tạo ra một **quy trình** vẽ `wave`; ta đang xây dựng cả một **ngôn ngữ** cho phép tạo ra vô số hình ảnh. Cũng như với bất kỳ ngôn ngữ nào, ta có khả năng kết hợp các yếu tố đơn giản thành các cấu trúc phức tạp tùy ý.[^3]

**Bài tập 2.52:** Hãy thực hiện các thay đổi sau đây với ngôn ngữ hình ảnh được xây dựng trong phần này:

a. Thêm một số đoạn thẳng vào họa sĩ nguyên thủy `wave` từ Bài tập 2.49d (ví dụ, thêm nụ cười vào khuôn mặt).

b. Thay đổi **quy trình** `corner-split` bằng cách dùng một bản sao thay vì hai bản sao của `up-split` và `right-split`. Điều này làm thay đổi hình ảnh theo những cách thú vị.

c. Sửa đổi phiên bản `square-limit` bằng cách dùng `square-of-four` để lắp ráp những góc theo một mẫu hình khác. (Ví dụ, bạn có thể tạo sao cho Rogers nhìn ra ngoài từ mỗi góc của hình vuông thay vì nhìn vào trong.)

---

[^1]: Ngôn ngữ hình ảnh dựa trên ngôn ngữ do Peter Henderson tạo ra để xây dựng hình ảnh dựa trên bức tranh M.C. Escher's _Square Limit_. Xem Henderson 1982 để biết chi tiết.

[^2]: `rotate180` xoay _họa sĩ_ 180 độ, tương đương với `flip-vert` kết hợp với `flip-horiz`. `identity` là _họa sĩ_ không thay đổi gì.

[^3]: William Barton Rogers (1804–1882) là nhà địa chất học và nhà giáo dục người Mỹ, người sáng lập Viện Công nghệ Massachusetts (MIT). Ông là người đầu tiên đề xuất kế hoạch thành lập trường.

---

> **Lưu ý cho bạn:** Section 2.2.4 có nhiều hình ảnh. Bạn cần thêm các ảnh sau vào thư mục `./figures/`:
> - `figure7.png` — Họa sĩ `wave` trong bốn khung khác nhau (Figure 2.10 trong sách)
> - `figure8.png` — Họa sĩ `rogers` trong bốn khung (Figure 2.11)
> - `figure9.png` — `wave2` và `wave4` (Figure 2.12)
> - `figure10.png` — `right-split` và `corner-split` của wave và rogers (Figure 2.13)
> - `figure11.png` — `square-limit` của wave và rogers (Figure 2.14)
> - `figure12.png` — Mô tả khung với ba vectơ (Figure 2.15)

---

[`procedure`](GLOSSARY.md#procedure) [`closure`](GLOSSARY.md#closure) [`data abstraction`](GLOSSARY.md#data-abstraction) [`abstraction barrier`](GLOSSARY.md#abstraction-barrier) [`higher-order procedure`](GLOSSARY.md#higher-order-procedure) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`pair`](GLOSSARY.md#pair) [`list`](GLOSSARY.md#list) [`data`](GLOSSARY.md#data)
