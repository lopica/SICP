# 3.4 Tính đồng thời: thời gian là điều cốt lõi

Đã có trong tay mô hình môi trường để hiểu **phép gán**, ta thấy rằng việc đưa **phép gán** vào ngôn ngữ đã buộc ta phải đối mặt với thời gian như một yếu tố bắt buộc trong mô hình tính toán. Trước khi có **phép gán**, tất cả các **chương trình** đều có thể là bất thời gian — nghĩa là kết quả tính toán không phụ thuộc vào thứ tự thực hiện từng bước. Nhưng khi cho phép **phép gán** tác động lên các biến, ta bắt buộc phải quan tâm đến _thứ tự_ của các thao tác truy cập và cập nhật biến đó.[^1]

Nếu một **tiến trình** trong máy tính mô phỏng một tập hợp các đối tượng trong thế giới thực, thì ở thế giới thực các đối tượng này thường không hoạt động tuần tự mà hoạt động _đồng thời_ — cùng một lúc. Vì vậy, sẽ tự nhiên hơn nếu ta cũng cho phép hệ thống máy tính mô hình hóa chúng theo cách đồng thời, thay vì ép tất cả mọi thứ vào một trình tự duy nhất. Cũng như vậy, các hệ thống máy tính hiện đại thường hoạt động song song, với nhiều bộ xử lý hay nhiều **tiến trình** chạy cùng lúc. Nếu biết khai thác tốt, _tính đồng thời_ (concurrency) có thể tăng tốc đáng kể hiệu năng của chương trình.

Tuy nhiên, kết hợp đồng thời với **phép gán** dẫn đến những vấn đề khó mà ta chưa từng gặp trong lập trình tuần tự. Bản chất của những khó khăn này có liên hệ sâu sắc với ý nghĩa của trạng thái trong một hệ thống tính toán.

---

[^1]: Trong thực tế, vấn đề phức tạp hơn thế. Dù trong một **tiến trình** đơn thì thứ tự thực hiện đã được ngôn ngữ quy định, nhưng trong các hệ thống chạy song song hoặc dùng bộ nhớ đệm, ngay cả "thứ tự tuần tự tưởng chừng hiển nhiên" cũng có thể không còn đảm bảo nếu bộ xử lý thực hiện các tối ưu hóa vi kiến trúc.

---

[`assignment`](GLOSSARY.md#assignment) [`process`](GLOSSARY.md#process) [`program`](GLOSSARY.md#program) [`variable`](GLOSSARY.md#variable)
