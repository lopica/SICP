# 3.5.1 Dòng chảy là danh sách trì hoãn

Như ta đã thấy ở mục 2.2.3, các _dãy_ đóng vai trò là giao diện quy ước để ghép nối các **mô đun** chương trình lại với nhau. Những phép trừu tượng mạnh như `map`, `filter`, và `accumulate` cho phép biểu đạt chương trình một cách thanh lịch. Nhưng khi biểu diễn các _dãy_ dưới dạng _danh sách_, sự thanh lịch đó đổi lại bằng cái giá về cả thời gian lẫn không gian lưu trữ, đặc biệt khi ta cần thao tác trên các **dòng chảy** lớn.

Xét hai cách tính tổng tất cả các số nguyên tố trong một khoảng. Cách thứ nhất viết theo phong cách lặp thông thường:[^1]

```lisp
(define (sum-primes a b)
  (define (iter count accum)
    (cond ((> count b) accum)
          ((prime? count)
           (iter (+ count 1)
                 (+ count accum)))
          (else (iter (+ count 1) accum))))
  (iter a 0))
```

Cách thứ hai dùng các phép toán trên _dãy_:

```lisp
(define (sum-primes a b)
  (accumulate 
   +
   0
   (filter prime? (enumerate-interval a b))))
```

Khi thực thi, cách đầu tiên chỉ cần lưu tổng đang tích lũy. Cách thứ hai thì khác: `enumerate-interval` phải xây dựng toàn bộ _danh sách_ các số trong khoảng trước, rồi `filter` tạo thêm một _danh sách_ mới, và cuối cùng `accumulate` mới thu gọn xuống thành một tổng. Bộ nhớ trung gian khổng lồ đó hoàn toàn không cần thiết.

Sự kém hiệu quả còn nghiêm trọng hơn khi ta chỉ muốn tìm số nguyên tố thứ hai trong khoảng từ 10.000 đến 1.000.000:

```lisp
(car (cdr 
      (filter 
       prime?
       (enumerate-interval 10000 1000000))))
```

Đoạn mã này xây dựng một _danh sách_ gần một triệu số nguyên để lọc ra hai số nguyên tố đầu tiên — rồi bỏ đi gần hết. Lập trình truyền thống sẽ không làm vậy: nó sẽ liệt kê từng số, kiểm tra nguyên tố, và dừng lại ngay khi tìm thấy số thứ hai.

**Dòng chảy** là giải pháp. Ý tưởng cốt lõi là xây dựng _dãy_ từng phần, chuyển phần đã xây dựng đến phần chương trình đang "tiêu thụ". Nếu phần tiêu thụ cần thêm phần tử mà chưa được tạo ra, **dòng chảy** sẽ tự động tạo thêm vừa đủ. Từ bên ngoài, chương trình nhìn thấy như đang làm việc với một _dãy_ hoàn chỉnh, trong khi thực tế quá trình tạo và tiêu thụ được xen kẽ nhau.

## Các phép toán trên dòng chảy

Nhìn bề ngoài, **dòng chảy** giống _danh sách_ nhưng với tên gọi khác. Ta có **bộ tạo** `cons-stream` và **bộ chọn** `stream-car`, `stream-cdr` thỏa mãn:

```
(stream-car (cons-stream x y)) = x
(stream-cdr (cons-stream x y)) = y
```

Có một đối tượng đặc biệt `the-empty-stream` không thể là kết quả của bất kỳ `cons-stream` nào, và được nhận biết bởi **mệnh đề điều kiện** `stream-null?`.[^2] Ta có thể dùng **dòng chảy** để biểu diễn các tập hợp dữ liệu tuần tự, với các phép toán tương tự như trên _danh sách_:

```lisp
(define (stream-ref s n)
  (if (= n 0)
      (stream-car s)
      (stream-ref (stream-cdr s) (- n 1))))

(define (stream-map proc s)
  (if (stream-null? s)
      the-empty-stream
      (cons-stream 
       (proc (stream-car s))
       (stream-map proc (stream-cdr s)))))

(define (stream-for-each proc s)
  (if (stream-null? s)
      'done
      (begin 
        (proc (stream-car s))
        (stream-for-each proc 
                         (stream-cdr s)))))
```

`stream-for-each` rất tiện để quan sát nội dung **dòng chảy**:

```lisp
(define (display-stream s)
  (stream-for-each display-line s))

(define (display-line x)
  (newline)
  (display x))
```

## Cài đặt thực thi trì hoãn

Cách để xen kẽ tự động quá trình xây dựng và sử dụng **dòng chảy** là: phần `cdr` của **dòng chảy** chỉ được **thực thi** khi ta gọi `stream-cdr`, không phải lúc `cons-stream` tạo ra nó. Điều này tương tự quyết định thiết kế ở mục 2.1.2: ta có thể chọn rút gọn phân số lúc tạo hoặc lúc lấy ra. Cả hai đều đúng, nhưng lựa chọn ảnh hưởng đến hiệu năng.

**Dòng chảy** dựa trên một trường hợp đặc biệt tên là `delay`. Khi **thực thi** `(delay ⟨exp⟩)`, biểu thức `⟨exp⟩` không được tính ngay — thay vào đó ta nhận về một _đối tượng trì hoãn_ (delayed object), một "lời hứa" sẽ tính `⟨exp⟩` vào lúc nào đó trong tương lai. Cặp đôi với `delay` là **quy trình** `force`, nhận một đối tượng trì hoãn và thực hiện **thực thi** đó — tức là thực hiện lời hứa.

`cons-stream` là một trường hợp đặc biệt sao cho:

```lisp
(cons-stream ⟨a⟩ ⟨b⟩)
```

tương đương với:

```lisp
(cons ⟨a⟩ (delay ⟨b⟩))
```

Nghĩa là, **dòng chảy** dùng cặp `cons` thông thường, nhưng giá trị phần còn lại của **dòng chảy** được thay bằng lời hứa sẽ tính nếu cần. Khi đó `stream-car` và `stream-cdr` là các **quy trình** thông thường:

```lisp
(define (stream-car stream) 
  (car stream))

(define (stream-cdr stream) 
  (force (cdr stream)))
```

`stream-car` lấy `car` của cặp; `stream-cdr` lấy `cdr` của cặp rồi **thực thi** biểu thức trì hoãn tìm thấy ở đó để thu được phần còn lại của **dòng chảy**.

## Dòng chảy hoạt động như thế nào

Hãy quay lại bài toán tìm số nguyên tố thứ hai trong khoảng 10.000 đến 1.000.000, nhưng viết lại bằng **dòng chảy**:

```lisp
(stream-car 
 (stream-cdr
  (stream-filter 
   prime? (stream-enumerate-interval 
           10000 1000000))))
```

Lần này mọi thứ diễn ra hiệu quả. `stream-enumerate-interval` là phiên bản **dòng chảy** của `enumerate-interval`:

```lisp
(define (stream-enumerate-interval low high)
  (if (> low high)
      the-empty-stream
      (cons-stream
       low
       (stream-enumerate-interval (+ low 1)
                                  high))))
```

Khi gọi `(stream-enumerate-interval 10000 1000000)`, kết quả trả về là:

```lisp
(cons 10000
      (delay 
        (stream-enumerate-interval 10001 1000000)))
```

Đó là một cặp với `car` bằng 10.000 và `cdr` là lời hứa sẽ liệt kê thêm nếu cần. **Dòng chảy** này được lọc qua `stream-filter`:

```lisp
(define (stream-filter pred stream)
  (cond ((stream-null? stream) 
         the-empty-stream)
        ((pred (stream-car stream))
         (cons-stream 
          (stream-car stream)
          (stream-filter 
           pred
           (stream-cdr stream))))
        (else (stream-filter 
               pred 
               (stream-cdr stream)))))
```

`stream-filter` kiểm tra `stream-car` (tức là 10.000). Vì 10.000 không phải số nguyên tố, nó gọi `stream-cdr`. Lúc này lời hứa trong `cdr` được thực hiện — `stream-enumerate-interval` được **thực thi** để trả về:

```lisp
(cons 10001
      (delay 
        (stream-enumerate-interval 10002 1000000)))
```

Quá trình cứ thế tiếp diễn cho đến khi 10.007 — số nguyên tố đầu tiên — được tìm thấy. Lúc đó `stream-filter` trả về:

```lisp
(cons 10007
      (delay
        (stream-filter
         prime?
         (cons 10008
               (delay
                 (stream-enumerate-interval 
                  10009 1000000))))))
```

Kết quả này được đưa vào `stream-cdr` trong biểu thức ban đầu. `stream-cdr` thực hiện lời hứa — `stream-filter` tiếp tục lọc cho đến khi tìm được số nguyên tố tiếp theo là 10.009. Cuối cùng, `stream-car` nhận được:

```lisp
(cons 10009
      (delay
        (stream-filter
         prime?
         (cons 10010
               (delay
                 (stream-enumerate-interval 
                  10011 1000000))))))
```

và trả về 10.009. Toàn bộ quá trình chỉ kiểm tra đúng số phần tử cần thiết để tìm ra số nguyên tố thứ hai, không hơn không kém.

Nhìn chung, _thực thi trì hoãn_ thể hiện kiểu lập trình _theo nhu cầu_ (demand-driven): mỗi giai đoạn trong chuỗi xử lý **dòng chảy** chỉ kích hoạt đúng mức cần thiết để thỏa mãn giai đoạn tiếp theo. Nhờ đó, thứ tự tính toán thực tế được tách rời hoàn toàn khỏi cấu trúc hiển nhiên của **quy trình** — ta có thể viết **quy trình** như thể **dòng chảy** đã tồn tại đầy đủ, trong khi thực tế mọi thứ diễn ra từng bước một, đúng như lập trình truyền thống.

## Cài đặt `delay` và `force`

Dù `delay` và `force` có vẻ bí ẩn, cài đặt của chúng thực ra rất thẳng thắn. `delay` đóng gói một biểu thức để tính sau bằng cách biến nó thành thân của một **quy trình**. Cụ thể, `delay` là một trường hợp đặc biệt sao cho:

```lisp
(delay ⟨exp⟩)
```

là cú pháp rút gọn của:

```lisp
(lambda () ⟨exp⟩)
```

`force` đơn giản là gọi **quy trình** không có **đối số** mà `delay` tạo ra:

```lisp
(define (force delayed-object)
  (delayed-object))
```

Đây là đủ để mọi thứ hoạt động, nhưng vẫn có một tối ưu quan trọng. Nhiều ứng dụng thực tế sẽ `force` cùng một đối tượng trì hoãn nhiều lần, và nếu mỗi lần `force` đều tính lại từ đầu, hiệu năng sẽ tệ đi nghiêm trọng trong các chương trình **dòng chảy đệ quy**. Giải pháp là xây dựng đối tượng trì hoãn theo kiểu _ghi nhớ hóa_ (memoization): lần đầu `force` thì tính và lưu kết quả; các lần sau chỉ trả về kết quả đã lưu, không tính lại. Cách cài đặt `delay` theo kiểu ghi nhớ hóa tương tự bài tập 3.27. Ta dùng **quy trình** trợ giúp:[^3]

```lisp
(define (memo-proc proc)
  (let ((already-run? false) (result false))
    (lambda ()
      (if (not already-run?)
          (begin (set! result (proc))
                 (set! already-run? true)
                 result)
          result))))
```

Khi đó `delay` được định nghĩa sao cho `(delay ⟨exp⟩)` tương đương với:

```lisp
(memo-proc (lambda () ⟨exp⟩))
```

và `force` giữ nguyên như định nghĩa ở trên.

---

[^1]: Chương trình `sum-primes` đầu tiên được viết theo phong cách lặp thông thường.

[^2]: Ở nhiều cài đặt Scheme, `the-empty-stream` là `'()` và `stream-null?` kiểm tra bằng `null?`. Nhưng điều này không bắt buộc theo chuẩn ngôn ngữ — miễn là `the-empty-stream` không thể là kết quả của `cons-stream` và `stream-null?` phân biệt nó với mọi **dòng chảy** không rỗng.

[^3]: Kỹ thuật tối ưu này, còn gọi là _ghi nhớ hóa_ hay _tính toán lười_ (lazy evaluation), được sử dụng như nền tảng của một số ngôn ngữ lập trình hàm, nổi bật nhất là Haskell. Trong những ngôn ngữ đó, mọi biểu thức đều được **thực thi** theo cơ chế tương tự — chỉ tính khi thực sự cần đến.

---

[`stream`](GLOSSARY.md#stream) [`sequence`](GLOSSARY.md#sequence) [`list`](GLOSSARY.md#list) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`predicate`](GLOSSARY.md#predicate) [`procedure`](GLOSSARY.md#procedure) [`evaluation`](GLOSSARY.md#evaluation) [`memoization`](GLOSSARY.md#memoization) [`module`](GLOSSARY.md#module)
