# 2.3.4 Ví dụ: Cây mã hóa Huffman

Phần này trình bày một ứng dụng thực tế của biểu diễn dữ liệu ký hiệu: _mã hóa Huffman_ (Huffman encoding). Đây là một phương pháp biểu diễn dữ liệu dưới dạng chuỗi các bit nhị phân và là ví dụ hay về cách kết hợp nhiều kỹ thuật biểu diễn dữ liệu.

## Mã có độ dài cố định và mã có độ dài thay đổi

Giả sử ta có một thông điệp gồm các ký hiệu — chẳng hạn các chữ cái — và muốn mã hóa nó thành chuỗi bit nhị phân. Ví dụ, mã ASCII chuẩn dùng 7 bit cho mỗi ký tự, tức là mỗi ký tự trong tổng số 128 ký tự đều được biểu diễn bằng đúng 7 bit. Nếu ta chỉ cần biểu diễn 8 ký hiệu A, B, C, D, E, F, G, H, ta có thể dùng 3 bit mỗi ký hiệu, ví dụ:

```
A  000    C  010    E  100    G  110
B  001    D  011    F  101    H  111
```

Với bảng mã này, thông điệp "BACADAEAFABBAAAGAH" được mã hóa thành chuỗi 54 bit:

```
001000010000011000100000101000001001000000000110000111
```

Các mã như ASCII hay bảng A-đến-H trên được gọi là _mã có độ dài cố định_ (fixed-length code), vì chúng biểu diễn mỗi ký hiệu trong thông điệp bằng cùng số lượng bit. Đôi khi hiệu quả hơn nếu dùng _mã có độ dài thay đổi_ (variable-length code), gán cho các ký hiệu xuất hiện thường xuyên các mã ngắn hơn, và cho các ký hiệu ít phổ biến hơn các mã dài hơn. Chẳng hạn, mã Morse không dùng số ký hiệu như nhau cho mỗi chữ cái của bảng chữ cái. Chữ E (ký tự phổ biến nhất) có mã chỉ gồm một chấm, trong khi chữ Q ít dùng hơn có mã -- - .. Nhìn chung, nếu ta biết tần suất xuất hiện của các ký hiệu trong thông điệp, ta có thể thấy rằng cách tối ưu nhất để mã hóa thông điệp đó là mã có độ dài thay đổi sao cho mỗi ký hiệu được gán một mã dài tỉ lệ nghịch với tần suất xuất hiện của nó.[^1]

Xét thông điệp 8 ký hiệu gồm các chữ cái A đến H, trong đó có các tần suất xuất hiện tương đối như sau:

```
A  8    B  3    C  1    D  1
E  1    F  1    G  1    H  1
```

Với bảng mã có độ dài cố định 3 bit, thông điệp "BACADAEAFABBAAAGAH" sẽ cần 54 bit. Với mã Huffman được xây dựng từ các tần suất này, thông điệp đó chỉ cần 42 bit.[^2]

## Cây Huffman

_Mã Huffman_ được thiết kế dựa trên một cây nhị phân (binary tree) trong đó mỗi lá là một ký hiệu của bảng chữ cái. Con đường từ gốc xuống một lá cho ta mã nhị phân của ký hiệu đó: rẽ trái ứng với `0`, rẽ phải ứng với `1`. Mỗi ký hiệu được gán cho một lá ở độ sâu tương ứng với tần suất của nó — ký hiệu xuất hiện thường xuyên nằm gần gốc hơn (đường ngắn hơn, mã ngắn hơn).

Mỗi nút trong cây mang một _trọng số_ (weight). Với một lá, trọng số là tần suất xuất hiện của ký hiệu đó. Với một nút trong, trọng số là tổng trọng số của tất cả các lá trong cây con của nó.

Ví dụ với bảng tần suất trên, cây Huffman trông như sau (hình ảnh minh họa):

![Cây Huffman cho 8 ký hiệu A-H](./figures/figure6.png)

> Lưu ý: bạn cần thêm `figures/figure6.png` (ảnh chụp từ sách) cho hình minh họa cây Huffman.

Với cây này:
- A được biểu diễn bằng `0` (1 bit)
- B được biểu diễn bằng `100` (3 bit)
- C được biểu diễn bằng `1010` (4 bit)
- D được biểu diễn bằng `1011` (4 bit)
- E được biểu diễn bằng `1100` (4 bit)
- F được biểu diễn bằng `1101` (4 bit)
- G được biểu diễn bằng `1110` (4 bit)
- H được biểu diễn bằng `1111` (4 bit)

Giải mã chuỗi bit dùng cây Huffman rất đơn giản: bắt đầu từ gốc, đọc lần lượt từng bit, rẽ trái nếu bit là `0` và rẽ phải nếu là `1`, đến khi chạm lá thì đọc ký hiệu và quay về gốc để tiếp tục.

## Biểu diễn cây Huffman

Trong cài đặt của chúng ta, các lá được biểu diễn bằng danh sách gồm ký hiệu `leaf`, tên ký hiệu, và trọng số:

```lisp
(define (make-leaf symbol weight)
  (list 'leaf symbol weight))

(define (leaf? object)
  (eq? (car object) 'leaf))

(define (symbol-leaf x) (cadr x))

(define (weight-leaf x) (caddr x))
```

Một cây tổng quát (nút trong) được xây dựng từ cây con trái, cây con phải, danh sách tất cả ký hiệu trong cây con đó, và tổng trọng số:

```lisp
(define (make-code-tree left right)
  (list left
        right
        (append (symbols left) (symbols right))
        (+ (weight left) (weight right))))
```

Nếu ta tạo ra cây theo cách này, ta có các bộ chọn sau:

```lisp
(define (left-branch tree) (car tree))

(define (right-branch tree) (cadr tree))

(define (symbols tree)
  (if (leaf? tree)
      (list (symbol-leaf tree))
      (caddr tree)))

(define (weight tree)
  (if (leaf? tree)
      (weight-leaf tree)
      (cadddr tree)))
```

Các quy trình `symbols` và `weight` phải làm việc hơi khác nhau tùy theo ta đang xét một lá hay một cây, đây là ví dụ đơn giản về _phân phối theo kiểu_ (generic procedure) — một chủ đề ta sẽ khảo sát kỹ hơn ở các mục 2.4 và 2.5.

## Quy trình giải mã

Đây là quy trình giải mã:

```lisp
(define (decode bits tree)
  (define (decode-1 bits current-branch)
    (if (null? bits)
        '()
        (let ((next-branch
               (choose-branch (car bits) current-branch)))
          (if (leaf? next-branch)
              (cons (symbol-leaf next-branch)
                    (decode-1 (cdr bits) tree))
              (decode-1 (cdr bits) next-branch)))))
  (decode-1 bits tree))

(define (choose-branch bit branch)
  (cond ((= bit 0) (left-branch branch))
        ((= bit 1) (right-branch branch))
        (else (error "bad bit -- CHOOSE-BRANCH" bit))))
```

## Tập hợp lá có trọng số

Trong quy trình xây dựng cây Huffman, ta cần duy trì một tập hợp các cây lá và cây trung gian theo thứ tự trọng số tăng dần. Quy trình sau chèn một mục mới vào tập hợp đã được sắp xếp:

```lisp
(define (adjoin-set x set)
  (cond ((null? set) (list x))
        ((< (weight x) (weight (car set))) (cons x set))
        (else (cons (car set)
                    (adjoin-set x (cdr set))))))
```

Hàm dưới đây nhận một danh sách các cặp `(ký-hiệu tần-suất)` — như `((A 4) (B 2) (C 1) (D 1))` — và tạo ra tập hợp lá ban đầu được sắp xếp theo trọng số, sẵn sàng cho quy trình xây dựng cây Huffman:

```lisp
(define (make-leaf-set pairs)
  (if (null? pairs)
      '()
      (let ((pair (car pairs)))
        (adjoin-set (make-leaf (car pair)
                               (cadr pair))
                    (make-leaf-set (cdr pairs))))))
```

[^1]: Nhận xét này là trực giác đúng, nhưng chứng minh chặt chẽ đòi hỏi các khái niệm từ lý thuyết thông tin; xem Shannon và Weaver 1949.

[^2]: Xem bài tập 2.70.

---

[exercise 2.67](exercises/2.67.md) [exercise 2.68](exercises/2.68.md) [exercise 2.69](exercises/2.69.md) [exercise 2.70](exercises/2.70.md) [exercise 2.71](exercises/2.71.md) [exercise 2.72](exercises/2.72.md) [`symbol`](GLOSSARY.md#symbol) [`list`](GLOSSARY.md#list) [`pair`](GLOSSARY.md#pair) [`procedure`](GLOSSARY.md#procedure) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`tree recursion`](GLOSSARY.md#tree-recursion) [`data abstraction`](GLOSSARY.md#data-abstraction) [`data`](GLOSSARY.md#data) [`recursive process`](GLOSSARY.md#recursive-process)
