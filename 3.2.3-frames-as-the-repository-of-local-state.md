# 3.2.3 Khung như kho lưu trạng thái cục bộ

_Mô hình môi trường_ cho ta thấy cách **quy trình** kết hợp với **phép gán** có thể được dùng để biểu diễn các đối tượng có trạng thái cục bộ. Hãy quay lại ví dụ "bộ xử lý rút tiền" từ mục 3.1.1 — đối tượng được tạo ra khi gọi quy trình:

```lisp
(define (make-withdraw balance)
  (lambda (amount)
    (if (>= balance amount)
        (begin (set! balance 
                     (- balance amount))
               balance)
        "Insufficient funds")))
```

Ta hãy mô tả quá trình thực thi `(define W1 (make-withdraw 100))` rồi tiếp theo `(W1 50)`.

Hình 3.6 cho thấy kết quả của việc định nghĩa quy trình `make-withdraw` trong **môi trường toàn cục**. Quá trình này tạo ra một đối tượng quy trình chứa một con trỏ đến **môi trường toàn cục**. Cho đến đây thì chưa có gì đặc biệt so với những ví dụ đã thấy.

![Hình 3.6: Kết quả của việc định nghĩa `make-withdraw` trong môi trường toàn cục.](./figures/figure28.png)

Bước thú vị xảy ra khi ta tạo ra đối tượng rút tiền mới bằng cách gọi `make-withdraw`:

```lisp
(define W1 (make-withdraw 100))
```

Thực thi biểu thức này theo _quy tắc_ áp dụng quy trình ở mục 3.2.1: ta tạo một môi trường mới E1 có khung ràng buộc `balance` với `100`, và khung này nằm trong **môi trường toàn cục**. Trong E1, ta thực thi thân của `make-withdraw`, tức là biểu thức λ:

```lisp
(lambda (amount)
  (if (>= balance amount)
      (begin (set! balance 
                   (- balance amount))
             balance)
      "Insufficient funds"))
```

Biểu thức λ này tạo ra một đối tượng quy trình mới có mã là `(lambda (amount) ...)` và môi trường liên kết là E1 — tức là môi trường tại thời điểm biểu thức λ được thực thi. Đối tượng quy trình này chính là giá trị được trả về bởi lời gọi `make-withdraw`, và sau đó được ràng buộc với `W1` trong **môi trường toàn cục**.

Hình 3.7 cho thấy cấu trúc môi trường sau bước này. `W1` là một đối tượng quy trình trỏ đến E1, nơi `balance` đang bằng `100`. Điểm mấu chốt ở đây: E1 và ràng buộc `balance` bên trong nó _vẫn tồn tại_ sau khi lời gọi `make-withdraw` kết thúc, vì đối tượng quy trình `W1` vẫn còn giữ một tham chiếu đến E1.

![Hình 3.7: Kết quả của việc thực thi `(define W1 (make-withdraw 100))`.](./figures/figure29.png)

Bây giờ ta phân tích điều gì xảy ra khi gọi `W1`:

```lisp
(W1 50)
50
```

Ta xây dựng khung ràng buộc `amount` với `50`, và khung này có E1 là môi trường bao ngoài, vì đó là môi trường được chỉ định bởi đối tượng quy trình `W1`. Gọi môi trường mới này là E2. Trong E2, ta thực thi thân của `W1`:

```lisp
(if (>= balance amount)
    (begin (set! balance 
                 (- balance amount))
           balance)
    "Insufficient funds")
```

Biểu thức con `balance` được tra cứu: trước tiên tìm trong khung của E2 (không có), rồi lên E1 — tìm thấy, giá trị là `100`. Biểu thức con `amount` được tra cứu trong E2 — tìm thấy, giá trị là `50`. Điều kiện `(>= 100 50)` là đúng, nên ta thực thi biểu thức `begin`.

Lệnh `set!` thay đổi ràng buộc của `balance` trong E1 thành `50`. Sau khi lệnh này hoàn thành, `balance` trong E1 có giá trị mới là `50`, và quy trình trả về `50`.

Hình 3.8 minh họa cấu trúc môi trường sau khi lời gọi `(W1 50)` hoàn tất.

![Hình 3.8: Môi trường được tạo ra khi gọi `W1`.](./figures/figure30.png)

Sau khi lời gọi hoàn tất, môi trường E2 không còn được tham chiếu nữa — không một phần nào của chương trình còn giữ con trỏ đến nó. Nó sẽ được thu hồi. Còn E1 thì vẫn còn sống, vì `W1` vẫn đang tham chiếu đến nó. Đặc biệt, E1 bây giờ chứa ràng buộc `balance = 50`. Đây chính là điều lỳ diệu: lần gọi tiếp theo `(W1 n)` bất kỳ sẽ tìm thấy `balance` là `50`, không phải `100`.

Hãy quan sát điều gì xảy ra khi ta tạo thêm một đối tượng rút tiền thứ hai:

```lisp
(define W2 (make-withdraw 100))
```

Lần gọi `make-withdraw` này tạo ra một môi trường mới E3, hoàn toàn riêng biệt với E1, có `balance` ràng buộc với `100`. Đối tượng quy trình `W2` trỏ đến E3.

Hình 3.9 cho thấy cấu trúc sau khi định nghĩa `W2`. Hình 3.10 cho thấy cấu trúc sau khi gọi `(W2 80)`.

![Hình 3.9: Cấu trúc môi trường sau khi định nghĩa `W2`.](./figures/figure31.png)

![Hình 3.10: Môi trường sau khi gọi `(W2 80)`.](./figures/figure32.png)

`W1` và `W2` là hai đối tượng hoàn toàn độc lập — mỗi cái có trạng thái cục bộ riêng của nó. Rút tiền từ `W1` hoàn toàn không ảnh hưởng đến số dư của `W2`, và ngược lại. Điều này xảy ra vì mỗi lời gọi `make-withdraw` tạo ra một _khung mới_ với ràng buộc `balance` riêng. Khi đối tượng quy trình được trả về, nó "mang theo" môi trường đó như một phần không thể tách rời của bản thân.

Cơ chế này — _khung_ đóng vai trò kho lưu trạng thái cục bộ — chính là nền tảng của việc mô phỏng đối tượng có trạng thái trong _mô hình môi trường_. Biến `balance` không phải là biến toàn cục, cũng không phải biến chỉ tồn tại trong một lần gọi; nó tồn tại trong một khung có tuổi thọ gắn với đối tượng quy trình đang giữ tham chiếu đến khung đó.

---

[exercise 3.10](exercises/3.10.md) [`environment model`](GLOSSARY.md#environment-model) [`frame`](GLOSSARY.md#frame) [`environment`](GLOSSARY.md#environment) [`global environment`](GLOSSARY.md#global-environment) [`procedure`](GLOSSARY.md#procedure) [`compound procedure`](GLOSSARY.md#compound-procedure) [`assignment`](GLOSSARY.md#assignment) [`variable`](GLOSSARY.md#variable) [`state variables`](GLOSSARY.md#state-variables) [`local`](GLOSSARY.md#local) [`binding`](GLOSSARY.md#binding) [`evaluation`](GLOSSARY.md#evaluation)
