# 3.4.1 Bản chất của thời gian trong các hệ thống đồng thời

Thoạt nhìn, thời gian có vẻ đơn giản: nó là một thứ tự được áp đặt lên các sự kiện. Với bất kỳ hai sự kiện A và B nào, ta luôn có thể nói A xảy ra trước B, hai sự kiện diễn ra đồng thời, hoặc A xảy ra sau B.

Lấy ví dụ về tài khoản ngân hàng. Giả sử Peter rút $10 và Paul rút $25 từ một tài khoản chung ban đầu có $100. Sau khi cả hai giao dịch hoàn tất, số dư tài khoản là $65. Tùy theo thứ tự thực hiện, số dư sẽ biến đổi theo hai chuỗi: $100 → $90 → $65 hoặc $100 → $75 → $65 — nhưng kết quả cuối cùng đều giống nhau.

Trong hệ thống ngân hàng thực tế, mọi thứ phức tạp hơn nhiều. Giả sử Peter và Paul thực hiện giao dịch từ các máy khác nhau trên toàn cầu, đồng thời. Quy trình rút tiền thực hiện như sau:

```lisp
(define (withdraw amount)
  (if (>= balance amount)
      (begin (set! balance (- balance amount))
             balance)
      "Insufficient funds"))
```

Nếu hai **tiến trình** chạy độc lập, Peter có thể kiểm tra `balance` và tìm thấy đủ tiền. Nhưng trước khi Peter kịp trừ tiền, Paul cũng kiểm tra và trừ tiền — lúc đó giá trị `balance` mà Peter đang thao tác không còn chính xác nữa.

Vấn đề cốt lõi là câu lệnh `(set! balance (- balance amount))` thực chất gồm ba bước riêng rẽ: (1) đọc giá trị `balance` hiện tại, (2) tính giá trị mới `(- balance amount)`, và (3) ghi giá trị mới vào `balance`. Nếu hai **tiến trình** xen kẽ nhau ở giữa các bước này, kết quả sẽ sai.

![Hình 3.29: Sơ đồ thời gian cho thấy hai **tiến trình** truy cập tài khoản chung đồng thời.](./figures/figure60.png)

Hình 3.29 minh họa một tình huống tệ nhất: `balance` ban đầu là 100. Peter đọc được 100 và tính giá trị mới là 90. Trước khi Peter ghi, Paul cũng đọc được 100 (chứ không phải 90) và tính giá trị mới là 75. Sau đó Peter ghi 90, rồi Paul ghi 75 — ghi đè lên kết quả của Peter. Cuối cùng `balance` là 75, dù đáng ra phải là 65. Peter rút được $10 nhưng tài khoản chỉ bị trừ $25 thay vì $35. Tiền đã biến mất!

Điều cần lưu ý: trước các giao dịch, tổng tài sản là $100. Sau đó Peter có $10, Paul có $25, và ngân hàng còn $75 — tổng vẫn là $110 thay vì $100. Hệ thống tạo ra tiền từ không khí!

## Hành vi chính xác của hệ thống đồng thời

Bản chất của sự cố trên là hai **tiến trình** chia sẻ một **biến** `balance`, và tương tác theo cách mà kết quả phụ thuộc vào thứ tự xen kẽ của các bước thực thi — thứ tự mà ta không kiểm soát được. Điều này tạo ra _tính không xác định_ (_nondeterminism_) trong hành vi chương trình.

Để tránh sự cố, ta cần ràng buộc rằng: khi một khách hàng thay đổi số dư theo cách phụ thuộc vào giá trị hiện tại, anh ta phải có thể giả định rằng ngay trước thời điểm thay đổi đó, số dư vẫn là con số anh ta đã đọc — không ai được chen vào giữa.

Một yêu cầu ít nghiêm ngặt hơn, nhưng vẫn đủ để đảm bảo tính đúng đắn, là: kết quả của một hệ thống đồng thời phải giống như kết quả sẽ thu được nếu các **tiến trình** chạy _tuần tự_ theo một thứ tự nào đó. Điều kiện này không yêu cầu các **tiến trình** phải thực sự chạy tuần tự, chỉ cần kết quả _tương đương_ với một thứ tự tuần tự nào đó.[^1]

---

[^1]: Yêu cầu về tính tương đương tuần tự này, hay gọi là _khả năng tuần tự hóa_ (serializability), là tiêu chuẩn đúng đắn thông dụng nhất cho các hệ thống đồng thời. Tuy nhiên, trong một số hệ thống, người ta chấp nhận các ràng buộc yếu hơn để đổi lấy hiệu năng cao hơn.

---

[`assignment`](GLOSSARY.md#assignment) [`variable`](GLOSSARY.md#variable) [`process`](GLOSSARY.md#process) [`state variables`](GLOSSARY.md#state-variables)
