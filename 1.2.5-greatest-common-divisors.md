# 1.2.5 Ước số chung lớn nhất

**Ước số chung lớn nhất** (USCLN) của hai số nguyên $a$ và $b$ là số nguyên lớn nhất chia hết cả $a$ lẫn $b$ (không dư). Chẳng hạn, USCLN của $16$ và $28$ là $4$. Trong chương 2, khi xây dựng số hữu tỉ, ta sẽ cần tính USCLN để rút gọn phân số về dạng tối giản bằng cách chia cả tử số lẫn mẫu số cho USCLN của chúng.

Một cách tìm USCLN là phân tích từng số ra thừa số nguyên tố rồi tìm thừa số chung. Nhưng có một phương pháp hiệu quả hơn nhiều, dựa trên nhận xét sau: nếu $r$ là số dư khi chia $a$ cho $b$, thì các ước chung của $a$ và $b$ chính xác là các ước chung của $b$ và $r$. Do đó ta có thể dùng phép rút gọn:

$$\text{USCLN}(a, b) = \text{USCLN}(b, r)$$

để liên tục đổi cặp số cần tìm USCLN sang một cặp nhỏ hơn, cho đến khi đạt được cặp mà số thứ hai bằng `0`. Khi đó, USCLN là số đầu tiên còn lại. Ví dụ:

$$\text{USCLN}(206, 40) = \text{USCLN}(40, 6)$$
$$= \text{USCLN}(6, 4)$$
$$= \text{USCLN}(4, 2)$$
$$= \text{USCLN}(2, 0) = 2$$

Phương pháp này được gọi là **Thuật toán Euclid**.[^1]

Ta dễ dàng diễn đạt thuật toán Euclid thành một quy trình:

```lisp
(define (gcd a b)
  (if (= b 0)
      a
      (gcd b (remainder a b))))
```

Quy trình này tạo ra một tiến trình lặp, có số bước tăng theo bậc logarithm của các số liên quan.

Có thể chứng minh điều này nhờ **Định lý Lamé**: Nếu Thuật toán Euclid cần $k$ bước để tính USCLN của một cặp số nào đó, thì số nhỏ hơn trong cặp đó phải lớn hơn hoặc bằng số **Fibonacci** thứ $k$.[^2]

Ta có thể dùng định lý này để ước lượng _bậc tăng trưởng_ của Thuật toán Euclid. Gọi $n$ là số nhỏ hơn trong cặp đầu vào. Nếu tiến trình cần $k$ bước, thì theo Định lý Lamé, $n \geq \text{Fib}(k) \approx \varphi^k / \sqrt{5}$. Từ đó $k \leq \log_\varphi (n\sqrt{5})$, tức là số bước tăng theo bậc $\Theta(\log n)$. Đây là một kết quả ấn tượng — thuật toán đơn giản dùng phép chia dư lại có hiệu năng logarithmic.

[^1]: Thuật toán Euclid được mô tả trong cuốn _Elements_ của Euclid (khoảng năm 300 trước Công nguyên). Theo E. Knuth (1973), có thể coi đây là thuật toán không tầm thường cổ xưa nhất còn được biết đến. Nền tảng lý thuyết của số học cho thấy điều khiến phương pháp của Euclid đúng là: nếu $d$ chia hết $a$ và $b$, thì $d$ chia hết $a - b$ (vì $a - b = a - b$), và ngược lại, nếu $d$ chia hết $b$ và $a - b$, thì $d$ chia hết $a = b + (a - b)$.

[^2]: Định lý này được chứng minh năm 1845 bởi Gabriel Lamé, một nhà toán học và kỹ sư người Pháp chủ yếu được biết đến qua công trình toán học vật lý. Để chứng minh định lý, hãy xét dãy các cặp số $(a_1, b_1), (a_2, b_2), \ldots$ sao cho $(a_{k+1}, b_{k+1}) = (b_k, \text{remainder}(a_k, b_k))$. Ta có $a_{k+1} = b_k$ và $b_{k+1} \leq b_k / 2$ — vì phép chia dư không thể cho kết quả lớn hơn một nửa số bị chia khi số chia nhỏ hơn một nửa số bị chia, và trong trường hợp ngược lại thì phép chia cho kết quả nhỏ hơn số chia. Kết hợp điều này với bất đẳng thức $\text{Fib}(k) \geq \varphi^{k-2}$ (hệ quả từ định nghĩa của Fibonacci) ta được kết quả.

---

[exercise 1.19](exercises/1.19.md) [exercise 1.20](exercises/1.20.md) [`greatest common divisor`](GLOSSARY.md#greatest-common-divisor) [`Euclid's Algorithm`](GLOSSARY.md#euclids-algorithm) [`Fibonacci numbers`](GLOSSARY.md#fibonacci-numbers) [`golden ratio`](GLOSSARY.md#golden-ratio) [`order of growth`](GLOSSARY.md#order-of-growth) [`iterative process`](GLOSSARY.md#iterative-process) [`procedure`](GLOSSARY.md#procedure) [`Lamé's theorem`](GLOSSARY.md#lames-theorem) [`normal-order evaluation`](GLOSSARY.md#normal-order-evaluation) [`applicative-order evaluation`](GLOSSARY.md#applicative-order-evaluation) [`successive squaring`](GLOSSARY.md#successive-squaring)
