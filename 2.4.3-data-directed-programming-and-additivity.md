# 2.4.3 Lập trình hướng dữ liệu và tính mở rộng

Chiến lược tổng quát là kiểm tra kiểu của một dữ liệu rồi gọi quy trình tương ứng — ta gọi kỹ thuật này là _phân nhánh theo kiểu_ (dispatching on type). Đây là một cách hay để đạt được tính mô đun, nhưng cài đặt trong mục 2.4.2 vẫn còn hai điểm yếu.

Điểm yếu thứ nhất: các quy trình giao diện tổng quát (`real-part`, `imag-part`, v.v.) phải biết về tất cả các cách biểu diễn. Mỗi khi thêm một cách biểu diễn mới, ta phải định danh nó với một thẻ kiểu, rồi thêm nhánh điều kiện vào mỗi quy trình giao diện tổng quát.

Điểm yếu thứ hai: dù các cách biểu diễn có thể được thiết kế độc lập với nhau, ta vẫn phải đảm bảo không có hai **quy trình** nào trong toàn hệ thống trùng tên — chính vì vậy Ben và Alyssa mới phải đổi tên các quy trình ban đầu của mình.

Cả hai điểm yếu nằm ở chỗ kỹ thuật này _không mở rộng được_ (non-additive). Người cài đặt các bộ chọn tổng quát phải sửa đổi chúng mỗi khi có thêm cách biểu diễn mới; người tạo ra cách biểu diễn phải sửa đổi code để tránh xung đột tên. Với chỉ hai cách biểu diễn thì còn quản lý được, nhưng khi hệ thống có hàng trăm kiểu và hàng chục bộ chọn tổng quát, vấn đề trở nên rất nan giải. Chưa kể, sẽ không có giao diện duy nhất để ai đó có thể tích hợp một gói mới vào một hệ thống đã hoạt động.

_Lập trình hướng dữ liệu_ (data-directed programming) là một kỹ thuật giúp ta lập trình trực tiếp với bảng hai chiều phép tính — kiểu đó. Quan sát then chốt là tập hợp các phép tính tổng quát dùng chung cho nhiều kiểu khác nhau có thể hình dung như một bảng, với các phép tính trên một trục và các kiểu trên trục còn lại. Các ô trong bảng là các quy trình cài đặt từng phép tính cho từng kiểu.

Thay vì phân tán sự tương ứng này vào nhiều nhánh điều kiện, lập trình hướng dữ liệu dùng bảng đó một cách trực tiếp. Một quy trình duy nhất tra cứu cặp (phép tính, kiểu) để tìm **quy trình** phù hợp, rồi áp dụng nó. Muốn thêm một cách biểu diễn mới, ta chỉ cần thêm các hàng mới vào bảng — không cần sửa đổi bất kỳ **quy trình** hiện có nào.

## Cài đặt

Để cài đặt giao diện bảng, ta giả sử đã có hai **quy trình** thao tác với bảng phép tính—kiểu:

- `(put ⟨op⟩ ⟨type⟩ ⟨item⟩)` ghi `item` vào bảng, được đánh chỉ mục bởi `op` và `type`.
- `(get ⟨op⟩ ⟨type⟩)` tra cứu và trả về phần tử ứng với `op` và `type`, hoặc trả về giá trị sai nếu không tìm thấy.

Chương 3 sẽ chỉ cho ta cách cài đặt bảng này. Hiện tại, ta cứ coi chúng là những **quy trình** đã có sẵn.

### Gói chữ nhật của Ben

```lisp
(define (install-rectangular-package)
  ;; các quy trình nội bộ
  (define (real-part z) (car z))
  (define (imag-part z) (cdr z))
  (define (make-from-real-imag x y)
    (cons x y))
  (define (magnitude z)
    (sqrt (+ (square (real-part z))
             (square (imag-part z)))))
  (define (angle z)
    (atan (imag-part z) (real-part z)))
  (define (make-from-mag-ang r a)
    (cons (* r (cos a)) (* r (sin a))))
  ;; giao diện với phần còn lại của hệ thống
  (define (tag x)
    (attach-tag 'rectangular x))
  (put 'real-part '(rectangular) real-part)
  (put 'imag-part '(rectangular) imag-part)
  (put 'magnitude '(rectangular) magnitude)
  (put 'angle '(rectangular) angle)
  (put 'make-from-real-imag 'rectangular
       (lambda (x y)
         (tag (make-from-real-imag x y))))
  (put 'make-from-mag-ang 'rectangular
       (lambda (r a)
         (tag (make-from-mag-ang r a))))
  'done)
```

Lưu ý rằng các quy trình nội bộ hoàn toàn giống với mục 2.4.1 — Ben không cần lo xung đột tên vì chúng được định nghĩa bên trong `install-rectangular-package`. Phần giao diện ghi quy trình `real-part` của Ben vào bảng với tên phép tính là `real-part` và kiểu là `(rectangular)`, tương tự với các bộ chọn còn lại. Các bộ tạo thì y hệt, trừ việc chúng gắn thêm thẻ kiểu.[^1][^2]

### Gói cực của Alyssa

```lisp
(define (install-polar-package)
  ;; các quy trình nội bộ
  (define (magnitude z) (car z))
  (define (angle z) (cdr z))
  (define (make-from-mag-ang r a) (cons r a))
  (define (real-part z)
    (* (magnitude z) (cos (angle z))))
  (define (imag-part z)
    (* (magnitude z) (sin (angle z))))
  (define (make-from-real-imag x y)
    (cons (sqrt (+ (square x) (square y)))
          (atan y x)))
  ;; giao diện với phần còn lại của hệ thống
  (define (tag x) (attach-tag 'polar x))
  (put 'real-part '(polar) real-part)
  (put 'imag-part '(polar) imag-part)
  (put 'magnitude '(polar) magnitude)
  (put 'angle '(polar) angle)
  (put 'make-from-real-imag 'polar
       (lambda (x y)
         (tag (make-from-real-imag x y))))
  (put 'make-from-mag-ang 'polar
       (lambda (r a)
         (tag (make-from-mag-ang r a))))
  'done)
```

Dù Ben và Alyssa đều định nghĩa các **quy trình** cùng tên (chẳng hạn `real-part`), nhưng chúng nằm bên trong các **quy trình** khác nhau nên không hề có xung đột (xem mục 1.1.8).

### Bộ chọn tổng quát

Các bộ chọn cho số phức truy cập vào bảng thông qua `apply-generic` — một **quy trình** áp dụng một phép tính tổng quát lên các đối số của nó:[^3]

```lisp
(define (apply-generic op . args)
  (let ((type-tags (map type-tag args)))
    (let ((proc (get op type-tags)))
      (if proc
          (apply proc (map contents args))
          (error
            "No method for these types: APPLY-GENERIC"
            (list op type-tags))))))
```

Với `apply-generic`, ta định nghĩa các bộ chọn tổng quát như sau:

```lisp
(define (real-part z)
  (apply-generic 'real-part z))
(define (imag-part z)
  (apply-generic 'imag-part z))
(define (magnitude z)
  (apply-generic 'magnitude z))
(define (angle z)
  (apply-generic 'angle z))
```

Các **quy trình** này không cần thay đổi khi ta thêm một cách biểu diễn mới vào hệ thống.

### Bộ tạo dùng từ bên ngoài

Ta cũng cần các bộ tạo ở tầng ngoài — tạo số phức dạng chữ nhật từ phần thực và phần ảo, hoặc dạng cực từ mô đun và góc:

```lisp
(define (make-from-real-imag x y)
  ((get 'make-from-real-imag 'rectangular) x y))

(define (make-from-mag-ang r a)
  ((get 'make-from-mag-ang 'polar) r a))
```

## Truyền thông điệp

Còn một cách tiếp cận khác cũng đáng chú ý: phân rã bảng phép tính—kiểu theo từng cột, rồi dùng _đối tượng dữ liệu thông minh_ (intelligent data objects) — thay vì có phép tính thông minh phân nhánh theo kiểu dữ liệu, ta để chính đối tượng dữ liệu phân nhánh theo tên phép tính.

Chẳng hạn, một số phức dạng chữ nhật được biểu diễn như một **quy trình** nhận tên phép tính cần thực hiện:

```lisp
(define (make-from-real-imag x y)
  (define (dispatch op)
    (cond ((eq? op 'real-part) x)
          ((eq? op 'imag-part) y)
          ((eq? op 'magnitude)
           (sqrt (+ (square x) (square y))))
          ((eq? op 'angle) (atan y x))
          (else
           (error "Unknown op: MAKE-FROM-REAL-IMAG" op))))
  dispatch)
```

`apply-generic` tương ứng chỉ đơn giản là chuyển tên phép tính cho đối tượng dữ liệu:

```lisp
(define (apply-generic op arg) (arg op))
```

Giá trị trả về của `make-from-real-imag` chính là **quy trình** `dispatch` bên trong, và nó sẽ được gọi mỗi khi `apply-generic` yêu cầu một phép tính.

Phong cách này được gọi là _truyền thông điệp_ (message passing). Cái tên phản ánh hình ảnh đối tượng dữ liệu nhận tên phép tính được yêu cầu như một "thông điệp". Thực ra ta đã thấy một ví dụ ở mục 2.1.3, khi `cons`, `car` và `cdr` được định nghĩa hoàn toàn bằng **quy trình**. Truyền thông điệp không chỉ là trò ảo thuật toán học — đây là một kỹ thuật hữu ích để tổ chức các hệ thống phép tính tổng quát. Chương này sẽ tiếp tục dùng lập trình hướng dữ liệu cho hệ số học tổng quát, nhưng Chương 3 sẽ quay lại truyền thông điệp và cho thấy sức mạnh của nó trong việc xây dựng các chương trình mô phỏng.[^4]

[^1]: Ta dùng danh sách `(rectangular)` thay vì ký hiệu `rectangular` để dành chỗ cho khả năng có các phép tính nhận nhiều đối số, không nhất thiết cùng kiểu.

[^2]: Kiểu mà các bộ tạo được đăng ký không cần là một danh sách vì bộ tạo luôn được dùng để tạo đối tượng của đúng một kiểu cụ thể.

[^3]: `apply-generic` dùng ký hiệu đuôi chấm trong danh sách tham số vì các phép tính tổng quát khác nhau có thể nhận số lượng đối số khác nhau. Trong `apply-generic`, `op` nhận giá trị là đối số đầu tiên, còn `args` là một danh sách các đối số còn lại. `apply-generic` cũng dùng quy trình nguồn `apply`, nhận hai đối số là một **quy trình** và một danh sách, rồi áp dụng **quy trình** đó với các phần tử của danh sách làm đối số. Ví dụ: `(apply + (list 1 2 3 4))` trả về `10`.

[^4]: Một hạn chế của cách tổ chức này là nó chỉ cho phép các phép tính tổng quát nhận đúng một đối số.

---

[exercise 2.73](exercises/2.73.md) [exercise 2.74](exercises/2.74.md) [exercise 2.75](exercises/2.75.md) [exercise 2.76](exercises/2.76.md) [`data-directed`](GLOSSARY.md#data-directed) [`procedure`](GLOSSARY.md#procedure) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`module`](GLOSSARY.md#module) [`argument`](GLOSSARY.md#argument) [`data`](GLOSSARY.md#data)
