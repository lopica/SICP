# 2.1.2 Hàng rào trừu tượng

Trước khi tiếp tục với thêm nhiều ví dụ về dữ liệu tổng hợp và trừu tượng hóa dữ liệu, hãy cùng xem xét một số vấn đề mà ví dụ số hữu tỉ đặt ra. Chúng ta đã định nghĩa các phép tính trên số hữu tỉ dựa trên một hàm tạo `make-rat` và hai bộ chọn `numer` và `denom`. Về mặt tổng quát, ý tưởng cốt lõi của _trừu tượng hóa dữ liệu_ là: với mỗi kiểu đối tượng dữ liệu, ta xác định một tập hợp các phép tính cơ bản, và toàn bộ việc thao túng các đối tượng đó sẽ chỉ được biểu diễn thông qua những phép tính đó — không dùng gì khác.

Ta có thể hình dung cấu trúc của hệ thống số hữu tỉ như trong Hình 2.1. Các đường ngang biểu diễn những _hàng rào trừu tượng_ tách biệt các "tầng" khác nhau của hệ thống. Ở mỗi tầng, hàng rào ngăn cách các chương trình ở trên (phần sử dụng trừu tượng hóa dữ liệu) với các chương trình ở dưới (phần triển khai trừu tượng hóa dữ liệu). Các chương trình sử dụng số hữu tỉ chỉ thao tác với chúng thông qua các quy trình được "công bố" bởi gói số hữu tỉ: `add-rat`, `sub-rat`, `mul-rat`, `div-rat`, và `equal-rat?`. Những quy trình này, đến lượt mình, chỉ được triển khai dựa trên hàm tạo và bộ chọn `make-rat`, `numer`, và `denom`, mà chính chúng lại được triển khai dựa trên cặp giá trị thông qua `cons`, `car`, và `cdr`. Chi tiết về cách cặp giá trị được triển khai không liên quan đến phần còn lại của gói số hữu tỉ, miễn là cặp giá trị có thể được thao tác bởi `cons`, `car`, và `cdr`. Thực chất, các quy trình ở mỗi tầng chính là giao diện định nghĩa các hàng rào trừu tượng và kết nối các tầng khác nhau với nhau.

![Các hàng rào trừu tượng hóa dữ liệu trong gói số hữu tỉ](./figures/figure6.png)

Ý tưởng đơn giản này có nhiều ưu điểm. Một ưu điểm là nó giúp các chương trình dễ bảo trì và dễ chỉnh sửa hơn nhiều. Bất kỳ cấu trúc dữ liệu phức tạp nào cũng có thể được biểu diễn theo nhiều cách khác nhau dựa trên các cấu trúc **dữ liệu** nguồn mà ngôn ngữ lập trình cung cấp. Tất nhiên, việc chọn cách biểu diễn nào sẽ ảnh hưởng đến các chương trình thao tác trên nó; do đó, nếu cách biểu diễn thay đổi ở một thời điểm nào đó trong tương lai, tất cả các chương trình đó có thể sẽ phải sửa theo. Công việc này có thể tốn rất nhiều thời gian và công sức đối với các chương trình lớn — trừ khi sự phụ thuộc vào cách biểu diễn được thiết kế để chỉ gói gọn trong rất ít mô đun chương trình.

Ví dụ, một cách khác để giải quyết bài toán rút gọn số hữu tỉ về dạng tối giản là thực hiện việc rút gọn mỗi khi ta truy cập các thành phần của số hữu tỉ, thay vì khi ta xây dựng nó. Cách này dẫn đến bộ hàm tạo và bộ chọn khác:

```lisp
(define (make-rat n d)
  (cons n d))

(define (numer x)
  (let ((g (gcd (car x) (cdr x))))
    (/ (car x) g)))

(define (denom x)
  (let ((g (gcd (car x) (cdr x))))
    (/ (cdr x) g)))
```

Sự khác biệt giữa cách triển khai này và cách trước nằm ở thời điểm ta tính `gcd`. Nếu trong cách sử dụng điển hình của chúng ta, ta truy cập tử số và mẫu số của cùng một số hữu tỉ nhiều lần, thì sẽ tốt hơn nếu tính `gcd` ngay khi xây dựng số hữu tỉ. Nếu không, ta có thể sẽ tốt hơn nếu trì hoãn việc tính `gcd` đến lúc truy cập. Dù chọn cách nào, khi ta chuyển từ cách biểu diễn này sang cách biểu diễn kia, các quy trình `add-rat`, `sub-rat`, v.v. không cần sửa gì cả.

Việc giới hạn sự phụ thuộc vào cách biểu diễn trong một vài quy trình giao diện không chỉ giúp ta bảo trì chương trình mà còn giúp ta thiết kế nó, vì nó cho phép ta duy trì sự linh hoạt để xem xét các cách triển khai thay thế. Để tiếp tục với ví dụ đơn giản này, giả sử ta đang thiết kế một gói số hữu tỉ và ban đầu chưa quyết định được nên tính `gcd` lúc xây dựng hay lúc truy cập. Phương pháp luận trừu tượng hóa dữ liệu cho ta cách trì hoãn quyết định đó mà vẫn tiếp tục tiến triển trên phần còn lại của hệ thống.

---

> **Lưu ý:** Hình 2.1 (sơ đồ các hàng rào trừu tượng trong gói số hữu tỉ) được lưu tại `./figures/figure6.png`. Bạn cần thêm ảnh này từ nguồn gốc SICP.

---

[exercise 2.2](exercises/2.2.md) [exercise 2.3](exercises/2.3.md) [`data abstraction`](GLOSSARY.md#data-abstraction) [`abstraction barrier`](GLOSSARY.md#abstraction-barrier) [`data`](GLOSSARY.md#data) [`primitive data`](GLOSSARY.md#primitive-data) [`procedure`](GLOSSARY.md#procedure)
