# 3.5.2 Dòng chảy vô hạn

Tính năng thú vị nhất của _thực thi trì hoãn_ là nó cho phép ta biểu diễn các dãy **vô hạn** dưới dạng **dòng chảy**. Ví dụ, hãy xét **dòng chảy** các số nguyên dương sau:

```lisp
(define (integers-starting-from n)
  (cons-stream n (integers-starting-from (+ n 1))))

(define integers (integers-starting-from 1))
```

Định nghĩa này hợp lệ vì `integers` là một cặp mà `car` của nó là `1` còn `cdr` là một lời hứa sẽ sinh ra các số nguyên bắt đầu từ `2`. Đây là một **dòng chảy** dài vô hạn, nhưng tại bất kỳ thời điểm nào ta cũng chỉ khảo sát được một phần hữu hạn của nó.

Có `integers` rồi, ta có thể xây dựng những **dòng chảy** khác từ đó. Chẳng hạn:

```lisp
(define (divisible? x y) (= (remainder x y) 0))

(define no-sevens
  (stream-filter (lambda (x) (not (divisible? x 7)))
                 integers))
```

Kết quả là `(stream-ref no-sevens 100)` cho ra `117`.

Tương tự, ta có thể định nghĩa **dòng chảy** Fibonacci:

```lisp
(define (fibgen a b)
  (cons-stream a (fibgen b (+ a b))))

(define fibs (fibgen 0 1))
```

## Sàng Eratosthenes

Một ví dụ ấn tượng hơn là sinh số nguyên tố bằng _sàng Eratosthenes_.[^1] Ta bắt đầu với **dòng chảy** các số nguyên từ `2`. Để _sàng_ một **dòng chảy** $S$: lấy phần tử đầu của $S$ làm số nguyên tố tiếp theo, rồi lọc bỏ tất cả bội số của nó khỏi phần còn lại của $S$, và tiếp tục sàng kết quả đó.

```lisp
(define (sieve stream)
  (cons-stream
   (stream-car stream)
   (sieve (stream-filter
           (lambda (x)
             (not (divisible? x (stream-car stream))))
           (stream-cdr stream)))))

(define primes (sieve (integers-starting-from 2)))
```

Chẳng hạn, `(stream-ref primes 50)` cho ra `233`.

Điều thú vị ở đây là không chỉ **dòng chảy** là vô hạn — bản thân bộ xử lý tín hiệu cũng vô hạn, vì mỗi `sieve` lại chứa một `sieve` bên trong nó. Hình 3.31 là _sơ đồ Henderson_ mô tả quá trình này.[^2]

![Sơ đồ Henderson minh họa quá trình sàng số nguyên tố](./figures/figure70.png)

## Định nghĩa dòng chảy theo kiểu ngầm định

Ở trên, `integers` và `fibs` được định nghĩa bằng các **quy trình** sinh phần tử theo từng bước. Còn một cách khác: tận dụng _thực thi trì hoãn_ để định nghĩa **dòng chảy** theo kiểu _ngầm định_ — tức là tự tham chiếu đến chính nó.

Ví dụ đơn giản nhất là **dòng chảy** các số `1` vô tận:

```lisp
(define ones (cons-stream 1 ones))
```

Cái này hoạt động giống như **quy trình** đệ quy: `ones` là một cặp có `car` là `1` và `cdr` là lời hứa sẽ tính lại `ones`. Khi `cdr` được tính, ta lại nhận được `1` và thêm một lời hứa tính `ones`, cứ thế mãi.

Ta có thể khai thác điều này để xây dựng **quy trình** `add-streams` cộng từng phần tử tương ứng của hai **dòng chảy**:[^3]

```lisp
(define (add-streams s1 s2)
  (stream-map + s1 s2))
```

Giờ thì `integers` có thể định nghĩa lại một cách thanh lịch:

```lisp
(define integers
  (cons-stream 1 (add-streams ones integers)))
```

Định nghĩa này nói: `integers` là **dòng chảy** bắt đầu bằng `1`, phần còn lại thu được bằng cách cộng `ones` vào `integers`. Lúc này phải đảm bảo rằng tại bất kỳ thời điểm nào, đủ phần tử của `integers` đã được sinh ra để có thể cung cấp cho chính nó.

**Dòng chảy** Fibonacci cũng định nghĩa được theo kiểu ngầm định:

```lisp
(define fibs
  (cons-stream
   0
   (cons-stream
    1
    (add-streams (stream-cdr fibs) fibs))))
```

Định nghĩa này nói: `fibs` bắt đầu bằng `0` và `1`; phần còn lại là tổng của `fibs` với `fibs` dịch chuyển đi một vị trí.

Tiếp theo, ta định nghĩa **quy trình** `scale-stream` để nhân mỗi phần tử với một hệ số:

```lisp
(define (scale-stream stream factor)
  (stream-map (lambda (x) (* x factor)) stream))
```

Và **dòng chảy** các lũy thừa của 2:

```lisp
(define double
  (cons-stream 1 (scale-stream double 2)))
```

---

Sau đây là một định nghĩa khác của **dòng chảy** số nguyên tố, dùng **mệnh đề điều kiện** `prime?`:

```lisp
(define primes
  (cons-stream
   2
   (stream-filter prime? (integers-starting-from 3))))

(define (prime? n)
  (define (iter ps)
    (cond ((> (square (stream-car ps)) n) true)
          ((divisible? n (stream-car ps)) false)
          (else (iter (stream-cdr ps)))))
  (iter primes))
```

Đây là định nghĩa đệ quy: `primes` được định nghĩa dựa trên `prime?`, mà `prime?` lại dùng chính `primes`.[^4] Điều này hoạt động được vì khi kiểm tra xem $n$ có phải số nguyên tố không, ta chỉ cần các số nguyên tố nhỏ hơn hoặc bằng $\sqrt{n}$, và những số đó đã được sinh ra rồi.

[^1]: Thực ra đây không đúng là sàng mà _số học gia_ Eratosthenes đã dùng. Sàng thực sự hoạt động bằng cách đánh dấu bội số trực tiếp trên danh sách số nguyên, còn phiên bản dùng **dòng chảy** ở đây có cơ chế khác. Xem Hưởng (1990) để biết thêm chi tiết về sự khác biệt.

[^2]: Cách nhìn hệ thống xử lý tín hiệu được gọi theo tên của Peter Henderson, người đã đề xuất nó.

[^3]: Đây là định nghĩa `add-streams` tổng quát. Trong một số phiên bản trước của chương trình, `add-streams` đôi khi được định nghĩa hẹp hơn chỉ cho hai đối số kiểu số.

[^4]: Cụ thể hơn: với mọi $n$ cần kiểm tra, hoặc $n$ không phải số nguyên tố (và đã có số nguyên tố nhỏ hơn $n$ chia hết được cho $n$ đã được sinh ra), hoặc $n$ là số nguyên tố (và đã có số nguyên tố nhỏ hơn $n$ lớn hơn $\sqrt{n}$ được sinh ra rồi).

---

[exercise 3.53](exercises/3.53.md) [exercise 3.54](exercises/3.54.md) [exercise 3.55](exercises/3.55.md) [exercise 3.56](exercises/3.56.md) [exercise 3.57](exercises/3.57.md) [exercise 3.58](exercises/3.58.md) [exercise 3.59](exercises/3.59.md) [exercise 3.60](exercises/3.60.md) [exercise 3.61](exercises/3.61.md) [exercise 3.62](exercises/3.62.md) [`stream`](GLOSSARY.md#stream) [`sequence`](GLOSSARY.md#sequence) [`procedure`](GLOSSARY.md#procedure) [`predicate`](GLOSSARY.md#predicate) [`pair`](GLOSSARY.md#pair) [`list`](GLOSSARY.md#list) [`recursive`](GLOSSARY.md#recursive) [`recursive-process`](GLOSSARY.md#recursive-process)
