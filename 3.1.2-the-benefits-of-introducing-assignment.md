# 3.1.2 Lợi ích của việc đưa vào phép gán

Như ta sẽ thấy, việc đưa phép gán vào ngôn ngữ lập trình dẫn chúng ta vào một mê cung đầy những vấn đề khái niệm nan giải. Dù vậy, cách nhìn hệ thống như một tập hợp các đối tượng có **trạng thái** cục bộ là một kỹ thuật mạnh mẽ để duy trì thiết kế mô đun. Hãy xét một ví dụ đơn giản: thiết kế một **quy trình** `rand` mà mỗi lần gọi đều trả về một số nguyên được chọn ngẫu nhiên.

Cụm từ "được chọn ngẫu nhiên" có nghĩa là gì? Điều ta muốn là các lần gọi liên tiếp đến `rand` sẽ tạo ra một dãy số có tính chất thống kê của phân phối đều. Ta sẽ không bàn đến cách sinh ra các dãy như vậy ở đây. Thay vào đó, hãy giả sử ta có sẵn một **quy trình** `rand-update` với tính chất: nếu bắt đầu từ một giá trị $x_1$ và lần lượt tính

$$x_2 = \texttt{(rand-update } x_1\texttt{)}$$
$$x_3 = \texttt{(rand-update } x_2\texttt{)}$$

thì dãy các giá trị $x_1, x_2, x_3, \ldots$ sẽ có đúng tính chất thống kê mong muốn.[^1]

Ta có thể cài đặt `rand` như một **quy trình** có biến trạng thái cục bộ `x` được khởi tạo bằng một giá trị cố định `random-init`. Mỗi lần gọi `rand`, ta tính `rand-update` trên giá trị hiện tại của `x`, trả về kết quả đó như số ngẫu nhiên, đồng thời lưu lại giá trị mới vào `x`:

```lisp
(define rand
  (let ((x random-init))
    (lambda () (set! x (rand-update x)) x)))
```

Tất nhiên ta cũng có thể sinh ra cùng dãy số ngẫu nhiên đó mà không dùng phép gán, bằng cách gọi trực tiếp `rand-update`. Tuy nhiên, khi đó bất kỳ phần nào trong chương trình cần số ngẫu nhiên đều phải tự nhớ và truyền giá trị hiện tại của `x` cho `rand-update`. Để thấy điều đó phiền phức như thế nào, hãy xét việc dùng số ngẫu nhiên để cài đặt một kỹ thuật gọi là _mô phỏng Monte Carlo_.

Phương pháp Monte Carlo bao gồm việc chọn ngẫu nhiên các thí nghiệm mẫu từ một tập lớn, rồi rút ra các kết luận dựa trên xác suất ước lượng từ kết quả của những thí nghiệm đó. Ví dụ, ta có thể xấp xỉ $\pi$ nhờ vào thực tế rằng $6/\pi^2$ là xác suất để hai số nguyên được chọn ngẫu nhiên không có ước số chung, tức là ước số chung lớn nhất của chúng bằng 1.[^2] Để thu được xấp xỉ của $\pi$, ta tiến hành một số lượng lớn thí nghiệm. Trong mỗi thí nghiệm, ta chọn ngẫu nhiên hai số nguyên và kiểm tra xem ước số chung lớn nhất của chúng có bằng 1 hay không. Tỉ lệ các thí nghiệm thỏa mãn điều kiện cho ta ước lượng $6/\pi^2$, từ đó tính ra $\pi$.

Trung tâm của chương trình là **quy trình** `monte-carlo`, nhận vào số lần thử nghiệm và bản thân thí nghiệm — biểu diễn bằng một **quy trình** không có đối số, mỗi lần gọi trả về đúng hoặc sai. `monte-carlo` chạy thí nghiệm đủ số lần và trả về tỉ lệ các lần thí nghiệm thành công:

```lisp
(define (estimate-pi trials)
  (sqrt (/ 6 (monte-carlo trials cesaro-test))))

(define (cesaro-test)
  (= (gcd (rand) (rand)) 1))

(define (monte-carlo trials experiment)
  (define (iter trials-remaining trials-passed)
    (cond ((= trials-remaining 0)
           (/ trials-passed trials))
          ((experiment)
           (iter (- trials-remaining 1)
                 (+ trials-passed 1)))
          (else
           (iter (- trials-remaining 1)
                 trials-passed))))
  (iter trials 0))
```

Bây giờ hãy thử thực hiện phép tính tương tự nhưng dùng trực tiếp `rand-update` thay vì `rand` — như ta sẽ bị buộc phải làm nếu không dùng phép gán để mô hình hóa trạng thái cục bộ:

```lisp
(define (estimate-pi trials)
  (sqrt (/ 6 (random-gcd-test trials random-init))))

(define (random-gcd-test trials initial-x)
  (define (iter trials-remaining
                trials-passed
                x)
    (let ((x1 (rand-update x)))
      (let ((x2 (rand-update x1)))
        (cond ((= trials-remaining 0)
               (/ trials-passed trials))
              ((= (gcd x1 x2) 1)
               (iter (- trials-remaining 1)
                     (+ trials-passed 1)
                     x2))
              (else
               (iter (- trials-remaining 1)
                     trials-passed
                     x2))))))
  (iter trials 0 initial-x))
```

Chương trình này tuy vẫn đơn giản, nhưng đã cho thấy những vết nứt đáng lo ngại về tính mô đun. Trong phiên bản đầu dùng `rand`, ta có thể biểu diễn trực tiếp phương pháp Monte Carlo như một **quy trình** `monte-carlo` tổng quát nhận bất kỳ **quy trình** `experiment` nào làm đối số. Trong phiên bản thứ hai, vì bộ sinh số ngẫu nhiên không có trạng thái cục bộ, `random-gcd-test` phải xử lý thủ công hai số ngẫu nhiên `x1` và `x2`, và phải đưa `x2` qua vòng lặp để làm đầu vào tiếp theo cho `rand-update`. Cách xử lý tường minh này làm cho cấu trúc tích lũy kết quả thí nghiệm bị ràng buộc với chi tiết của thí nghiệm cụ thể — rằng ta cần đúng hai số ngẫu nhiên — trong khi các thí nghiệm Monte Carlo khác có thể cần một hoặc ba số. Ngay cả **quy trình** cấp cao nhất `estimate-pi` cũng phải lo cung cấp giá trị khởi tạo cho bộ sinh số ngẫu nhiên. Khi nội bộ của bộ sinh số ngẫu nhiên rò rỉ ra các phần khác của chương trình, ta rất khó có thể cô lập ý tưởng Monte Carlo để áp dụng cho các bài toán khác. Trong phiên bản đầu, phép gán đã đóng gói trạng thái của bộ sinh số ngẫu nhiên bên trong **quy trình** `rand`, khiến chi tiết sinh số ngẫu nhiên không còn ảnh hưởng đến phần còn lại của chương trình.

Hiện tượng tổng quát mà ví dụ Monte Carlo minh họa là: xét từ góc nhìn của một phần trong một **tiến trình** phức tạp, các phần khác có vẻ như thay đổi theo thời gian — chúng có trạng thái cục bộ thay đổi ẩn bên trong. Nếu ta muốn viết các **chương trình** máy tính phản ánh sự phân rã này, ta tạo ra các đối tượng tính toán (như tài khoản ngân hàng hay bộ sinh số ngẫu nhiên) mà hành vi của chúng thay đổi theo thời gian. Ta mô hình hóa trạng thái bằng các **biến trạng thái** cục bộ, và mô hình hóa sự thay đổi trạng thái bằng phép gán vào những biến đó.

Thật hấp dẫn khi kết luận rằng: bằng cách đưa vào phép gán và kỹ thuật che giấu trạng thái trong **biến** cục bộ, ta có thể cấu trúc hệ thống theo lối mô đun hơn so với cách thao tác trực tiếp mọi trạng thái bằng cách truyền thêm **tham số**. Tiếc thay, như ta sẽ thấy, câu chuyện không đơn giản như vậy.

[^1]: Bất kỳ dãy "số ngẫu nhiên" nào được sinh ra bởi một thuật toán xác định đều thực chất là _giả ngẫu nhiên_. Mặc dù tính chất thống kê của chúng có thể rất tốt, nhưng dãy đó không thực sự ngẫu nhiên theo nghĩa thông thường của từ này. Vấn đề sinh số ngẫu nhiên có chất lượng cao là đề tài nghiên cứu lâu dài. Knuth (1981) có một chương tổng quan về vấn đề này; xem phần 3.2.2 trong sách đó để biết thêm về lý thuyết và thực hành. Một phương pháp phổ biến là dùng quy tắc $x \leftarrow ax + b \pmod{m}$, trong đó $a$, $b$, $m$ là các hằng số được chọn cẩn thận.

[^2]: Định lý này là do Cesàro phát biểu (xem Knuth 1981, phần 4.5.2). Kết quả chính xác hơn: xác suất để hai số nguyên dương chọn ngẫu nhiên (theo phân phối đều) có ước số chung lớn nhất bằng 1 là $6/\pi^2$.

[^3]: MIT Scheme cung cấp một **quy trình** `random` trả về một số không âm nhỏ hơn đầu vào của nó. Nếu đầu vào là số nguyên, kết quả là số nguyên; nếu đầu vào là số thực, kết quả là số thực.

---

[exercise 3.3](exercises/3.3.md) [exercise 3.4](exercises/3.4.md) [`procedure`](GLOSSARY.md#procedure) [`variable`](GLOSSARY.md#variable) [`state-variables`](GLOSSARY.md#state-variables) [`local`](GLOSSARY.md#local) [`environment`](GLOSSARY.md#environment) [`module`](GLOSSARY.md#module) [`parameter`](GLOSSARY.md#parameter) [`process`](GLOSSARY.md#process) [`program`](GLOSSARY.md#program)
