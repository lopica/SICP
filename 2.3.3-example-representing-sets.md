# 2.3.3 Ví dụ: Biểu diễn tập hợp

Trong các ví dụ trước, ta đã kết hợp các cặp để biểu diễn nhiều loại dữ liệu như số hữu tỉ hay cây. Bây giờ hãy xem xét một loại dữ liệu thú vị hơn: tập hợp. Về mặt hình thức, một _tập hợp_ là một bộ sưu tập phần tử phân biệt. Ngôn ngữ Scheme đã có sẵn các kiểu dữ liệu tổng hợp như danh sách, vậy tại sao không dùng luôn? Câu trả lời là ở đây ta muốn trừu tượng hóa: không phụ thuộc vào cách biểu diễn cụ thể, mà chỉ làm việc qua các **quy trình** sau:

- `union-set`: hợp của hai tập hợp
- `intersection-set`: giao của hai tập hợp
- `element-of-set?`: kiểm tra một phần tử có thuộc tập hợp không
- `adjoin-set`: thêm một phần tử vào tập hợp

Nhờ _trừu tượng hóa dữ liệu_, ta có thể thay thế cách biểu diễn bất cứ lúc nào mà không cần sửa code dùng các phép toán đó. Mục này sẽ lần lượt khảo sát ba cách biểu diễn tập hợp, mỗi cách có ưu nhược điểm khác nhau.

## Tập hợp như danh sách không có thứ tự

Cách đơn giản nhất để biểu diễn tập hợp là dùng một _danh sách_ các phần tử, trong đó không có phần tử nào xuất hiện quá một lần. Phần tử rỗng được biểu diễn bằng danh sách rỗng.

`element-of-set?` kiểm tra một phần tử có trong danh sách không:

```lisp
(define (element-of-set? x set)
  (cond ((null? set) false)
        ((equal? x (car set)) true)
        (else (element-of-set? x (cdr set)))))
```

Dùng `element-of-set?`, ta viết được `adjoin-set`: nếu phần tử chưa có thì thêm vào đầu, ngược lại giữ nguyên:

```lisp
(define (adjoin-set x set)
  (if (element-of-set? x set)
      set
      (cons x set)))
```

`intersection-set` xây dựng giao tập bằng cách lọc các phần tử của tập thứ nhất xem có thuộc tập thứ hai không:

```lisp
(define (intersection-set set1 set2)
  (cond ((or (null? set1) (null? set2)) '())
        ((element-of-set? (car set1) set2)
         (cons (car set1)
               (intersection-set (cdr set1) set2)))
        (else (intersection-set (cdr set1) set2))))
```

Khi thiết kế một cách biểu diễn, ta cần chú ý đến hiệu năng. Hãy xét `element-of-set?`: trong trường hợp xấu nhất, ta phải duyệt toàn bộ danh sách, nên số bước tăng theo $\Theta(n)$ với $n$ là số phần tử. `adjoin-set` cũng dùng `element-of-set?` nên cũng là $\Theta(n)$. Còn `intersection-set` duyệt lồng nhau: với mỗi phần tử của `set1` ta lại duyệt qua `set2`, nên tổng số bước là $\Theta(n^2)$.

**Bài tập 2.59–2.60** sẽ yêu cầu bạn cài đặt `union-set` và khảo sát phiên bản cho phép trùng lặp.

## Tập hợp như danh sách có thứ tự

Một cách cải thiện hiệu năng là giữ danh sách theo thứ tự tăng dần. Để đơn giản, ta giả sử các phần tử là số nguyên, tuy nhiên ý tưởng mở rộng được cho mọi kiểu có quan hệ thứ tự.

Lợi thế rõ nhất xuất hiện ở `element-of-set?`: nếu phần tử cần tìm nhỏ hơn phần tử đầu danh sách, ta biết ngay nó không có mặt — không cần duyệt hết:

```lisp
(define (element-of-set? x set)
  (cond ((null? set) false)
        ((= x (car set)) true)
        ((< x (car set)) false)
        (else (element-of-set? x (cdr set)))))
```

Trung bình, quy trình này chỉ cần duyệt khoảng một nửa danh sách, vẫn là $\Theta(n)$, nhưng hằng số nhỏ hơn đáng kể.

Lợi thế lớn hơn thể hiện ở `intersection-set`. Thay vì kiểm tra từng phần tử của `set1` với toàn bộ `set2`, ta có thể tận dụng thứ tự để tiến hành song song:

```lisp
(define (intersection-set set1 set2)
  (if (or (null? set1) (null? set2))
      '()
      (let ((x1 (car set1)) (x2 (car set2)))
        (cond ((= x1 x2)
               (cons x1
                     (intersection-set (cdr set1)
                                       (cdr set2))))
              ((< x1 x2)
               (intersection-set (cdr set1) set2))
              ((< x2 x1)
               (intersection-set set1 (cdr set2)))))))
```

Tại mỗi bước, ta loại bỏ ít nhất một phần tử khỏi một trong hai danh sách, nên tổng số bước tỉ lệ với $n_1 + n_2$ thay vì $n_1 \cdot n_2$ — cải thiện từ $\Theta(n^2)$ xuống $\Theta(n)$.

**Bài tập 2.61–2.62** yêu cầu bạn cài đặt `adjoin-set` và `union-set` cho biểu diễn có thứ tự.

## Tập hợp như cây tìm kiếm nhị phân

Ta có thể làm tốt hơn nữa bằng cách sắp xếp các phần tử thành _cây tìm kiếm nhị phân_. Mỗi nút chứa một phần tử, cây con trái chứa các phần tử nhỏ hơn, cây con phải chứa các phần tử lớn hơn.

![Cây tìm kiếm nhị phân biểu diễn tập hợp {1, 3, 5, 7, 9, 11}](./figures/figure18.png)

Hình trên cho thấy có nhiều cây khác nhau cùng biểu diễn một tập hợp. Điều kiện bắt buộc là: với mỗi nút, mọi phần tử ở cây con trái nhỏ hơn nó, và mọi phần tử ở cây con phải lớn hơn nó.

Ta biểu diễn cây bằng danh sách: mỗi nút là một danh sách ba phần tử — giá trị tại nút, cây con trái, và cây con phải. Danh sách rỗng biểu diễn cây rỗng:

```lisp
(define (entry tree) (car tree))
(define (left-branch tree) (cadr tree))
(define (right-branch tree) (caddr tree))
(define (make-tree entry left right)
  (list entry left right))
```

Để kiểm tra một phần tử có trong cây không, ta so sánh nó với giá trị tại nút gốc. Nếu bằng, tìm thấy. Nếu nhỏ hơn, chỉ cần tìm trong cây con trái. Nếu lớn hơn, chỉ tìm trong cây con phải:

```lisp
(define (element-of-set? x set)
  (cond ((null? set) false)
        ((= x (entry set)) true)
        ((< x (entry set))
         (element-of-set? x (left-branch set)))
        ((> x (entry set))
         (element-of-set? x (right-branch set)))))
```

Mỗi bước, ta giảm bài toán xuống một cây con có kích thước bằng khoảng một nửa. Vì vậy, số bước cần thiết tăng theo $\Theta(\log n)$.[^1]

`adjoin-set` hoạt động tương tự: ta tìm vị trí đúng và chèn phần tử vào đó:

```lisp
(define (adjoin-set x set)
  (cond ((null? set) (make-tree x '() '()))
        ((= x (entry set)) set)
        ((< x (entry set))
         (make-tree (entry set)
                    (adjoin-set x (left-branch set))
                    (right-branch set)))
        ((> x (entry set))
         (make-tree (entry set)
                    (left-branch set)
                    (adjoin-set x (right-branch set))))))
```

Lưu ý rằng hai quy trình trên chỉ đạt $\Theta(\log n)$ khi cây _cân bằng_ — nghĩa là độ sâu hai cây con tại mỗi nút xấp xỉ nhau. Nếu cây mất cân bằng (ví dụ, ta chèn các phần tử theo thứ tự tăng dần), cây sẽ thoái hóa thành một danh sách và hiệu năng giảm xuống $\Theta(n)$.

Làm thế nào để giữ cây cân bằng? Có nhiều kỹ thuật, trong đó đơn giản nhất là định kỳ _sắp xếp lại_ (rebalance) cây. Cách tiếp cận chung: chuyển cây thành danh sách đã sắp xếp, rồi xây lại cây từ danh sách đó theo cách chia đôi đệ quy.

**Bài tập 2.63–2.65** yêu cầu bạn cài đặt chuyển đổi giữa cây và danh sách, và cài đặt `union-set` và `intersection-set` cho biểu diễn cây.

## Tập hợp và tra cứu thông tin

Ta có thể dùng kỹ thuật biểu diễn tập hợp trong rất nhiều bài toán thực tế. Ví dụ: một _cơ sở dữ liệu_ (database) lưu các bản ghi nhân viên có thể được tổ chức như một tập hợp các bản ghi. Mỗi bản ghi có thể bao gồm nhiều trường: số hiệu nhân viên, tên, địa chỉ, mức lương,... Để tra cứu bản ghi theo khóa (ví dụ số hiệu nhân viên), ta cần thao tác:

```lisp
(define (lookup given-key set-of-records)
  (cond ((null? set-of-records) false)
        ((equal? given-key (key (car set-of-records)))
         (car set-of-records))
        (else (lookup given-key (cdr set-of-records)))))
```

Tất nhiên, hiệu năng tra cứu phụ thuộc vào cách biểu diễn tập hợp. Với danh sách không có thứ tự, đây là $\Theta(n)$. Với cây tìm kiếm nhị phân cân bằng, ta giảm xuống $\Theta(\log n)$.

**Bài tập 2.66** yêu cầu bạn cài đặt `lookup` cho trường hợp tập hợp được biểu diễn bằng cây tìm kiếm nhị phân.

---

[^1]: Khẳng định này đúng với điều kiện cây đủ cân bằng — nghĩa là mỗi nút đều có cây con trái và cây con phải chứa xấp xỉ cùng số lượng phần tử. Cách đảm bảo cây luôn cân bằng là một vấn đề thú vị nhưng không đơn giản. Có những kỹ thuật giải quyết nó — các bài tập 2.63–2.65 sẽ đề cập đến một hướng tiếp cận.

---

[`set`](GLOSSARY.md#set) [`data abstraction`](GLOSSARY.md#data-abstraction) [`list`](GLOSSARY.md#list) [`pair`](GLOSSARY.md#pair) [`element`](GLOSSARY.md#element) [`procedure`](GLOSSARY.md#procedure) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`recursive process`](GLOSSARY.md#recursive-process) [`order of growth`](GLOSSARY.md#order-of-growth) [`binary search tree`](GLOSSARY.md#binary-search-tree)
