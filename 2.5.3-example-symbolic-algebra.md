# 2.5.3 Ví dụ: Đại số ký hiệu

Việc thao tác với các biểu thức đại số ký hiệu là một quá trình phức tạp, minh họa nhiều vấn đề khó nhất xuất hiện khi thiết kế các hệ thống lớn. Nói chung, một biểu thức đại số có thể được nhìn nhận như một cấu trúc phân cấp — một cây các phép toán áp dụng lên các toán hạng. Ta xây dựng biểu thức đại số bằng cách bắt đầu từ một tập các đối tượng nguồn như hằng số và biến số, rồi kết hợp chúng bằng các phép toán đại số như cộng và nhân. Cũng như trong các ngôn ngữ khác, ta tạo ra các trừu tượng để có thể gọi tên những đối tượng tổng hợp bằng những từ ngắn gọn. Các trừu tượng điển hình trong đại số ký hiệu bao gồm: tổ hợp tuyến tính, đa thức, hàm hữu tỉ, hay hàm lượng giác. Ta có thể coi những thứ này là các "kiểu" tổng hợp, thường rất hữu ích để định hướng quá trình xử lý biểu thức.

Chúng ta sẽ không cố xây dựng một hệ thống thao tác đại số hoàn chỉnh ở đây — những hệ thống như vậy cực kỳ phức tạp, đòi hỏi kiến thức đại số sâu và các thuật toán tinh tế. Thay vào đó, ta sẽ xem xét một phần nhỏ nhưng quan trọng của thao tác đại số: **phép tính số học trên đa thức**. Qua đó ta sẽ thấy những quyết định mà người thiết kế hệ thống như vậy phải đối mặt, và cách áp dụng các ý tưởng về dữ liệu trừu tượng và phép toán tổng quát để tổ chức công việc.

## Phép tính số học trên đa thức

Nhiệm vụ đầu tiên khi thiết kế hệ thống tính số học trên đa thức là quyết định một đa thức là gì. Thông thường, đa thức được định nghĩa theo một số biến số nhất định (gọi là _ẩn số_ của đa thức). Để đơn giản, ta chỉ xét đa thức một ẩn (univariate polynomial). Ta định nghĩa một đa thức là một tổng các hạng tử, mỗi hạng tử là một hệ số, một lũy thừa của ẩn, hoặc tích của hệ số và lũy thừa của ẩn. Một hệ số được định nghĩa là một biểu thức đại số không phụ thuộc vào ẩn của đa thức.

Ở đây ta đã lướt qua một số vấn đề phức tạp. Chẳng hạn, đa thức $x^2 + 2x + 1$ theo biến $x$ có phải là cùng một đa thức với $y^2 + 2y + 1$ theo biến $y$ không? Câu trả lời hợp lý là: "Có, nếu ta coi đa thức thuần túy là một hàm toán học, nhưng không, nếu ta coi đa thức là một dạng cú pháp." Ngoài ra, đa thức $x^2 + xy + y^2$ đại số tương đương với một đa thức một biến $x$ có hệ số là các đa thức theo $y$. Liệu hệ thống của ta có nhận ra điều này không? Hơn nữa, còn có những cách khác để biểu diễn đa thức — chẳng hạn dưới dạng tích các thừa số, hay (với đa thức một ẩn) dưới dạng tập nghiệm, hay là danh sách các giá trị tại một tập điểm cho trước. Ta có thể né tránh những câu hỏi này bằng cách quyết định rằng trong hệ thống thao tác đại số của ta, một "đa thức" sẽ là một _dạng cú pháp cụ thể_, không phải ý nghĩa toán học bên dưới của nó.

Bây giờ ta cần xem xét cách thực hiện phép tính số học trên đa thức. Trong hệ thống đơn giản này, ta chỉ xét phép cộng và phép nhân. Hơn nữa, ta yêu cầu hai đa thức được kết hợp phải có cùng ẩn số.

Ta tiếp cận thiết kế theo đúng phương pháp trừu tượng hóa dữ liệu quen thuộc. Ta biểu diễn đa thức bằng một cấu trúc dữ liệu gọi là `poly`, gồm một biến và một tập hạng tử. Ta giả sử đã có các **bộ chọn** `variable` và `term-list` trích xuất từng phần từ một `poly`, và một **bộ tạo** `make-poly` tạo một `poly` từ biến và danh sách hạng tử cho trước. Biến sẽ chỉ là một **ký hiệu**, vì vậy ta có thể dùng quy trình `same-variable?` để so sánh các biến. Các **quy trình** sau đây định nghĩa phép cộng và phép nhân đa thức:

```lisp
(define (add-poly p1 p2)
  (if (same-variable? (variable p1) 
                      (variable p2))
      (make-poly 
       (variable p1)
       (add-terms (term-list p1)
                  (term-list p2)))
      (error "Polys not in same var: 
              ADD-POLY"
             (list p1 p2))))

(define (mul-poly p1 p2)
  (if (same-variable? (variable p1) 
                      (variable p2))
      (make-poly 
       (variable p1)
       (mul-terms (term-list p1)
                  (term-list p2)))
      (error "Polys not in same var: 
              MUL-POLY"
             (list p1 p2))))
```

Để tích hợp đa thức vào hệ thống số học tổng quát, ta cần gắn thẻ kiểu cho chúng. Ta dùng thẻ `polynomial` và đăng ký các phép toán phù hợp trên đa thức đã gắn thẻ vào bảng phép toán. Ta đặt toàn bộ code vào một quy trình cài đặt cho gói đa thức, tương tự như các gói trong mục 2.5.1:

```lisp
(define (install-polynomial-package)
  ;; các quy trình nội bộ
  ;; biểu diễn poly
  (define (make-poly variable term-list)
    (cons variable term-list))
  (define (variable p) (car p))
  (define (term-list p) (cdr p))
  <các quy trình same-variable? và variable? từ mục 2.3.2>

  ;; biểu diễn hạng tử và danh sách hạng tử
  <các quy trình adjoin-term ... coeff từ phần dưới>

  (define (add-poly p1 p2) ...)
  <các quy trình dùng bởi add-poly>
  (define (mul-poly p1 p2) ...)
  <các quy trình dùng bởi mul-poly>

  ;; giao diện với phần còn lại của hệ thống
  (define (tag p) (attach-tag 'polynomial p))
  (put 'add '(polynomial polynomial)
       (lambda (p1 p2) 
         (tag (add-poly p1 p2))))
  (put 'mul '(polynomial polynomial)
       (lambda (p1 p2) 
         (tag (mul-poly p1 p2))))
  (put 'make 'polynomial
       (lambda (var terms) 
         (tag (make-poly var terms))))
  'done)
```

Phép cộng đa thức được thực hiện theo từng hạng tử. Các hạng tử cùng bậc (tức cùng lũy thừa của ẩn) phải được kết hợp với nhau — bằng cách tạo một hạng tử mới cùng bậc với hệ số là tổng các hệ số của hai hạng tử đó. Các hạng tử của một đa thức mà không có hạng tử nào cùng bậc trong đa thức kia thì được đưa thẳng vào đa thức tổng.

Để thao tác với danh sách hạng tử, ta giả sử đã có bộ tạo `the-empty-termlist` trả về danh sách hạng tử rỗng, và bộ tạo `adjoin-term` gắn một hạng tử mới vào danh sách. Ta cũng giả sử có _mệnh đề điều kiện_ `empty-termlist?` kiểm tra xem danh sách hạng tử có rỗng không, **bộ chọn** `first-term` trích xuất hạng tử bậc cao nhất từ danh sách, và **bộ chọn** `rest-terms` trả về tất cả các hạng tử còn lại. Để thao tác với hạng tử đơn lẻ, ta giả sử có bộ tạo `make-term` tạo một hạng tử với bậc và hệ số cho trước, và **bộ chọn** `order` và `coeff` lần lượt trả về bậc và hệ số của hạng tử. Những phép toán này cho phép ta coi cả hạng tử lẫn danh sách hạng tử là các trừu tượng dữ liệu, mà phần cài đặt cụ thể của chúng ta có thể lo sau.

Dưới đây là quy trình xây dựng danh sách hạng tử cho tổng của hai đa thức:

```lisp
(define (add-terms L1 L2)
  (cond ((empty-termlist? L1) L2)
        ((empty-termlist? L2) L1)
        (else
         (let ((t1 (first-term L1)) 
               (t2 (first-term L2)))
           (cond ((> (order t1) (order t2))
                  (adjoin-term
                   t1 
                   (add-terms (rest-terms L1) 
                              L2)))
                 ((< (order t1) (order t2))
                  (adjoin-term
                   t2 
                   (add-terms 
                    L1 
                    (rest-terms L2))))
                 (else
                  (adjoin-term
                   (make-term 
                    (order t1)
                    (add (coeff t1) 
                         (coeff t2)))
                   (add-terms 
                    (rest-terms L1)
                    (rest-terms L2)))))))))
```

Điều quan trọng nhất cần lưu ý ở đây là ta đã dùng **quy trình** cộng tổng quát `add` để cộng các hệ số của hai hạng tử. Điều này có hệ quả mạnh mẽ, như ta sẽ thấy ngay sau đây.

Để nhân hai danh sách hạng tử, ta nhân từng hạng tử của danh sách thứ nhất với tất cả hạng tử của danh sách kia, dùng quy trình `mul-term-by-all-terms` lặp đi lặp lại. Các danh sách hạng tử kết quả (một cho mỗi hạng tử của danh sách đầu tiên) được tích lũy thành một tổng. Tích của hai hạng tử tạo ra một hạng tử mới có bậc bằng tổng bậc của hai thừa số và hệ số bằng tích các hệ số:

```lisp
(define (mul-terms L1 L2)
  (if (empty-termlist? L1)
      (the-empty-termlist)
      (add-terms 
       (mul-term-by-all-terms 
        (first-term L1) L2)
       (mul-terms (rest-terms L1) L2))))

(define (mul-term-by-all-terms t1 L)
  (if (empty-termlist? L)
      (the-empty-termlist)
      (let ((t2 (first-term L)))
        (adjoin-term
         (make-term 
          (+ (order t1) (order t2))
          (mul (coeff t1) (coeff t2)))
         (mul-term-by-all-terms 
          t1 
          (rest-terms L))))))
```

Đó thực sự là tất cả những gì cần thiết cho phép cộng và nhân đa thức. Hãy chú ý rằng, vì ta thao tác trên hạng tử thông qua các **quy trình** tổng quát `add` và `mul`, gói đa thức của ta tự động có khả năng xử lý bất kỳ kiểu hệ số nào đã được biết đến bởi gói số học tổng quát. Nếu ta tích hợp cơ chế ép kiểu như một trong những cơ chế đã thảo luận trong mục 2.5.2, ta còn tự động xử lý được các phép toán trên đa thức có hệ số thuộc các kiểu khác nhau. Vì ta đã đăng ký các quy trình cộng và nhân đa thức `add-poly` và `mul-poly` vào hệ thống số học tổng quát như là các phép toán `add` và `mul` cho kiểu `polynomial`, hệ thống của ta cũng tự động xử lý được các phép toán trên đa thức. Lý do là khi hệ thống cố kết hợp các hệ số, nó sẽ phân nhánh qua `add` và `mul`. Vì các hệ số bản thân cũng là đa thức, chúng sẽ được kết hợp bằng `add-poly` và `mul-poly`. Kết quả là một dạng _"đệ quy hướng dữ liệu"_, trong đó, chẳng hạn, một lời gọi tới `mul-poly` sẽ dẫn đến các lời gọi **đệ quy** tới `mul-poly` để nhân các hệ số.

## Biểu diễn danh sách hạng tử

Cuối cùng, ta phải đối mặt với việc cài đặt một biểu diễn tốt cho danh sách hạng tử. Về thực chất, danh sách hạng tử là một tập hợp các hệ số được đánh chỉ mục theo bậc của hạng tử. Vì vậy, bất kỳ phương pháp nào để biểu diễn tập hợp, như đã thảo luận trong mục 2.3.3, đều có thể áp dụng cho nhiệm vụ này. Mặt khác, các quy trình `add-terms` và `mul-terms` của ta luôn truy cập danh sách hạng tử tuần tự từ bậc cao nhất xuống thấp nhất. Do đó, ta sẽ dùng một dạng biểu diễn danh sách có thứ tự.

Nên cấu trúc danh sách biểu diễn danh sách hạng tử như thế nào? Một điều cần cân nhắc là "độ dày đặc" của các đa thức ta định thao tác. Một đa thức được gọi là _dày đặc_ (dense) nếu nó có hệ số khác không ở hầu hết các bậc. Nếu nó có nhiều hạng tử bằng không, nó được gọi là _thưa_ (sparse).

Chẳng hạn, $A: x^5 + 2x^3 + x - 1$ là dày đặc, trong khi $B: x^{100} + 2x^2 + 1$ là thưa. Khi biểu diễn đa thức dày đặc, ta nên chỉ lưu hệ số, vì mục đích: $A$ được biểu diễn là `(1 0 2 0 1 -1)`. Đối với đa thức thưa, việc liệt kê tất cả hệ số — kể cả các số không — thật lãng phí, và ta nên biểu diễn danh sách hạng tử như một danh sách các hạng tử khác không. Do đó, $B$ được biểu diễn là `((100 1) (2 2) (0 1))`, theo nghĩa là mỗi hạng tử là một danh sách gồm bậc và hệ số của nó.

Ta sẽ xây dựng hệ thống đa thức của mình để hoạt động với biểu diễn danh sách hạng tử thưa: một danh sách các hạng tử khác không, sắp xếp từ bậc cao nhất xuống. Chúng ta cài đặt các **bộ tạo** và **bộ chọn** như sau:[^1]

```lisp
(define (adjoin-term term term-list)
  (if (=zero? (coeff term))
      term-list
      (cons term term-list)))

(define (the-empty-termlist) '())
(define (first-term term-list) (car term-list))
(define (rest-terms term-list) (cdr term-list))
(define (empty-termlist? term-list) (null? term-list))

(define (make-term order coeff) (list order coeff))
(define (order term) (car term))
(define (coeff term) (cadr term))
```

trong đó `=zero?` là quy trình kiểm tra xem một hệ số có bằng không hay không — ta cần nó để quy trình `adjoin-term` có thể loại bỏ các hạng tử bằng không khi gắn thêm vào danh sách. (Xem bài tập 2.87.)

Người dùng gói đa thức sẽ tạo đa thức (kiểu đã gắn thẻ) bằng quy trình:

```lisp
(define (make-polynomial var terms)
  ((get 'make 'polynomial) var terms))
```

**Bài tập 2.87:** Cài đặt `=zero?` cho đa thức trong gói số học tổng quát. Điều này sẽ cho phép `adjoin-term` hoạt động với các đa thức có hệ số là đa thức.

**Bài tập 2.88:** Mở rộng hệ thống đa thức để bao gồm phép trừ đa thức. (Gợi ý: bạn có thể muốn định nghĩa một quy trình `negate-terms` tổng quát.)

**Bài tập 2.89:** Định nghĩa các quy trình để cài đặt biểu diễn danh sách hạng tử phù hợp cho đa thức dày đặc được mô tả ở trên.

**Bài tập 2.90:** Giả sử ta muốn có một hệ thống đa thức hiệu quả cho cả đa thức dày đặc lẫn thưa. Một cách làm là cho phép tồn tại hai biểu diễn khác nhau trong hệ thống. Điều này đòi hỏi một _hệ thống_ với các phép toán tổng quát dạng hướng dữ liệu. Hãy thiết kế và cài đặt một hệ thống đa thức bao gồm cả hai biểu diễn. Bạn nên bao gồm `adjoin-term` và tất cả các quy trình danh sách hạng tử thích hợp.

**Bài tập 2.91:** Một đa thức đơn biến có thể chia cho một đa thức khác để tạo ra thương và số dư. Chẳng hạn, $\frac{x^5 - 1}{x^2 - 1} = x^3 + x$, dư $x - 1$. Phép chia có thể được thực hiện theo thuật toán "chia đơn thức lớn nhất": nếu bậc của số bị chia ít hơn bậc của số chia, kết quả thương là 0 và số dư là số bị chia. Ngược lại, lấy thương của hạng tử bậc cao nhất của số bị chia cho hạng tử bậc cao nhất của số chia. Kết quả này là hạng tử đầu tiên của thương. Tiếp theo, nhân kết quả này với số chia, trừ tích đó khỏi số bị chia, và tiếp tục đệ quy với hiệu thu được cùng số chia không thay đổi.

Điền vào phần còn thiếu trong định nghĩa `div-terms` dưới đây, rồi định nghĩa `div-poly` theo phép tương tự với `add-poly` và `mul-poly`. (Quy trình này sẽ trả về một danh sách gồm thương và số dư của phép chia.)

```lisp
(define (div-terms L1 L2)
  (if (empty-termlist? L1)
      (list (the-empty-termlist) 
            (the-empty-termlist))
      (let ((t1 (first-term L1))
            (t2 (first-term L2)))
        (if (> (order t2) (order t1))
            (list (the-empty-termlist) L1)
            (let ((new-c (div (coeff t1) 
                              (coeff t2)))
                  (new-o (- (order t1) 
                            (order t2))))
              (let ((rest-of-result
                     <tính phần còn lại của kết quả>
                    ))
                <tạo kết quả từ new-c, new-o và rest-of-result>
              ))))))
```

## Phân cấp kiểu và tháp kiểu

Gói đa thức của ta minh họa một cách thú vị rằng các gói con có thể tự nhiên có cấu trúc phân cấp. Ở đây, các hệ số đa thức bản thân có thể là đa thức. Vì vậy, chính hệ thống đại số ký hiệu của ta cũng có một cấu trúc tháp, tương tự như hệ thống số học số của ta: các số nguyên là một loại đặc biệt của số hữu tỉ, số hữu tỉ là một loại đặc biệt của số thực, v.v. Ở đây, các đa thức theo $x$ với hệ số số nguyên là một loại đặc biệt, các đa thức theo $x$ với hệ số số hữu tỉ là một loại khác, và các đa thức theo $x$ với hệ số là đa thức theo $y$ (tức là các đa thức hai biến biểu diễn theo cách nhất định) lại là một loại khác nữa.

Không may là, không phải mọi chuỗi số hiển nhiên đều cấu thành một tháp. Nếu ta có nhiều kiểu kết hợp nhau, phương pháp tháp sẽ hoạt động tốt nếu tất cả các tổ hợp có ý nghĩa đều có thể sắp xếp tuyến tính. Tuy nhiên, nếu không thể sắp xếp tuyến tính thì việc thiết kế hệ thống xử lý đúng tất cả các tổ hợp có thể sẽ rất đáng kể. Điều này dẫn tới các kỹ thuật phức tạp hơn, đôi khi được gọi là _truyền bá kiểu_ (type coercion) hay _phân giải giao thoa_ (intersection resolution). Việc thiết kế những cơ chế kiểu tổng quát là một lĩnh vực nghiên cứu đang hoạt động.

**Bài tập 2.92:** Bằng cách áp đặt một thứ tự trên các biến, mở rộng hệ thống đa thức để cộng và nhân đa thức theo các biến khác nhau. (Điều này không hề đơn giản!)

## Kết quả mở rộng: số hữu tỉ và đa thức

Ngoài các phép toán trên đa thức, ta cũng muốn thực hiện các phép toán trên biểu thức hữu tỉ — chẳng hạn $\frac{p_1}{p_2}$ trong đó $p_1$ và $p_2$ là đa thức. Hãy xem xét vấn đề rút gọn phân số hữu tỉ về dạng tối giản bằng cách chia tử số và mẫu số cho ước số chung lớn nhất của chúng (GCD). Khi làm việc với các số hữu tỉ nguyên, GCD được tính theo thuật toán Euclid. Việc mở rộng ý tưởng này sang đa thức thực ra khá đơn giản nếu ta có `div-terms` từ bài tập 2.91.

**Bài tập 2.93:** Sửa đổi gói số hữu tỉ để dùng phép tính số học tổng quát thay cho các phép toán số học thông thường. Kiểm tra hệ thống bằng cách tạo số hữu tỉ có tử số và mẫu số là đa thức:

```lisp
(define p1 (make-polynomial 'x '((2 1)(0 1))))
(define p2 (make-polynomial 'x '((3 1)(0 1))))
(define rf (make-rational p2 p1))
```

Bây giờ hãy thêm `rf` với chính nó, dùng `add`. Bạn sẽ thấy rằng quy trình này không rút gọn phân số.

Chúng ta có thể khắc phục vấn đề này nếu sửa đổi `make-rat` để rút gọn phân số khi tạo. Nếu ta có thuật toán Euclid cho đa thức, ta có thể dùng nó để tính GCD của tử số và mẫu số rồi chia cả hai cho GCD. Ta có thể sử dụng `gcd-terms` (tương tự `add-terms`) để tính GCD của hai danh sách hạng tử:

```lisp
(define (gcd-terms a b)
  (if (empty-termlist? b)
      a
      (gcd-terms b (remainder-terms a b))))
```

trong đó `remainder-terms` trích phần dư từ kết quả của `div-terms`:

```lisp
(define (remainder-terms a b)
  (cadr (div-terms a b)))
```

**Bài tập 2.94:** Dùng `div-terms` để cài đặt quy trình `remainder-terms` và dùng nó để định nghĩa `gcd-terms` như trên. Bây giờ hãy viết quy trình `gcd-poly` tính GCD của hai đa thức. (Quy trình này cần kiểm tra xem hai đa thức có cùng biến không.) Cài đặt nó vào hệ thống bằng cách cài một quy trình `greatest-common-divisor` tổng quát hoạt động với cả số nguyên thông thường lẫn đa thức. Để kiểm tra kết quả, hãy thử:

```lisp
(define p1 (make-polynomial 'x '((4 1)(3 -1)(2 -2)(1 2))))
(define p2 (make-polynomial 'x '((3 1)(1 -1))))
(greatest-common-divisor p1 p2)
```

rồi đối chiếu với kết quả mong đợi bằng cách tính tay.

**Bài tập 2.95:** Định nghĩa $P_1$, $P_2$, và $P_3$ là các đa thức sau:

$$P_1: x^2 - 2x + 1$$
$$P_2: 11x^2 + 7$$
$$P_3: 13x + 5$$

Bây giờ định nghĩa $Q_1$ là tích $P_1 \cdot P_2$ và $Q_2$ là tích $P_1 \cdot P_3$, và dùng `greatest-common-divisor` (bài tập 2.94) để tính GCD của $Q_1$ và $Q_2$. Hãy lưu ý rằng câu trả lời không giống với $P_1$. Ví dụ này giới thiệu vấn đề phình hệ số (coefficient explosion): ngay cả khi bắt đầu với đa thức hệ số nguyên, kết quả trung gian có thể là phân số.

Một cách để xử lý vấn đề này là giới thiệu một thao tác gọi là _chia giả_ (pseudodivision). Ta định nghĩa _thương giả_ và _dư giả_ của đa thức $P_1$ chia cho $P_2$ là kết quả mà ta sẽ nhận được nếu nhân $P_1$ với một số nguyên đủ lớn. Cụ thể, nếu $P_1$ có bậc $O_1$ và $P_2$ có bậc $O_2$, hãy nhân $P_1$ với _integerizing factor_ $c = (c_2)^{1 + O_1 - O_2}$, trong đó $c_2$ là hệ số của hạng tử bậc cao nhất của $P_2$. Điều này sẽ bảo đảm phép chia không tạo ra phân số. Thuật toán kết quả gọi là _pseudodivision_ của $P_1$ cho $P_2$.

**Bài tập 2.96:**

a. Cài đặt quy trình `pseudoremainder-terms` trả về dư sau pseudodivision. Sửa đổi `gcd-terms` để dùng `pseudoremainder-terms`. Kiểm tra lại ví dụ $Q_1$ và $Q_2$ trên, xác nhận rằng bây giờ GCD được tính toán đúng.

b. GCD bây giờ không còn phình hệ số, nhưng kết quả có thể vẫn có hệ số nguyên không phải là ước chung lớn nhất của tất cả các hệ số kết quả. Điều chỉnh `gcd-terms` bằng cách chia kết quả cuối cùng cho ước chung lớn nhất (GCD) của các hệ số của nó trước khi trả về kết quả, để đảm bảo rằng GCD kết quả có tất cả các hệ số nguyên và ước chung lớn nhất của tất cả các hệ số là 1. Đây được gọi là _đa thức nguyên nguyên thủy_ (primitive polynomial). Ví dụ, sau khi rút gọn, $\frac{1}{2}x^2-\frac{1}{2}$ nên trở thành $x^2 - 1$.

**Bài tập 2.97:**

a. Cài đặt quy trình `reduce-terms` thực hiện rút gọn tử số và mẫu số xuống dạng tối giản như được phác thảo ở trên. Quy trình này nên nhận hai danh sách hạng tử `n` và `d` làm đối số và trả về một danh sách `nn`, `dd`, là `n` và `d` sau khi đã chia cho GCD của chúng. Hãy sử dụng GCD nguyên nguyên thủy từ bài tập 2.96b. Để kiểm tra, hãy thử:

```lisp
(define p1 (make-polynomial 'x '((1 1)(0 1))))
(define p2 (make-polynomial 'x '((3 1)(0 -1))))
(define p3 (make-polynomial 'x '((1 1))))
(define p4 (make-polynomial 'x '((2 1)(0 -1))))

(define rf1 (make-rational p1 p2))
(define rf2 (make-rational p3 p4))

(add rf1 rf2)
```

và kiểm tra xem câu trả lời của bạn có đúng không, bằng cách quy về một phân số chung và rút gọn thủ công.

b. Định nghĩa quy trình `reduce-poly` tương tự `add-poly` để gọi `reduce-terms`. Cài đặt nó vào hệ thống tổng quát với tên `reduce`. Cuối cùng, định nghĩa lại gói số hữu tỉ để nó dùng `reduce` thay vì `gcd`:

```lisp
(define (make-rat n d)
  (let ((g (reduce n d)))
    (cons (div n (car g))
          (div d (cadr g)))))
```

Và kiểm tra lại bài toán ở đầu mục bằng ví dụ trong bài tập 2.93.

---

[^1]: Biểu diễn thưa dễ cài đặt nhưng không phải lúc nào cũng hiệu quả nhất. Khi nói đến hiệu năng thực tế, lựa chọn biểu diễn tốt nhất phụ thuộc vào loại đa thức mà hệ thống sẽ phải xử lý nhiều nhất.

---

[`procedure`](GLOSSARY.md#procedure) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`symbol`](GLOSSARY.md#symbol) [`data abstraction`](GLOSSARY.md#data-abstraction) [`predicate`](GLOSSARY.md#predicate) [`data-directed`](GLOSSARY.md#data-directed) [`recursive`](GLOSSARY.md#recursive) [`greatest-common-divisor`](GLOSSARY.md#greatest-common-divisor) [`symbolic-differentiation`](GLOSSARY.md#symbolic-differentiation)
