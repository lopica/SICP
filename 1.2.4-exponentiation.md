# 1.2.4 Lũy thừa

Hãy xét bài toán tính lũy thừa của một số cho trước. Ta muốn có một quy trình nhận vào cơ số $b$ và một số nguyên dương $n$ làm **đối số**, rồi tính ra $b^n$. Một cách tiếp cận là dùng định nghĩa đệ quy

$$b^n = b \cdot b^{n-1}, \quad b^0 = 1,$$

từ đó dịch thẳng thành một **quy trình**:

```lisp
(define (expt b n)
  (if (= n 0)
      1
      (* b (expt b (- n 1)))))
```

Đây là một **tiến trình đệ quy tuyến tính**, cần $\Theta(n)$ bước và $\Theta(n)$ không gian. Cũng như với giai thừa, ta dễ dàng viết lại thành một tiến trình lặp tương đương:

```lisp
(define (expt b n)
  (expt-iter b n 1))

(define (expt-iter b counter product)
  (if (= counter 0)
      product
      (expt-iter b
                 (- counter 1)
                 (* b product))))
```

Phiên bản này cần $\Theta(n)$ bước nhưng chỉ $\Theta(1)$ không gian.

Ta có thể tính lũy thừa với ít phép nhân hơn nhiều nhờ kỹ thuật **bình phương liên tiếp**. Chẳng hạn, thay vì tính $b^8$ theo cách

$$b \cdot (b \cdot (b \cdot (b \cdot (b \cdot (b \cdot (b \cdot b)))))),$$

ta có thể dùng chỉ ba phép nhân:

$$b^2 = b \cdot b,$$
$$b^4 = b^2 \cdot b^2,$$
$$b^8 = b^4 \cdot b^4.$$

Phương pháp này hoạt động tốt với các lũy thừa là lũy thừa của 2. Ta cũng có thể tận dụng bình phương liên tiếp trong trường hợp tổng quát, dựa vào quy tắc:

$$b^n = (b^{n/2})^2 \quad \text{nếu } n \text{ chẵn},$$
$$b^n = b \cdot b^{n-1} \quad \text{nếu } n \text{ lẻ}.$$

Quy tắc này được diễn đạt thành quy trình sau:

```lisp
(define (fast-expt b n)
  (cond ((= n 0)
         1)
        ((even? n)
         (square (fast-expt b (/ n 2))))
        (else
         (* b (fast-expt b (- n 1))))))
```

trong đó mệnh đề điều kiện kiểm tra tính chẵn lẻ của một số nguyên được định nghĩa qua quy trình nguồn `remainder`:

```lisp
(define (even? n)
  (= (remainder n 2) 0))
```

Tiến trình được `fast-expt` tạo ra tăng trưởng theo bậc $\Theta(\log n)$ cả về số bước lẫn không gian.[^1] Để thấy điều này, hãy chú ý rằng tính $b^{2n}$ bằng `fast-expt` chỉ cần thêm một phép nhân so với tính $b^n$. Vậy mỗi lần được dùng thêm một phép nhân, số mũ ta có thể tính được tăng gần gấp đôi. Do đó, số phép nhân cần để tính $b^n$ tăng xấp xỉ nhanh bằng $\log_2 n$.

Sự khác biệt giữa bậc tăng trưởng $\Theta(\log n)$ và $\Theta(n)$ trở nên rõ rệt khi $n$ lớn. Chẳng hạn, `fast-expt` với $n = 1000$ chỉ cần khoảng 14 phép nhân.[^2] Ngoài ra, ta cũng có thể dùng ý tưởng bình phương liên tiếp để xây dựng một thuật toán lặp tính lũy thừa trong số bước logarithmic (xem Bài tập 1.16), dù như thường thấy với các thuật toán lặp, cách viết không trực tiếp và rõ ràng bằng thuật toán đệ quy.[^3]

[^1]: Nói chính xác hơn, số phép nhân cần thực hiện bằng $1$ cộng với tổng số bit `1` và `0` trong biểu diễn nhị phân của $n$ trừ đi `1`. Điều này không lớn hơn $2 \log_2 n$, do đó bậc tăng trưởng là $\Theta(\log n)$.

[^2]: Bạn có thể kiểm chứng: số nhị phân của $1000$ là $1111101000$, có $10$ bit, trong đó có $6$ bit `1`. Vì vậy cần đúng $14$ phép nhân: $9$ lần bình phương cộng $5$ lần nhân thêm.

[^3]: Thuật toán lặp không hiển nhiên vì trong trường hợp chẵn, thay vì đệ quy với cơ số mới và số mũ giảm một nửa, ta phải lưu lại phần tích lũy cho số mũ lẻ.

[^4]: Thuật toán này đôi khi được gọi là "phương pháp nhân của người Nga" hay "nhân bằng bình phương", và đã được người Ai Cập cổ đại biết đến. Ví dụ về cách dùng có thể thấy trong tài liệu toán học Ai Cập Rhind khoảng năm 1700 trước Công nguyên.

---

[exercise 1.16](exercises/1.16.md) [exercise 1.17](exercises/1.17.md) [exercise 1.18](exercises/1.18.md) [`procedure`](GLOSSARY.md#procedure) [`process`](GLOSSARY.md#process) [`linear recursive process`](GLOSSARY.md#linear-recursive-process) [`order of growth`](GLOSSARY.md#order-of-growth) [`state variables`](GLOSSARY.md#state-variables) [`iterative process`](GLOSSARY.md#iterative-process) [`parameter`](GLOSSARY.md#parameter) [`argument`](GLOSSARY.md#argument) [`successive squaring`](GLOSSARY.md#successive-squaring)
