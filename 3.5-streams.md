# 3.5 Dòng chảy

Chúng ta đã hiểu tương đối rõ **phép gán** như một công cụ mô hình hóa, đồng thời cũng nhận thấy những vấn đề phức tạp mà nó kéo theo. Đã đến lúc tự hỏi: liệu có cách tiếp cận khác không — một cách tránh được những vấn đề đó? Trong mục này, chúng ta sẽ khảo sát một hướng thay thế để mô hình hóa trạng thái, dựa trên cấu trúc dữ liệu gọi là _dòng chảy_. Như ta sẽ thấy, _dòng chảy_ có thể giúp giải tỏa một phần sự phức tạp vốn gắn liền với việc mô hình hóa trạng thái.

Hãy lùi lại một chút để xem sự phức tạp đó xuất phát từ đâu. Khi cố mô phỏng các hiện tượng trong thế giới thực, chúng ta đã đưa ra một số quyết định tưởng chừng hợp lý: mô hình hóa đối tượng thực bằng đối tượng tính toán có trạng thái cục bộ; đồng nhất thời gian trong thế giới thực với thời gian trong máy tính; và thể hiện sự biến đổi trạng thái theo thời gian bằng cách gán lại các **biến** cục bộ của đối tượng tính toán.

Liệu có cách khác không? Có nhất thiết phải đồng nhất "thời gian trong máy tính" với "thời gian trong thế giới được mô hình hóa" không? Có bắt buộc phải cho mô hình thay đổi theo thời gian mới có thể mô phỏng thế giới đang thay đổi không? Hãy nghĩ đến câu hỏi này qua lăng kính của toán học. Ta có thể mô tả hành vi thay đổi theo thời gian của một đại lượng $x$ như một hàm của thời gian $x(t)$. Nếu chú ý vào từng thời điểm, ta thấy $x$ như một giá trị đang thay đổi. Nhưng nếu nhìn vào toàn bộ lịch sử giá trị theo thời gian, ta không cần nhấn mạnh vào sự thay đổi nữa — bản thân hàm số không hề thay đổi.[^1]

Khi thời gian được đo theo các bước rời rạc, ta có thể mô hình hóa một hàm thời gian như một dãy (có thể vô hạn). Trong mục này, chúng ta sẽ thấy cách mô hình hóa sự biến đổi bằng những dãy như vậy — mỗi dãy đại diện cho toàn bộ lịch sử của một hệ thống. Để làm được điều đó, ta giới thiệu cấu trúc dữ liệu mới gọi là **dòng chảy**. Nhìn từ góc độ trừu tượng, **dòng chảy** chẳng qua là một _dãy_. Tuy nhiên, ta sẽ thấy rằng cách cài đặt **dòng chảy** trực tiếp như một _danh sách_ (giống mục 2.2.1) chưa bộc lộ hết sức mạnh của phương pháp xử lý theo dòng chảy. Thay vào đó, ta sẽ giới thiệu kỹ thuật _thực thi trì hoãn_ (delayed evaluation), cho phép biểu diễn các dãy rất lớn — thậm chí vô hạn — dưới dạng **dòng chảy**.

Xử lý theo **dòng chảy** cho phép ta mô hình hóa các hệ thống có trạng thái mà không bao giờ cần dùng **phép gán** hay dữ liệu có thể thay đổi. Điều này có ý nghĩa cả về lý thuyết lẫn thực tiễn, vì ta có thể xây dựng mô hình tránh được mọi nhược điểm vốn có của **phép gán**. Tuy nhiên, cách tiếp cận theo **dòng chảy** cũng có những khó khăn riêng của nó, và câu hỏi về kỹ thuật mô hình nào dẫn đến hệ thống mô đun hơn, dễ bảo trì hơn, vẫn còn để ngỏ.

[^1]: Thao tác toán học này — nhìn hàm $x(t)$ như một đối tượng tĩnh thay vì một dòng giá trị biến thiên — chính là nền tảng của ngành _giải tích hàm_ (functional analysis). _Phép biến đổi tín hiệu_ (signal processing) khai thác tư tưởng này để xử lý các tín hiệu biến thiên theo thời gian dưới dạng các đối tượng bất biến trong không gian tần số.

---

[`stream`](GLOSSARY.md#stream) [`sequence`](GLOSSARY.md#sequence) [`assignment`](GLOSSARY.md#assignment) [`variable`](GLOSSARY.md#variable) [`list`](GLOSSARY.md#list)
