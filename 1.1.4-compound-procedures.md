# 1.1.4. Tổng hợp các quy trình
Chúng ta đã phân biệt một số thành phần trong Lisp mà cũng phải có trong bất kỳ ngôn ngữ lập trình mạnh:
- Các số và phép biến đổi là dữ liệu nguồn và quy trình nguồn
- Lồng các `sự kết hợp` là phương tiện để  kết hợp các phép biến đổi
- Định nghĩa mối liên hệ giữa các tên và giá trị cung cấp một cách hạn chế cho
trừu tượng

Bây giờ chúng ta sẽ học về định nghĩa các quy trình, một kỹ thuật trừu tượng mạnh hơn hay có thể nói là một phép tổng hợp có thể liên kết với một tên và ta có thể dùng nó như một đơn vị.

Chúng ta bắt đầu bằng việc thử biểu diễn bình phương. Chúng ta có thể nói, để bình phương một số, lấy số đó nhân với chính nó. Điều này được biểu diễn trong lisp như sau:
```bash
(define (square x) (* x x))
```
Chúng ta có thể hiểu đoạn code trên như này
```bash
(define (square x)   (*        x       x))
  |      |      |     |        |       |
 To square something, multiply it by itself.
```
Chúng ta đã có ở đây một quy trình tổng hợp, và nó được gắn với tên "square". Quy trình thể hiện phép biến đổi nhân một số gì đó với chính nó. Cái được nhân có tên cục bộ là x. Thực thi định nghĩa này tạo quy trình tổng hợp và liên kết nó với tên "square".

Cấu trúc chung của một định nghĩa quy trình là
```bash
(define (⟨name⟩ ⟨formal parameters⟩) ⟨body⟩)
```
⟨name⟩ là ký hiệu mà có liên kết với định nghĩa của quy trình trong môi trường. ⟨formal parameters⟩ là các tên được dùng trong quy trình và cũng là đối số của quy trình. ⟨body⟩ là một biểu thức mà sẽ là trả ra giá trị khi áp dụng quy trình và các tham số được thay bằng các đôi số thật tại nơi mà quy trình được áp dụng. ⟨name⟩ và ⟨formal parameters⟩ được gộp chung vào trong cặp ngoặc đơn, y như khi ta gọi chúng sau khi được định nghĩa.

Sau khi đã định nghĩa `square`, chúng ta có thể dùng nó:
```bash
(square 21)
441

(square (+ 2 5))
49

(square (square 3))
81
```
Chúng ta cũng có thể dùng `square` như một đơn vị để định nghĩa các quy trình khác. Ví dụ, $x^2 + y^2$ có thể được biểu diễn như này:
```bash
(+ (square x) (square y))
```
Chúng ta có thể dễ dàng định nghĩa quy trình `sum-of-squares`, với 2 số bất kỳ là đối số, trả ra tổng bình phương 2 số:
```bash
(define (sum-of-squares x y)
  (+ (square x) (square y)))

(sum-of-squares 3 4)
25
```
Bây giờ chúng ta có thể dùng `sum-of-squares` làm đơn vị để tạo ra các quy trình khác:
```bash
(define (f a)
  (sum-of-squares (+ a 1) (* a 2)))

(f 5)
136
```Quy trình tổng hợp được dùng y như các quy trình nguồn. Quả thật, một người mới nhìn qua không thể nói `square` trong `sum-of-squares` là quy trình có sẵn hay là một quy trình tổng hợp.
