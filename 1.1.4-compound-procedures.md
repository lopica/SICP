# 1.1.4. Các quy trình tổng hợp

Chúng ta đã phân biệt một số phần tử trong Lisp mà cũng phải có trong bất kỳ ngôn ngữ lập trình mạnh nào khác:

- Các số và **phép biến đổi** là _dữ liệu nguồn_ và _quy trình nguồn_

- Lồng các _biểu thức kết hợp_ là phương tiện để kết hợp các **phép biến đổi**

- Định nghĩa _mối liên hệ_ giữa các **tên** và giá trị cung cấp một cách hạn chế cho sự trừu tượng

Bây giờ chúng ta sẽ học về định nghĩa các **quy trình**, một kỹ thuật trừu tượng mạnh hơn hay có thể nói là một _phép tính tổng hợp_ có thể _liên kết_ với một **tên** và ta có thể dùng nó như một **đơn vị**.

Chúng ta bắt đầu bằng việc thử biểu diễn bình phương. Chúng ta có thể nói: "để bình phương một số, lấy số đó nhân với chính nó". Điều này được biểu diễn trong Lisp như sau:

```bash
(define (square x) (* x x))
```

Chúng ta có thể hiểu đoạn code trên như này

```bash
(define (square x)   (*        x       x))
  |      |      |     |        |       |
 To square something, multiply it by itself.
```

Chúng ta đã có ở đây một _quy trình tổng hợp_, và nó được gắn với **tên** `square`. Quy trình thể hiện **phép biến đổi** nhân một số gì đó với chính nó. Cái được nhân có **tên cục bộ** là `x`. **Thực thi** định nghĩa này tạo _quy trình tổng hợp_ và _liên kết_ nó với **tên** `square`.[^1]

Cấu trúc chung của một định nghĩa **quy trình** là

```bash
(define (⟨name⟩ ⟨formal parameters⟩) ⟨body⟩)
```

⟨name⟩ là **ký hiệu** mà có _liên kết_ với định nghĩa của **quy trình** trong **môi trường**. ⟨formal parameters⟩ là các **tên** được dùng trong **quy trình** và cũng là đối số của **quy trình**. ⟨body⟩ là một **biểu thức** mà sẽ trả ra giá trị khi áp dụng **quy trình** với các **tham số** được thay bằng các **đôi số** thật tại nơi mà **quy trình** được áp dụng. ⟨name⟩ và ⟨formal parameters⟩ được gộp chung vào trong cặp ngoặc đơn, y như khi ta gọi chúng sau khi được định nghĩa.[^2][^3]

Sau khi đã định nghĩa `square`, chúng ta có thể dùng nó:

```bash
(square 21)
441

(square (+ 2 5))
49

(square (square 3))
81
```

Chúng ta cũng có thể dùng `square` như một **đơn vị** để định nghĩa các **quy trình** khác. Ví dụ, $x^2 + y^2$ có thể được biểu diễn như này:

```bash
(+ (square x) (square y))
```

Chúng ta có thể dễ dàng định nghĩa **quy trình** `sum-of-squares`, với 2 số bất kỳ là **đối số**, trả ra tổng bình phương 2 số:

```bash
(define (sum-of-squares x y)
  (+ (square x) (square y)))

(sum-of-squares 3 4)
25
```

Bây giờ chúng ta có thể dùng `sum-of-squares` làm **đơn vị** để tạo ra các **quy trình** khác:

```bash
(define (f a)
  (sum-of-squares (+ a 1) (* a 2)))

(f 5)
136
```

_Quy trình tổng hợp_ được dùng y như các _quy trình nguồn_. Quả thật, một người mới nhìn qua không thể nói `square` trong `sum-of-squares` là **quy trình** có sẵn hay là một _quy trình tổng hợp_.

[^1]: Chú ý rằng có hai thao tác khác nhau đang được kết hợp ở đây: chúng ta vừa tạo ra _quy trình_, vừa đặt **tên** `square` cho nó. Thực ra điều quan trọng là có thể tách hai khái niệm này — tạo quy trình mà không đặt tên, và đặt tên cho các quy trình đã được tạo sẵn. Chúng ta sẽ xem cách làm điều này ở mục 1.3.2.

[^2]: Xuyên suốt quyển sách này, chúng ta sẽ mô tả cú pháp tổng quát của các biểu thức bằng cách dùng các ký hiệu nghiêng đặt trong dấu ngoặc nhọn — ví dụ ⟨name⟩ — để biểu thị những "vị trí" trong biểu thức cần được điền vào khi biểu thức đó thực sự được dùng.

[^3]: Tổng quát hơn, phần thân của **quy trình** có thể là một chuỗi các **biểu thức**. Trong trường hợp đó, **trình phiên dịch** thực thi lần lượt từng **biểu thức** trong chuỗi và trả về giá trị của **biểu thức** cuối cùng làm kết quả của việc áp dụng **quy trình**.

---

`compound procedure` `operator` `primitive data` `primitive procedure` `combination` `name` `parameter`
