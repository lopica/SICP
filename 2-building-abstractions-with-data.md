### 2. Xây dựng các trừu tượng qua dữ liệu

> Chúng ta đến đây với bước quyết định của sự trừu tượng hóa toán học: chúng ta quên đi ý nghĩa của các ký hiệu. … [Nhà toán học] không cần ngồi không; có rất nhiều phép tính mà anh ta có thể thực hiện với các ký hiệu đó mà không cần nhìn lại xem chúng đại diện cho điều gì.
>
> — Hermann Weyl, *The Mathematical Way of Thinking*

Trong Chương 1, chúng ta tập trung vào **tiến trình** tính toán và vai trò của **quy trình** trong thiết kế **chương trình**. Ta đã thấy cách dùng _dữ liệu nguồn_ (các con số) và _quy trình nguồn_ (các phép toán số học), cách kết hợp **quy trình** thành _quy trình tổng hợp_ qua phép hợp thành, các biểu thức điều kiện và **tham số**, và cách trừu tượng hóa **quy trình** bằng `define`. Ta đã thấy rằng một **quy trình** có thể xem như khuôn mẫu để mô tả sự phát triển cục bộ của một **tiến trình** — ta đã phân loại, lý luận, và phân tích đơn giản một số dạng **tiến trình** phổ biến được thể hiện qua **quy trình**. Ta cũng thấy rằng các **quy trình** bậc cao giúp ngôn ngữ mạnh hơn bằng cách cho phép ta thao tác, và qua đó lý luận được, về các phương pháp tính toán tổng quát. Đó là phần cốt lõi của lập trình.

Trong chương này, chúng ta sẽ khám phá những **dữ liệu** phức tạp hơn. Tất cả các **quy trình** trong Chương 1 đều thao tác trên **dữ liệu** số đơn giản, mà **dữ liệu** đơn giản không đủ để giải quyết nhiều bài toán mà ta muốn lập trình giải quyết. Các **chương trình** thường được thiết kế để mô phỏng các hiện tượng phức tạp, và thường thì ta phải xây dựng các đối tượng tính toán có nhiều thành phần để mô phỏng các hiện tượng thực tế có nhiều mặt khác nhau. Vì vậy, trong khi Chương 1 tập trung vào xây dựng **sự trừu tượng quy trình** bằng cách kết hợp các **quy trình** thành _quy trình tổng hợp_, thì chương này xoay sang một khía cạnh then chốt khác của bất kỳ ngôn ngữ lập trình nào: phương tiện xây dựng sự trừu tượng bằng cách kết hợp các đối tượng dữ liệu thành _dữ liệu tổng hợp_.

Tại sao ta cần _dữ liệu tổng hợp_ trong ngôn ngữ lập trình? Cũng vì những lý do tương tự ta cần _quy trình tổng hợp_: nâng cao tầm nhìn khái niệm khi thiết kế **chương trình**, tăng tính **mô đun**, và mở rộng sức biểu đạt của ngôn ngữ. Cũng như khả năng định nghĩa **quy trình** cho phép ta làm việc với **tiến trình** ở tầng khái niệm cao hơn so với các phép tính nguyên thủy của ngôn ngữ, khả năng xây dựng các đối tượng _dữ liệu tổng hợp_ cho phép ta làm việc với **dữ liệu** ở tầng khái niệm cao hơn so với các đối tượng _dữ liệu nguồn_ của ngôn ngữ.

Hãy xem bài toán thiết kế một hệ thống thực hiện các phép toán với số hữu tỉ. Ta có thể hình dung một phép tính `add-rat` nhận vào hai số hữu tỉ và trả về tổng của chúng. Về mặt **dữ liệu** đơn giản, một số hữu tỉ có thể được nghĩ đến như hai số nguyên: tử số và mẫu số. Như vậy, ta có thể thiết kế một **chương trình** trong đó mỗi số hữu tỉ được biểu diễn bởi hai số nguyên (tử số và mẫu số), và `add-rat` sẽ được hiện thực bởi hai **quy trình** (một **quy trình** tính tử số của tổng và một **quy trình** tính mẫu số của tổng). Nhưng làm vậy thì phiền phức, vì ta phải tự theo dõi xem tử số nào khớp với mẫu số nào. Trong một hệ thống phải thực hiện nhiều phép toán trên nhiều số hữu tỉ, những công việc theo dõi như vậy sẽ làm **chương trình** rối loạn đáng kể. Sẽ tốt hơn nhiều nếu ta có thể "dán" tử số và mẫu số lại thành một cặp — một _đối tượng dữ liệu tổng hợp_ — mà **chương trình** có thể thao tác theo cách nhất quán với việc xem một số hữu tỉ là một đơn vị khái niệm thống nhất.

Dùng _dữ liệu tổng hợp_ cũng giúp tăng tính **mô đun** của **chương trình**. Nếu ta có thể thao tác trực tiếp với số hữu tỉ như những đối tượng độc lập, thì ta có thể tách phần **chương trình** làm việc với số hữu tỉ ra khỏi chi tiết biểu diễn số hữu tỉ dưới dạng cặp số nguyên. Kỹ thuật tổng quát là tách phần **chương trình** xử lý cách biểu diễn đối tượng dữ liệu ra khỏi phần xử lý cách sử dụng đối tượng dữ liệu — đây là một phương pháp thiết kế mạnh mẽ gọi là _trừu tượng dữ liệu_. Ta sẽ thấy _trừu tượng dữ liệu_ làm cho các **chương trình** dễ thiết kế, dễ bảo trì và dễ sửa đổi hơn nhiều.

Dùng _dữ liệu tổng hợp_ còn dẫn đến sự mở rộng thực sự về sức biểu đạt của ngôn ngữ lập trình. Hãy xem ý tưởng tạo ra một "tổ hợp tuyến tính" $ax + by$. Ta muốn viết một **quy trình** nhận $a$, $b$, $x$, $y$ làm **đối số** và trả về giá trị của $ax + by$. Nếu các **đối số** là số thì không có gì khó — ta dễ dàng định nghĩa:

```lisp
(define (linear-combination a b x y)
  (+ (* a x) (* b y)))
```

Nhưng giả sử ta không chỉ quan tâm đến số. Giả sử ta muốn diễn đạt, bằng ngôn ngữ **quy trình**, ý tưởng rằng ta có thể tạo tổ hợp tuyến tính bất cứ khi nào phép cộng và phép nhân được xác định — với số hữu tỉ, số phức, đa thức, hay bất cứ kiểu nào. Ta có thể viết dưới dạng:

```lisp
(define (linear-combination a b x y)
  (add (mul a x) (mul b y)))
```

ở đây `add` và `mul` không phải là các _quy trình nguồn_ `+` và `*`, mà là các thứ phức tạp hơn sẽ thực hiện các phép tính phù hợp với bất kỳ kiểu dữ liệu nào được truyền vào qua `a`, `b`, `x`, `y`. Điều quan trọng là **quy trình** `linear-combination` chỉ cần biết rằng `add` và `mul` sẽ thực hiện đúng các thao tác cần thiết. Từ góc độ của `linear-combination`, loại dữ liệu thực sự của `a`, `b`, `x`, `y` là gì hoàn toàn không quan trọng — càng không quan trọng hơn là chúng được biểu diễn như thế nào ở tầng **dữ liệu** nguyên thủy hơn. Ví dụ này cũng cho thấy tại sao ngôn ngữ lập trình cần cung cấp khả năng thao tác trực tiếp các đối tượng tổng hợp: nếu không, không có cách nào để **quy trình** như `linear-combination` chuyển **đối số** của nó đến `add` và `mul` mà không phải hiểu cấu trúc chi tiết của chúng.[^1]

Ta bắt đầu chương này bằng cách hiện thực hệ thống số hữu tỉ đã đề cập ở trên. Đây sẽ là nền tảng cho phần thảo luận về _dữ liệu tổng hợp_ và _trừu tượng dữ liệu_. Cũng như _quy trình tổng hợp_, vấn đề trung tâm cần giải quyết là sự trừu tượng như một kỹ thuật đối phó với độ phức tạp, và ta sẽ thấy _trừu tượng dữ liệu_ cho phép ta dựng lên những _hàng rào trừu tượng_ phù hợp giữa các phần khác nhau của **chương trình**.

Ta sẽ thấy rằng chìa khóa để tạo ra _dữ liệu tổng hợp_ là ngôn ngữ lập trình phải cung cấp một loại "keo" nào đó để các đối tượng dữ liệu có thể được kết hợp thành đối tượng phức tạp hơn. Có nhiều kiểu keo khác nhau. Thật đáng ngạc nhiên, ta sẽ khám phá ra cách tạo _dữ liệu tổng hợp_ mà không cần bất kỳ phép tính "dữ liệu" đặc biệt nào — chỉ dùng **quy trình**. Điều này tiếp tục làm mờ ranh giới giữa "**quy trình**" và "**dữ liệu**", một ranh giới vốn đã bắt đầu mờ dần về cuối Chương 1. Ta cũng sẽ khám phá một số kỹ thuật quy ước để biểu diễn dãy và cây. Một ý tưởng then chốt khi làm việc với _dữ liệu tổng hợp_ là khái niệm _bao đóng_ — "keo" ta dùng để kết hợp các đối tượng dữ liệu phải cho phép kết hợp không chỉ các _dữ liệu nguồn_ mà cả các đối tượng _dữ liệu tổng hợp_. Một ý tưởng quan trọng khác là các đối tượng _dữ liệu tổng hợp_ có thể đóng vai trò là _giao diện quy ước_ để kết hợp các **mô đun** **chương trình** theo cách ghép tuỳ ý. Ta sẽ minh họa một số ý tưởng này qua một ngôn ngữ đồ họa đơn giản khai thác _bao đóng_.

Tiếp theo, ta sẽ mở rộng sức biểu đạt của ngôn ngữ bằng cách giới thiệu _biểu thức ký hiệu_ — **dữ liệu** mà các phần tử cơ bản có thể là các **ký hiệu** tùy ý chứ không chỉ là số. Ta sẽ khám phá nhiều cách biểu diễn khác nhau cho tập hợp các đối tượng. Ta sẽ thấy rằng, cũng như một hàm số đã cho có thể được tính bởi nhiều **tiến trình** tính toán khác nhau, một cấu trúc dữ liệu đã cho cũng có thể được biểu diễn theo nhiều cách dùng các đối tượng đơn giản hơn, và lựa chọn biểu diễn có thể ảnh hưởng đáng kể đến yêu cầu về thời gian và không gian của các **tiến trình** thao tác **dữ liệu**. Ta sẽ tìm hiểu những ý tưởng này qua ngữ cảnh vi phân ký hiệu, biểu diễn tập hợp, và mã hóa thông tin.

Sau đó ta sẽ giải quyết bài toán làm việc với **dữ liệu** có thể được biểu diễn theo nhiều cách khác nhau ở các phần khác nhau của **chương trình**. Điều này đặt ra nhu cầu hiện thực các _phép toán tổng quát_, tức là các phép toán phải xử lý được nhiều kiểu dữ liệu. Để duy trì tính **mô đun** khi có các _phép toán tổng quát_, ta cần những _hàng rào trừu tượng_ mạnh hơn so với chỉ dùng _trừu tượng dữ liệu_ đơn thuần. Đặc biệt, ta sẽ giới thiệu _lập trình hướng dữ liệu_ như một kỹ thuật cho phép thiết kế từng biểu diễn dữ liệu một cách độc lập rồi kết hợp chúng một cách _cộng thêm_ (tức là không cần sửa đổi). Để minh họa sức mạnh của cách tiếp cận này, ta sẽ kết thúc chương bằng cách xây dựng một gói tính toán ký hiệu trên đa thức, trong đó các hệ số của đa thức có thể là số nguyên, số hữu tỉ, số phức, hay thậm chí là các đa thức khác.

---

[^1]: Khả năng thao tác trực tiếp các **quy trình** cũng mang lại sự mở rộng sức biểu đạt tương tự cho ngôn ngữ lập trình. Ví dụ, trong 1.3.1 ta đã giới thiệu **quy trình** `sum`, nhận một **quy trình** `term` làm **đối số** và tính tổng các giá trị của `term` trên một khoảng xác định. Để định nghĩa `sum`, điều then chốt là ta phải có thể nói đến một **quy trình** như `term` như một thực thể độc lập, không phụ thuộc vào cách `term` được xây dựng từ các phép tính nguyên thủy hơn. Thật vậy, nếu ta không có khái niệm "một **quy trình**", thì có lẽ ta sẽ không bao giờ nghĩ đến khả năng định nghĩa một phép tính như `sum`. Hơn nữa, về mặt thực hiện phép tính tổng, chi tiết về cách `term` được xây dựng từ các phép tính nguyên thủy hơn hoàn toàn không liên quan.

---

[`data`](GLOSSARY.md#data) [`compound data`](GLOSSARY.md#compound-data) [`procedure`](GLOSSARY.md#procedure) [`compound procedure`](GLOSSARY.md#compound-procedure) [`process`](GLOSSARY.md#process) [`program`](GLOSSARY.md#program) [`procedural abstraction`](GLOSSARY.md#procedural-abstraction) [`primitive data`](GLOSSARY.md#primitive-data) [`primitive procedure`](GLOSSARY.md#primitive-procedure) [`argument`](GLOSSARY.md#argument) [`parameter`](GLOSSARY.md#parameter) [`module`](GLOSSARY.md#module)
