# 2.2.1 Biểu diễn dãy

Một trong những cấu trúc hữu ích mà ta có thể xây dựng từ _cặp_ là một _dãy_ (sequence) — một tập hợp dữ liệu có thứ tự. Tất nhiên có nhiều cách biểu diễn dãy bằng cặp. Một cách đặc biệt tự nhiên là biểu diễn như hình 2.4: ta nối các cặp lại với nhau thành một chuỗi, trong đó `car` của mỗi cặp là phần tử tương ứng của dãy, còn `cdr` của cặp là cặp tiếp theo trong chuỗi. `cdr` của cặp cuối cùng trỏ đến một giá trị đặc biệt — không phải cặp nào cả — biểu thị rằng dãy đã kết thúc. Toàn bộ chuỗi này được gọi là _danh sách_ (list).[^1]

![Danh sách (1 2 3 4) được biểu diễn bằng các cặp nối tiếp nhau](./figures/figure21.png)

Dãy `1, 2, 3, 4` được biểu diễn như sau:

```lisp
(cons 1
      (cons 2
            (cons 3
                  (cons 4 nil))))
```

Ở đây, `nil` là giá trị kết thúc chuỗi, biểu thị danh sách rỗng.[^2] Toàn bộ danh sách có thể được hình dung như trong hình 2.4. Scheme cung cấp sẵn quy trình nguồn `list` để tiện xây dựng danh sách:[^3]

```lisp
(list ⟨a₁⟩ ⟨a₂⟩ … ⟨aₙ⟩)
```

Cách viết trên tương đương hoàn toàn với:

```lisp
(cons ⟨a₁⟩ (cons ⟨a₂⟩ (cons … (cons ⟨aₙ⟩ nil) …)))
```

Ví dụ:

```lisp
(list 1 2 3 4)
(1 2 3 4)
```

Các hệ thống Lisp in danh sách theo quy ước dưới dạng các phần tử nằm trong ngoặc đơn. Vì vậy, cách xây dựng bằng `cons` lồng nhau phía trên sẽ được in ra là `(1 2 3 4)`.

Hãy lưu ý một sự khác biệt quan trọng: `(list 1 2 3 4)` là một **biểu thức** — một lời gọi quy trình `list` — còn `(1 2 3 4)` là kết quả in ra của danh sách đó. Nếu bạn thử viết `(1 2 3 4)` trực tiếp để trình phiên dịch thực thi thì sẽ gặp lỗi, vì trình thông dịch sẽ hiểu đây là lời gọi quy trình `1` với các đối số `2`, `3`, `4`.

Ta có thể hình dung `one-through-four` là tên gọi của danh sách đó:

```lisp
(define one-through-four (list 1 2 3 4))

(car one-through-four)
1

(cdr one-through-four)
(2 3 4)

(car (cdr one-through-four))
2

(cons 10 one-through-four)
(10 1 2 3 4)

(cons 5 one-through-four)
(5 1 2 3 4)
```

Giá trị của `car` là phần tử đầu tiên của danh sách; giá trị của `cdr` là phần còn lại của danh sách — tức là danh sách trừ đi phần tử đầu tiên. Gọi `cons` với một phần tử và một danh sách sẽ tạo ra một danh sách mới với phần tử đó ở đầu. Danh sách rỗng là giá trị của `nil`, và nó được in ra là `()`.

## Các phép tính trên danh sách

Ngôn ngữ Scheme cung cấp quy trình nguồn `null?` để kiểm tra xem danh sách có rỗng không. Ta sẽ sử dụng liên tiếp các phép `cdr` trên danh sách — đây là kỹ thuật lập trình quy ước khi làm việc với danh sách.

Quy trình `list-ref` nhận một danh sách và một số `n`, trả về phần tử thứ `n` của danh sách (bắt đầu từ `0`):[^4]

- Khi `n = 0`, trả về `car` của danh sách.
- Ngược lại, trả về phần tử thứ `(n - 1)` của `cdr` của danh sách.

```lisp
(define (list-ref items n)
  (if (= n 0)
      (car items)
      (list-ref (cdr items) (- n 1))))

(define squares (list 1 4 9 16 25))

(list-ref squares 3)
16
```

Để tính độ dài của một danh sách, ta hay dùng kỹ thuật gọi đệ quy trên `cdr`:

```lisp
(define (length items)
  (if (null? items)
      0
      (+ 1 (length (cdr items)))))

(define odds (list 1 3 5 7))

(length odds)
4
```

Quy trình `length` dùng một mẫu đệ quy phổ biến: độ dài của bất kỳ danh sách nào bằng `1` cộng với độ dài của `cdr` của nó, với trường hợp cơ sở là danh sách rỗng có độ dài bằng `0`.

Ta cũng có thể tính độ dài theo cách lặp:

```lisp
(define (length items)
  (define (length-iter a count)
    (if (null? a)
        count
        (length-iter (cdr a) (+ 1 count))))
  (length-iter items 0))
```

Một phép tính khác trên danh sách là `append` — nối hai danh sách lại thành một:

```lisp
(append squares odds)
(1 4 9 16 25 1 3 5 7)

(append odds squares)
(1 3 5 7 1 4 9 16 25)
```

`append` được cài đặt cũng bằng kỹ thuật đệ quy theo `cdr`. Để nối `list1` và `list2`, ta suy nghĩ như sau:

- Nếu `list1` rỗng thì kết quả là `list2`.
- Ngược lại, kết quả là `cons` của `car` của `list1` lên kết quả của `append` giữa `cdr` của `list1` và `list2`.

```lisp
(define (append list1 list2)
  (if (null? list1)
      list2
      (cons (car list1)
            (append (cdr list1) list2))))
```

## Ánh xạ trên danh sách

Một thao tác rất hữu ích là áp dụng một phép biến đổi lên mỗi phần tử của danh sách và thu về danh sách kết quả. Chẳng hạn, quy trình sau nhân mỗi phần tử với một hệ số `factor`:

```lisp
(define (scale-list items factor)
  (if (null? items)
      nil
      (cons (* (car items) factor)
            (scale-list (cdr items) factor))))

(scale-list (list 1 2 3 4 5) 10)
(10 20 30 40 50)
```

Ta có thể trừu tượng hóa ý tưởng này thành quy trình `map`. `map` nhận một **quy trình** một đối số và một danh sách, trả về danh sách kết quả sau khi áp dụng **quy trình** đó lên từng phần tử:[^5]

```lisp
(define (map proc items)
  (if (null? items)
      nil
      (cons (proc (car items))
            (map proc (cdr items)))))

(map abs (list -10 2.5 -11.6 17))
(10 2.5 11.6 17)

(map (lambda (x) (* x x))
     (list 1 2 3 4))
(1 4 9 16)
```

Với `map`, ta viết lại `scale-list` gọn hơn:

```lisp
(define (scale-list items factor)
  (map (lambda (x) (* x factor))
       items))
```

`map` là một công cụ quan trọng không chỉ vì nó tiện dụng, mà còn vì nó thiết lập một _hàng rào trừu tượng_ (abstraction barrier) tách biệt việc cài đặt **quy trình** biến đổi danh sách khỏi chi tiết trích xuất và tổng hợp các phần tử của danh sách. Ví dụ, `scale-list` viết bằng `map` làm rõ rằng phép nhân hệ số biến đổi từng phần tử thành kết quả — bất kể cách danh sách được biểu diễn bên dưới là gì. Tính trừu tượng này giúp ta tự do thay đổi cách biểu diễn danh sách mà không ảnh hưởng đến các quy trình bên trên.

[^1]: Scheme sử dụng giá trị `'()` (danh sách rỗng) thay cho `nil` trong nhiều cài đặt hiện đại, nhưng tên `nil` vẫn được dùng trong sách này để chỉ danh sách kết thúc chuỗi cặp.

[^2]: `nil` là viết tắt của từ Latin _nihil_, nghĩa là "không có gì".

[^3]: Scheme cung cấp sẵn quy trình nguồn `list` để dễ xây dựng danh sách. `(list ⟨a₁⟩ ⟨a₂⟩ … ⟨aₙ⟩)` tương đương với `(cons ⟨a₁⟩ (cons ⟨a₂⟩ (cons … (cons ⟨aₙ⟩ nil) …)))`.

[^4]: Scheme cũng có các quy trình viết tắt cho các chuỗi `car`/`cdr` liên tiếp. Ví dụ, `cadr` là viết tắt của `(car (cdr ⟨x⟩))`, `caddr` là viết tắt của `(car (cdr (cdr ⟨x⟩)))`, và cứ thế. Trong các bài tập, ta sẽ dùng cả cách viết đầy đủ và viết tắt.

[^5]: Scheme cung cấp sẵn **quy trình** `map` có thể xử lý nhiều danh sách cùng lúc. Ví dụ, `(map + (list 1 2 3) (list 40 50 60) (list 700 800 900))` cho ra `(741 852 963)`. Trong phần này ta chỉ dùng `map` một danh sách.

---

[exercise 2.17](exercises/2.17.md) [exercise 2.18](exercises/2.18.md) [exercise 2.19](exercises/2.19.md) [exercise 2.20](exercises/2.20.md) [exercise 2.21](exercises/2.21.md) [exercise 2.22](exercises/2.22.md) [exercise 2.23](exercises/2.23.md) [`pair`](GLOSSARY.md#pair) [`list`](GLOSSARY.md#list) [`list-structured data`](GLOSSARY.md#list-structured-data) [`sequence`](GLOSSARY.md#sequence) [`procedure`](GLOSSARY.md#procedure) [`primitive procedure`](GLOSSARY.md#primitive-procedure) [`data`](GLOSSARY.md#data) [`element`](GLOSSARY.md#element) [`higher-order procedure`](GLOSSARY.md#higher-order-procedure) [`abstraction barrier`](GLOSSARY.md#abstraction-barrier)
