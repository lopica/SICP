# 2.4.1 Biểu diễn số phức

Hãy cùng phát triển một hệ thống thực hiện các phép tính số học trên số phức làm ví dụ đơn giản cho một chương trình có nhiều cách biểu diễn. Chúng ta sẽ bắt đầu bằng cách thảo luận về hai biểu diễn khả thi cho số phức. Số phức có thể được biểu diễn theo _dạng trực giao_ $x + yi$ (phần thực và phần ảo), hoặc theo _dạng cực_ $re^{i\theta}$ (mô đun và argument). Quan hệ giữa hai dạng này là:

$$x + yi = r e^{i\theta}$$

trong đó:

$$r = \sqrt{x^2 + y^2}, \qquad \tan\theta = \frac{y}{x}$$

và ngược lại:

$$x = r\cos\theta, \qquad y = r\sin\theta$$

Số phức có thể được coi là điểm trong mặt phẳng, trong đó trục thực là trục ngang và trục ảo là trục dọc. Với cách nhìn như vậy, dạng trực giao $(x, y)$ và dạng cực $(r, \theta)$ là hai cách đặc tả khác nhau cho cùng một điểm.

![Số phức như điểm trên mặt phẳng](./figures/figure71.png)

> **Lưu ý:** Bạn cần thêm `figures/figure71.png` — ảnh chụp từ hình 2.20 trong sách (Complex numbers as points in the plane).

Phép tính số học trên số phức sẽ trở nên rõ ràng nhất theo từng dạng biểu diễn:

$$\text{Phần thực}(z_1 + z_2) = \text{Phần thực}(z_1) + \text{Phần thực}(z_2)$$

$$\text{Phần ảo}(z_1 + z_2) = \text{Phần ảo}(z_1) + \text{Phần ảo}(z_2)$$

$$\text{Mô đun}(z_1 \cdot z_2) = \text{Mô đun}(z_1) \cdot \text{Mô đun}(z_2)$$

$$\text{Argument}(z_1 \cdot z_2) = \text{Argument}(z_1) + \text{Argument}(z_2)$$

Như vậy, phép cộng tự nhiên nhất với dạng trực giao, còn phép nhân tự nhiên nhất với dạng cực.[^1]

## Thiết kế với selectors và constructors trừu tượng

Dù cách biểu diễn nào được dùng ở tầng thấp, ta muốn một giao diện thống nhất ở tầng cao. Giả sử ta có bốn **bộ chọn**: `real-part`, `imag-part`, `magnitude`, và `angle`; và hai **bộ tạo**: `make-from-real-imag` tạo số phức từ phần thực và phần ảo, `make-from-mag-ang` tạo số phức từ mô đun và argument. Khi đó, các phép tính số học có thể được viết như sau:

```lisp
(define (add-complex z1 z2)
  (make-from-real-imag (+ (real-part z1) (real-part z2))
                       (+ (imag-part z1) (imag-part z2))))

(define (sub-complex z1 z2)
  (make-from-real-imag (- (real-part z1) (real-part z2))
                       (- (imag-part z1) (imag-part z2))))

(define (mul-complex z1 z2)
  (make-from-mag-ang (* (magnitude z1) (magnitude z2))
                     (+ (angle z1) (angle z2))))

(define (div-complex z1 z2)
  (make-from-mag-ang (/ (magnitude z1) (magnitude z2))
                     (- (angle z1) (angle z2))))
```

Để hoàn thiện hệ thống số phức, ta chỉ cần cung cấp một cách biểu diễn cụ thể và cài đặt các selectors cùng constructors. Có hai cách làm: Ben chọn dạng trực giao, Alyssa chọn dạng cực.

## Cách biểu diễn của Ben (dạng trực giao)

Ben Bitdiddle biểu diễn số phức bằng cách lưu trực tiếp phần thực và phần ảo. Với cách này, selector và constructor được định nghĩa như sau (lưu ý rằng `atan` trong Scheme nhận hai đối số):[^2]

```lisp
(define (real-part z) (car z))
(define (imag-part z) (cdr z))

(define (magnitude z)
  (sqrt (+ (square (real-part z))
           (square (imag-part z)))))

(define (angle z)
  (atan (imag-part z) (real-part z)))

(define (make-from-real-imag x y) (cons x y))

(define (make-from-mag-ang r a)
  (cons (* r (cos a)) (* r (sin a))))
```

## Cách biểu diễn của Alyssa (dạng cực)

Alyssa P. Hacker lại chọn dạng cực, lưu trực tiếp mô đun và argument:

```lisp
(define (real-part z)
  (* (magnitude z) (cos (angle z))))

(define (imag-part z)
  (* (magnitude z) (sin (angle z))))

(define (magnitude z) (car z))
(define (angle z) (cdr z))

(define (make-from-real-imag x y)
  (cons (sqrt (+ (square x) (square y)))
        (atan y x)))

(define (make-from-mag-ang r a) (cons r a))
```

## Sự xung đột giữa hai cách biểu diễn

Cả Ben lẫn Alyssa đều có thể cài đặt đầy đủ hệ thống số phức — phép cộng, trừ, nhân, chia đều hoạt động đúng trong hệ thống của mỗi người. Tuy nhiên, vấn đề nảy sinh khi ta muốn dùng cả hai cách biểu diễn trong cùng một hệ thống: nếu cả hai cùng định nghĩa `real-part`, `imag-part`, v.v. thì sẽ xung đột.

Giải pháp là đặt tên lại để phân biệt: gọi các **quy trình** của Ben là `real-part-rectangular`, `imag-part-rectangular`, v.v., và của Alyssa là `real-part-polar`, `imag-part-polar`, v.v. Khi đó, hệ thống cần một cơ chế để biết khi nào dùng các quy trình của Ben và khi nào của Alyssa. Cơ chế này là _thẻ kiểu_ (type tag) — chủ đề của mục 2.4.2.

[^1]: Trong thực tế, dạng trực giao thường được ưu tiên hơn dạng cực vì các sai số làm tròn khi chuyển đổi qua lại giữa hai dạng. Đây cũng là lý do ví dụ số phức ở đây có phần không thực tế. Tuy nhiên, nó cung cấp một minh họa rõ ràng cho thiết kế hệ thống dùng phép tính tổng quát, và là bước dạo đầu tốt cho các hệ thống thực chất hơn mà ta sẽ xây dựng sau trong chương này.

[^2]: Hàm arctangent ở đây được tính bởi quy trình `atan` trong Scheme, nhận hai đối số `y` và `x`, trả về góc có tang là $y/x$. Dấu của từng đối số xác định góc phần tư của góc đó.

---

[`procedure`](GLOSSARY.md#procedure) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`data abstraction`](GLOSSARY.md#data-abstraction) [`data`](GLOSSARY.md#data) [`compound data`](GLOSSARY.md#compound-data) [`pair`](GLOSSARY.md#pair) [`type tag`](GLOSSARY.md#type-tag) [`generic operation`](GLOSSARY.md#generic-operation)
