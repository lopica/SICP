# 2.1.1 Ví dụ: Phép tính số học trên số hữu tỉ

Giả sử chúng ta muốn thực hiện các phép tính số học trên số hữu tỉ. Ta muốn có thể cộng, trừ, nhân, chia và kiểm tra xem hai số hữu tỉ có bằng nhau hay không.

Hãy bắt đầu bằng cách giả định rằng ta đã có sẵn một cách nào đó để tạo ra một số hữu tỉ từ tử số và mẫu số. Ta cũng giả định rằng với một số hữu tỉ đã có, ta có thể trích xuất tử số và mẫu số của nó. Tức là, ta giả định đã có ba **quy trình** sau:

- `(make-rat <n> <d>)` trả về một số hữu tỉ có tử số là số nguyên `<n>` và mẫu số là số nguyên `<d>`.
- `(numer <x>)` trả về tử số của số hữu tỉ `<x>`.
- `(denom <x>)` trả về mẫu số của số hữu tỉ `<x>`.

Ở đây ta đang áp dụng một chiến lược xây dựng rất hiệu quả: _tư duy ước muốn_ (wishful thinking). Ta chưa hề nói số hữu tỉ được biểu diễn như thế nào, hay các quy trình `numer`, `denom` và `make-rat` cần được cài đặt ra sao. Dù vậy, nếu ta đã có ba **quy trình** này, ta có thể cộng, trừ, nhân, chia và kiểm tra bằng nhau dựa trên các quan hệ sau:

$$\frac{n_1}{d_1} + \frac{n_2}{d_2} = \frac{n_1 d_2 + n_2 d_1}{d_1 d_2}$$

$$\frac{n_1}{d_1} - \frac{n_2}{d_2} = \frac{n_1 d_2 - n_2 d_1}{d_1 d_2}$$

$$\frac{n_1}{d_1} \cdot \frac{n_2}{d_2} = \frac{n_1 n_2}{d_1 d_2}$$

$$\frac{n_1 / d_1}{n_2 / d_2} = \frac{n_1 d_2}{d_1 n_2}$$

$$\frac{n_1}{d_1} = \frac{n_2}{d_2} \text{ khi và chỉ khi } n_1 d_2 = n_2 d_1$$

Ta có thể biểu diễn các quy tắc này thành các **quy trình**:

```lisp
(define (add-rat x y)
  (make-rat (+ (* (numer x) (denom y))
               (* (numer y) (denom x)))
            (* (denom x) (denom y))))

(define (sub-rat x y)
  (make-rat (- (* (numer x) (denom y))
               (* (numer y) (denom x)))
            (* (denom x) (denom y))))

(define (mul-rat x y)
  (make-rat (* (numer x) (numer y))
            (* (denom x) (denom y))))

(define (div-rat x y)
  (make-rat (* (numer x) (denom y))
            (* (denom x) (numer y))))

(define (equal-rat? x y)
  (= (* (numer x) (denom y))
     (* (numer y) (denom x))))
```

Đến đây, ta đã định nghĩa xong các phép tính trên số hữu tỉ thông qua các quy trình selector và constructor là `numer`, `denom` và `make-rat`. Nhưng ta vẫn chưa định nghĩa những quy trình đó.

## Cặp (Pairs)

Để có thể cài đặt phần cụ thể của **dữ liệu** trừu tượng, ngôn ngữ Scheme cung cấp một cấu trúc tổng hợp gọi là _cặp_ (pair), được tạo ra bởi quy trình nguồn `cons`. Quy trình này nhận hai đối số và trả về một **dữ liệu** tổng hợp chứa cả hai đối số đó. Để lấy lại từng phần, ta dùng các quy trình nguồn `car` và `cdr`:[^1]

```lisp
(define x (cons 1 2))

(car x)
1

(cdr x)
2
```

Lưu ý rằng một cặp là một **dữ liệu** có thể được dùng như một đối tượng hoàn chỉnh. Cụ thể, ta có thể dùng `cons` để tạo một cặp mà các thành phần của nó cũng là các cặp, và cứ thế tiếp tục:

```lisp
(define x (cons 1 2))
(define y (cons 3 4))
(define z (cons x y))

(car (car z))
1

(car (cdr z))
3
```

Ở chương 2 này ta sẽ thấy rằng cách xây dựng cặp lồng nhau như vậy có thể được dùng làm nền tảng phổ quát để biểu diễn mọi loại **dữ liệu**. Vì vậy, _dữ liệu có cấu trúc danh sách_ (list-structured data) được xây dựng từ các cặp cũng thường được gọi là _dữ liệu cặp_ (pair data). Quy trình nguồn `cons`, `car` và `cdr` là "keo" duy nhất mà ta cần.

## Biểu diễn số hữu tỉ

Cặp cho ta cách hoàn chỉnh hệ thống số hữu tỉ. Đơn giản là biểu diễn một số hữu tỉ như một cặp gồm hai số nguyên: tử số và mẫu số. `make-rat`, `numer` và `denom` khi đó được cài đặt rất tự nhiên:[^2]

```lisp
(define (make-rat n d) (cons n d))
(define (numer x) (car x))
(define (denom x) (cdr x))
```

Thêm vào đó, để in kết quả tính toán ra màn hình, ta có thể in một số hữu tỉ bằng cách in tử số, dấu gạch chéo và mẫu số:[^3]

```lisp
(define (print-rat x)
  (newline)
  (display (numer x))
  (display "/")
  (display (denom x)))
```

Bây giờ ta có thể thử dùng các quy trình số hữu tỉ của mình:

```lisp
(define one-half (make-rat 1 2))

(print-rat one-half)
1/2

(define one-third (make-rat 1 3))

(print-rat (add-rat one-half one-third))
5/6

(print-rat (mul-rat one-half one-third))
1/6

(print-rat (add-rat one-third one-third))
6/9
```

Như ví dụ cuối cho thấy, cài đặt số hữu tỉ của ta chưa rút gọn phân số về dạng tối giản. Ta có thể khắc phục điều này bằng cách thay đổi `make-rat`. Nếu ta có quy trình `gcd` như đã định nghĩa trong mục 1.2.5 để tìm ước số chung lớn nhất của hai số nguyên, ta có thể dùng nó để rút gọn tử số và mẫu số về dạng tối giản trước khi tạo cặp:

```lisp
(define (make-rat n d)
  (let ((g (gcd n d)))
    (cons (/ n g) (/ d g))))
```

Bây giờ ta có:

```lisp
(print-rat (add-rat one-third one-third))
2/3
```

Như ta thấy, sự thay đổi này được thực hiện chỉ bằng cách sửa constructor `make-rat`, mà không cần động chạm gì đến bất kỳ quy trình nào cài đặt các phép tính thực sự (chẳng hạn `add-rat` hay `mul-rat`).

[^1]: Tên `cons` là viết tắt của "construct" (xây dựng). Tên `car` và `cdr` xuất phát từ cách cài đặt Lisp đầu tiên trên máy IBM 704. Máy đó có một cơ chế địa chỉ cho phép tham chiếu đến phần "address" (địa chỉ) và phần "decrement" (giảm) của một ô nhớ. `car` là viết tắt của "Contents of Address part of Register" (nội dung phần địa chỉ của thanh ghi) và `cdr` (đọc là "could-er") là viết tắt của "Contents of Decrement part of Register" (nội dung phần giảm của thanh ghi).

[^2]: Cách khác để định nghĩa selector và constructor là:

    ```lisp
    (define make-rat cons)
    (define numer car)
    (define denom cdr)
    ```

    Cách này hiệu quả hơn vì `make-rat` _là_ `cons` thay vì _gọi_ `cons`, tránh một lớp gọi quy trình thừa.

[^3]: `display` là quy trình nguồn của Scheme để in dữ liệu ra màn hình. `newline` là quy trình nguồn để xuống dòng. Cả hai quy trình này đều không trả về giá trị có ý nghĩa, vì vậy trong các ví dụ dùng `print-rat` bên dưới, ta chỉ hiển thị những gì `print-rat` in ra, chứ không phải giá trị mà trình phiên dịch trả về từ lời gọi `print-rat`.

---

[exercise 2.1](exercises/2.1.md) [`data`](GLOSSARY.md#data) [`procedure`](GLOSSARY.md#procedure) [`compound data`](GLOSSARY.md#compound-data) [`primitive procedure`](GLOSSARY.md#primitive-procedure) [`pair`](GLOSSARY.md#pair) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`data abstraction`](GLOSSARY.md#data-abstraction) [`wishful thinking`](GLOSSARY.md#wishful-thinking)
