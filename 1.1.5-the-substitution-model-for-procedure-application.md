# 1.1.5. Mô hình thay thế cho áp dụng quy trình
Để thực thi `sự kết hợp` mà phép biến đổi là một quy trình tổng hợp, trình phiên dịch theo tiến trình tương tự như khi `sự kết hợp` thực thi phép biến đổi nguồn. Đó là điều chúng ta đã thảo luận ở 1.1.3. Nó là như vậy, trình phiên dịch thực thi các thành phần của `sự kết hợp` và áp dụng vào quy trình (cái là kết quả của thực thi phép biến đổi của `sự kết hợp` ) thông qua đối số (là các giá trị trong toán tử của `sự kết hợp` ).

Chúng ta có thể giả sử cơ chế áp dụng quy trình nguồn với các đối số được tích hợp sẵn trong trình phiên dịch. Với quy trình tổng hợp, tính trình áp dụng được thực hiện như sau:

> Để áp dụng một quy trình tổng hợp với các đối số, thực thi phần thân của quy 
> trình với mỗi tham số được thay bằng đối số tương ứng.

Để miêu tả tiến trình này, chúng ta hãy cùng thực thi `sự kết hợp` này:
```bash
(f 5)
```
với `f` là quy trình được định nghĩa ở 1.1.4. Chúng ta bắt đầu thực thi bằng cách xem lại phần thân của `f`:
```bash
(sum-of-squares (+ a 1) (* a 2))
```
Sau đó chúng ta thay các tham số với đối số `5`:
```bash
(sum-of-squares (+ 5 1) (* 5 2))
```
Do đó vấn đề được giảm xuống là thực thi một `sự kết hợp` với 2 toán tử và phép biến đổi là `sum-of-squares`. Thực thi `sự kết hợp` này có 3 vấn đề con. Chúng ta cần thực thi phép biến đổi để biết quy trình cần áp dụng, và chúng ta cần thực thi toán tử để có đối số. Bây giờ `(+ 5 1)` trả ra `6` và `(* 5 2)` trả ra `10`, do đó chúng ta áp dụng quy trình `sum-of-squares` với `6` và `10`. Các giá trị này là sự thay thế cho cho các tham số x và y trong phần thân của `sum-of-squares`, biến nó thành biểu thức:
```bash
(+ (square 6) (square 10))
```
Nếu chúng ta tiếp tục dùng định nghĩa của `square`, nó biến thành:
```bash
(+ (* 6 6) (* 10 10))
```
và nó lại tiếp tục biến thành
```bash
(+ 36 100)
```
và cuối cùng
```bash
136
```
Tiến trình chúng ta vừa miêu tả được gọi là "mô hình sự thay thế cho áp dụng quy trình". Nó có thể dùng như mô hình để xác định phương tiện cho việc sử dụng quy trình, đó là những gì chương này đề cập đến quy trình cho đến thời điểm hiện tại. Tuy nhiên, có 2 điều cần được lưu ý:

- Mục đích của sự thay thế là để giúp chúng ta nghĩ về việc áp dụng quy trình,
không phải là cách trình phiên dịch hoạt động. Các trình phiên dịch thông thường không thực thi việc áp dụng quy trình bằng cách thay đổi chữ của một quy trình sang giá trị thay thế ở chỗ các tham số. Trong thực tế, sự thay thế được tạo ra bằng cách dùng một môi trường cục bộ cho các tham số đó. Chúng ta sẽ thảo luận kỹ hơn ở chương 3 và 4, nơi quan sát việc thực thi của trình phiên dịch kỹ hơn.
- Thông qua quyển sách này, chúng ta sẽ biểu diễn một chuỗi các mô hình phức tạp
thể hiện cách trình phiên dịch hoạt động, và cuối cùng là toàn bộ phần triển khai của trình phiên dịch và trình biên dịch ở chương 5. Mô hình thay thế chỉ là mô hình đầu tiên, là điểm bắt đầu để chúng ta bắt đầu nghĩ về cách thực thi tiến trình. Nhìn chung, khi mô hình hóa hiện tượng trong khoa học và kĩ thuật, chúng ta bắt đầu với những mô hình đơn giản, không hoàn thiện. Và khi chúng ta đi vào cụ thể, các mô hình đơn giản không còn đáp ứng được và cần phải thay thế bởi các mô hình hoàn chỉ hơn. Mô hình thay thế không phải là ngoại lệ. Cụ thể, khi chúng ta giải quyết việc sử dụng quy trình với dữ liệu có thể thay đổi ở chương 3, chúng ta sẽ thấy rằng mô hình thay thế này sụp đỏ và cần phải được thay thế bởi một mô hình phức tạp hơn mô tả việc áp dụng quy trình.

# Thứ tự áp dụng và thứ tự thông thường
Theo như mô tả về sự thực thi trong 1.1.3, trình phiên dịch đầu tiên thực thi phép biến đổi và các toán tử và sau đó áp dụng kết quả thực thi của phép biến đổi với kết quả thực thi các toán tử và giờ là các đối số. Đây không phải là cách duy nhất để thực thi. Một mô hình thực thi thay thế khác là không thực thi các toán tử cho đến khi ta cần chúng. Nó sẽ đầu tiên thay thế các biểu thức toán tử thay vì tham số cho đến khi có biểu thức toàn các phép biến đổi nguồn. và sau đó thực thi. Nếu chúng ta dùng phương pháp này, sự thực thi của `(f 5)` sẽ được xử lý theo chuỗi mở rộng:
```bash
(sum-of-squares (+ 5 1) (* 5 2))

(+ (square (+ 5 1)) 
   (square (* 5 2)))

(+ (* (+ 5 1) (+ 5 1)) 
   (* (* 5 2) (* 5 2)))
```
và sau đó biến đổi
```bash
(+ (* 6 6) 
   (* 10 10))

(+ 36 100)

136
```
Nó trả ra kết quả giống như mô hình cũ, nhưng quá trình là khác nhau. Cụ thể, sự thực thi của `(+ 5 1)` và `(* 5 2)` diễn ra 2 lần, đến từ việc biến đổi biểu thức `(* x x)` bằng cách thay thế x thành `(+ 5 1)` và `(+ 5 2)`.

Cách thực thi này còn được gọi là "thực thi thông thường", ngược với thực thi đối số và sau đó áp dụng, cách mà trình phiên dịch thực tế dùng, và còn được gọi là "thực thi theo thứ tự áp dụng". Có thể thấy rằng, với các áp dụng quy trình mà dùng sự thay thế (bao gồm toàn bộ các quy trình ở 2 chương đầu) và trả về các giá trị hợp lệ, "thực thi thông thường" và "thực thi áp dụng" cho ra cùng kết quả

Lisp dùng "thực thi áp dụng", một phần vì hiệu năng, và còn vì "thực thi thông thường" khó để triển khai nếu chúng ta rời bỏ thế giới của quy trình mà có thể mô hình bởi sự thay thế. Nói cách khác, "thực thi thông thường" có thể là một công cụ hữu ích, và chúng ta sẽ quan sát một chút phần triển khai của nó ở chương 3 và 4.
