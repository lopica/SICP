# 1.1.3. Thực thi `sự kết hợp`
Một trong các mục tiêu trong chương này là làm rõ vấn đề về việc tư duy có quy
trình. Tại thời điểm này, khi chúng ta nghĩ về nó, khi thực thi `sự kết hợp`, trình phiên dịch tự nó đã tuân theo một quy trình.

Để thực thi `sự kết hợp`, nó làm theo các bước sau:

1. Thực thi các biểu thức con của `sự kết hợp`
2. Ápp dụng quy trình mà quy trình đó chính là giá trị nằm bên trái ngoài cùng (chính là phép biến đổi) với đối số chính là các giá trị của các biểu thức con khác (các toán tử)

Luật này tuy đơn giản nhưng nó thể hiện một số tính chất chung quan trọng của
tiến trình. Đầu tiên, ta thấy rằng bước 1 thể hiện rằng để hoàn thành việc thực
thi một tiến trình của `sự kết hợp`, chúng ta cần trước tiên thực thi các tiến
trình ở mỗi thành phần của `sự kết hợp`. Do đó, luật thực thi là "đệ quy" theo 
tự nhiên; đó là tất cả, nó bao gồm việc trong từng bước, nó cần thực thi luật 
của nó.

Để ý rằng ý tưởng đệ quy có thể dùng để diễn giải xúc tích, trong trường hợp mà
`sự kết hợp` lồng nhau rất sâu, cũng sẽ chỉ được coi là một tiến trình phức tạp
hơn. Ví dụ, thực thi
```bash
(* (+ 2 (* 4 6)) (+ 3 5 7))
```
yêu cầu luật thực thi được áp dụng dụng cho 4 `sự kết hợp` khác nhau. Chúng ta
có thể thấy được bức tranh của tiến trình này bằng cách biểu diễn `sự kết hợp`
dưới dạng một cái cây, như ở hình dưới đây:

![Cây đánh giá](./figures/figure1.png)
