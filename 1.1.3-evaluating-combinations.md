# 1.1.3. Thực thi `sự kết hợp`

Một trong các mục tiêu trong chương này là làm rõ vấn đề về việc tư duy có quy
trình. Tại thời điểm này, khi chúng ta nghĩ về nó, khi thực thi `sự kết hợp`, trình phiên dịch tự nó đã tuân theo một quy trình.

Để thực thi `sự kết hợp`, nó làm theo các bước sau:

1. Thực thi các biểu thức con của `sự kết hợp`
2. Ápp dụng quy trình mà quy trình đó chính là giá trị nằm bên trái ngoài cùng (chính là phép biến đổi) với đối số chính là các giá trị của các biểu thức con khác (các toán tử)

Luật này tuy đơn giản nhưng nó thể hiện một số tính chất chung quan trọng của
tiến trình. Đầu tiên, ta thấy rằng bước 1 thể hiện rằng để hoàn thành việc thực
thi một tiến trình của `sự kết hợp`, chúng ta cần trước tiên thực thi các tiến
trình ở mỗi thành phần của `sự kết hợp`. Do đó, luật thực thi là "đệ quy" theo
tự nhiên; đó là tất cả, nó bao gồm việc trong từng bước, nó cần thực thi luật
của nó.

Để ý rằng ý tưởng đệ quy có thể dùng để diễn giải xúc tích, trong trường hợp mà
`sự kết hợp` lồng nhau rất sâu, cũng sẽ chỉ được coi là một tiến trình phức tạp
hơn. Ví dụ, thực thi

```bash
(* (+ 2 (* 4 6)) (+ 3 5 7))
```

yêu cầu luật thực thi được áp dụng dụng cho 4 `sự kết hợp` khác nhau. Chúng ta
có thể thấy được bức tranh của tiến trình này bằng cách biểu diễn `sự kết hợp`
dưới dạng một cái cây, như ở hình dưới đây:

![Cây đánh giá](./figures/figure1.png)

Tiếp theo, ta thấy việc áp dụng lặp lại của bước 1 khiến ta nhận ra rằng cái
chúng ta cần thực thi, không phải là `sự kết hợp`, mà biểu thức nguồn như là số,
phép biến đổi có sẵn, hoặc một số thứ khác. Chúng ta đảm bảo xử lý cho các
trường hợp nguồn bằng cách:

- các giá trị của số chính là các số được viết
- các giá trị của các phép biến đổi là một chuỗi hướng dẫn máy để thực hiện phép
  biến đổi đó, và
- các giá trị khác là các đối tượng có liên kết với các tên trong môi trường

Chúng ta có thể coi luật thứ 2 là một trường hợp đặc biệt của luật thứ 3 bằng cách quy định các ký hiệu như + hay \* cũng được chứa ở trong môi trường toàn cục
và có liên kết với chuỗi hướng dẫn máy rằng giá trị của dấu chính là giá trị của
nó. Ý chính cần nắm ở đây là vai trò của môi trường trong việc xác định nghĩa
nghĩa của ký hiệu trong các biểu thức. Trong một ngôn ngữ có tính tương tác cao như Lisp, là vô nghĩa khi nói về giá trị của biểu thức như là `(+ x 1)` mà không
nói rõ về môi trường mà cung cấp ý nghĩa cho ký hiệu x (hay thậm chí là ký hiệu +).
Chúng ta sẽ thấy ở chương 3, ý tưởng chung của môi trường, là cung cấp ngữ cảnh
mà việc thực thi diễn ra, sẽ đóng vai trò quan trọng để chúng ta hiểu được sự
thực thi của chương trình.

Để ý là việc thực thi các luật ở trên không xử lý phần dịnh nghĩa. Ví dụ, thực
thi `(define x 3)` không áp dụng `define` với 2 đối số kia, một cái là giá trị
mà ký hiệu x chứa và một cái là 3, bởi vì mục đích của `define` là liên kết ký hiệu
x với một giá trị. (nó là như vậy, `(define x 3)` không phải là `sự kết hợp`)

Một số ngoại lệ của luật thực thi chung được gọi là "trường hợp đặc biệt".
`define` chỉ là 1 ví dụ trong số các "trường hợp đặc biệt", nhưng chúng ta sẽ
thấy cái cái khác sớm thôi. Mỗi "trường hợp đặc biệt" lại có luật thực thi riêng
Sự đa dạng của các biểu thức (mỗi cái lại gắn với một luật) tạo ra cú pháp của
ngôn ngữ lập trình. Khi so sánh với đa số ngôn ngữ lập trình khác, Lisp có cú
pháp rất đơn giản; luật thực thi cho các biểu thức có thể được miêu tả bằng một
luật chung với các luật đặc biệt cho số lượng nhỏ các "trường hợp đặc biệt".
