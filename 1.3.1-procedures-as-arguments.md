# 1.3.1 Quy trình như là đối số

Hãy xét ba quy trình sau đây. Quy trình thứ nhất tính tổng các số nguyên từ `a` đến `b`:

```lisp
(define (sum-integers a b)
  (if (> a b)
      0
      (+ a (sum-integers (+ a 1) b))))
```

Quy trình thứ hai tính tổng lập phương của các số nguyên trong cùng khoảng đó:

```lisp
(define (sum-cubes a b)
  (if (> a b)
      0
      (+ (cube a)
         (sum-cubes (+ a 1) b))))
```

Quy trình thứ ba tính tổng của một dãy các số hạng trong chuỗi

$$\frac{1}{1 \cdot 3} + \frac{1}{5 \cdot 7} + \frac{1}{9 \cdot 11} + \cdots$$

chuỗi này hội tụ đến $\pi/8$ (rất chậm):[^1]

```lisp
(define (pi-sum a b)
  (if (> a b)
      0
      (+ (/ 1.0 (* a (+ a 2)))
         (pi-sum (+ a 4) b))))
```

Ba quy trình này có chung một khuôn mẫu bên dưới. Chúng gần như giống hệt nhau, chỉ khác ở tên quy trình, hàm dùng để tính số hạng cần cộng, và hàm tính giá trị tiếp theo của `a`. Ta có thể tạo ra mỗi quy trình bằng cách điền vào những "ô trống" trong cùng một khuôn mẫu:

```lisp
(define (⟨name⟩ a b)
  (if (> a b)
      0
      (+ (⟨term⟩ a)
         (⟨name⟩ (⟨next⟩ a) b))))
```

Sự có mặt của khuôn mẫu chung này chứng tỏ có một sự trừu tượng hữu ích đang chờ được nắm bắt. Các nhà toán học từ lâu đã nhận ra điều này và phát minh ra _ký hiệu sigma_ để diễn đạt khái niệm này:

$$\sum_{n=a}^{b} f(n) = f(a) + \cdots + f(b)$$

Sức mạnh của ký hiệu sigma nằm ở chỗ nó cho phép các nhà toán học làm việc với khái niệm tổng hóa (_summation_) như một khái niệm độc lập, chứ không chỉ là các phép tính cộng cụ thể. Tương tự vậy, trong vai trò những người lập trình, ta nên muốn ngôn ngữ của mình đủ mạnh để viết một quy trình biểu đạt chính khái niệm tổng hóa đó, thay vì chỉ những quy trình tính các tổng cụ thể. Điều này ta làm được dễ dàng trong ngôn ngữ quy trình của mình, bằng cách lấy khuôn mẫu trên và biến những "ô trống" thành **tham số hình thức**:

```lisp
(define (sum term a next b)
  (if (> a b)
      0
      (+ (term a)
         (sum term (next a) next b))))
```

Lưu ý rằng `sum` nhận vào cận dưới `a` và cận trên `b`, cùng với hai quy trình `term` và `next` làm **đối số**. Ta có thể dùng `sum` hệt như bất kỳ quy trình nào. Ví dụ, ta có thể dùng nó để định nghĩa `sum-cubes`:

```lisp
(define (inc n) (+ n 1))

(define (sum-cubes a b)
  (sum cube a inc b))
```

Dùng nó ta thu được:[^2]

```lisp
(sum-cubes 1 10)
3025
```

Với quy trình `identity` (hàm đồng nhất), ta cũng có thể định nghĩa `sum-integers` bằng `sum`:

```lisp
(define (identity x) x)

(define (sum-integers a b)
  (sum identity a inc b))
```

Sau đó:

```lisp
(sum-integers 1 10)
55
```

Ta cũng có thể định nghĩa `pi-sum` theo cách tương tự:[^3]

```lisp
(define (pi-sum a b)
  (define (pi-term x)
    (/ 1.0 (* x (+ x 2))))
  (define (pi-next x)
    (+ x 4))
  (sum pi-term a pi-next b))
```

Với điều này, ta có thể tính một xấp xỉ của $\pi$:

```lisp
(* 8 (pi-sum 1 1000))
3.139592655589783
```

Khi đã có `sum` như một khái niệm nền tảng, ta có thể xây dựng thêm các khái niệm khác. Chẳng hạn, tích phân xác định của một hàm $f$ giữa các cận $a$ và $b$ có thể được xấp xỉ bằng công thức số

$$\int_a^b f = \left[ f\!\left(a + \frac{dx}{2}\right) + f\!\left(a + dx + \frac{dx}{2}\right) + f\!\left(a + 2dx + \frac{dx}{2}\right) + \cdots \right] dx$$

với $dx$ đủ nhỏ. Ta có thể biểu đạt điều này trực tiếp như một quy trình:

```lisp
(define (integral f a b dx)
  (define (add-dx x) (+ x dx))
  (* (sum f (+ a (/ dx 2.0)) add-dx b)
     dx))
```

```lisp
(integral cube 0 1 0.01)
.24998750000000042

(integral cube 0 1 0.001)
.249999875000001
```

(Giá trị chính xác của tích phân của `cube` từ `0` đến `1` là $\frac{1}{4}$.)

---

[^1]: Chuỗi này được biết đến liên quan đến công thức Leibniz–Gregory; nó hội tụ quá chậm để tính $\pi$ trong thực tế.

[^2]: Nhận xét rằng `sum-cubes` và `sum-integers` bây giờ không còn cần phải biết gì về kết cấu nội bộ của `sum` nữa — chúng chỉ cần biết `sum` nhận `term`, `a`, `next`, `b` làm đối số.

[^3]: Ở đây ta dùng _định nghĩa nội bộ_ (`pi-term` và `pi-next`) để giữ các quy trình phụ trong phạm vi của `pi-sum`. Cách dùng `lambda` để viết gọn hơn sẽ được trình bày trong mục 1.3.2.

---

[`procedure`](GLOSSARY.md#procedure) [`parameter`](GLOSSARY.md#parameter) [`argument`](GLOSSARY.md#argument) [`higher-order procedure`](GLOSSARY.md#higher-order-procedure) [`formal parameter`](GLOSSARY.md#formal-parameter) [`internal definitions`](GLOSSARY.md#internal-definitions)
