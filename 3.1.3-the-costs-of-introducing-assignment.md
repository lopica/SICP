# 3.1.3 Chi phí của việc đưa vào phép gán

Như ta đã thấy, phép tính `set!` cho phép mô hình hóa các đối tượng có **trạng thái** cục bộ. Tuy nhiên, ưu điểm đó đi kèm với một cái giá: ngôn ngữ lập trình của ta không còn có thể giải thích bằng _mô hình sự thay thế_ của việc áp dụng **quy trình** mà ta đã giới thiệu ở mục 1.1.5. Hơn nữa, sẽ không có mô hình đơn giản nào với tính chất toán học "đẹp" có thể là nền tảng đầy đủ để lập luận về các đối tượng và phép gán trong ngôn ngữ lập trình.

Để hiểu tại sao phép gán lại làm phức tạp mọi thứ, hãy xét một phiên bản rút gọn của **quy trình** `make-withdraw` ở mục 3.1.1 — phiên bản không kiểm tra xem số tiền rút có vượt quá số dư hay không:

```lisp
(define (make-simplified-withdraw balance)
  (lambda (amount)
    (set! balance (- balance amount))
    balance))

(define W (make-simplified-withdraw 25))

(W 20)
5

(W 10)
-5
```

Hãy so sánh với **quy trình** sau, không dùng `set!`:

```lisp
(define (make-decrementer balance)
  (lambda (amount)
    (- balance amount)))
```

`make-decrementer` trả về một **quy trình** trừ đầu vào của nó khỏi một số dư cố định, nhưng không có hiệu ứng phụ tích lũy giống `make-simplified-withdraw`:

```lisp
(define D (make-decrementer 25))

(D 20)
5

(D 10)
15
```

Ta có thể dùng _mô hình sự thay thế_ để giải thích cách `make-decrementer` hoạt động. Ví dụ, hãy phân tích việc thực thi biểu thức

```lisp
((make-decrementer 25) 20)
```

Đầu tiên ta rút gọn toán hạng của **biểu thức kết hợp** bên ngoài. `make-decrementer` thay thế `balance` bằng `25`, trả về `(lambda (amount) (- 25 amount))`. Sau đó ta áp dụng **quy trình** này với đối số `20`:

```lisp
((lambda (amount) (- 25 amount)) 20)
```

Thay `amount` bằng `20`, ta có:

```lisp
(- 25 20)
```

Kết quả cuối cùng là `5`.

Bây giờ hãy thử phân tích tương tự với `make-simplified-withdraw`:

```lisp
((make-simplified-withdraw 25) 20)
```

Rút gọn toán hạng bên ngoài, thay `balance` bằng `25`:

```lisp
((lambda (amount) (set! balance (- 25 amount)) 25) 20)
```

Rồi áp dụng kết quả với `20`, thay `amount` bằng `20`:

```lisp
(set! balance (- 25 20)) 25
```

Nếu tuân theo mô hình sự thay thế, ta sẽ phải nói rằng ý nghĩa của việc áp dụng **quy trình** này là: đầu tiên gán `balance` bằng `5`, rồi trả về `25` làm giá trị của **biểu thức**. Nhưng kết quả đó sai — `(W 20)` trả về `5`, không phải `25`. Vấn đề là: nếu đặt `25` vào chỗ `balance` trong `set!`, ta sẽ nhận được `(set! 25 (- 25 amount))` — một biểu thức vô nghĩa.[^1]

Vấn đề cốt lõi ở đây là: mô hình sự thay thế dựa trên giả định rằng các ký hiệu trong ngôn ngữ của ta về bản chất chỉ là _tên_ của các giá trị. Nhưng ngay khi ta đưa vào `set!` và ý niệm rằng giá trị của một **biến** có thể thay đổi, **biến** không còn là tên đơn thuần nữa. Lúc này **biến** chỉ đến một _vị trí_ nơi giá trị được lưu trữ, và giá trị tại vị trí đó có thể thay đổi. Phần 3.2 sẽ giới thiệu _mô hình môi trường_ của việc thực thi **quy trình** — một cách nhìn giúp ta hiểu đúng `set!`.

## Giống nhau và thay đổi

Sự phức tạp mà ta vừa gặp còn đi sâu hơn. Khi một ngôn ngữ có phép gán, khái niệm "giống nhau" (sameness) và "thay đổi" (change) trở nên rất khó nắm bắt chính xác.

Giả sử ta gọi `make-decrementer` hai lần với cùng đối số, tạo ra hai **quy trình**:

```lisp
(define D1 (make-decrementer 25))
(define D2 (make-decrementer 25))
```

`D1` và `D2` có "giống nhau" không? Có — vì chúng hoàn toàn tương đương nhau về hành vi: gọi `D1` hay `D2` với cùng một đối số đều cho cùng kết quả. Trên thực tế, `D1` có thể thay thế `D2` ở bất kỳ chỗ nào trong tính toán mà không làm thay đổi kết quả.

Ngược lại, hãy tạo hai đối tượng rút tiền:

```lisp
(define W1 (make-simplified-withdraw 25))
(define W2 (make-simplified-withdraw 25))

(W1 20)
5

(W1 20)
-15

(W2 20)
5
```

`W1` và `W2` không "giống nhau" — các lần gọi đến `W1` và `W2` có những tác dụng riêng biệt. Lần gọi `(W1 20)` thứ hai trả về `-15` trong khi `(W2 20)` vẫn trả về `5`, dù cả hai được tạo ra từ cùng một lời gọi `make-simplified-withdraw` với cùng đối số ban đầu.

Một ngôn ngữ hỗ trợ tính chất "đẳng thức có thể thay thế cho đẳng thức" trong một **biểu thức** mà không làm thay đổi giá trị của **biểu thức** đó được gọi là ngôn ngữ có _tính trong suốt tham chiếu_ (_referential transparency_). Khi ta đưa `set!` vào ngôn ngữ, tính trong suốt tham chiếu bị vi phạm. Điều này khiến việc xác định khi nào ta có thể đơn giản hóa **biểu thức** bằng cách thay thế các **biểu thức** tương đương trở nên khó khăn. Do đó, lập luận về các **chương trình** có phép gán trở nên khó hơn rất nhiều.

Khi ta từ bỏ tính trong suốt tham chiếu, khái niệm "hai đối tượng có giống nhau không" trở nên rất khó diễn đạt chính xác. Thực ra, ý nghĩa của "giống nhau" và "thay đổi" phụ thuộc lẫn nhau theo vòng tròn: để xác định xem "mọi thứ có thay đổi không", ta cần kiểm tra xem "nó vẫn giống như trước không"; nhưng để biết điều đó, ta phải có khái niệm "giống nhau" trước khi quan sát sự thay đổi.

Ví dụ về tài khoản ngân hàng chung làm rõ điều này. Giả sử Peter và Paul đều có tài khoản:

```lisp
(define peter-acc (make-account 100))
(define paul-acc (make-account 100))
```

Đây là hai tài khoản độc lập. Các giao dịch vào tài khoản của Peter không ảnh hưởng đến tài khoản của Paul, và ngược lại. Nhưng nếu thay vào đó:

```lisp
(define peter-acc (make-account 100))
(define paul-acc peter-acc)
```

thì `peter-acc` và `paul-acc` là hai tên của _cùng một_ đối tượng. Nếu Peter rút tiền thì số dư tài khoản của Paul cũng giảm theo. Nếu ta đang tìm kiếm mọi nơi trong **chương trình** mà `paul-acc` có thể bị thay đổi, ta phải nhớ cả những chỗ thay đổi `peter-acc`.[^2]

Hiện tượng một đối tượng tính toán duy nhất được truy cập bởi nhiều tên được gọi là _bí danh_ (_aliasing_). Trong ví dụ ngân hàng, đây là trường hợp đơn giản. Ở mục 3.3 ta sẽ gặp những cấu trúc phức tạp hơn, trong đó những phần "khác nhau" lại chia sẻ chung một cấu trúc con. Phép gán kết hợp với bí danh có thể gây ra các lỗi rất khó phát hiện vì người lập trình quên mất mình đang dùng bí danh.

Nhìn lại, trong chừng mực ta không bao giờ sửa đổi các đối tượng dữ liệu, ta có thể coi một **dữ liệu** tổng hợp như chính xác là tổng thể của các phần của nó. Ví dụ, một số hữu tỉ được xác định hoàn toàn bởi tử số và mẫu số của nó. Khi ta đưa vào sự thay đổi, một đối tượng tổng hợp có một _lịch sử_ riêng, và bản sắc của nó trở nên tách biệt với các phần tạo nên nó.

## Cạm bẫy của lập trình mệnh lệnh

Trái ngược với _lập trình hàm_ (functional programming) — không dùng phép gán — lập trình dựa nhiều vào phép gán được gọi là _lập trình mệnh lệnh_ (_imperative programming_). Ngoài những rắc rối đã đề cập về trạng thái và giống nhau, các **chương trình** mệnh lệnh còn dễ sinh ra lỗi không thể xảy ra trong lập trình hàm.

Hãy nhớ lại **quy trình** tính giai thừa theo **tiến trình lặp** từ mục 1.2.1:

```lisp
(define (factorial n)
  (define (iter product counter)
    (if (> counter n)
        product
        (iter (* counter product)
              (+ counter 1))))
  (iter 1 1))
```

Thay vì truyền trạng thái qua **tham số**, ta có thể viết lại bằng cách dùng `set!` để cập nhật các **biến** trực tiếp:

```lisp
(define (factorial n)
  (let ((product 1)
        (counter 1))
    (define (iter)
      (if (> counter n)
          product
          (begin (set! product (* counter product))
                 (set! counter (+ counter 1))
                 (iter))))
    (iter)))
```

Phiên bản này tính ra đúng kết quả, nhưng thứ tự hai lệnh gán không phải tùy ý. Nếu ta đổi thứ tự:

```lisp
(set! counter (+ counter 1))
(set! product (* counter product))
```

ta sẽ nhân `product` với giá trị _mới_ của `counter` thay vì giá trị cũ, và kết quả sẽ sai.

Nói chung, lập trình với phép gán buộc ta phải cẩn thận xem xét thứ tự tương đối của các lệnh gán, để đảm bảo mỗi câu lệnh đang sử dụng phiên bản đúng của các biến đã thay đổi. Vấn đề này hoàn toàn không phát sinh trong các **chương trình** hàm.[^3]

Sự phức tạp do phép gán gây ra sẽ còn tệ hơn nhiều khi ta xét các **tiến trình** chạy đồng thời. Ta sẽ quay lại vấn đề này ở mục 3.4. Nhưng trước tiên, ta sẽ giải quyết vấn đề cung cấp một mô hình tính toán có thể giải thích các **biểu thức** liên quan đến phép gán.

---

[^1]: Ta không thay thế `balance` trong biểu thức `set!` vì tên `⟨name⟩` trong `set!` không được thực thi. Nếu ta thay thế vào đó, ta sẽ nhận được `(set! 25 (- 25 amount))`, một điều vô nghĩa.

[^2]: Hiện tượng một đối tượng tính toán duy nhất được truy cập bởi nhiều tên được gọi là _bí danh_ (_aliasing_). Bí danh có thể dẫn đến các "lỗi tác dụng phụ" (side-effect bugs) rất khó tìm ra vì người lập trình quên mất rằng nhiều tên có thể chỉ về cùng một đối tượng.

[^3]: Điều trớ trêu là lập trình mệnh lệnh thường được dạy cho người mới bắt đầu, có lẽ do những niềm tin từ thập niên 1960-1970 rằng phong cách lập trình này hiệu quả hơn, hoặc vì quan điểm cho rằng nó "trực quan" hơn. Dù lý do là gì, đây là quan niệm không có lợi — nó áp đặt lên người học những mối lo về thứ tự thực thi mà che khuất những ý tưởng quan trọng hơn (xem Steele 1977).

---

[`procedure`](GLOSSARY.md#procedure) [`variable`](GLOSSARY.md#variable) [`state-variables`](GLOSSARY.md#state-variables) [`substitution-model`](GLOSSARY.md#substitution-model) [`environment`](GLOSSARY.md#environment) [`expression`](GLOSSARY.md#expression) [`combination`](GLOSSARY.md#combination) [`program`](GLOSSARY.md#program) [`iterative-process`](GLOSSARY.md#iterative-process) [`parameter`](GLOSSARY.md#parameter) [`data`](GLOSSARY.md#data)
