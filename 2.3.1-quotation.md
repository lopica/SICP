# 2.3.1 Trích dẫn

Nếu ta có thể tạo danh sách từ các ký hiệu tùy ý, thì ta cần phân biệt được hai trường hợp: `a` với tư cách là tên của một giá trị nào đó, và `a` với tư cách là bản thân ký hiệu `a`. Trong ngôn ngữ tự nhiên, ta làm điều này bằng dấu trích dẫn. Khi tôi nói "Hãy đọc to _tên của bạn_", tôi muốn bạn đọc tên thật của mình; còn khi tôi nói "Hãy đọc to _'tên của bạn'_", tôi muốn bạn đọc đúng cụm từ "tên của bạn". Trong ngôn ngữ tự nhiên, ta ngầm hiểu từ nào đang được trích dẫn qua ngữ cảnh. Trong Scheme, ta cần một quy ước rõ ràng hơn để làm điều này.

Cách thông thường là dùng cú pháp _trích dẫn_ (quotation): đặt dấu nháy đơn `'` trước đối tượng muốn trích dẫn.[^1] Điều này cho phép ta phân biệt giữa các **ký hiệu** và giá trị của chúng:

```lisp
(define a 1)
(define b 2)

(list a b)
(1 2)

(list 'a 'b)
(a b)

(list 'a b)
(a 2)
```

Trích dẫn cũng áp dụng được cho các danh sách tổng hợp, không chỉ cho các ký hiệu đơn lẻ. Điều này cho phép ta dùng danh sách chữ (literal list):

```lisp
(car '(a b c))
a

(cdr '(a b c))
(b c)
```

Bây giờ ta hãy xem xét kỹ hơn ý nghĩa của dấu nháy đơn. Biểu thức `'<đối-tượng>` thực ra chỉ là cú pháp rút gọn cho `(quote <đối-tượng>)`. Vậy `'a` tương đương với `(quote a)`, và `'(a b c)` tương đương với `(quote (a b c))`. `quote` là một _trường hợp đặc biệt_ trong ngôn ngữ — nó không thực thi biểu thức con của mình, mà trả về biểu thức đó y nguyên như là dữ liệu.[^2]

Với dấu nháy đơn, ta có thể biểu diễn các danh sách các ký hiệu, thậm chí cả các danh sách chứa những thứ trông giống biểu thức Scheme:

```lisp
(car '(*1* (*2* (*3*))))
*1*

(cdr '(*1* (*2* (*3*))))
((*2* (*3*)))
```

Ngôn ngữ cũng cung cấp _ký hiệu nguyên thủy_ là `nil` biểu diễn danh sách rỗng.

Một phép toán hữu ích khi làm việc với các **ký hiệu** là `eq?`, kiểm tra xem hai **ký hiệu** có giống nhau hay không:

```lisp
(eq? 'a 'a)
true

(eq? 'a 'b)
false
```

Dùng `eq?`, ta có thể cài đặt một quy trình hữu ích tên là `memq`. Nó nhận một ký hiệu và một danh sách làm đối số, rồi trả về `false` nếu ký hiệu không có trong danh sách, hoặc trả về phần đuôi của danh sách bắt đầu từ vị trí đầu tiên tìm thấy ký hiệu đó:

```lisp
(define (memq item x)
  (cond ((null? x) false)
        ((eq? item (car x)) x)
        (else (memq item (cdr x)))))
```

Ví dụ:

```lisp
(memq 'apple '(pear banana prune))
false

(memq 'apple '(x (apple sauce) y apple pear))
(apple pear)
```

**Bài tập 2.53:** Với mỗi biểu thức dưới đây, trình phiên dịch sẽ in ra gì?

```lisp
(list 'a 'b 'c)
(list (list 'george))
(cdr '((x1 x2) (y1 y2)))
(cadr '((x1 x2) (y1 y2)))
(pair? (car '(a short list)))
(memq 'red '((red shoes) (blue socks)))
(memq 'red '(red shoes blue socks))
```

**Bài tập 2.54:** Hai danh sách được coi là `equal?` nếu chúng chứa các phần tử bằng nhau theo cùng thứ tự. Ví dụ:

```lisp
(equal? '(this is a list) '(this is a list))
true

(equal? '(this is a list) '(this (is a) list))
false
```

Theo nghĩa rộng hơn, `equal?` là dạng tổng quát của `eq?` — `eq?` chỉ so sánh các **ký hiệu**, còn `equal?` so sánh được cả danh sách. Ta có thể cài đặt `equal?` theo kiểu **đệ quy**: hai đối số `a` và `b` là `equal?` nếu chúng đều là **ký hiệu** và `eq?` với nhau, hoặc đều là danh sách mà `(car a)` bằng `(car b)` và `(cdr a)` bằng `(cdr b)`. Dùng ý tưởng này, hãy cài đặt `equal?` như một quy trình.

**Bài tập 2.55:** Eva Lu Ator gõ vào trình phiên dịch biểu thức:

```lisp
(car ''abracadabra)
```

Thật bất ngờ, trình phiên dịch in ra `quote`. Hãy giải thích tại sao.

---

[^1]: Dấu nháy đơn `'` chỉ là cú pháp tiện lợi cho `(quote ...)`. Trình đọc (reader) của Scheme sẽ tự động chuyển `'<biểu-thức>` thành `(quote <biểu-thức>)` trước khi thực thi.

[^2]: Trích dẫn thể hiện một vấn đề triết học thú vị về ngôn ngữ. Trong toán học ta thường viết $2 + 3 = 5$, và điều này là đúng. Nhưng nếu ta hỏi "tên của 2 + 3 là gì?", câu trả lời là "$2 + 3$", không phải "$5$". Trong Scheme, `(+ 2 3)` cho giá trị `5`, còn `'(+ 2 3)` cho chính danh sách `(+ 2 3)` — cái tên chứ không phải cái trị.

---

[`symbol`](GLOSSARY.md#symbol) [`list`](GLOSSARY.md#list) [`pair`](GLOSSARY.md#pair) [`special form`](GLOSSARY.md#special-form) [`primitive procedure`](GLOSSARY.md#primitive-procedure) [`predicate`](GLOSSARY.md#predicate) [`expression`](GLOSSARY.md#expression) [`data`](GLOSSARY.md#data) [`recursive`](GLOSSARY.md#recursive)
