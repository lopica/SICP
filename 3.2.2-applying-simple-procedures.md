# 3.2.2 Áp dụng các quy trình đơn giản

Khi ta giới thiệu _mô hình sự thay thế_ ở mục 1.1.5, ta đã cho thấy biểu thức kết hợp `(f 5)` cho kết quả là `136`, với các định nghĩa quy trình sau:

```lisp
(define (square x)
  (* x x))
(define (sum-of-squares x y)
  (+ (square x) (square y)))
(define (f a)
  (sum-of-squares (+ a 1) (* a 2)))
```

Ta có thể phân tích ví dụ tương tự bằng _mô hình môi trường_. Hình 3.4 minh họa ba đối tượng quy trình được tạo ra khi thực thi các định nghĩa `f`, `square` và `sum-of-squares` trong **môi trường toàn cục**. Mỗi đối tượng quy trình gồm một đoạn mã cùng một con trỏ đến **môi trường toàn cục**.

![Hình 3.4: Các đối tượng quy trình trong khung toàn cục.](./figures/figure26.png)

Hình 3.5 cho thấy cấu trúc **môi trường** được tạo ra khi thực thi biểu thức `(f 5)`. Lời gọi đến `f` tạo ra một môi trường mới E1, bắt đầu bằng một khung trong đó `a` — tham số hình thức của `f` — được ràng buộc với đối số `5`. Trong E1, ta thực thi thân quy trình của `f`:

```lisp
(sum-of-squares (+ a 1) (* a 2))
```

![Hình 3.5: Các môi trường được tạo ra khi thực thi `(f 5)` với các quy trình ở Hình 3.4.](./figures/figure27.png)

Để thực thi biểu thức kết hợp này, ta trước tiên thực thi các biểu thức con. Biểu thức con đầu tiên, `sum-of-squares`, có giá trị là một đối tượng quy trình. (Chú ý cách tìm giá trị đó: ta trước hết nhìn vào khung đầu tiên của E1, thấy không có ràng buộc nào cho `sum-of-squares`, rồi đi đến môi trường bao ngoài là **môi trường toàn cục** và tìm thấy ràng buộc như trong Hình 3.4.) Hai biểu thức con còn lại được thực thi bằng cách áp dụng các phép biến đổi nguồn `+` và `*` lên `(+ a 1)` và `(* a 2)`, cho kết quả lần lượt là `6` và `10`.

Tiếp theo ta áp dụng đối tượng quy trình `sum-of-squares` lên các đối số `6` và `10`. Điều này tạo ra môi trường mới E2, trong đó các tham số hình thức `x` và `y` được ràng buộc với các đối số. Trong E2, ta thực thi biểu thức kết hợp `(+ (square x) (square y))`. Điều này dẫn ta đến việc thực thi `(square x)`, với `square` được tìm thấy trong khung toàn cục và `x` có giá trị `6`. Ta lại tạo ra một môi trường mới E3, trong đó `x` được ràng buộc với `6`, và trong đó ta thực thi thân của `square` là `(* x x)`. Cũng là một phần của việc áp dụng `sum-of-squares`, ta phải thực thi biểu thức con `(square y)`, với `y` là `10`. Lần gọi thứ hai đến `square` tạo ra môi trường E4, trong đó `x` — tham số hình thức của `square` — được ràng buộc với `10`. Trong E4 ta thực thi `(* x x)`.

Điểm quan trọng cần nhận ra là mỗi lần gọi `square` đều tạo ra một môi trường mới chứa ràng buộc riêng cho `x`. Ta thấy ở đây cách các khung khác nhau giúp tách biệt các **biến** cục bộ đều cùng tên `x`. Chú ý rằng mỗi khung được tạo ra bởi `square` đều trỏ đến **môi trường toàn cục**, vì đó là môi trường được chỉ định trong đối tượng quy trình `square`.

Sau khi các biểu thức con được thực thi xong, kết quả được trả về. Hai giá trị từ hai lần gọi `square` được cộng lại bởi `sum-of-squares`, và kết quả cuối cùng được trả về bởi `f`. Ở đây ta tập trung vào cấu trúc môi trường nên không đi sâu vào cách các giá trị trả về được truyền qua lại giữa các lời gọi; tuy nhiên, đây cũng là một khía cạnh quan trọng của quá trình **thực thi**, và ta sẽ trở lại với nó chi tiết hơn ở Chương 5.

---

[exercise 3.9](exercises/3.9.md) [`environment`](GLOSSARY.md#environment) [`global environment`](GLOSSARY.md#global-environment) [`procedure`](GLOSSARY.md#procedure) [`compound procedure`](GLOSSARY.md#compound-procedure) [`evaluation`](GLOSSARY.md#evaluation) [`combination`](GLOSSARY.md#combination) [`formal parameter`](GLOSSARY.md#formal-parameter) [`substitution model`](GLOSSARY.md#substitution-model) [`variable`](GLOSSARY.md#variable) [`local`](GLOSSARY.md#local)
