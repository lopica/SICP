# 3.3.1 Cấu trúc danh sách có thể thay đổi

Các phép tính cơ bản trên _cặp_ — `cons`, `car`, và `cdr` — cho phép ta xây dựng và truy cập cấu trúc danh sách, nhưng không thể sửa đổi chúng. Điều tương tự cũng đúng với các phép tính bậc cao hơn như `append` hay `list`. Để sửa đổi cấu trúc danh sách, ta cần những công cụ mới.

Hai _bộ biến đổi_ nguồn cho _cặp_ là `set-car!` và `set-cdr!`. `set-car!` nhận hai **đối số**: một _cặp_ và một giá trị mới, rồi thay thế con trỏ `car` của _cặp_ đó bằng một con trỏ trỏ đến **đối số** thứ hai.[^1]

Để hiểu cách hai phép biến đổi này hoạt động, giả sử `x` được ràng buộc với danh sách `((a b) c d)` và `y` với `(e f)`, như minh họa ở Hình 3.12.

![Hình 3.12: Hai danh sách `x`: `((a b) c d)` và `y`: `(e f)`.](./figures/figure34.png)

Thực thi `(set-car! x y)` sẽ sửa đổi _cặp_ mà `x` đang trỏ đến, thay con trỏ `car` của nó bằng một con trỏ trỏ đến `y`. Kết quả được minh họa ở Hình 3.13. Cấu trúc `x` lúc này in ra là `((e f) c d)`. _Cặp_ trước đây được `car` của `x` trỏ đến — tức là _cặp_ biểu diễn danh sách `(a b)` — giờ không còn liên kết với `x` nữa, mặc dù cặp đó vẫn còn tồn tại trong bộ nhớ nếu vẫn có tham chiếu khác đến nó.

![Hình 3.13: Kết quả sau khi thực thi `(set-car! x y)`.](./figures/figure35.png)

So sánh với phép tính `(define z (cons y (cdr x)))` — đây là cách tiếp cận không phá hủy: nó tạo ra một _cặp_ mới mà không hề sửa đổi `x`. Kết quả được minh họa ở Hình 3.14.

![Hình 3.14: Kết quả sau khi thực thi `(define z (cons y (cdr x)))`.](./figures/figure36.png)

Phép tính `set-cdr!` hoạt động tương tự: nó thay thế con trỏ `cdr` của _cặp_ bằng một con trỏ mới. Thực thi `(set-cdr! x y)` sẽ thay con trỏ `cdr` của `x` bằng con trỏ trỏ đến `y`, cắt đứt phần đuôi `(c d)` khỏi cấu trúc. Kết quả được minh họa ở Hình 3.15.

![Hình 3.15: Kết quả sau khi thực thi `(set-cdr! x y)`.](./figures/figure37.png)

`cons` xây dựng _cặp_ mới bằng cách cấp phát bộ nhớ, trong khi `set-car!` và `set-cdr!` sửa đổi các _cặp_ đã tồn tại. Thực ra, `cons` hoàn toàn có thể được cài đặt từ `set-car!` và `set-cdr!`, cùng với một **quy trình** `get-new-pair` cung cấp các _cặp_ rỗng mới:

```lisp
(define (cons x y)
  (let ((new (get-new-pair)))
    (set-car! new x)
    (set-cdr! new y)
    new))
```

**Chia sẻ và danh tính**

Một ảnh hưởng quan trọng khi cho phép biến đổi là các _cặp_ có thể được _chia sẻ_ giữa nhiều đối tượng dữ liệu khác nhau. Hãy xét hai danh sách được xây dựng theo hai cách khác nhau:

```lisp
(define x (list 'a 'b))
(define z1 (cons x x))
(define z2 (cons (list 'a 'b) (list 'a 'b)))
```

`z1` và `z2` đều in ra như `((a b) a b)`, nhưng cấu trúc bên trong của chúng hoàn toàn khác: trong `z1`, cả `car` lẫn `cdr` đều trỏ đến _cùng một cặp_ `x`, còn `z2` có hai cặp `(a b)` riêng biệt.

Sự khác biệt này trở nên hiển thị khi ta áp dụng phép biến đổi. Xét **quy trình**:

```lisp
(define (set-to-wow! x)
  (set-car! (car x) 'wow)
  x)
```

Ta có:

```lisp
(set-to-wow! z1)
((wow b) wow b)

(set-to-wow! z2)
((wow b) a b)
```

Với `z1`, việc thay đổi _cặp_ đầu tiên thông qua `(car z1)` cũng đồng thời thay đổi `(cdr z1)` — vì chúng là cùng một đối tượng. Còn với `z2`, thao tác chỉ ảnh hưởng đến nửa bên trái.

Để phân biệt liệu hai biến có trỏ đến _cùng một đối tượng_ hay chỉ là hai đối tượng _có giá trị bằng nhau_, ta dùng **mệnh đề điều kiện** `eq?`:

```lisp
(eq? (car z1) (cdr z1))
#t

(eq? (car z2) (cdr z2))
#f
```

Khái niệm _"cùng là một đối tượng"_ này có ý nghĩa rất quan trọng khi làm việc với dữ liệu có thể thay đổi. Hai đối tượng có thể _bằng nhau về giá trị_ (`equal?`) nhưng vẫn là _hai đối tượng riêng biệt_ — và khi biến đổi một cái, cái kia không hề bị ảnh hưởng. Ngược lại, nếu hai biến trỏ đến _cùng một cặp_ (là _bí danh_ cho nhau), thì thay đổi thông qua một biến sẽ được "nhìn thấy" qua biến kia.

[^1]: Trong phần lớn các cài đặt Lisp, `set-car!` và `set-cdr!` trả về một giá trị không xác định. Ký hiệu dấu chấm than (`!`) theo quy ước chỉ các **quy trình** gây ra tác dụng phụ.

---

[exercise 3.12](exercises/3.12.md) [exercise 3.13](exercises/3.13.md) [exercise 3.14](exercises/3.14.md) [exercise 3.15](exercises/3.15.md) [exercise 3.16](exercises/3.16.md) [exercise 3.17](exercises/3.17.md) [exercise 3.18](exercises/3.18.md) [exercise 3.19](exercises/3.19.md) [exercise 3.20](exercises/3.20.md) [`pair`](GLOSSARY.md#pair) [`list`](GLOSSARY.md#list) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`assignment`](GLOSSARY.md#assignment) [`aliasing`](GLOSSARY.md#aliasing) [`predicate`](GLOSSARY.md#predicate)
