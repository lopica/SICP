# 2.2.2 Cấu trúc phân cấp

Khả năng biểu diễn các dãy bằng cặp cho phép ta mở rộng tự nhiên sang các dãy mà chính các phần tử của nó cũng là những dãy. Chẳng hạn, ta có thể coi đối tượng `((1 2) 3 4)` được tạo bởi

```lisp
(cons (list 1 2) (list 3 4))
```

là một danh sách gồm ba phần tử, trong đó phần tử đầu tiên là chính một danh sách, `(1 2)`. Hình 2.5 cho thấy cấu trúc của đối tượng này dưới dạng ký hiệu hộp và con trỏ. Có thể nhìn nhận cấu trúc này theo một cách khác: xem các _dãy của dãy_ như những _cây_. Khi đó, các phần tử của dãy là các nhánh của cây, còn những phần tử bản thân là dãy thì là các cây con. Hình 2.6 minh họa cấu trúc trong Hình 2.5 khi nhìn như một cây.

![Cấu trúc được tạo bởi (cons (list 1 2) (list 3 4))](./figures/figure21.png)

![Cấu trúc danh sách trong Hình 2.5 nhìn như một cây](./figures/figure22.png)

> **Lưu ý:** Hình 2.5 (ký hiệu hộp và con trỏ của `(cons (list 1 2) (list 3 4))`) cần được lưu tại `./figures/figure21.png`. Hình 2.6 (cấu trúc cây tương ứng) cần được lưu tại `./figures/figure22.png`. Bạn cần thêm hai ảnh này từ nguồn gốc SICP.

**Đệ quy** là công cụ tự nhiên để thao tác trên cây, bởi vì ta thường có thể rút gọn thao tác trên cây thành các thao tác trên từng nhánh, rồi từng nhánh đó lại rút gọn tiếp thành các thao tác trên từng nhánh nhỏ hơn, cho đến khi chạm đến lá.

Lấy ví dụ so sánh với quy trình `length` trong mục 2.2.1: ta có thể đếm số _lá_ của một cây thay vì độ dài của một dãy. Với đối tượng `x` được định nghĩa như trên:

```lisp
(define x (cons (list 1 2) (list 3 4)))

(length x)
3

(count-leaves x)
4

(list x x)
(((1 2) 3 4) ((1 2) 3 4))

(length (list x x))
2

(count-leaves (list x x))
8
```

Để cài đặt `count-leaves`, ta nhớ lại kế hoạch đệ quy để tính `length`:

- `length` của một danh sách `x` bằng `1` cộng `length` của `cdr` của `x`.
- `length` của danh sách rỗng bằng `0`.

Quy trình `count-leaves` tương tự, nhưng phải xử lý thêm trường hợp _lá_ (leaf) — tức là các phần tử không phải cặp:

- `count-leaves` của một cây `x` bằng tổng của `count-leaves` trên `car` và `cdr` của `x`.
- `count-leaves` của một lá (không phải cặp) bằng `1`.
- `count-leaves` của danh sách rỗng bằng `0`.

Để phân biệt lá với cặp, Scheme cung cấp _mệnh đề điều kiện_ nguồn `pair?`, trả về `true` khi đối số là một cặp.[^1]

```lisp
(define (count-leaves x)
  (cond ((null? x) 0)
        ((not (pair? x)) 1)
        (else (+ (count-leaves (car x))
                 (count-leaves (cdr x))))))
```

## Ánh xạ trên cây

Cũng như `map` là một công cụ mạnh mẽ khi làm việc với dãy, ta có thể kết hợp `map` với **đệ quy** để thao tác trên cây. Chẳng hạn, `scale-tree` — tương tự quy trình `scale-list` trong mục 2.2.1 — nhân mỗi lá của cây với một hệ số cho trước:

```lisp
(scale-tree (list 1
                  (list 2 (list 3 4) 5)
                  (list 6 7))
            10)
(10 (20 (30 40) 50) (60 70))
```

Dưới đây là hai cách cài đặt `scale-tree`. Cách thứ nhất dùng đệ quy trực tiếp, tương tự `count-leaves`:

```lisp
(define (scale-tree tree factor)
  (cond ((null? tree) nil)
        ((not (pair? tree))
         (* tree factor))
        (else
         (cons (scale-tree (car tree) factor)
               (scale-tree (cdr tree) factor)))))
```

Cách thứ hai dùng `map` để ánh xạ qua các nhánh của cây, sau đó đệ quy vào mỗi nhánh con:

```lisp
(define (scale-tree tree factor)
  (map (lambda (sub-tree)
         (if (pair? sub-tree)
             (scale-tree sub-tree factor)
             (* sub-tree factor)))
       tree))
```

Nhiều thao tác trên cây có thể cài đặt theo cả hai phong cách này. Phong cách dùng `map` thường ngắn gọn và dễ đọc hơn; phong cách đệ quy trực tiếp thường bộc lộ rõ hơn cấu trúc của bài toán. Bài tập 2.31 sẽ yêu cầu bạn trừu tượng hóa mẫu này thành `tree-map`.

**Bài tập 2.24:** Giả sử ta thực thi biểu thức `(list 1 (list 2 (list 3 4)))`. Hãy cho biết kết quả mà trình phiên dịch in ra, cấu trúc hộp-và-con-trỏ tương ứng, và cách diễn giải cấu trúc đó như một cây (như trong Hình 2.6).

**Bài tập 2.25:** Cho biết tổ hợp các `car` và `cdr` để lấy ra số `7` từ mỗi danh sách sau:

```lisp
(1 3 (5 7) 9)

((7))

(1 (2 (3 (4 (5 (6 7))))))
```

**Bài tập 2.26:** Giả sử `x` và `y` là hai danh sách:

```lisp
(define x (list 1 2 3))
(define y (list 4 5 6))
```

Kết quả nào được trình phiên dịch in ra khi thực thi từng biểu thức sau?

```lisp
(append x y)
(cons x y)
(list x y)
```

**Bài tập 2.27:** Hãy sửa quy trình `reverse` trong Bài tập 2.18 để tạo ra quy trình `deep-reverse` nhận một danh sách làm đối số và trả về danh sách đó với thứ tự các phần tử bị đảo ngược, đồng thời tất cả các danh sách con cũng được đảo ngược sâu. Ví dụ:

```lisp
(define x (list (list 1 2) (list 3 4)))

x
((1 2) (3 4))

(reverse x)
((3 4) (1 2))

(deep-reverse x)
((4 3) (2 1))
```

**Bài tập 2.28:** Viết một quy trình `fringe` nhận một cây (biểu diễn dưới dạng danh sách) làm đối số và trả về một danh sách gồm tất cả các lá của cây theo thứ tự từ trái sang phải. Ví dụ:

```lisp
(define x (list (list 1 2) (list 3 4)))

(fringe x)
(1 2 3 4)

(fringe (list x x))
(1 2 3 4 1 2 3 4)
```

**Bài tập 2.29:** Một _cái mobile nhị phân_ (binary mobile) gồm hai nhánh: nhánh trái và nhánh phải. Mỗi nhánh là một thanh có độ dài nhất định, từ đó treo xuống hoặc là một quả nặng, hoặc là một mobile nhị phân khác. Ta có thể biểu diễn một mobile nhị phân bằng dữ liệu tổng hợp bằng cách tạo ra một mobile từ hai nhánh của nó (ví dụ, dùng `list`):

```lisp
(define (make-mobile left right)
  (list left right))
```

Một nhánh được tạo từ độ dài `length` (một số) và một cấu trúc `structure`, có thể là số (biểu diễn quả nặng đơn giản) hoặc là một mobile khác:

```lisp
(define (make-branch length structure)
  (list length structure))
```

a. Viết các bộ chọn tương ứng `left-branch` và `right-branch` trả về các nhánh của một mobile, và `branch-length` và `branch-structure` trả về các thành phần của một nhánh.

b. Dùng các bộ chọn đó, định nghĩa quy trình `total-weight` trả về tổng trọng lượng của một mobile.

c. Một mobile được gọi là _cân bằng_ nếu mô-men xoắn của nhánh trái trên cùng bằng mô-men xoắn của nhánh phải trên cùng (tức là, độ dài của thanh trái nhân với trọng lượng treo vào nó bằng tích tương ứng cho bên phải), và nếu mỗi mobile con treo vào các nhánh đều cân bằng. Hãy thiết kế một _mệnh đề điều kiện_ kiểm tra xem một mobile nhị phân có cân bằng hay không.

d. Giả sử ta thay đổi cách biểu diễn mobile sao cho các constructor trở thành:

```lisp
(define (make-mobile left right)
  (cons left right))

(define (make-branch length structure)
  (cons length structure))
```

Bạn cần thay đổi bao nhiêu trong các quy trình đã viết để chuyển sang cách biểu diễn mới?

**Bài tập 2.30:** Định nghĩa quy trình `square-tree` tương tự như quy trình `square-list` trong Bài tập 2.21. Tức là, `square-tree` cần hoạt động như sau:

```lisp
(square-tree
 (list 1
       (list 2 (list 3 4) 5)
       (list 6 7)))
(1 (4 (9 16) 25) (36 49))
```

Định nghĩa `square-tree` cả hai cách: trực tiếp (không dùng quy trình bậc cao nào) và bằng cách dùng `map` kết hợp với đệ quy.

**Bài tập 2.31:** Trừu tượng hóa câu trả lời của bạn cho Bài tập 2.30 để tạo ra quy trình `tree-map` với tính chất là `square-tree` có thể được định nghĩa như sau:

```lisp
(define (square-tree tree)
  (tree-map square tree))
```

**Bài tập 2.32:** Ta có thể biểu diễn một tập hợp như một danh sách các phần tử phân biệt, và biểu diễn tập hợp tất cả các tập con của nó như một danh sách các danh sách. Ví dụ, nếu tập hợp là `(1 2 3)`, thì tập hợp tất cả các tập con là `(() (3) (2) (2 3) (1) (1 3) (1 2) (1 2 3))`. Hãy hoàn chỉnh định nghĩa sau của quy trình sinh ra tập hợp tất cả các tập con của một tập hợp, và giải thích rõ tại sao nó đúng:

```lisp
(define (subsets s)
  (if (null? s)
      (list nil)
      (let ((rest (subsets (cdr s))))
        (append rest (map ⟨??⟩ rest)))))
```

---

[^1]: Quy trình `pair?` kiểm tra xem đối số của nó có phải là cặp hay không. Để hỗ trợ viết các **quy trình** đệ quy trên cây, Scheme cung cấp _mệnh đề điều kiện_ nguồn `pair?`.

---

[`pair`](GLOSSARY.md#pair) [`list`](GLOSSARY.md#list) [`recursive`](GLOSSARY.md#recursive) [`tree recursion`](GLOSSARY.md#tree-recursion) [`procedure`](GLOSSARY.md#procedure) [`predicate`](GLOSSARY.md#predicate) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`data`](GLOSSARY.md#data) [`tree`](GLOSSARY.md#tree) [`leaf`](GLOSSARY.md#leaf) [`higher-order procedure`](GLOSSARY.md#higher-order-procedure)
