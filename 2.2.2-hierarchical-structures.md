# 2.2.2 Cấu trúc phân cấp

Khả năng biểu diễn các dãy bằng cặp cho phép ta mở rộng tự nhiên sang các dãy mà chính các phần tử của nó cũng là những dãy. Chẳng hạn, ta có thể coi đối tượng `((1 2) 3 4)` được tạo bởi

```lisp
(cons (list 1 2) (list 3 4))
```

là một danh sách gồm ba phần tử, trong đó phần tử đầu tiên là chính một danh sách, `(1 2)`. Hình 2.5 cho thấy cấu trúc của đối tượng này dưới dạng ký hiệu hộp và con trỏ. Có thể nhìn nhận cấu trúc này theo một cách khác: xem các _dãy của dãy_ như những _cây_. Khi đó, các phần tử của dãy là các nhánh của cây, còn những phần tử bản thân là dãy thì là các cây con. Hình 2.6 minh họa cấu trúc trong Hình 2.5 khi nhìn như một cây.

![Cấu trúc được tạo bởi (cons (list 1 2) (list 3 4))](./figures/figure21.png)

![Cấu trúc danh sách trong Hình 2.5 nhìn như một cây](./figures/figure22.png)

> **Lưu ý:** Hình 2.5 (ký hiệu hộp và con trỏ của `(cons (list 1 2) (list 3 4))`) cần được lưu tại `./figures/figure21.png`. Hình 2.6 (cấu trúc cây tương ứng) cần được lưu tại `./figures/figure22.png`. Bạn cần thêm hai ảnh này từ nguồn gốc SICP.

**Đệ quy** là công cụ tự nhiên để thao tác trên cây, bởi vì ta thường có thể rút gọn thao tác trên cây thành các thao tác trên từng nhánh, rồi từng nhánh đó lại rút gọn tiếp thành các thao tác trên từng nhánh nhỏ hơn, cho đến khi chạm đến lá.

Lấy ví dụ so sánh với quy trình `length` trong mục 2.2.1: ta có thể đếm số _lá_ của một cây thay vì độ dài của một dãy. Với đối tượng `x` được định nghĩa như trên:

```lisp
(define x (cons (list 1 2) (list 3 4)))

(length x)
3

(count-leaves x)
4

(list x x)
(((1 2) 3 4) ((1 2) 3 4))

(length (list x x))
2

(count-leaves (list x x))
8
```

Để cài đặt `count-leaves`, ta nhớ lại kế hoạch đệ quy để tính `length`:

- `length` của một danh sách `x` bằng `1` cộng `length` của `cdr` của `x`.
- `length` của danh sách rỗng bằng `0`.

Quy trình `count-leaves` tương tự, nhưng phải xử lý thêm trường hợp _lá_ (leaf) — tức là các phần tử không phải cặp:

- `count-leaves` của một cây `x` bằng tổng của `count-leaves` trên `car` và `cdr` của `x`.
- `count-leaves` của một lá (không phải cặp) bằng `1`.
- `count-leaves` của danh sách rỗng bằng `0`.

Để phân biệt lá với cặp, Scheme cung cấp _mệnh đề điều kiện_ nguồn `pair?`, trả về `true` khi đối số là một cặp.[^1]

```lisp
(define (count-leaves x)
  (cond ((null? x) 0)
        ((not (pair? x)) 1)
        (else (+ (count-leaves (car x))
                 (count-leaves (cdr x))))))
```

## Ánh xạ trên cây

Cũng như `map` là một công cụ mạnh mẽ khi làm việc với dãy, ta có thể kết hợp `map` với **đệ quy** để thao tác trên cây. Chẳng hạn, `scale-tree` — tương tự quy trình `scale-list` trong mục 2.2.1 — nhân mỗi lá của cây với một hệ số cho trước:

```lisp
(scale-tree (list 1
                  (list 2 (list 3 4) 5)
                  (list 6 7))
            10)
(10 (20 (30 40) 50) (60 70))
```

Dưới đây là hai cách cài đặt `scale-tree`. Cách thứ nhất dùng đệ quy trực tiếp, tương tự `count-leaves`:

```lisp
(define (scale-tree tree factor)
  (cond ((null? tree) nil)
        ((not (pair? tree))
         (* tree factor))
        (else
         (cons (scale-tree (car tree) factor)
               (scale-tree (cdr tree) factor)))))
```

Cách thứ hai dùng `map` để ánh xạ qua các nhánh của cây, sau đó đệ quy vào mỗi nhánh con:

```lisp
(define (scale-tree tree factor)
  (map (lambda (sub-tree)
         (if (pair? sub-tree)
             (scale-tree sub-tree factor)
             (* sub-tree factor)))
       tree))
```

Nhiều thao tác trên cây có thể cài đặt theo cả hai phong cách này. Phong cách dùng `map` thường ngắn gọn và dễ đọc hơn; phong cách đệ quy trực tiếp thường bộc lộ rõ hơn cấu trúc của bài toán. Bài tập 2.31 sẽ yêu cầu bạn trừu tượng hóa mẫu này thành `tree-map`.

[^1]: Quy trình `pair?` kiểm tra xem đối số của nó có phải là cặp hay không. Để hỗ trợ viết các **quy trình** đệ quy trên cây, Scheme cung cấp _mệnh đề điều kiện_ nguồn `pair?`.

---

[exercise 2.24](exercises/2.24.md) [exercise 2.25](exercises/2.25.md) [exercise 2.26](exercises/2.26.md) [exercise 2.27](exercises/2.27.md) [exercise 2.28](exercises/2.28.md) [exercise 2.29](exercises/2.29.md) [exercise 2.30](exercises/2.30.md) [exercise 2.31](exercises/2.31.md) [exercise 2.32](exercises/2.32.md) [`pair`](GLOSSARY.md#pair) [`list`](GLOSSARY.md#list) [`recursive`](GLOSSARY.md#recursive) [`tree recursion`](GLOSSARY.md#tree-recursion) [`procedure`](GLOSSARY.md#procedure) [`predicate`](GLOSSARY.md#predicate) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`data`](GLOSSARY.md#data) [`tree`](GLOSSARY.md#tree) [`leaf`](GLOSSARY.md#leaf) [`higher-order procedure`](GLOSSARY.md#higher-order-procedure)
