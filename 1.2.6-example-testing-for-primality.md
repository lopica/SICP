# 1.2.6 Ví dụ: Kiểm tra tính nguyên tố

Mục này mô tả hai phương pháp để kiểm tra xem một số nguyên $n$ có phải là số nguyên tố hay không — một phương pháp có _bậc tăng trưởng_ $\Theta(\sqrt{n})$, và một thuật toán "ngẫu nhiên xác suất" với bậc tăng trưởng $\Theta(\log n)$.

## Tìm ước số nhỏ nhất

Một cách kiểm tra tính nguyên tố của một số $n$ là tìm ước số của nó. Quy trình sau đây tìm ước số nguyên nhỏ nhất (lớn hơn 1) của một số nguyên $n$:

```lisp
(define (smallest-divisor n)
  (find-divisor n 2))

(define (find-divisor n test-divisor)
  (cond ((> (square test-divisor) n) n)
        ((divides? test-divisor n) test-divisor)
        (else (find-divisor n (+ test-divisor 1)))))

(define (divides? a b)
  (= (remainder b a) 0))
```

Ta có thể kiểm tra tính nguyên tố của $n$ như sau: $n$ là nguyên tố khi và chỉ khi ước số nhỏ nhất của nó chính là $n$:

```lisp
(define (prime? n)
  (= n (smallest-divisor n)))
```

Điểm mấu chốt ở đây là `find-divisor` chỉ cần kiểm tra các ước số đến $\sqrt{n}$. Bởi vì nếu $n$ có một ước số $d > \sqrt{n}$, thì tất yếu phải tồn tại ước số $n/d < \sqrt{n}$. Điều này có nghĩa là thuật toán chỉ cần kiểm tra các số chia trong khoảng từ $2$ đến $\sqrt{n}$, nên số bước thực hiện là $\Theta(\sqrt{n})$.

## Phép thử Fermat

Phép thử Fermat có _bậc tăng trưởng_ $\Theta(\log n)$ và dựa trên một kết quả nổi tiếng trong lý thuyết số — _định lý nhỏ Fermat_.[^1]

_Định lý nhỏ Fermat_: Nếu $n$ là số nguyên tố và $a$ là số nguyên dương bất kỳ nhỏ hơn $n$, thì $a$ lũy thừa $n$ đồng dư với $a$ modulo $n$.

Hai số được gọi là _đồng dư modulo $n$_ nếu chúng có cùng số dư khi chia cho $n$. Số dư của $a$ khi chia cho $n$ được gọi là $a$ _modulo $n$_, hay viết tắt là $a \bmod n$.

Nếu $n$ không phải số nguyên tố, thì nói chung hầu hết các số $a < n$ sẽ không thỏa mãn quan hệ trên. Điều đó dẫn đến thuật toán sau để kiểm tra tính nguyên tố: chọn ngẫu nhiên một số $a$ với $1 \le a < n$ và tính $a^n \bmod n$. Nếu kết quả khác $a$, thì $n$ chắc chắn không phải số nguyên tố. Nếu kết quả bằng $a$, thì $n$ có khả năng là số nguyên tố. Thực hiện thêm nhiều lần với các giá trị $a$ khác nhau, mỗi lần kiểm tra thành công lại làm tăng thêm sự tin tưởng vào tính nguyên tố của $n$. Thuật toán này được gọi là _phép thử Fermat_.

Để cài đặt phép thử Fermat, ta cần có một quy trình tính $a^n \bmod n$ một cách hiệu quả. Quy trình `expmod` dưới đây thực hiện điều này bằng cách dùng phép lũy thừa bằng cách bình phương liên tiếp, tương tự quy trình `fast-expt` ở mục 1.2.4:[^2]

```lisp
(define (expmod base exp m)
  (cond ((= exp 0) 1)
        ((even? exp)
         (remainder (square (expmod base (/ exp 2) m))
                    m))
        (else
         (remainder (* base (expmod base (- exp 1) m))
                    m))))
```

Phép thử Fermat được thực hiện bằng cách chọn ngẫu nhiên một số $a$ trong khoảng từ $1$ đến $n-1$ và kiểm tra xem phần dư của $a^n$ modulo $n$ có bằng $a$ hay không:

```lisp
(define (fermat-test n)
  (define (try-it a)
    (= (expmod a n n) a))
  (try-it (+ 1 (random (- n 1)))))
```

Quy trình sau chạy phép thử Fermat một số lần cho trước (tham số `times`). Nếu mọi lần thử đều thành công, trả về `true`; nếu có bất kỳ lần nào thất bại, trả về `false`:

```lisp
(define (fast-prime? n times)
  (cond ((= times 0) true)
        ((fermat-test n) (fast-prime? n (- times 1)))
        (else false)))
```

## Phương pháp xác suất

Phép thử Fermat khác với hầu hết các thuật toán quen thuộc ở một điểm quan trọng. Một thuật toán thông thường được gọi là _đúng_ theo nghĩa: với bất kỳ đầu vào $n$ nào, nó luôn cho ra kết quả đúng. Nhưng với phép thử Fermat, kết quả thu được chỉ có tính _xác suất_: nếu $n$ vượt qua phép thử, ta chỉ biết chắc rằng $n$ _có khả năng_ là số nguyên tố.

Điểm thú vị là nếu ta thực hiện phép thử đủ nhiều lần và $n$ luôn vượt qua, thì xác suất $n$ không là nguyên tố sẽ rất nhỏ — nhỏ tùy ý theo ý ta muốn. Với hầu hết các ứng dụng thực tế, điều này hoàn toàn đủ đảm bảo.

Tuy nhiên có một trở ngại: tồn tại một số rất ít số hợp đặc biệt, gọi là _số Carmichael_, khiến phép thử Fermat bị đánh lừa. Đây là những số không phải nguyên tố nhưng vẫn vượt qua phép thử Fermat với mọi giá trị $a < n$. Các số Carmichael rất hiếm — dưới $10^{15}$ chỉ có khoảng 255,000 số như vậy trong tổng số $10^{15}$ số — nên trong thực tế phép thử Fermat hoàn toàn đáng tin cậy. Đã có những biến thể của phép thử này không thể bị đánh lừa bởi số Carmichael; hai trong số đó được nêu trong bài tập 1.28.[^3]

Những thuật toán đưa ra câu trả lời chỉ mang tính xác suất như trên được gọi là _thuật toán xác suất_ (probabilistic algorithms). Chúng là một lĩnh vực nghiên cứu sôi nổi và tạo ra nhiều ứng dụng thực tiễn quan trọng. Bài tập 1.22 đến 1.28 cung cấp thêm thực hành về các ý tưởng liên quan.

[^1]: Nhỏ (little) ở đây để phân biệt với _định lý lớn Fermat_ (Fermat's Last Theorem), một kết quả nổi tiếng khác của Fermat được Pierre de Fermat phát biểu vào năm 1637 và được chứng minh bởi Andrew Wiles vào năm 1995.

[^2]: Điểm hay của quy trình `expmod` là ở mỗi bước nó chỉ làm việc với các số nhỏ hơn $m$, nhờ áp dụng phép `remainder` ngay sau mỗi phép tính trung gian. Không có số trung gian nào lớn hơn $m^2$. Điều này trái ngược với cách làm ngây thơ: trước hết tính $a^n$ rồi mới lấy modulo, khiến các số trung gian có thể cực kỳ lớn.

[^3]: Các số duy nhất có thể đánh lừa phép thử Fermat là các số Carmichael, và các số này rất hiếm. Phép thử Miller-Rabin trong bài tập 1.28 không thể bị đánh lừa bởi bất kỳ số nào.

---

[exercise 1.21](exercises/1.21.md) [exercise 1.22](exercises/1.22.md) [exercise 1.23](exercises/1.23.md) [exercise 1.24](exercises/1.24.md) [exercise 1.25](exercises/1.25.md) [exercise 1.26](exercises/1.26.md) [exercise 1.27](exercises/1.27.md) [exercise 1.28](exercises/1.28.md) [`order of growth`](GLOSSARY.md#order-of-growth) [`procedure`](GLOSSARY.md#procedure) [`process`](GLOSSARY.md#process) [`iterative process`](GLOSSARY.md#iterative-process)
