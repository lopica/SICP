# 3.2.1 Các quy tắc thực thi

Đặc tả tổng quát về cách trình phiên dịch thực thi một _biểu thức kết hợp_ vẫn giữ nguyên như khi ta lần đầu giới thiệu ở mục 1.1.3:

- Để thực thi một biểu thức kết hợp:
  1. Thực thi các _biểu thức con_ của biểu thức kết hợp.[^1]
  2. Áp dụng giá trị của biểu thức con là phép biến đổi lên các giá trị của các biểu thức con là phần tử.

_Mô hình môi trường_ của sự **thực thi** thay thế _mô hình sự thay thế_ trong việc xác định ý nghĩa của việc áp dụng một **quy trình tổng hợp** lên các đối số.

Trong mô hình môi trường, một **quy trình** luôn là một cặp gồm một đoạn mã và một con trỏ đến một **môi trường**. Quy trình chỉ được tạo ra theo một cách duy nhất: bằng cách thực thi một biểu thức λ. Kết quả là một quy trình có mã lấy từ nội dung của biểu thức λ đó và môi trường là môi trường mà trong đó biểu thức λ được thực thi. Chẳng hạn, xét định nghĩa quy trình:

```lisp
(define (square x)
  (* x x))
```

được thực thi trong **môi trường toàn cục**. Cú pháp định nghĩa quy trình chỉ là đường cú pháp cho một biểu thức λ ẩn bên dưới. Nó tương đương với:

```lisp
(define square
  (lambda (x) (* x x)))
```

Biểu thức này thực thi `(lambda (x) (* x x))` rồi ràng buộc `square` với giá trị thu được, tất cả trong **môi trường toàn cục**.

Hình 3.2 minh họa kết quả của việc thực thi biểu thức `define` trên. Đối tượng quy trình là một cặp: phần mã xác định rằng quy trình có một tham số hình thức là `x` và thân quy trình là `(* x x)`. Phần môi trường của quy trình là con trỏ đến **môi trường toàn cục**, vì đó là môi trường trong đó biểu thức λ được thực thi để tạo ra quy trình. Một ràng buộc mới liên kết đối tượng quy trình với ký hiệu `square` đã được thêm vào khung toàn cục. Nói chung, `define` tạo ra các định nghĩa bằng cách thêm ràng buộc vào các khung.

![Hình 3.2: Cấu trúc môi trường được tạo ra khi thực thi `(define (square x) (* x x))` trong môi trường toàn cục.](./figures/figure24.png)

Giờ ta đã thấy cách quy trình được tạo ra, ta có thể mô tả cách quy trình được áp dụng. Mô hình môi trường quy định: "Để áp dụng một quy trình lên các đối số, hãy tạo một **môi trường** mới chứa một khung ràng buộc các tham số với giá trị của các đối số. Môi trường bao ngoài của khung này là môi trường được chỉ định bởi quy trình. Sau đó, trong môi trường mới đó, thực thi thân quy trình."

Để minh họa cách quy tắc này được thực hiện, Hình 3.3 cho thấy cấu trúc môi trường được tạo ra khi thực thi biểu thức `(square 5)` trong **môi trường toàn cục**, với `square` là quy trình được tạo ra ở Hình 3.2. Áp dụng quy trình dẫn đến việc tạo ra một môi trường mới, ký hiệu là E1, bắt đầu bằng một khung trong đó `x` — tham số hình thức của quy trình — được ràng buộc với đối số `5`. Con trỏ hướng lên từ khung này cho thấy môi trường bao ngoài của nó là **môi trường toàn cục**. Môi trường toàn cục được chọn ở đây vì đó là môi trường được chỉ định trong đối tượng quy trình `square`. Trong E1, ta thực thi thân quy trình `(* x x)`. Vì giá trị của `x` trong E1 là `5`, kết quả là `(* 5 5)` hay `25`.

![Hình 3.3: Môi trường được tạo ra khi thực thi `(square 5)` trong môi trường toàn cục.](./figures/figure25.png)

Mô hình môi trường của việc áp dụng quy trình có thể được tóm tắt bằng hai quy tắc:

- Một đối tượng quy trình được áp dụng lên một tập đối số bằng cách xây dựng một khung, ràng buộc các tham số hình thức của quy trình với các đối số của lời gọi, rồi thực thi thân quy trình trong ngữ cảnh của môi trường mới vừa tạo. Môi trường bao ngoài của khung mới chính là phần môi trường của đối tượng quy trình đang được áp dụng.

- Một quy trình được tạo ra bằng cách thực thi một biểu thức λ trong một môi trường cho trước. Đối tượng quy trình thu được là một cặp gồm nội dung của biểu thức λ và một con trỏ đến môi trường mà trong đó quy trình được tạo ra.

Ngoài ra, ta cũng quy định: việc định nghĩa một ký hiệu bằng `define` tạo ra một ràng buộc trong khung môi trường hiện tại và gán cho ký hiệu đó giá trị được chỉ định.[^2] Cuối cùng, ta quy định hành vi của `set!` — phép toán buộc ta phải giới thiệu mô hình môi trường ngay từ đầu. Thực thi biểu thức `(set! <biến> <giá trị>)` trong một môi trường nào đó sẽ tìm ràng buộc của biến đó trong môi trường và thay đổi ràng buộc đó để chỉ đến giá trị mới. Cụ thể, ta tìm khung đầu tiên trong môi trường có chứa ràng buộc cho biến đó rồi sửa đổi khung đó. Nếu biến không bị ràng buộc trong môi trường, `set!` báo lỗi.

Các _quy tắc_ thực thi này, dù phức tạp hơn _mô hình sự thay thế_ đáng kể, vẫn khá rõ ràng và dễ theo dõi. Hơn nữa, mô hình thực thi, dù trừu tượng, cung cấp một mô tả chính xác về cách trình phiên dịch thực thi các biểu thức. Ở Chương 4 ta sẽ thấy mô hình này có thể dùng làm bản thiết kế để cài đặt một trình phiên dịch thực sự. Các mục tiếp theo sẽ làm rõ chi tiết của mô hình qua một số chương trình minh họa.

---

[^1]: Phép gán đưa vào một sự tinh tế trong bước 1 của quy tắc thực thi. Như bài tập 3.8 cho thấy, sự có mặt của phép gán cho phép ta viết những biểu thức có thể cho ra giá trị khác nhau tùy theo thứ tự thực thi các biểu thức con trong một biểu thức kết hợp. Vì vậy, nói cho chính xác, ta cần xác định một thứ tự thực thi trong bước 1 (ví dụ: từ trái sang phải hay từ phải sang trái). Tuy nhiên, thứ tự đó nên luôn được coi là chi tiết cài đặt, và không nên bao giờ viết các chương trình phụ thuộc vào một thứ tự cụ thể. Chẳng hạn, một trình dịch tinh vi có thể tối ưu hóa chương trình bằng cách thay đổi thứ tự thực thi các biểu thức con.

[^2]: Nếu đã có ràng buộc cho biến đó trong khung hiện tại, ràng buộc đó sẽ bị thay đổi. Điều này tiện lợi vì cho phép định nghĩa lại các ký hiệu; tuy nhiên, nó cũng có nghĩa là `define` có thể được dùng để thay đổi giá trị mà không cần dùng `set!` một cách tường minh. Chính vì vậy, có người cho rằng nên để việc định nghĩa lại ký hiệu đã tồn tại gây ra lỗi hoặc cảnh báo.

---

[`environment`](GLOSSARY.md#environment) [`global environment`](GLOSSARY.md#global-environment) [`procedure`](GLOSSARY.md#procedure) [`compound procedure`](GLOSSARY.md#compound-procedure) [`primitive procedure`](GLOSSARY.md#primitive-procedure) [`evaluation`](GLOSSARY.md#evaluation) [`expression`](GLOSSARY.md#expression) [`combination`](GLOSSARY.md#combination) [`subexpression`](GLOSSARY.md#subexpression) [`substitution model`](GLOSSARY.md#substitution-model) [`formal parameter`](GLOSSARY.md#formal-parameter) [`rule`](GLOSSARY.md#rule)
