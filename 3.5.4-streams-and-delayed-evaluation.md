# 3.5.4 Dòng chảy và thực thi trì hoãn

Ở mục 3.5.1, ta đã thấy cách biểu diễn hệ thống xử lý tín hiệu bằng **dòng chảy**. Nhờ _thực thi trì hoãn_, các **dòng chảy** được sinh ra từng bước một, và toàn bộ chuỗi xử lý hoạt động như thể xử lý các **dòng chảy** vô hạn — thực ra tại mỗi bước chỉ có một phần nhỏ được tính toán. Tuy nhiên, cách ta đã dùng `delay` cho đến nay có một hạn chế tinh tế: _thực thi trì hoãn_ chỉ xảy ra trong các bộ tạo nguyên thủy của **dòng chảy**, cụ thể là ẩn trong `cons-stream`. Chẳng hạn, **quy trình** `integral` ở mục 3.5.3 yêu cầu **dòng chảy** tích phân phải có mặt sẵn trước khi `integral` có thể bắt đầu sinh phần tử:

```lisp
(define (integral integrand initial-value dt)
  (define int
    (cons-stream initial-value
                 (add-streams (scale-stream integrand dt)
                              int)))
  int)
```

Cách định nghĩa tự tham chiếu này phù hợp vì `cons-stream` trì hoãn việc tính phần đuôi — `int` ở vế phải chưa cần có giá trị ngay lúc định nghĩa. Nhờ đó, `integral` hoạt động được khi `integrand` đã biết từ đầu.

Nhưng khi ta muốn giải hệ phương trình vi phân có phản hồi — tức là trường hợp đầu vào của một phần tích phân lại phụ thuộc vào đầu ra của một phần tích phân khác — mọi thứ trở nên phức tạp hơn. Xét hệ:

$$\frac{dy}{dt} = f(y)$$

với điều kiện đầu $y_0$. Ta có thể viết lời giải dưới dạng **dòng chảy** như sau:

```lisp
(define (solve f y0 dt)
  (define y (integral dy y0 dt))
  (define dy (stream-map f y))
  y)
```

**Quy trình** `solve` này tạo ra **dòng chảy** `y` là tích phân của `dy`, trong khi `dy` lại là ánh xạ của `f` lên `y`. Thế nhưng cách định nghĩa trên sẽ không hoạt động! Khi `integral` được gọi, nó cần `dy` — nhưng `dy` chưa được định nghĩa tại thời điểm đó. Trong **dòng chảy** ngầm định như `int`, _thực thi trì hoãn_ ẩn trong `cons-stream` đủ để phá vỡ vòng phụ thuộc. Nhưng ở đây, `integral` được định nghĩa theo kiểu cũ — nó gọi `add-streams` ngay lập tức, cần `integrand` phải sẵn sàng.

Giải pháp là cho phép `integral` nhận `integrand` dưới dạng **đối tượng trì hoãn** và chỉ `force` nó khi cần thiết. Khi đó ta viết lại `integral` để nhận một đối số trì hoãn tường minh:

```lisp
(define (integral delayed-integrand initial-value dt)
  (define int
    (cons-stream
     initial-value
     (let ((integrand (force delayed-integrand)))
       (add-streams (scale-stream integrand dt) int))))
  int)
```

Bây giờ `integral` trì hoãn việc truy cập `integrand` cho đến khi cần tính phần tử thứ hai của **dòng chảy** trở đi. Ta viết lại `solve` để truyền `dy` dưới dạng trì hoãn:

```lisp
(define (solve f y0 dt)
  (define y (integral (delay dy) y0 dt))
  (define dy (stream-map f y))
  y)
```

Với `(delay dy)`, biểu thức `dy` chưa cần được tính khi `integral` được gọi. Khi `integral` cần dùng `integrand` (để tính phần tử thứ hai), lúc ấy `dy` đã được định nghĩa xong. Nhờ đó vòng phụ thuộc được phá vỡ một cách thanh lịch.

Ví dụ, ta có thể tính $e^1 \approx 2{,}718$ bằng cách giải phương trình $dy/dt = y$ với $y_0 = 1$:

```lisp
(stream-ref (solve (lambda (y) y) 1 0.001) 1000)
```

Kết quả xấp xỉ `2.716924`, rất gần $e$.[^1]

Điều thú vị ở kỹ thuật này là `delay` được dùng một cách **tường minh** trong phần thân của **quy trình** người dùng, không phải ẩn trong các nguyên thủy của **dòng chảy**. Đây là một ví dụ thực sự về _thực thi trì hoãn_ như là một cơ chế lập trình linh hoạt: khi nào trì hoãn gì hoàn toàn do người dùng kiểm soát, không phải do ngôn ngữ tự động quyết định.

Tuy nhiên, kỹ thuật này cũng làm nảy sinh một căng thẳng về thiết kế. Bây giờ `integral` có giao diện không đồng nhất: trong `solve`, `integrand` phải được bọc trong `delay`; nhưng trong các cách dùng khác mà `integrand` đã có sẵn, ta vẫn phải nhớ bọc. Có hai hướng giải quyết. Hướng thứ nhất: chấp nhận sự không đồng nhất và yêu cầu người dùng viết `(delay ...)` khi cần — đây là cách ta đang theo. Hướng thứ hai: làm cho ngôn ngữ luôn trì hoãn việc tính mọi đối số của **quy trình** theo mặc định (đây là cách các ngôn ngữ _tính toán lười_ như Haskell hoạt động).[^2]

[^1]: Với bước thời gian `dt = 0.001` và 1000 bước, ta ước lượng $y(1) = e^1$. Kết quả không hoàn toàn chính xác vì phương pháp tích phân Euler có sai số tỉ lệ với `dt`.

[^2]: Trong ngôn ngữ _lười biếng hoàn toàn_ (fully lazy), mọi biểu thức đều bị trì hoãn cho đến khi thực sự cần giá trị. Điều này loại bỏ nhu cầu dùng `delay` tường minh, nhưng làm cho việc suy luận về tác dụng phụ và thứ tự tính toán trở nên phức tạp hơn nhiều.

---

[exercise 3.73](exercises/3.73.md) [exercise 3.74](exercises/3.74.md) [exercise 3.75](exercises/3.75.md) [exercise 3.76](exercises/3.76.md) [exercise 3.77](exercises/3.77.md) [exercise 3.78](exercises/3.78.md) [exercise 3.79](exercises/3.79.md) [exercise 3.80](exercises/3.80.md) [`stream`](GLOSSARY.md#stream) [`delayed evaluation`](GLOSSARY.md#delayed-evaluation) [`delayed object`](GLOSSARY.md#delayed-object) [`procedure`](GLOSSARY.md#procedure) [`evaluation`](GLOSSARY.md#evaluation) [`functional programming`](GLOSSARY.md#functional-programming) [`sequence`](GLOSSARY.md#sequence)
