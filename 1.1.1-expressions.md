# 1.1.1. Biểu thức

Một cách đơn giản để bắt đầu lập trình là gõ thử một số thứ vào trình phiên dịch cho Schema của Lisp. Hãy tưởng tượng bạn đang ngồi trước terminal của máy tính, bạn gõ biểu thức, và trình phiên dịch trả ra kết quá bằng cách thực hiện biểu thức đó.

Một trong các biểu thức nguồn là số. Ví dụ như bạn gõ:
```bash
486
```
trình phiên dịch sẽ phản hồi bằng cách in ra:
```bash
486
```
Các biểu thức là số có thể kết hợp với các biểu thức là quy trình nguồn (như là +, -) để tạo ra biểu thức tổng hợp, cái mà thể hiện kết quả thực thi quy trình với các số đó. Ví dụ:
```bash
(+ 137 349)
486

(- 1000 334)
666

(* 5 99)
495

(/ 10 5)
2

(+ 2.7 10)
12.7
```
Các biểu thức trên được tạo ra bằng cách ngăn cách các biểu thức với cặp dấu ngoặc đơn (), đồng thời thể hiện biểu thức đã được thực thi, và chúng được gọi là `sự kết hợp`. Thành phần trái ngoài cùng trong danh sách gọi là phép biến đổi còn các thành phần còn lại gọi là phần tử. Giá trị của `sự kết hợp` được tạo ra bằng cách thực thi quy trình là phép biến đổi cùng các đối số là các phần tử.

Quy ước đặt phép biến đổi ở bên trái các phần tử được gọi là ký hiệu tiền tố, và bằng cách nào đó có thể gây khó hiểu lúc đầu vì nó khác hẳn với các quy tắc toán học thông thường. Ký hiệu tiền tố có rất nhiều lợi thế, một trong số đó là nó có thể giúp hàm lấy số lượng đối số bất kỳ. Ví dụ:
```bash
(+ 21 35 12 7)
75

(* 25 4 12)
1200
```
Mọi thứ đều khá rõ ràng, phép biến đổi bên trái, và `sự kết hợp` thì được ngăn cách bởi cặp dấu ngoặc đơn.

Một lợi ích nữa của ký hiệu tiền tố là nó cho phép `sự kết hợp` có thể lồng nhau
```bash
(+ (* 3 5) (- 10 6))
19
```
Không có giới hạn cho việc lồng các `sự kết hợp` vào nhau, cũng như độ phức tạp của biểu thức mà trình phiên dịch có thể thực thi. Chúng ta có thể dễ dàng nhầm lẫn khi tính biểu thức này:
```bash
(+ (* 3 (+ (* 2 4) (+ 3 5))) (+ (- 10 7) 6))
```
Trong khi đó, trình phiên dịch đã trả ra 57. Chúng ta có thể cứu mình bằng cách viết biểu thức dễ nhìn hơn:
```bash
(+ (* 3
      (+ (* 2 4)
         (+ 3 5)))
   (+ (- 10 7)
      6))
```
Quy ước format ở trên gọi là "pretty-printing", nó sẽ sắp xếp sao cho các phần tử căn dọc.

Kể cả với biểu thức phức tạp, trình phiên dịch luôn tính toán theo một chu trình cơ bản: nó đọc biểu thức từ terminal, thực hiện biểu thức, và in kết quả. Cái kiểu thực hiện phép biến đổi này còn được nói là trình phiên dịch chạy trong vòng lặp đọc-thực thi-in. Để ý kỹ thì việc bảo trình phiên dịch in kết quả của biểu thức là không cần thiết.
