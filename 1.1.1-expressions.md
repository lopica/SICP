# 1.1.1. Biểu thức

Một cách đơn giản để bắt đầu lập trình là gõ thử một số thứ vào **trình phiên dịch** Schema. Hãy tưởng tượng bạn đang ngồi trước **terminal** của máy tính, bạn gõ **biểu thức**, và **trình phiên dịch** trả ra kết quá bằng cách **thực thi** **biểu thức** đó.

Một trong các _biểu thức nguồn_ là số.[^1] Ví dụ như bạn gõ:

```bash
486
```

**trình phiên dịch** sẽ phản hồi bằng cách in ra:[^2]

```bash
486
```

Các **biểu thức** là số có thể kết hợp với các **biểu thức** là _quy trình nguồn_ (như là +, -) để tạo ra một _biểu thức tổng hợp_, cái mà thể hiện _kết quả áp dụng_ quy trình với các số đó. Ví dụ:

```bash
(+ 137 349)
486

(- 1000 334)
666

(* 5 99)
495

(/ 10 5)
2

(+ 2.7 10)
12.7
```

Các **biểu thức** trên được tạo ra bằng cách ngăn cách các **biểu thức** với cặp dấu ngoặc đơn (), đồng thời thể hiện **biểu thức** đã được áp dụng, và chúng được gọi là _biểu thức kết hợp_. Thành phần trái ngoài cùng trong danh sách gọi là **phép biến đổi** còn các thành phần còn lại gọi là _phần tử_. Giá trị của _biểu thức kết hợp_ được tạo ra bằng cách thực thi quy trình là phép biến đổi cùng các đối số là các _phần tử_.

Quy ước đặt phép biến đổi ở bên trái các _phần tử_ được gọi là ký hiệu tiền tố, và bằng cách nào đó có thể gây khó hiểu lúc đầu vì nó khác hẳn với các quy tắc toán học thông thường. Ký hiệu tiền tố có rất nhiều lợi thế, một trong số đó là nó có thể giúp hàm lấy số lượng đối số bất kỳ. Ví dụ:

```bash
(+ 21 35 12 7)
75

(* 25 4 12)
1200
```

Mọi thứ đều khá rõ ràng, phép biến đổi bên trái, và _biểu thức kết hợp_ thì được ngăn cách bởi cặp dấu ngoặc đơn.

Một lợi ích nữa của ký hiệu tiền tố là nó cho phép _biểu thức kết hợp_ có thể lồng nhau

```bash
(+ (* 3 5) (- 10 6))
19
```

Không có giới hạn cho việc lồng các _biểu thức kết hợp_ vào nhau, cũng như độ phức tạp của **biểu thức** mà **trình phiên dịch** có thể **thực thi**. Chúng ta có thể dễ dàng nhầm lẫn khi tính **biểu thức** này:

```bash
(+ (* 3 (+ (* 2 4) (+ 3 5))) (+ (- 10 7) 6))
```

Trong khi đó, **trình phiên dịch** đã trả ra 57. Chúng ta có thể cứu mình bằng cách viết **biểu thức** dễ nhìn hơn:

```bash
(+ (* 3
      (+ (* 2 4)
         (+ 3 5)))
   (+ (- 10 7)
      6))
```

Quy ước viết ở trên gọi là "pretty-printing", nó sẽ sắp xếp sao cho các _phần tử_ căn dọc.[^3]

Kể cả với **biểu thức** phức tạp, **trình phiên dịch** luôn tính toán theo một **chu trình** cơ bản: nó đọc **biểu thức** từ **terminal**, **thực thi** **biểu thức**, và in kết quả. Cái kiểu thực hiện **phép tính** này còn được nói là trình phiên dịch chạy trong vòng lặp đọc-**thực thi**-in. Để ý kỹ thì việc bảo **trình phiên dịch** in kết quả của **biểu thức** là không cần thiết.[^4]

[^1]: Việc mô tả số là "dữ liệu nguồn" thực ra là một sự đơn giản hóa quá mức. Thực tế, xử lý số là một trong những khía cạnh phức tạp và dễ gây nhầm lẫn nhất trong bất kỳ ngôn ngữ lập trình nào. Một số vấn đề điển hình: Một số hệ thống máy tính phân biệt số nguyên (như `2`) với số thực (như `2.71`). Liệu `2.00` có khác `2` không? Phép tính số nguyên có giống phép tính số thực không? `6` chia `2` cho ra `3` hay `3.0`? Ta biểu diễn được số lớn đến đâu? Được bao nhiêu chữ số thập phân? Phạm vi số nguyên và số thực có như nhau không? Ngoài ra còn có cả vấn đề làm tròn và cắt bớt số — đó là toàn bộ lĩnh vực phân tích số. Vì trọng tâm của quyển sách này là thiết kế chương trình ở quy mô lớn chứ không phải các kỹ thuật số, chúng ta sẽ bỏ qua những vấn đề này. Các ví dụ số trong chương này sẽ có các hành vi làm tròn thông thường mà ta thấy khi dùng các phép tính số học chỉ giữ lại một số hữu hạn chữ số thập phân.

[^2]: Xuyên suốt quyển sách này, khi muốn nhấn mạnh sự khác biệt giữa nội dung người dùng gõ vào và kết quả **trình phiên dịch** in ra, chúng ta sẽ hiển thị kết quả bằng chữ nghiêng.

[^3]: Các hệ thống Lisp thường cung cấp tính năng hỗ trợ định dạng biểu thức. Hai tính năng đặc biệt hữu ích là: tự động thụt lề đúng vị trí pretty-print mỗi khi sang dòng mới, và tô sáng dấu ngoặc mở tương ứng mỗi khi người dùng gõ dấu ngoặc đóng.

[^4]: Lisp tuân theo quy ước rằng mọi biểu thức đều có giá trị. Quy ước này, cộng với tiếng xấu cũ của Lisp là ngôn ngữ kém hiệu năng, chính là nguồn cảm hứng cho câu nói của Alan Perlis (phỏng theo Oscar Wilde) rằng "Lập trình viên Lisp biết giá trị của mọi thứ nhưng không biết chi phí của bất cứ thứ gì."

---

[`expression`](GLOSSARY.md#expression) [`interpreter`](GLOSSARY.md#interpreter) [`evaluation`](GLOSSARY.md#evaluation) [`primitive expression`](GLOSSARY.md#primitive-expression) [`primitive procedure`](GLOSSARY.md#primitive-procedure) [`compound expression`](GLOSSARY.md#compound-expression) [`application`](GLOSSARY.md#application) [`combination`](GLOSSARY.md#combination) [`operator`](GLOSSARY.md#operator) [`argument`](GLOSSARY.md#argument) [`operand`](GLOSSARY.md#operand) [`prefix notation`](GLOSSARY.md#prefix-notation) [`pretty-printing`](GLOSSARY.md#pretty-printing) [`read-eval-print loop`](GLOSSARY.md#read-eval-print-loop)
