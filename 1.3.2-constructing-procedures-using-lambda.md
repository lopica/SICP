# 1.3.2 Xây dựng quy trình bằng Lambda

Trong phần trước, chúng ta đã phải định nghĩa `pi-term` và `pi-next` như những quy trình riêng biệt chỉ để có thể truyền chúng vào `pi-sum`, dù bản thân chúng không có ý nghĩa gì khi đứng độc lập. Sẽ tiện hơn nhiều nếu ta có cách trực tiếp chỉ định "quy trình nhận `x` và trả về `x + 4`" hay "quy trình nhận `x` và trả về $\frac{1}{x(x+2)}$", mà không cần phải đặt tên cho chúng. Ta làm được điều này bằng cách dùng **trường hợp đặc biệt** `lambda`, tạo ra các quy trình ngay tại chỗ. Với `lambda`, ta viết lại `pi-sum` như sau:

```lisp
(define (pi-sum a b)
  (sum (lambda (x) (/ 1.0 (* x (+ x 2))))
       a
       (lambda (x) (+ x 4))
       b))
```

Tương tự, ta có thể viết `integral` mà không cần định nghĩa quy trình phụ `add-dx`:

```lisp
(define (integral f a b dx)
  (* (sum f
          (+ a (/ dx 2.0))
          (lambda (x) (+ x dx))
          b)
     dx))
```

Tổng quát, `lambda` tạo ra quy trình theo đúng cùng cách như `define`, chỉ khác là không gắn tên cho quy trình đó:

```lisp
(lambda (⟨formal-parameters⟩) ⟨body⟩)
```

Quy trình tạo ra bởi `lambda` hoàn toàn giống với quy trình tạo ra bởi `define`. Điểm khác biệt duy nhất là nó không được liên kết với bất kỳ tên nào trong **môi trường**.[^1] Ta có thể đọc một biểu thức `lambda` như sau:

```lisp
(lambda             (x)         (+    x      4))
   |                 |           |    |      |
quy trình nhận đối số x    cộng  x  với  4
```

Như vậy, định nghĩa này:

```lisp
(define (plus4 x) (+ x 4))
```

tương đương với:

```lisp
(define plus4 (lambda (x) (+ x 4)))
```

Ta có thể dùng biểu thức `lambda` trực tiếp như toán tử trong một _biểu thức kết hợp_:

```lisp
((lambda (x y z) (+ x y (square z))) 1 2 3)
12
```

## Dùng `let` để tạo biến cục bộ

Một cách dùng `lambda` rất hay gặp là tạo ra các **biến** cục bộ. Giả sử ta muốn tính hàm

$$f(x, y) = x(1 + xy)^2 + y(1 - y) + (1 + xy)(1 - y)$$

cũng có thể viết thành

$$f(x, y) = xa^2 + yb + ab,$$

trong đó $a = 1 + xy$ và $b = 1 - y$.

Khi viết quy trình tính $f$, ta muốn có các biến cục bộ `a` và `b`, chứ không phải viết đi viết lại các biểu thức phức tạp ấy. Một cách là dùng quy trình phụ để gắn tên cho các giá trị đó:

```lisp
(define (f x y)
  (define (f-helper a b)
    (+ (* x (square a))
       (* y b)
       (* a b)))
  (f-helper (+ 1 (* x y))
            (- 1 y)))
```

Dùng `lambda` ta có thể diễn đạt trực tiếp hơn:

```lisp
(define (f x y)
  ((lambda (a b)
     (+ (* x (square a))
        (* y b)
        (* a b)))
   (+ 1 (* x y))
   (- 1 y)))
```

Cách viết này tiện đến mức có hẳn một _trường hợp đặc biệt_ dành riêng cho nó — `let` — để làm cho việc này trông tự nhiên hơn:

```lisp
(define (f x y)
  (let ((a (+ 1 (* x y)))
        (b (- 1 y)))
    (+ (* x (square a))
       (* y b)
       (* a b))))
```

Dạng tổng quát của biểu thức `let` là:

```lisp
(let ((⟨var₁⟩ ⟨exp₁⟩)
      (⟨var₂⟩ ⟨exp₂⟩)
      ⋯
      (⟨varₙ⟩ ⟨expₙ⟩))
  ⟨body⟩)
```

Ta có thể đọc cú pháp này như sau:

> cho `⟨var₁⟩` có giá trị `⟨exp₁⟩`  
> và `⟨var₂⟩` có giá trị `⟨exp₂⟩`  
> …  
> và `⟨varₙ⟩` có giá trị `⟨expₙ⟩`  
> trong biểu thức `⟨body⟩`

Phần đầu của `let` là một danh sách các cặp tên–biểu thức. Phần thân được thực thi trong **phạm vi** mà các tên đó đã được gắn với các giá trị tương ứng. Về bản chất, biểu thức `let` chỉ là _đường cú pháp_ (syntactic sugar) cho biểu thức `lambda` bên dưới:

```lisp
((lambda (⟨var₁⟩ ⋯ ⟨varₙ⟩)
   ⟨body⟩)
 ⟨exp₁⟩ ⋯ ⟨expₙ⟩)
```

Điều này không có gì bí ẩn: `let` được trình phiên dịch giải thích thành đúng như vậy. Nó cũng có nghĩa là `let` không cần bất kỳ cơ chế mới nào trong trình phiên dịch — `let` luôn có thể được viết lại thành `lambda`.

Phạm vi của biến do `let` khai báo chính là phần thân của `let`. Ví dụ, nếu giá trị của `x` là `5`, thì biểu thức sau có giá trị `38`:

```lisp
(+ (let ((x 3))
     (+ x (* x 10)))
   x)
```

vì bên trong `let`, `x` là `3`, còn bên ngoài `let`, `x` vẫn là `5`.

Một điểm quan trọng: các biểu thức tính giá trị của biến trong `let` được tính toán trong phạm vi **bên ngoài** `let`, không phải bên trong. Do đó, nếu `x` là `2`, biểu thức sau có giá trị `12`:

```lisp
(let ((x 3)
      (y (+ x 2)))
  (* x y))
```

vì `y` được tính là `(+ x 2)` với `x = 2` (giá trị bên ngoài `let`), cho ra `y = 4`, rồi trong thân `let` ta tính `(* 3 4) = 12`.

Đôi khi ta có thể dùng _định nghĩa nội bộ_ để cùng đạt mục đích tương tự. Tuy nhiên, ta ưa dùng `let` trong những tình huống như thế này, còn `define` nội bộ chỉ dành cho quy trình nội bộ.[^2]

[^1]: Thực ra, `lambda` có thể dùng ở bất kỳ chỗ nào mà thông thường ta dùng tên quy trình — không nhất thiết chỉ làm đối số cho quy trình bậc cao.

[^2]: Sự hiểu biết sâu hơn về _định nghĩa nội bộ_ sẽ đến ở mục 1.3.4, khi chúng ta thảo luận về cách trình phiên dịch xử lý các `define` bên trong thân quy trình.

---

[exercise 1.34](exercises/1.34.md) [`procedure`](GLOSSARY.md#procedure) [`special form`](GLOSSARY.md#special-form) [`environment`](GLOSSARY.md#environment) [`variable`](GLOSSARY.md#variable) [`scope`](GLOSSARY.md#scope) [`combination`](GLOSSARY.md#combination) [`internal definitions`](GLOSSARY.md#internal-definitions) [`parameter`](GLOSSARY.md#parameter) [`block structure`](GLOSSARY.md#block-structure)
