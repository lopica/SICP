## 2.1 Giới thiệu về trừu tượng dữ liệu

Ở mục 1.1.8, ta đã nhận xét rằng một **quy trình** được dùng như một thành phần trong quá trình tạo ra **quy trình** phức tạp hơn có thể được xem không chỉ là một tập hợp các phép tính cụ thể, mà còn là _sự trừu tượng quy trình_. Tức là, ta có thể bỏ qua chi tiết hiện thực của **quy trình** đó, và thay thế nó bằng bất kỳ **quy trình** nào khác có hành vi tổng thể tương đương. Nói cách khác, ta có thể tạo ra một sự trừu tượng tách biệt cách **quy trình** được sử dụng khỏi chi tiết cách **quy trình** được hiện thực bằng các **quy trình** nguồn cơ bản hơn.

Khái niệm tương tự áp dụng cho _dữ liệu tổng hợp_ được gọi là _trừu tượng dữ liệu_. _Trừu tượng dữ liệu_ là một phương pháp luận giúp ta tách biệt cách sử dụng một đối tượng _dữ liệu tổng hợp_ khỏi chi tiết cách đối tượng đó được xây dựng từ các đối tượng **dữ liệu** nguyên thủy.

Ý tưởng cốt lõi của _trừu tượng dữ liệu_ là cấu trúc các **chương trình** dùng đối tượng _dữ liệu tổng hợp_ sao cho chúng làm việc trên "dữ liệu trừu tượng". Tức là, **chương trình** của ta nên sử dụng **dữ liệu** theo cách không đưa ra bất kỳ giả định nào về **dữ liệu** ngoài những gì thực sự cần thiết để hoàn thành nhiệm vụ. Đồng thời, một biểu diễn "cụ thể" của **dữ liệu** được định nghĩa độc lập với các **chương trình** sử dụng **dữ liệu** đó. Giao diện giữa hai phần này sẽ là một tập hợp các **quy trình** gọi là _bộ chọn_ và _bộ khởi tạo_, hiện thực **dữ liệu** trừu tượng theo biểu diễn cụ thể. Để minh họa kỹ thuật này, ta sẽ xem xét cách thiết kế một tập **quy trình** để thao tác với số hữu tỉ.

---

[`data`](GLOSSARY.md#data) [`compound data`](GLOSSARY.md#compound-data) [`procedure`](GLOSSARY.md#procedure) [`procedural abstraction`](GLOSSARY.md#procedural-abstraction) [`primitive data`](GLOSSARY.md#primitive-data) [`program`](GLOSSARY.md#program)
