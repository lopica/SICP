# 1.3.4 Quy trình như là giá trị trả về

Ví dụ về _làm mờ trung bình_ (average damping) trong mục trước đã minh họa một kỹ thuật hữu ích: đôi khi ta có thể hiểu sâu hơn về một bài toán nếu ta xây dựng thêm các trừu tượng mới — không chỉ đơn giản là viết quy trình chạy được. Là nhà toán học ta có thể muốn đặt tên cho sự biến đổi này và nghiên cứu nó trực tiếp. Ví dụ, **làm mờ trung bình** (average damping) là một kỹ thuật: với hàm $f$, ta tạo ra hàm mới mà tại mỗi điểm $x$ trả về trung bình của $x$ và $f(x)$. Ta có thể diễn đạt ý tưởng này bằng quy trình sau:

```lisp
(define (average-damp f)
  (lambda (x) (average x (f x))))
```

`average-damp` là một quy trình nhận một quy trình `f` làm đối số và trả về một quy trình (được tạo bởi `lambda`) mà khi áp dụng cho số `x` sẽ cho ra trung bình của `x` và `(f x)`. Ví dụ, áp dụng `average-damp` cho quy trình `square` sẽ tạo ra một quy trình mà tại số `10` trả về trung bình của `10` và `100`, tức là `55`:[^1]

```lisp
((average-damp square) 10)
55
```

Nhờ `average-damp`, ta có thể viết lại quy trình tìm căn bậc hai từ mục 1.3.3 một cách rõ ràng hơn:

```lisp
(define (sqrt x)
  (fixed-point (average-damp (lambda (y) (/ x y)))
               1.0))
```

Chú ý cách ta làm rõ ba ý tưởng riêng biệt trong đó: tìm điểm cố định, làm mờ trung bình, và hàm $y \mapsto x/y$. Thật đáng giá khi so sánh cách viết này với phiên bản đầu tiên trong mục 1.1.7:

```lisp
(define (sqrt x)
  (define (good-enough? guess)
    (< (abs (- (square guess) x)) 0.001))
  (define (improve guess)
    (average guess (/ x guess)))
  (define (sqrt-iter guess)
    (if (good-enough? guess)
        guess
        (sqrt-iter (improve guess))))
  (sqrt-iter 1.0))
```

Hãy nhớ lại rằng `sqrt` thực chất là một trường hợp đặc biệt của tìm điểm cố định. Điều đó làm ta nghĩ ngay đến việc tổng quát hóa: tại sao không áp dụng cùng phương pháp cho căn bậc ba? Căn bậc ba của $x$ là điểm cố định của hàm $y \mapsto x/y^2$, và quá trình tìm điểm cố định này không hội tụ nếu không có làm mờ trung bình. Bây giờ, một lần làm mờ trung bình cũng đủ để ổn định quá trình tìm căn bậc ba:

```lisp
(define (cube-root x)
  (fixed-point (average-damp (lambda (y) (/ x (square y))))
               1.0))
```

## Phương pháp Newton

Khi lần đầu giới thiệu phép tính căn bậc hai trong mục 1.1.7, ta đã đề cập đến đây là trường hợp đặc biệt của _phương pháp Newton_. Nếu $x \mapsto g(x)$ là một hàm khả vi, thì nghiệm của phương trình $g(x) = 0$ là điểm cố định của hàm $x \mapsto f(x)$, trong đó:

$$f(x) = x - \frac{g(x)}{Dg(x)}$$

và $Dg(x)$ là đạo hàm của $g$ tại $x$. Phương pháp Newton là cách sử dụng phép tính điểm cố định mà ta đã thấy ở trên để tìm xấp xỉ nghiệm của phương trình bằng cách tìm điểm cố định của hàm $f$.[^2] Với nhiều hàm $g$ và điểm xuất phát đủ tốt, phương pháp Newton hội tụ rất nhanh.

Để cài đặt phương pháp Newton như một quy trình, trước tiên ta cần diễn đạt ý niệm _đạo hàm_. Chú ý rằng "đạo hàm", cũng như "làm mờ trung bình", là một phép biến đổi từ hàm sang hàm. Đạo hàm của hàm $g$ là hàm mà tại mỗi số $x$ trả về:

$$Dg(x) = \frac{g(x + dx) - g(x)}{dx}$$

với $dx$ là một số nhỏ. Ta diễn đạt điều này bằng quy trình:

```lisp
(define (deriv g)
  (lambda (x)
    (/ (- (g (+ x dx)) (g x))
       dx)))
```

cùng với định nghĩa:

```lisp
(define dx 0.00001)
```

Cũng như `average-damp`, `deriv` là một quy trình nhận một quy trình và trả về một quy trình. Ví dụ, để xấp xỉ đạo hàm của $x \mapsto x^3$ tại $x = 5$ (giá trị đúng là $75$), ta viết:

```lisp
((deriv (lambda (x) (* x x x))) 5)
75.00014999664018
```

Có `deriv` trong tay, ta có thể diễn đạt biến đổi Newton như sau:

```lisp
(define (newton-transform g)
  (lambda (x)
    (- x (/ (g x) ((deriv g) x)))))
```

và định nghĩa phương pháp Newton như quy trình tìm điểm cố định của biến đổi đó:

```lisp
(define (newtons-method g guess)
  (fixed-point (newton-transform g) guess))
```

Quy trình `newton-transform` biểu diễn công thức ở trên, còn `newtons-method` áp dụng nó bắt đầu từ điểm khởi tạo ban đầu là `guess`. Bây giờ ta có thể tìm căn bậc hai của `x` bắt đầu từ điểm khởi tạo là `1.0` bằng cách dùng phương pháp Newton để tìm nghiệm bằng $0$ của hàm $y \mapsto y^2 - x$:[^3]

```lisp
(define (sqrt x)
  (newtons-method (lambda (y) (- (square y) x))
                  1.0))
```

## Trừu tượng hóa và quy trình hạng nhất

Ta đã thấy hai cách diễn đạt tính toán căn bậc hai như trường hợp của một quy trình tổng quát hơn — một lần như tìm điểm cố định của làm mờ trung bình, một lần như dùng phương pháp Newton. Vì bản thân phương pháp Newton cũng được diễn đạt như một quy trình tìm điểm cố định, thực chất ta đã thấy hai cách để diễn đạt `sqrt` như tìm điểm cố định. Mỗi cách đặt ý niệm thành một quy trình riêng:

```lisp
(define (fixed-point-of-transform g transform guess)
  (fixed-point (transform g) guess))
```

Quy trình tổng quát này nhận đối số là một hàm `g` tính toán hàm nào đó, một phép biến đổi `transform` biến đổi `g`, và một điểm xuất phát ban đầu `guess`. Kết quả trả về là điểm cố định của hàm đã được biến đổi.

Dùng trừu tượng này, ta có thể viết lại quy trình `sqrt` đầu tiên của mục này (tìm điểm cố định của phiên bản làm mờ trung bình của $y \mapsto x/y$):

```lisp
(define (sqrt x)
  (fixed-point-of-transform (lambda (y) (/ x y))
                             average-damp
                             1.0))
```

Tương tự, ta có thể diễn đạt `sqrt` theo phương pháp Newton từ phần này (điểm cố định của biến đổi Newton của $y \mapsto y^2 - x$):

```lisp
(define (sqrt x)
  (fixed-point-of-transform (lambda (y) (- (square y) x))
                             newton-transform
                             1.0))
```

Việc đặt tên và xây dựng tầng trừu tượng là chìa khóa để thiết kế những chương trình lớn và mạnh. Tuy không phải lúc nào cũng rõ ràng nên chọn mức độ trừu tượng nào, nhưng nhà lập trình giỏi phải nhận ra cơ hội để nhận biết các khái niệm ẩn sau công việc mình làm, và xây dựng trực tiếp trên đó. Không phải lúc nào ta cũng nên trừu tượng hóa mọi thứ có thể; song ta nên có khả năng suy nghĩ theo hướng đó và sẵn sàng áp dụng khi thấy phù hợp.

Tầm quan trọng của **quy trình hạng nhất** (first-class procedures) là hiển nhiên. Chúng mang lại cho ta khả năng diễn đạt các phương pháp tổng quát của tính toán và làm việc với chúng như với những đối tượng ngôn ngữ bình thường. Nhìn chung, các ngôn ngữ lập trình áp đặt hạn chế lên cách có thể dùng các phần tử trong ngôn ngữ. Các phần tử có ít hạn chế nhất được gọi là _hạng nhất_. Một số "quyền" của các phần tử hạng nhất là:[^4]

- Được đặt tên bằng biến.
- Được truyền như đối số cho quy trình.
- Được trả về như kết quả của quy trình.
- Được đưa vào cấu trúc dữ liệu.[^5]

Lisp, không giống phần lớn các ngôn ngữ lập trình khác, trao quyền hạng nhất đầy đủ cho **quy trình**. Điều này gây ra những thách thức cài đặt nhất định, nhưng lợi ích về mặt diễn đạt là rất lớn.[^6]

[^1]: Hãy quan sát cú pháp gọi quy trình: `((average-damp square) 10)`. Kết quả của `(average-damp square)` là một quy trình, và quy trình đó được áp dụng cho `10`. Chú ý điều này nhất quán với cách Lisp thực thi biểu thức: phần tử đầu tiên của một biểu thức kết hợp (ở đây là `(average-damp square)`) được thực thi để lấy về quy trình, rồi quy trình đó được áp dụng cho phần còn lại.

[^2]: Các công thức toán tử-đạo hàm như phương pháp Newton thường được quy kết cho nhà toán học người Anh Isaac Newton (1643–1727). Thực ra phương pháp này cũng được Joseph Raphson (1648–1715) phát hiện gần như cùng lúc, nên đôi khi được gọi là phương pháp Newton-Raphson.

[^3]: Để thấy điều này, hãy lưu ý rằng $y^2 - x = 0$ tương đương với $y^2 = x$, tức là $y = \sqrt{x}$.

[^4]: Danh sách các đặc quyền của phần tử hạng nhất là từ ngôn ngữ lập trình Christopher Strachey (1916–1975), người là tiên phong trong việc nghiên cứu các hàm hạng nhất trong ngôn ngữ lập trình.

[^5]: Khả năng lưu quy trình vào cấu trúc dữ liệu sẽ được trình bày trong chương 2 khi nói về _cặp_ (pairs) và _danh sách_ (lists).

[^6]: Thách thức cài đặt chủ yếu đến từ việc quy trình hạng nhất cần phải mang theo thông tin về môi trường của nó. Điều này dẫn đến cơ chế _bao đóng_ (closure) mà ta sẽ nghiên cứu kỹ hơn về sau.

---

[exercise 1.40](exercises/1.40.md) [exercise 1.41](exercises/1.41.md) [exercise 1.42](exercises/1.42.md) [exercise 1.43](exercises/1.43.md) [exercise 1.44](exercises/1.44.md) [exercise 1.45](exercises/1.45.md) [exercise 1.46](exercises/1.46.md) [`procedure`](GLOSSARY.md#procedure) [`environment`](GLOSSARY.md#environment) [`parameter`](GLOSSARY.md#parameter) [`variable`](GLOSSARY.md#variable) [`expression`](GLOSSARY.md#expression) [`compound procedure`](GLOSSARY.md#compound-procedure) [`special form`](GLOSSARY.md#special-form) [`average damping`](GLOSSARY.md#average-damping) [`first-class`](GLOSSARY.md#first-class)
