# 1.2.2 Đệ quy cây

Một cách tính toán phổ biến khác là **đệ quy cây**. Ví dụ, hãy xét việc tính dãy số Fibonacci, trong đó mỗi số là tổng của hai số đứng ngay trước nó:

$$ 0,\ 1,\ 1,\ 2,\ 3,\ 5,\ 8,\ 13,\ 21,\ \ldots $$

Tổng quát, các số Fibonacci có thể được định nghĩa bằng quy tắc:

$$
\mathrm{Fib}(n) =
\begin{cases}
0 & \text{nếu } n = 0 \\
1 & \text{nếu } n = 1 \\
\mathrm{Fib}(n-1) + \mathrm{Fib}(n-2) & \text{trong các trường hợp khác.}
\end{cases}
$$

Ta có thể chuyển ngay định nghĩa này thành một quy trình đệ quy để tính số Fibonacci:

```lisp
(define (fib n)
  (cond ((= n 0) 0)
        ((= n 1) 1)
        (else (+ (fib (- n 1))
                 (fib (- n 2))))))
```

Hãy quan sát hình thù của tiến trình này. Để tính `(fib 5)`, ta phải tính `(fib 4)` và `(fib 3)`. Để tính `(fib 4)` ta lại phải tính `(fib 3)` và `(fib 2)`, và cứ thế. Toàn bộ quá trình mở ra thành một cái cây như hình dưới.

![Tiến trình đệ quy cây khi tính (fib 5)](./figures/figure5.png)

Để ý rằng cái cây này chứa rất nhiều phần lặp lại: `(fib 3)` được tính tới hai lần, và gần một nửa công sức bị lãng phí cho việc tính lại những giá trị đã có. Thực tế, không khó để chứng minh rằng số lần quy trình tính `(fib 1)` hoặc `(fib 0)` (tức số lá của cây) chính xác bằng $\mathrm{Fib}(n+1)$. Để hình dung mức độ tệ của điều này, ta có thể chứng minh được rằng giá trị $\mathrm{Fib}(n)$ tăng theo cấp lũy thừa của `n`. Cụ thể hơn (xem Bài tập 1.13), $\mathrm{Fib}(n)$ là số nguyên gần nhất với $\varphi^n / \sqrt{5}$, trong đó

$$ \varphi = \frac{1 + \sqrt{5}}{2} \approx 1{,}6180 $$

là **tỉ lệ vàng**, thỏa mãn phương trình

$$ \varphi^2 = \varphi + 1. $$

Vậy nên tiến trình này dùng một số bước tăng theo cấp lũy thừa của đầu vào. Trong khi đó, không gian (bộ nhớ) chỉ tăng tuyến tính theo đầu vào, vì tại mỗi thời điểm ta chỉ cần lưu lại các nút nằm phía trên ta trên đường đi. Tổng quát, số bước của một **tiến trình đệ quy cây** tỉ lệ với số nút trong cây, còn không gian cần dùng tỉ lệ với độ sâu lớn nhất của cây.[^1]

Ta cũng có thể tạo ra số Fibonacci bằng một tiến trình lặp. Ý tưởng là dùng một cặp số nguyên `a` và `b`, khởi tạo lần lượt bằng $\mathrm{Fib}(1) = 1$ và $\mathrm{Fib}(0) = 0$, rồi liên tục áp dụng đồng thời phép biến đổi:

```
a ← a + b
b ← a
```

Không khó để chứng minh rằng sau khi áp dụng phép biến đổi này `n` lần, `a` và `b` sẽ lần lượt bằng $\mathrm{Fib}(n+1)$ và $\mathrm{Fib}(n)$. Vậy ta có thể tính số Fibonacci theo kiểu lặp bằng quy trình:

```lisp
(define (fib n)
  (fib-iter 1 0 n))

(define (fib-iter a b count)
  (if (= count 0)
      b
      (fib-iter (+ a b) a (- count 1))))
```

Cách thứ hai này tính `fib` bằng một tiến trình lặp tuyến tính. Sự chênh lệch về số bước giữa hai cách — một bên tuyến tính, một bên theo cấp lũy thừa — là rất lớn, ngay cả với những giá trị đầu vào nhỏ.

Tuy nhiên, không nên kết luận rằng các tiến trình đệ quy cây là vô dụng. Khi ta làm việc với dữ liệu có cấu trúc phân cấp thay vì các con số, đệ quy cây là một công cụ tự nhiên và mạnh mẽ. Ngay cả với các phép tính trên số, các tiến trình đệ quy cây cũng có thể giúp ta hiểu và thiết kế chương trình dễ dàng hơn. Chẳng hạn, dù quy trình `fib` đầu tiên kém hiệu quả hơn nhiều so với quy trình thứ hai, nó lại trực quan hơn, gần như chỉ là một bản dịch trực tiếp của định nghĩa toán học của dãy Fibonacci sang Lisp. Còn để viết được quy trình lặp, ta phải nhận ra rằng phép tính có thể được tổ chức lại quanh hai biến trạng thái.

## Ví dụ: đếm số cách đổi tiền

Để hình dung ích lợi của đệ quy cây, hãy xét bài toán sau: có bao nhiêu cách khác nhau để đổi một đồng `1.00` đô la ra các đồng xu lẻ, biết rằng ta có các loại xu mệnh giá nửa đô (50 xu), một phần tư đô (25 xu), một hào (10 xu), năm xu (5 xu) và một xu (1 xu)? Tổng quát hơn, liệu ta có thể viết một quy trình để tính số cách đổi một khoản tiền bất kỳ?

Bài toán này có một lời giải đệ quy đơn giản. Giả sử ta sắp xếp các loại xu theo một thứ tự nào đó. Khi đó ta có hệ thức sau:

Số cách đổi một khoản tiền `a` bằng `n` loại xu bằng:

- số cách đổi khoản tiền `a` mà **không** dùng loại xu đầu tiên, cộng với
- số cách đổi khoản tiền `a − d` bằng cả `n` loại xu, trong đó `d` là mệnh giá của loại xu đầu tiên.

Để hiểu vì sao điều này đúng, hãy để ý rằng các cách đổi tiền có thể được chia thành hai nhóm: nhóm **không** dùng đồng xu đầu tiên nào, và nhóm có dùng. Vậy tổng số cách đổi một khoản tiền bằng tổng của: số cách đổi khoản tiền đó mà không dùng đến loại xu đầu tiên, cộng với số cách đổi tiền có dùng đến loại xu đầu tiên. Mà con số thứ hai lại đúng bằng số cách đổi khoản tiền còn lại sau khi đã dùng một đồng xu đầu tiên.

Như vậy, ta có thể quy bài toán đổi một khoản tiền cho trước thành các bài toán đổi những khoản tiền nhỏ hơn bằng những loại xu ít hơn. Hãy xét kỹ quy tắc rút gọn này, và tự thuyết phục bản thân rằng ta có thể dùng nó để mô tả một thuật toán nếu xác định thêm các trường hợp suy biến sau:[^2]

- Nếu `a` đúng bằng `0`, ta tính là có `1` cách đổi tiền.
- Nếu `a` nhỏ hơn `0`, ta tính là có `0` cách đổi tiền.
- Nếu `n` bằng `0`, ta tính là có `0` cách đổi tiền.

Ta có thể chuyển dễ dàng mô tả này thành một quy trình đệ quy:

```lisp
(define (count-change amount)
  (cc amount 5))

(define (cc amount kinds-of-coins)
  (cond ((= amount 0) 1)
        ((or (< amount 0) 
             (= kinds-of-coins 0)) 
         0)
        (else 
         (+ (cc amount (- kinds-of-coins 1))
            (cc (- amount
                   (first-denomination 
                    kinds-of-coins))
                kinds-of-coins)))))

(define (first-denomination kinds-of-coins)
  (cond ((= kinds-of-coins 1) 1)
        ((= kinds-of-coins 2) 5)
        ((= kinds-of-coins 3) 10)
        ((= kinds-of-coins 4) 25)
        ((= kinds-of-coins 5) 50)))
```

Quy trình `first-denomination` nhận vào số loại xu hiện có và trả về mệnh giá của loại xu đầu tiên. Ở đây ta sắp xếp các loại xu từ lớn đến nhỏ, nhưng thứ tự nào cũng cho ra kết quả như nhau. Bây giờ ta có thể trả lời câu hỏi ban đầu về việc đổi một đồng đô la:

```lisp
(count-change 100)
292
```

Quy trình `count-change` tạo ra một tiến trình đệ quy cây với những phần tính toán lặp lại y như trường hợp `fib` đầu tiên của chúng ta. Một mặt, rất khó để thiết kế một thuật toán tốt hơn để tính ra kết quả, và ta sẽ để vấn đề này lại cho người đọc tự suy ngẫm. Mặt khác, để ý rằng một tiến trình đệ quy cây tệ về hiệu năng vẫn có thể được cải thiện đáng kể bằng cách ghi nhớ lại các giá trị đã tính (ta sẽ bàn đến kỹ thuật này về sau). Việc một tiến trình tốn nhiều tài nguyên không có nghĩa là quy trình mô tả nó vô dụng — nó thường là điểm khởi đầu trực quan để rồi ta tối ưu dần.

---

[^1]: Ví dụ, ta có thể tính chính xác số phép cộng cần thực hiện. Quy trình `fib` đệ quy cây không thực dụng để tính số Fibonacci, nhưng nó là một minh họa tuyệt vời cho tiến trình đệ quy cây. Số Fibonacci đã được nghiên cứu từ lâu, đặt theo tên Leonardo thành Pisa, nhà toán học thế kỷ 13 còn được gọi là Fibonacci, người đã đưa chúng vào trong một bài toán về sự sinh sản của loài thỏ. Dãy số này có vô số ứng dụng trong toán học và khoa học máy tính, và tỉ lệ vàng $\varphi$ cũng xuất hiện thường xuyên trong tự nhiên cũng như nghệ thuật.

[^2]: Việc các trường hợp suy biến được chọn như vậy có thể trông hơi tùy tiện, nhưng chúng được chọn sao cho quy tắc đệ quy cho ra kết quả đúng. Ví dụ, việc tính khoản tiền `0` là có đúng `1` cách đổi (cách "rỗng", không dùng đồng xu nào) là điều cần thiết để các phép cộng ở các tầng trên cho ra số đếm chính xác.

---

**đệ quy cây**: tree recursion <br />
**tiến trình đệ quy cây**: tree-recursive process <br />
số Fibonacci: Fibonacci numbers <br />
**tỉ lệ vàng**: golden ratio <br />
theo cấp lũy thừa: exponentially <br />
đếm số cách đổi tiền: counting change <br />
mệnh giá: denomination <br />
trường hợp suy biến: degenerate case <br />
