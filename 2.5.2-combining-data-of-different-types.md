# 2.5.2 Kết hợp dữ liệu của nhiều kiểu khác nhau

Ta đã thấy cách xây dựng một hệ thống số học thống nhất bao gồm số thông thường, số phức, số hữu tỉ, và bất kỳ loại số nào khác mà ta muốn thêm vào — nhưng ta đã bỏ qua một vấn đề quan trọng. Các phép tính ta định nghĩa từ trước đến nay xem các kiểu dữ liệu khác nhau là hoàn toàn độc lập với nhau. Vì vậy, có các gói riêng để cộng hai số thông thường, hay cộng hai số phức. Điều ta chưa xét là việc định nghĩa các phép tính _xuyên kiểu_ (cross-type) hoàn toàn có ý nghĩa — chẳng hạn, cộng một số phức với một số thông thường. Ta đã cẩn thận xây dựng các _hàng rào trừu tượng_ giữa các phần của chương trình để chúng có thể được phát triển và hiểu một cách độc lập. Ta muốn đưa các phép tính xuyên kiểu vào theo một cách có kiểm soát, để có thể hỗ trợ chúng mà không phá vỡ nghiêm trọng ranh giới **mô đun** của mình.

Một cách xử lý phép tính xuyên kiểu là thiết kế một **quy trình** riêng cho từng tổ hợp kiểu có thể. Ví dụ, ta có thể mở rộng gói số phức để nó cung cấp thêm một **quy trình** cộng số phức với số thông thường và đăng ký nó vào bảng với thẻ `(complex scheme-number)`:

```lisp
(define (add-complex-to-schemenum z x)
  (make-from-real-imag (+ (real-part z) x)
                       (imag-part z)))

(put 'add 
     '(complex scheme-number)
     (lambda (z x) 
       (tag (add-complex-to-schemenum z x))))
```

Cách này hoạt động, nhưng rất rườm rà. Với một hệ thống như vậy, chi phí để thêm một kiểu mới không chỉ là xây dựng gói các **quy trình** cho kiểu đó, mà còn phải xây dựng và đăng ký các **quy trình** cài đặt các phép tính xuyên kiểu. Phần code này rất dễ nhiều hơn cả phần cài đặt các phép tính cho chính kiểu đó. Cách tiếp cận này cũng làm suy yếu khả năng kết hợp các gói riêng lẻ một cách độc lập với nhau, hay ít nhất là hạn chế mức độ mà người cài đặt từng gói cần quan tâm đến các gói khác. Ví dụ, có vẻ hợp lý khi xử lý phép tính hỗn hợp trên số phức và số thông thường là trách nhiệm của gói số phức. Nhưng kết hợp số hữu tỉ với số phức thì lại có thể thuộc về gói số phức, gói số hữu tỉ, hoặc một gói thứ ba nào đó dùng các phép tính trích xuất từ hai gói kia. Xây dựng chính sách nhất quán về phân chia trách nhiệm giữa các gói là một bài toán rất khó trong các hệ thống có nhiều gói và nhiều phép tính xuyên kiểu.

## Ép kiểu

Trong trường hợp tổng quát của các phép tính hoàn toàn không liên quan đến nhau tác động lên các kiểu hoàn toàn không liên quan, việc cài đặt các phép tính xuyên kiểu tường minh — dù rườm rà — là điều tốt nhất có thể làm. May mắn là ta thường làm được tốt hơn nhờ tận dụng cấu trúc bổ sung tiềm ẩn trong hệ thống kiểu. Thông thường các kiểu dữ liệu khác nhau không hoàn toàn độc lập với nhau, và có những cách để xem một đối tượng thuộc kiểu này như thuộc kiểu kia. Quá trình này gọi là _ép kiểu_ (coercion). Ví dụ, nếu ta cần kết hợp số học một số thông thường với một số phức, ta có thể xem số thông thường đó như một số phức có phần ảo bằng không. Điều này biến bài toán thành kết hợp hai số phức, vốn có thể xử lý theo cách thông thường bởi gói số học phức.

Nhìn chung, ta cài đặt ý tưởng này bằng cách thiết kế các **quy trình** ép kiểu chuyển đổi đối tượng từ kiểu này thành đối tượng tương đương ở kiểu kia. Dưới đây là một **quy trình** ép kiểu điển hình, chuyển một số thông thường thành số phức có phần thực bằng chính số đó và phần ảo bằng không:

```lisp
(define (scheme-number->complex n)
  (make-complex-from-real-imag 
   (contents n) 0))
```

Ta đăng ký các **quy trình** ép kiểu này vào một bảng ép kiểu đặc biệt, được đánh chỉ mục theo tên của hai kiểu:[^1]

```lisp
(put-coercion 'scheme-number 'complex 
              scheme-number->complex)
```

Nhìn chung một số ô trong bảng sẽ trống, vì không phải lúc nào ta cũng có thể ép một đối tượng dữ liệu của kiểu này sang mọi kiểu khác. Chẳng hạn, không có cách nào ép một số phức tùy ý thành số thông thường, nên trong bảng sẽ không có **quy trình** `complex->scheme-number` chung nào cả.

Một khi bảng ép kiểu đã được thiết lập, ta có thể xử lý ép kiểu một cách thống nhất bằng cách sửa đổi **quy trình** `apply-generic` từ mục 2.4.3. Khi được yêu cầu áp dụng một phép tính, trước tiên ta kiểm tra xem phép tính đó có được định nghĩa cho kiểu của các đối số hay không, như trước đây. Nếu có, ta chuyển đến **quy trình** tìm được trong bảng phép tính—kiểu. Nếu không, ta thử ép kiểu. Để đơn giản, ta chỉ xét trường hợp có hai đối số.[^2] Ta tra bảng ép kiểu xem đối tượng của kiểu thứ nhất có thể ép sang kiểu thứ hai hay không. Nếu được, ta ép đối số thứ nhất và thử lại phép tính. Nếu không thể ép kiểu thứ nhất sang kiểu thứ hai nói chung, ta thử theo chiều ngược lại xem có thể ép đối số thứ hai sang kiểu của đối số thứ nhất hay không. Cuối cùng, nếu không có cách ép kiểu nào được biết theo cả hai chiều, ta bỏ cuộc. Đây là **quy trình**:

```lisp
(define (apply-generic op . args)
  (let ((type-tags (map type-tag args)))
    (let ((proc (get op type-tags)))
      (if proc
          (apply proc (map contents args))
          (if (= (length args) 2)
              (let ((type1 (car type-tags))
                    (type2 (cadr type-tags))
                    (a1 (car args))
                    (a2 (cadr args)))
                (let ((t1->t2 
                       (get-coercion type1
                                     type2))
                      (t2->t1 
                       (get-coercion type2 
                                     type1)))
                  (cond (t1->t2
                         (apply-generic 
                          op (t1->t2 a1) a2))
                        (t2->t1
                         (apply-generic 
                          op a1 (t2->t1 a2)))
                        (else
                         (error 
                          "No method for 
                           these types"
                          (list 
                           op 
                           type-tags))))))
              (error 
               "No method for these types"
               (list op type-tags)))))))
```

Cơ chế ép kiểu này có nhiều ưu điểm so với phương pháp định nghĩa các phép tính xuyên kiểu tường minh như nói ở trên. Dù ta vẫn cần viết các **quy trình** ép kiểu để liên hệ các kiểu (có thể lên tới $n^2$ **quy trình** cho hệ thống có $n$ kiểu), ta chỉ cần viết một **quy trình** cho mỗi cặp kiểu thay vì một **quy trình** khác nhau cho mỗi tổ hợp kiểu và mỗi phép tính tổng quát.[^3] Điều ta dựa vào ở đây là thực tế rằng phép chuyển đổi phù hợp giữa các kiểu chỉ phụ thuộc vào bản thân các kiểu, chứ không phụ thuộc vào phép tính sẽ áp dụng.

Mặt khác, có thể có những ứng dụng mà cơ chế ép kiểu của ta chưa đủ tổng quát. Ngay cả khi không đối tượng nào trong hai đối tượng cần kết hợp có thể chuyển đổi sang kiểu của đối tượng kia, vẫn có thể thực hiện phép tính bằng cách chuyển đổi cả hai đối tượng sang một kiểu thứ ba. Để xử lý sự phức tạp như vậy mà vẫn giữ được tính mô đun, thông thường ta cần xây dựng các hệ thống tận dụng thêm cấu trúc trong quan hệ giữa các kiểu, như ta sẽ thảo luận tiếp theo.

## Hệ thống phân cấp kiểu

Cơ chế ép kiểu ở trên dựa vào sự tồn tại của quan hệ tự nhiên giữa các cặp kiểu. Thông thường còn có cấu trúc "toàn cục" hơn trong cách các kiểu khác nhau liên hệ với nhau. Chẳng hạn, giả sử ta đang xây dựng một hệ thống số học tổng quát để xử lý số nguyên, số hữu tỉ, số thực và số phức. Trong hệ thống đó, rất tự nhiên khi xem số nguyên là một loại đặc biệt của số hữu tỉ, số hữu tỉ lại là một loại đặc biệt của số thực, và số thực là một loại đặc biệt của số phức. Thực ra ta có cái gọi là _hệ thống phân cấp kiểu_ (hierarchy of types), trong đó, chẳng hạn, số nguyên là _kiểu con_ (subtype) của số hữu tỉ (tức là, mọi phép tính có thể áp dụng cho số hữu tỉ đều tự động áp dụng được cho số nguyên). Ngược lại, ta nói số hữu tỉ tạo thành _kiểu cha_ (supertype) của số nguyên. Hệ thống phân cấp cụ thể ở đây có dạng rất đơn giản, trong đó mỗi kiểu có nhiều nhất một kiểu cha và nhiều nhất một kiểu con. Cấu trúc như vậy được gọi là _tháp kiểu_ (tower), được minh họa trong Hình 2.25.

![Hình 2.25: Tháp kiểu](./figures/figure85.png)

Nếu ta có cấu trúc tháp, ta có thể đơn giản hóa đáng kể bài toán thêm kiểu mới vào hệ thống phân cấp, vì ta chỉ cần xác định cách nhúng kiểu mới vào kiểu cha ngay trên nó và cách nó là kiểu cha của kiểu dưới nó. Ví dụ, nếu ta muốn cộng một số nguyên với một số phức, ta không cần định nghĩa tường minh **quy trình** ép kiểu `integer->complex` đặc biệt. Thay vào đó, ta định nghĩa cách chuyển số nguyên thành số hữu tỉ, số hữu tỉ thành số thực, và số thực thành số phức. Rồi ta để hệ thống tự chuyển đổi số nguyên thành số phức qua các bước này, sau đó cộng hai số phức.

Ta có thể thiết kế lại **quy trình** `apply-generic` theo cách sau: với mỗi kiểu, ta cần cung cấp một **quy trình** `raise` "nâng" các đối tượng của kiểu đó lên một bậc trong tháp. Khi hệ thống cần thực hiện phép tính trên các đối tượng của các kiểu khác nhau, nó có thể liên tiếp nâng các kiểu thấp hơn cho đến khi tất cả đối tượng ở cùng bậc trong tháp. (Bài tập 2.83 và Bài tập 2.84 đề cập đến chi tiết cài đặt chiến lược như vậy.)

Một ưu điểm khác của tháp là ta có thể dễ dàng cài đặt khái niệm mọi kiểu đều "kế thừa" tất cả các phép tính được định nghĩa trên kiểu cha. Ví dụ, nếu ta không cung cấp **quy trình** đặc biệt để tính phần thực của một số nguyên, ta vẫn nên kỳ vọng rằng `real-part` sẽ được định nghĩa cho số nguyên vì số nguyên là kiểu con của số phức. Trong một tháp, ta có thể sắp xếp để điều này xảy ra một cách thống nhất bằng cách sửa đổi `apply-generic`. Nếu phép tính yêu cầu không được định nghĩa trực tiếp cho kiểu của đối tượng, ta nâng đối tượng lên kiểu cha và thử lại. Ta cứ trèo lên tháp như vậy, biến đổi đối số dọc đường, cho đến khi tìm được bậc có thể thực hiện phép tính mong muốn hoặc chạm đến đỉnh (khi đó ta bỏ cuộc).

Một ưu điểm nữa của tháp so với hệ thống phân cấp tổng quát hơn là nó cho ta cách đơn giản để "hạ" một đối tượng dữ liệu xuống biểu diễn đơn giản nhất. Ví dụ, nếu ta cộng $2 + 3i$ với $4 - 3i$, sẽ tốt hơn nếu nhận được kết quả là số nguyên $6$ thay vì số phức $6 + 0i$. Bài tập 2.85 thảo luận về cách cài đặt phép toán hạ như vậy. (Mấu chốt là ta cần một cách tổng quát để phân biệt các đối tượng có thể hạ được, chẳng hạn $6 + 0i$, với các đối tượng không thể hạ, chẳng hạn $6 + 2i$.)

## Hạn chế của hệ thống phân cấp

Nếu các kiểu dữ liệu trong hệ thống của ta có thể sắp xếp tự nhiên theo dạng tháp, điều đó đơn giản hóa đáng kể bài toán xử lý các phép tính tổng quát trên nhiều kiểu, như ta đã thấy. Nhưng thực tế thường không phải vậy. Hình 2.26 minh họa một bố cục phức tạp hơn của các kiểu hỗn hợp, lần này thể hiện quan hệ giữa các kiểu hình học khác nhau. Ta thấy rằng, nhìn chung, một kiểu có thể có nhiều hơn một kiểu con. Ví dụ, tam giác và tứ giác đều là kiểu con của đa giác. Ngoài ra, một kiểu có thể có nhiều hơn một kiểu cha. Chẳng hạn, tam giác vuông cân có thể được xem là tam giác cân hoặc tam giác vuông. Vấn đề đa kiểu cha này đặc biệt rắc rối, vì nó có nghĩa là không có cách duy nhất để "nâng" một kiểu trong hệ thống phân cấp. Việc tìm kiểu cha "đúng" để áp dụng một phép tính lên một đối tượng có thể đòi hỏi một **quy trình** như `apply-generic` phải tìm kiếm đáng kể trên toàn bộ mạng lưới kiểu. Vì nhìn chung một kiểu có nhiều kiểu con, vấn đề tương tự cũng xảy ra khi ép giá trị "xuống" theo hệ thống phân cấp. Xử lý số lượng lớn các kiểu có quan hệ với nhau mà vẫn giữ được tính mô đun trong thiết kế các hệ thống lớn là một bài toán rất khó, và là lĩnh vực đang được nghiên cứu sôi nổi.

![Hình 2.26: Quan hệ giữa các kiểu hình học](./figures/figure86.png)

---

[^1]: Ta giả sử đã có sẵn các **quy trình** `put-coercion` và `get-coercion` để thao tác với bảng này.

[^2]: Chiến lược phác thảo ở đây chỉ hoạt động với trường hợp hai đối số. Với nhiều đối số hơn, ta cần một cách tiếp cận phức tạp hơn.

[^3]: Dù ta vẫn cần viết các **quy trình** ép kiểu để liên hệ các kiểu (có thể lên tới $n^2$ **quy trình** cho hệ thống có $n$ kiểu), ta chỉ cần viết một **quy trình** cho mỗi cặp kiểu thay vì một **quy trình** khác nhau cho mỗi tổ hợp kiểu và mỗi phép tính tổng quát.

---

[`procedure`](GLOSSARY.md#procedure) [`module`](GLOSSARY.md#module) [`data`](GLOSSARY.md#data) [`abstraction-barrier`](GLOSSARY.md#abstraction-barrier) [`coercion`](GLOSSARY.md#coercion) [`hierarchy of types`](GLOSSARY.md#hierarchy-of-types) [`subtype`](GLOSSARY.md#subtype) [`supertype`](GLOSSARY.md#supertype) [`tower`](GLOSSARY.md#tower) [`data-directed`](GLOSSARY.md#data-directed)
