# 2.3.2 Ví dụ: Đạo hàm ký hiệu

Để minh họa cho việc thao tác với **ký hiệu** và kỹ thuật _trừu tượng hóa dữ liệu_, ta hãy xem xét bài toán thiết kế một **quy trình** tính đạo hàm ký hiệu của các biểu thức đại số. Quy trình này nhận vào một biểu thức đại số và một biến, rồi trả về đạo hàm của biểu thức đó theo biến đã cho.

Trong quá trình xây dựng chương trình đạo hàm ký hiệu, ta sẽ đi theo đúng chiến lược _trừu tượng hóa dữ liệu_ mà ta đã dùng khi xây dựng hệ thống số hữu tỉ. Tức là, trước tiên ta sẽ định nghĩa thuật toán đạo hàm làm việc trên các đối tượng trừu tượng như "tổng," "tích," "hằng số," và "biến" — mà chưa cần quan tâm đến cách chúng được biểu diễn thực sự. Chỉ sau đó ta mới giải quyết bài toán biểu diễn.

Để đơn giản, chương trình của ta sẽ chỉ xử lý các biểu thức xây dựng từ phép cộng và phép nhân của hai đối số. Đạo hàm của bất kỳ biểu thức nào như vậy đều có thể tính được bằng các quy tắc sau:

$$\frac{dc}{dx} = 0 \quad \text{(với } c \text{ là hằng số hoặc biến khác } x\text{)}$$

$$\frac{dx}{dx} = 1$$

$$\frac{d(u + v)}{dx} = \frac{du}{dx} + \frac{dv}{dx}$$

$$\frac{d(uv)}{dx} = u\frac{dv}{dx} + v\frac{du}{dx}$$

## Chương trình đạo hàm với dữ liệu trừu tượng

Để hiện thực hóa các quy tắc này trong một **quy trình**, ta lại một lần nữa vận dụng _tư duy ước muốn_. Nếu ta đã có các mệnh đề điều kiện, bộ chọn và bộ tạo sau đây:

```
(variable? e)          e có phải là một biến không?
(same-variable? v1 v2) v1 và v2 có phải là cùng một biến không?
(sum? e)               e có phải là một tổng không?
(addend e)             Số hạng thứ nhất của tổng e.
(augend e)             Số hạng thứ hai của tổng e.
(make-sum a1 a2)       Tạo tổng của a1 và a2.
(product? e)           e có phải là một tích không?
(multiplier e)         Thừa số thứ nhất của tích e.
(multiplicand e)       Thừa số thứ hai của tích e.
(make-product m1 m2)   Tạo tích của m1 và m2.
```

thì ta có thể biểu diễn các quy tắc đạo hàm thành quy trình sau:

```lisp
(define (deriv exp var)
  (cond ((number? exp) 0)
        ((variable? exp)
         (if (same-variable? exp var) 1 0))
        ((sum? exp)
         (make-sum (deriv (addend exp) var)
                   (deriv (augend exp) var)))
        ((product? exp)
         (make-sum
           (make-product (multiplier exp)
                         (deriv (multiplicand exp) var))
           (make-product (deriv (multiplier exp) var)
                         (multiplicand exp))))
        (else (error "unknown expression type: DERIV" exp))))
```

Quy trình `deriv` này đã hiện thực hóa đầy đủ thuật toán đạo hàm. Vì nó được diễn đạt qua **dữ liệu** trừu tượng, nó sẽ hoạt động đúng bất kể ta chọn cách biểu diễn biểu thức đại số như thế nào, miễn là ta cung cấp đủ các quy trình trên.

## Biểu diễn biểu thức đại số

Ta có thể dùng ký hiệu tiền tố của Lisp để biểu diễn các biểu thức đại số. Ví dụ, biểu thức $ax + b$ sẽ được viết thành `(+ (* a x) b)`. Cách biểu diễn này tự nhiên phù hợp với cú pháp Lisp, và ta có thể triển khai trực tiếp các thao tác trên **dữ liệu**.

Biến là các **ký hiệu**, nhận biết bằng quy trình nguồn `symbol?`:

```lisp
(define (variable? x) (symbol? x))

(define (same-variable? v1 v2)
  (and (variable? v1)
       (variable? v2)
       (eq? v1 v2)))
```

Tổng và tích là các danh sách có ký hiệu phép toán ở đầu:

```lisp
(define (make-sum a1 a2) (list '+ a1 a2))

(define (make-product m1 m2) (list '* m1 m2))

(define (sum? x)
  (and (pair? x) (eq? (car x) '+)))

(define (addend s) (cadr s))

(define (augend s) (caddr s))

(define (product? x)
  (and (pair? x) (eq? (car x) '*)))

(define (multiplier p) (cadr p))

(define (multiplicand p) (caddr p))
```

Với cách biểu diễn này, `deriv` cho kết quả như sau:

```lisp
(deriv '(+ x 3) 'x)
(+ 1 0)

(deriv '(* x y) 'x)
(+ (* x 0) (* 1 y))

(deriv '(* (* x y) (+ x 3)) 'x)
(+ (* (* x y) (+ 1 0))
   (* (+ (* x 0) (* 1 y))
      (+ x 3)))
```

Kết quả về mặt toán học là đúng, nhưng chưa được rút gọn. Chẳng hạn, ta biết rằng $x \cdot 0 = 0$, $x \cdot 1 = x$, hay $0 + x = x$, nhưng chương trình vẫn giữ nguyên các dạng này mà không tự giản ước.

Ta có thể khắc phục vấn đề này bằng cách sửa các bộ tạo `make-sum` và `make-product` để chúng tự động rút gọn khi có thể. Điều này giống hệt cách ta sửa `make-rat` để tự rút gọn phân số. Ví dụ, nếu cả hai đối số của `make-sum` đều là số thì cộng luôn; nếu một trong hai bằng `0` thì trả về cái kia:

```lisp
(define (=number? exp num)
  (and (number? exp) (= exp num)))

(define (make-sum a1 a2)
  (cond ((=number? a1 0) a2)
        ((=number? a2 0) a1)
        ((and (number? a1) (number? a2))
         (+ a1 a2))
        (else (list '+ a1 a2))))

(define (make-product m1 m2)
  (cond ((or (=number? m1 0) (=number? m2 0)) 0)
        ((=number? m1 1) m2)
        ((=number? m2 1) m1)
        ((and (number? m1) (number? m2))
         (* m1 m2))
        (else (list '* m1 m2))))
```

Với phiên bản cải tiến này, các kết quả trở nên gọn gàng hơn nhiều:

```lisp
(deriv '(+ x 3) 'x)
1

(deriv '(* x y) 'x)
y

(deriv '(* (* x y) (+ x 3)) 'x)
(+ (* x y) (* y (+ x 3)))
```

Đây chưa phải dạng rút gọn đẹp nhất có thể — ví dụ, ta kỳ vọng kết quả cuối là $xy + y(x+3)$ và một người sẽ rút gọn thêm thành $y(2x+3)$ — nhưng việc giải quyết trường hợp tổng quát là cực kỳ khó. Bài toán rút gọn biểu thức đại số tổng quát thực ra là một vấn đề phức tạp. Một mặt, nó đòi hỏi ta nhận ra khi nào hai biểu thức tương đương; mặt khác, câu hỏi "dạng nào là dạng đơn giản nhất" cũng không có câu trả lời dứt khoát. Dù sao đi nữa, bài toán này minh họa rõ ràng sức mạnh của trừu tượng hóa dữ liệu: ta có thể thay đổi hoàn toàn cách biểu diễn hoặc rút gọn mà không cần động đến thuật toán đạo hàm cốt lõi trong `deriv`.

---

[exercise 2.56](exercises/2.56.md) [exercise 2.57](exercises/2.57.md) [exercise 2.58](exercises/2.58.md) [`symbol`](GLOSSARY.md#symbol) [`data abstraction`](GLOSSARY.md#data-abstraction) [`wishful thinking`](GLOSSARY.md#wishful-thinking) [`procedure`](GLOSSARY.md#procedure) [`predicate`](GLOSSARY.md#predicate) [`constructor`](GLOSSARY.md#constructor) [`selector`](GLOSSARY.md#selector) [`variable`](GLOSSARY.md#variable) [`data`](GLOSSARY.md#data) [`pair`](GLOSSARY.md#pair) [`expression`](GLOSSARY.md#expression)
