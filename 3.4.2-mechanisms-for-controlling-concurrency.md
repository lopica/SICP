# 3.4.2 Cơ chế kiểm soát tính đồng thời

Khó khăn cốt lõi khi xử lý các **tiến trình** đồng thời là phải tính đến sự xen kẽ của các sự kiện trong các **tiến trình** khác nhau. Ví dụ, nếu có hai **tiến trình**, mỗi tiến trình có ba sự kiện được sắp xếp theo thứ tự, thì số cách xen kẽ có thể xảy ra là $\binom{6}{3} = 20$. Số lượng này tăng vọt khi số **tiến trình** và số sự kiện tăng lên, khiến việc phân tích thủ công trở nên hoàn toàn bất khả thi trong hệ thống thực tế.

Thay vì phân tích từng tình huống xen kẽ có thể xảy ra, ta cần các cơ chế ràng buộc việc xen kẽ để đảm bảo hành vi chính xác của chương trình.

## Tuần tự hóa truy cập vào trạng thái chia sẻ

Một cơ chế phổ biến là _tuần tự hóa_ (_serialization_). Ý tưởng là tạo ra các nhóm **quy trình** đặc biệt sao cho trong mỗi nhóm, tại bất kỳ thời điểm nào cũng chỉ có một **quy trình** được thực thi. Nếu một **quy trình** trong nhóm đang chạy, thì bất kỳ **tiến trình** nào muốn chạy **quy trình** khác trong cùng nhóm đó sẽ phải đợi cho đến khi **quy trình** đầu tiên kết thúc.[^1]

Ta có thể dùng tuần tự hóa để kiểm soát truy cập vào các biến chia sẻ: nhóm tất cả các **quy trình** truy cập và cập nhật một **biến** vào cùng một tập tuần tự hóa. Như vậy, tại bất kỳ thời điểm nào cũng chỉ có một **quy trình** đang thao tác với **biến** đó.

## Tuần tự hóa trong Scheme

Để minh họa, ta dùng `parallel-execute` — một phần mở rộng của Scheme nhận vào nhiều thủ tục không có đối số và tạo ra các **tiến trình** riêng biệt chạy đồng thời:

```lisp
(define x 10)
(parallel-execute (lambda () (set! x (* x x)))
                  (lambda () (set! x (+ x 1))))
```

Không có tuần tự hóa, giá trị cuối của `x` có thể là một trong năm khả năng: 101, 121, 110, 11, hoặc 100 — tùy thuộc vào các bước xen kẽ cụ thể của hai **tiến trình**.

Để ràng buộc tính đồng thời, ta dùng `make-serializer`:

```lisp
(define x 10)
(define s (make-serializer))
(parallel-execute (s (lambda () (set! x (* x x))))
                  (s (lambda () (set! x (+ x 1)))))
```

`make-serializer` trả về một **bộ tuần tự hóa** (_serializer_). Khi áp dụng lên một **quy trình**, nó trả về một phiên bản đã được tuần tự hóa. Tất cả các **quy trình** được tạo ra từ cùng một _bộ tuần tự hóa_ thuộc cùng một tập tuần tự hóa — chúng không thể chạy đồng thời với nhau. Với đoạn code trên, chỉ còn hai giá trị có thể: 101 (nếu `(+ x 1)` chạy trước) hoặc 121 (nếu `(* x x)` chạy trước).

## Tuần tự hóa tài khoản ngân hàng

Ta có thể dùng _bộ tuần tự hóa_ để bảo vệ tài khoản ngân hàng:

```lisp
(define (make-account balance)
  (define (withdraw amount)
    (if (>= balance amount)
        (begin (set! balance (- balance amount))
               balance)
        "Insufficient funds"))
  (define (deposit amount)
    (set! balance (+ balance amount))
    balance)
  (let ((protected (make-serializer)))
    (define (dispatch m)
      (cond ((eq? m 'withdraw) (protected withdraw))
            ((eq? m 'deposit)  (protected deposit))
            ((eq? m 'balance)  balance)
            (else (error "Unknown request: MAKE-ACCOUNT" m))))
    dispatch))
```

Với cài đặt này, `withdraw` và `deposit` đã được tuần tự hóa: hai **tiến trình** không thể đồng thời rút hoặc nạp tiền vào cùng một tài khoản. Vì mỗi tài khoản có _bộ tuần tự hóa_ riêng, các giao dịch trên các tài khoản khác nhau vẫn có thể chạy đồng thời.

## Sự phức tạp khi có nhiều tài nguyên chia sẻ

Dùng _bộ tuần tự hóa_ trở nên phức tạp hơn khi có nhiều đối tượng chia sẻ cùng nhau. Xét **quy trình** hoán đổi số dư của hai tài khoản:

```lisp
(define (exchange account1 account2)
  (let ((difference (- (account1 'balance)
                       (account2 'balance))))
    ((account1 'withdraw) difference)
    ((account2 'deposit) difference)))
```

Nếu Peter trao đổi tài khoản $a_1$ và $a_2$ trong khi Paul đồng thời trao đổi $a_1$ và $a_3$, thì việc mỗi tài khoản có _bộ tuần tự hóa_ riêng không đủ để bảo vệ. Peter có thể đọc số dư của $a_1$ và $a_2$ để tính chênh lệch, nhưng trước khi anh ta kịp thực hiện giao dịch, Paul thay đổi số dư $a_1$ — kết quả của Peter sẽ sai.

Để đúng, ta cần ràng buộc rằng **quy trình** `exchange` chiếm quyền truy cập độc quyền vào _cả hai_ tài khoản trong suốt quá trình hoán đổi. Cách làm là xuất _bộ tuần tự hóa_ ra ngoài để người gọi có thể quản lý việc kết hợp:

```lisp
(define (make-account-and-serializer balance)
  (define (withdraw amount)
    (if (>= balance amount)
        (begin (set! balance (- balance amount))
               balance)
        "Insufficient funds"))
  (define (deposit amount)
    (set! balance (+ balance amount))
    balance)
  (let ((balance-serializer (make-serializer)))
    (define (dispatch m)
      (cond ((eq? m 'withdraw)   withdraw)
            ((eq? m 'deposit)    deposit)
            ((eq? m 'balance)    balance)
            ((eq? m 'serializer) balance-serializer)
            (else (error "Unknown request: MAKE-ACCOUNT" m))))
    dispatch))
```

Lưu ý: ở đây `withdraw` và `deposit` _không_ được tuần tự hóa sẵn — người gọi phải tự làm điều đó. Ví dụ, để nạp tiền:

```lisp
(define (deposit account amount)
  (let ((s (account 'serializer))
        (d (account 'deposit)))
    ((s d) amount)))
```

Và để hoán đổi an toàn, ta bọc cả **quy trình** `exchange` trong cả hai _bộ tuần tự hóa_:

```lisp
(define (serialized-exchange account1 account2)
  (let ((serializer1 (account1 'serializer))
        (serializer2 (account2 'serializer)))
    ((serializer1 (serializer2 exchange))
     account1
     account2)))
```

Như vậy, `exchange` chỉ chạy khi đã giữ được quyền truy cập độc quyền vào cả `account1` lẫn `account2` thông qua hai _bộ tuần tự hóa_ lồng nhau.

## Cài đặt bộ tuần tự hóa

Để cài đặt _bộ tuần tự hóa_, ta dùng một cơ chế cấp thấp hơn gọi là _mutex_ (mutual exclusion — loại trừ lẫn nhau). Một _mutex_ hỗ trợ hai thao tác: `acquire` (lấy) và `release` (giải phóng). Khi một _mutex_ đã được lấy, không có thao tác `acquire` nào khác trên cùng _mutex_ đó được tiến hành — đến khi nó được giải phóng.[^2]

```lisp
(define (make-serializer)
  (let ((mutex (make-mutex)))
    (lambda (p)
      (define (serialized-p . args)
        (mutex 'acquire)
        (let ((val (apply p args)))
          (mutex 'release)
          val))
      serialized-p)))
```

Mỗi _bộ tuần tự hóa_ dùng một _mutex_ duy nhất. Khi một **quy trình** đã được tuần tự hóa được gọi, nó đầu tiên lấy _mutex_, rồi chạy **quy trình** gốc, rồi giải phóng _mutex_.

### Cài đặt mutex

Một _mutex_ được biểu diễn bằng một ô (cell) — danh sách một phần tử — chứa `true` hoặc `false`. `false` có nghĩa là _mutex_ đang tự do; `true` là đang bị chiếm.

```lisp
(define (make-mutex)
  (let ((cell (list false)))
    (define (the-mutex m)
      (cond ((eq? m 'acquire)
             (if (test-and-set! cell)
                 (the-mutex 'acquire))) ; thử lại
            ((eq? m 'release) (clear! cell))))
    the-mutex))

(define (clear! cell)
  (set-car! cell false))
```

Thao tác then chốt là `test-and-set!`: nó kiểm tra ô và trả về giá trị của ô. Nếu ô là `false`, nó đặt ô thành `true` rồi trả về `false` (báo hiệu "đã lấy thành công"). Nếu ô là `true`, nó trả về `true` (báo hiệu "đang bị chiếm, thử lại"):

```lisp
(define (test-and-set! cell)
  (if (car cell)
      true
      (begin (set-car! cell true)
             false)))
```

Điều cực kỳ quan trọng: thao tác `test-and-set!` phải được thực thi _nguyên tử_ (atomically) — tức là không thể bị gián đoạn giữa chừng. Nếu hai **tiến trình** cùng kiểm tra một ô `false` nhưng trước khi **tiến trình** nào kịp đặt nó thành `true`, cả hai đều nghĩ mình đã lấy được _mutex_ — kết quả là thảm họa. Cài đặt `test-and-set!` nguyên tử phụ thuộc vào hệ thống: trong môi trường đơn luồng giả lập, ta có thể tắt **lập lịch** (_scheduling_) tạm thời; trong phần cứng thực, có lệnh máy đặc biệt hỗ trợ.[^3]

## Vấn đề tắc nghẽn

Dù _bộ tuần tự hóa_ giải quyết được bài toán truy cập đồng thời vào một tài nguyên, nó lại tạo ra một nguy cơ mới khi có nhiều tài nguyên: _tắc nghẽn_ (**deadlock**).

Hãy xét `serialized-exchange`: Peter muốn hoán đổi $a_1$ và $a_2$, đồng thời Paul muốn hoán đổi $a_2$ và $a_1$. Peter lấy được _bộ tuần tự hóa_ của $a_1$ rồi dừng lại (chờ) trước khi lấy của $a_2$. Trong khi đó Paul lấy được _bộ tuần tự hóa_ của $a_2$ rồi cố lấy của $a_1$. Kết quả: Peter giữ $a_1$ và chờ $a_2$; Paul giữ $a_2$ và chờ $a_1$ — hai bên chờ nhau mãi mãi. Đây là **deadlock**.

**Deadlock** là nguy cơ thường trực trong mọi hệ thống cung cấp truy cập đồng thời vào nhiều tài nguyên chia sẻ.

### Tránh tắc nghẽn

Một cách đơn giản để tránh **deadlock** trong bài toán trao đổi tài khoản là đánh số duy nhất cho mỗi tài khoản và quy định rằng các **tiến trình** luôn luôn lấy _bộ tuần tự hóa_ của tài khoản có số nhỏ hơn trước. Với quy tắc này, cả Peter và Paul sẽ cố lấy _bộ tuần tự hóa_ của $a_1$ trước — **tiến trình** nào thắng sẽ tiến hành lấy tiếp của $a_2$, còn **tiến trình** kia phải chờ. Không có vòng chờ đợi vòng tròn, nên **deadlock** không xảy ra.

Tuy nhiên, chiến lược này không áp dụng được cho mọi tình huống. Trong những bài toán mà một **tiến trình** phải truy cập tài nguyên trước khi biết mình cần thêm tài nguyên nào, không thể dùng quy tắc thứ tự cố định. Những tình huống như vậy đòi hỏi các kỹ thuật phức tạp hơn — hoặc đơn giản là chấp nhận rằng **deadlock** không thể hoàn toàn tránh được.[^4]

## Tính đồng thời, thời gian và giao tiếp

Cơ chế `test-and-set!` giả định rằng các **tiến trình** có thể kiểm tra một cờ hiệu toàn cục bất cứ lúc nào. Điều này vừa khó cài đặt vừa kém hiệu quả trên bộ vi xử lý hiện đại, nơi mà các kỹ thuật tối ưu hóa như pipeline và bộ nhớ đệm (cache) khiến bộ nhớ có thể ở trạng thái không nhất quán ở bất kỳ thời điểm nào. Các hệ thống đa xử lý hiện đại thường dùng những cơ chế khác thay thế.

Trong hệ thống phân tán quy mô lớn — ví dụ như ngân hàng với nhiều chi nhánh trên toàn quốc — mỗi chi nhánh duy trì bản sao cục bộ của số dư tài khoản và định kỳ đồng bộ hóa với nhau. Trong khoảng thời gian giữa các lần đồng bộ, số dư "thực sự" của tài khoản là không xác định. Điều này đặt ra câu hỏi triết học: liệu có ý nghĩa gì khi nói rằng số dư tài khoản là một giá trị nhất định vào một thời điểm cụ thể?

Câu hỏi đó dẫn đến một quan sát thú vị: **đồng bộ hóa** các **tiến trình** đồng thời, thiết lập trạng thái chia sẻ, hay áp đặt thứ tự lên các sự kiện — tất cả đều đòi hỏi _giao tiếp_ giữa các **tiến trình**. Và mọi khái niệm về thời gian trong kiểm soát tính đồng thời đều gắn chặt với giao tiếp đó.[^5]

---

[^1]: Cách tiếp cận tuần tự hóa không phải là cách duy nhất để đảm bảo tính đúng đắn. Có những cơ chế khác như _lập trình không khóa_ (lock-free programming) hay _bộ nhớ giao dịch phần mềm_ (software transactional memory), nhưng chúng đều phức tạp hơn.

[^2]: Trong tài liệu khoa học máy tính, đối tượng này đôi khi còn được gọi là _khóa_ (lock) hay _semaphore nhị phân_.

[^3]: Trên phần cứng x86 hiện đại, lệnh `CMPXCHG` (compare-and-exchange) cung cấp thao tác kiểm tra và đặt nguyên tử. Hầu hết kiến trúc vi xử lý khác đều có các lệnh tương đương.

[^4]: Các thuật toán tránh **deadlock** tổng quát hơn, như Thuật toán Banker của Dijkstra, yêu cầu mỗi **tiến trình** khai báo trước số lượng tài nguyên tối đa mà nó có thể cần, điều mà không phải lúc nào cũng biết trước được.

[^5]: Sự tương đồng sâu sắc giữa thời gian tính toán và thời gian vật lý đã được Lamport chỉ ra: ông cho thấy rằng khái niệm "đồng thời" trong hệ thống phân tán thực sự tương tự với khái niệm "đồng thời" trong thuyết tương đối hẹp. Trong cả hai trường hợp, thứ tự của các sự kiện chỉ có thể xác định được khi có tín hiệu truyền giữa chúng — và tốc độ lan truyền tín hiệu đặt ra giới hạn cơ bản cho sự đồng bộ hóa.

---

[`process`](GLOSSARY.md#process) [`variable`](GLOSSARY.md#variable) [`assignment`](GLOSSARY.md#assignment) [`procedure`](GLOSSARY.md#procedure) [`state variables`](GLOSSARY.md#state-variables)
