# 2.1.4 Bài tập mở rộng: Số học khoảng

Alyssa P. Hacker đang xây dựng một hệ thống để giúp các kỹ sư giải quyết những bài toán liên quan đến các đại lượng không chính xác nhưng đã biết độ chính xác. Một tính năng cô muốn cung cấp là khả năng thao tác với các đại lượng xấp xỉ (chẳng hạn như các thông số đo được của thiết bị vật lý) với độ chính xác đã biết trước, để khi tính toán trên những đại lượng gần đúng như vậy, kết quả thu được vẫn là các con số có độ chính xác xác định.

Các kỹ sư điện sẽ dùng hệ thống của Alyssa để tính các đại lượng điện. Đôi khi họ cần tính điện trở tương đương song song $R_p$ của hai điện trở $R_1$ và $R_2$ theo công thức:

$$R_p = \frac{1}{1/R_1 + 1/R_2}$$

Điện trở của thiết bị thường được nhà sản xuất ghi kèm dung sai. Ví dụ, ta có thể mua một điện trở "6,8 ôm với dung sai 10%", tức là giá trị thực của điện trở này nằm trong khoảng $6{,}8 \pm 0{,}68$ ôm, hay từ $6{,}12$ đến $7{,}48$ ôm. Như vậy, nếu ghép song song một điện trở 6,8 ôm dung sai 10% với một điện trở 4,7 ôm dung sai 5%, điện trở tương đương sẽ có giá trị nằm trong khoảng từ khoảng 2,58 ôm đến 2,97 ôm.

Ý tưởng của Alyssa là hiện thực _số học khoảng_ như một tập hợp các **phép tính** số học dành cho việc kết hợp các "khoảng" — tức là các đối tượng đại diện cho miền giá trị có thể có của một đại lượng không chính xác. Kết quả của việc cộng, trừ, nhân, hay chia hai khoảng sẽ là một khoảng mới, biểu diễn phạm vi của kết quả.

Alyssa giả thiết rằng tồn tại một _hàm tạo_ (constructor) `make-interval` nhận vào hai điểm đầu mút và tạo ra một khoảng. Cô cũng giả thiết có các _hàm chọn_ (selectors) `lower-bound` và `upper-bound` để truy xuất hai đầu mút của khoảng. Dựa vào đó, cô định nghĩa phép cộng khoảng như sau: tổng nhỏ nhất có thể là tổng của hai cận dưới, còn tổng lớn nhất là tổng của hai cận trên.

```lisp
(define (add-interval x y)
  (make-interval (+ (lower-bound x) (lower-bound y))
                 (+ (upper-bound x) (upper-bound y))))
```

Tích của hai khoảng có thể tính bằng cách lấy nhỏ nhất và lớn nhất trong bốn tích của các cặp đầu mút:

```lisp
(define (mul-interval x y)
  (let ((p1 (* (lower-bound x) (lower-bound y)))
        (p2 (* (lower-bound x) (upper-bound y)))
        (p3 (* (upper-bound x) (lower-bound y)))
        (p4 (* (upper-bound x) (upper-bound y))))
    (make-interval (min p1 p2 p3 p4)
                   (max p1 p2 p3 p4))))
```

Để chia hai khoảng, Alyssa nhân khoảng thứ nhất với nghịch đảo của khoảng thứ hai. Nghịch đảo của khoảng $[a, b]$ là khoảng $[1/b, 1/a]$:

```lisp
(define (div-interval x y)
  (mul-interval x
                (make-interval (/ 1.0 (upper-bound y))
                               (/ 1.0 (lower-bound y)))))
```

---

[exercise 2.7](exercises/2.7.md) [exercise 2.8](exercises/2.8.md) [exercise 2.9](exercises/2.9.md) [exercise 2.10](exercises/2.10.md) [exercise 2.11](exercises/2.11.md) [exercise 2.12](exercises/2.12.md) [exercise 2.13](exercises/2.13.md) [exercise 2.14](exercises/2.14.md) [exercise 2.15](exercises/2.15.md) [exercise 2.16](exercises/2.16.md) [`data`](GLOSSARY.md#data) [`procedure`](GLOSSARY.md#procedure) [`compound-data`](GLOSSARY.md#compound-data)
