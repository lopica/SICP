# 2.1.3 Dữ liệu nghĩa là gì?

Chúng ta đã bắt đầu triển khai số hữu tỉ ở mục 2.1.1 bằng cách biểu diễn các phép tính `add-rat`, `sub-rat`, v.v. qua ba quy trình chưa được xác định cụ thể: `make-rat`, `numer`, và `denom`. Lúc đó, ta có thể nghĩ các phép tính như được định nghĩa trên các đối tượng dữ liệu — tử số, mẫu số, và số hữu tỉ — mà hành vi của chúng được xác định bởi ba quy trình kể trên.

Nhưng thực ra "**dữ liệu**" là gì? Nói rằng "dữ liệu là bất cứ thứ gì được triển khai bởi các bộ chọn và hàm tạo đã cho" là chưa đủ. Rõ ràng là không phải bộ ba quy trình tùy tiện nào cũng có thể làm nền tảng hợp lệ cho việc triển khai số hữu tỉ. Ta cần đảm bảo rằng nếu ta xây dựng số hữu tỉ `x` từ một cặp số nguyên `n` và `d`, thì việc lấy `numer` và `denom` của `x` rồi chia chúng cho nhau phải cho kết quả giống như chia `n` cho `d`. Nói cách khác, `make-rat`, `numer`, và `denom` phải thỏa mãn điều kiện: với bất kỳ số nguyên `n` và số nguyên khác không `d` nào, nếu `x` là `(make-rat n d)`, thì

$$\frac{\text{(numer } x\text{)}}{\text{(denom } x\text{)}} = \frac{n}{d}$$

Thực ra, đây là điều kiện duy nhất mà `make-rat`, `numer`, và `denom` phải thỏa mãn để tạo nên một nền tảng hợp lệ cho việc biểu diễn số hữu tỉ. Nhìn chung, ta có thể hiểu **dữ liệu** là thứ được xác định bởi một tập hợp các bộ chọn và hàm tạo, cùng với các điều kiện được chỉ định mà các quy trình này phải thỏa mãn để tạo thành một cách biểu diễn hợp lệ.[^1]

Quan điểm này không chỉ áp dụng để định nghĩa các đối tượng dữ liệu ở "tầng cao" như số hữu tỉ, mà còn áp dụng cho các đối tượng ở tầng thấp hơn. Hãy xét khái niệm cặp giá trị, thứ ta đã dùng để định nghĩa số hữu tỉ. Ta chưa bao giờ nói thực ra cặp giá trị là gì, chỉ nói rằng ngôn ngữ cung cấp các quy trình `cons`, `car`, và `cdr` để thao tác trên cặp giá trị. Nhưng điều duy nhất ta cần biết về ba phép tính này là: nếu ta gắn hai đối tượng lại với nhau bằng `cons`, thì ta có thể lấy lại chúng bằng `car` và `cdr`. Tức là, các phép tính phải thỏa mãn điều kiện: với bất kỳ đối tượng `x` và `y` nào, nếu `z` là `(cons x y)` thì `(car z)` là `x` và `(cdr z)` là `y`. Thật vậy, ta đã đề cập rằng ba quy trình này được ngôn ngữ cung cấp như quy trình nguồn. Tuy nhiên, bất kỳ bộ ba quy trình nào thỏa mãn điều kiện trên đều có thể được dùng làm nền tảng để triển khai cặp giá trị. Điều này được minh họa một cách ấn tượng bởi thực tế là ta có thể triển khai `cons`, `car`, và `cdr` mà không dùng bất kỳ cấu trúc dữ liệu nào cả — chỉ dùng quy trình. Dưới đây là các định nghĩa:

```lisp
(define (cons x y)
  (define (dispatch m)
    (cond ((= m 0) x)
          ((= m 1) y)
          (else 
           (error "Argument not 0 or 1:
                   CONS" m))))
  dispatch)

(define (car z) (z 0))
(define (cdr z) (z 1))
```

Cách dùng quy trình này không hề giống với trực giác thông thường của ta về dữ liệu. Tuy nhiên, để chứng minh đây là cách biểu diễn cặp giá trị hợp lệ, ta chỉ cần kiểm tra rằng các quy trình này thỏa mãn điều kiện đã nêu ở trên.

Điều tinh tế cần chú ý là giá trị trả về bởi `(cons x y)` là một quy trình — cụ thể là quy trình `dispatch` được định nghĩa nội bộ, nhận một đối số và trả về `x` hay `y` tùy thuộc đối số là `0` hay `1`. Tương ứng, `(car z)` được định nghĩa để áp dụng `z` lên `0`. Do đó, nếu `z` là quy trình được tạo ra bởi `(cons x y)`, thì `z` áp dụng lên `0` sẽ trả về `x`. Như vậy, ta đã chứng minh rằng `(car (cons x y))` cho ra `x` như mong muốn. Tương tự, `(cdr (cons x y))` áp dụng quy trình được trả về bởi `(cons x y)` lên `1`, và trả về `y`. Vì vậy, cách triển khai cặp giá trị bằng quy trình này là hoàn toàn hợp lệ, và nếu ta chỉ truy cập cặp giá trị thông qua `cons`, `car`, và `cdr` thì ta không thể phân biệt được cách triển khai này với cách dùng "cấu trúc dữ liệu thực sự".

Mục đích của việc trình bày cách biểu diễn cặp giá trị bằng quy trình không phải là để nói rằng ngôn ngữ của ta hoạt động theo cách này (Scheme và các hệ thống Lisp nói chung đều triển khai cặp giá trị trực tiếp vì lý do hiệu suất), mà là để cho thấy rằng nó _có thể_ hoạt động theo cách này. Cách biểu diễn bằng quy trình, dù khó hiểu, là một cách biểu diễn cặp giá trị hoàn toàn đủ dùng, vì nó thỏa mãn điều kiện duy nhất mà cặp giá trị cần thỏa mãn. Ví dụ này cũng cho thấy rằng khả năng thao tác quy trình như các đối tượng tự động cung cấp khả năng biểu diễn _dữ liệu tổng hợp_. Điều này hiện tại có vẻ chỉ là một điều kỳ thú, nhưng các cách biểu diễn dữ liệu bằng quy trình sẽ đóng vai trò trung tâm trong kho công cụ lập trình của ta. Phong cách lập trình này thường được gọi là _truyền thông điệp_ (_message passing_), và ta sẽ dùng nó như một công cụ cơ bản trong Chương 3 khi ta đề cập đến các vấn đề mô hình hóa và mô phỏng.

**Bài tập 2.4.** Đây là một cách biểu diễn cặp giá trị bằng quy trình thay thế. Với cách biểu diễn này, hãy xác nhận rằng `(car (cons x y))` cho ra `x` với bất kỳ đối tượng `x` và `y` nào.

```lisp
(define (cons x y) 
  (lambda (m) (m x y)))

(define (car z) 
  (z (lambda (p q) p)))
```

Định nghĩa tương ứng của `cdr` là gì? (Gợi ý: Để xác nhận cách này hoạt động, hãy sử dụng mô hình thay thế ở mục 1.1.5.)

**Bài tập 2.5.** Hãy chứng minh rằng ta có thể biểu diễn các cặp số nguyên không âm chỉ bằng số và phép toán số học, nếu ta biểu diễn cặp $a$ và $b$ như số nguyên bằng tích $2^a 3^b$. Cho các định nghĩa tương ứng của các quy trình `cons`, `car`, và `cdr`.

**Bài tập 2.6.** Trong trường hợp biểu diễn cặp giá trị bằng quy trình vẫn chưa đủ gây choáng váng, hãy xem xét rằng, trong một ngôn ngữ có thể thao tác quy trình, ta có thể không cần dùng đến số (ít nhất là với các số nguyên không âm) bằng cách triển khai `0` và phép tính thêm `1` như sau:

```lisp
(define zero (lambda (f) (lambda (x) x)))

(define (add-1 n)
  (lambda (f) (lambda (x) (f ((n f) x)))))
```

Cách biểu diễn này được gọi là _chữ số Church_ (_Church numerals_), đặt theo tên người phát minh ra nó, Alonzo Church — nhà logic học đã phát minh ra λ-calculus.

Định nghĩa trực tiếp `one` và `two` (không dùng `zero` và `add-1`). (Gợi ý: Dùng phép thay thế để tính `(add-1 zero)`.) Cho một định nghĩa trực tiếp của quy trình cộng `+` (không dùng cách áp dụng `add-1` nhiều lần liên tiếp).

---

[^1]: Điều này nghe có vẻ đơn giản, nhưng thực ra rất khó để phát biểu chặt chẽ. Có hai hướng tiếp cận để hình thức hóa ý này. Một hướng, do C. A. R. Hoare (1972) tiên phong, được gọi là phương pháp mô hình trừu tượng. Nó hình thức hóa đặc tả "quy trình cộng điều kiện" theo cách được nêu trong ví dụ số hữu tỉ trên. Lưu ý rằng điều kiện trên cách biểu diễn số hữu tỉ được phát biểu dựa trên các tính chất của số nguyên (phép bằng nhau và phép chia). Nhìn chung, các mô hình trừu tượng định nghĩa các kiểu đối tượng dữ liệu mới dựa trên các kiểu đối tượng dữ liệu đã được định nghĩa trước đó. Vì vậy, các khẳng định về đối tượng dữ liệu có thể được kiểm tra bằng cách quy về các khẳng định về các đối tượng dữ liệu đã được định nghĩa trước. Một hướng khác, được giới thiệu bởi Zilles tại MIT, bởi Goguen, Thatcher, Wagner và Wright tại IBM (xem Thatcher et al. 1978), và bởi Guttag tại Toronto (xem Guttag 1977), được gọi là đặc tả đại số. Hướng này xem các "quy trình" như các phần tử của một hệ thống đại số trừu tượng mà hành vi của nó được xác định bởi các tiên đề tương ứng với "các điều kiện" của ta, và dùng kỹ thuật của đại số trừu tượng để kiểm tra các khẳng định về đối tượng dữ liệu. Cả hai phương pháp đều được khảo sát trong bài báo của Liskov và Zilles (1975).

---

[`data`](GLOSSARY.md#data) [`compound data`](GLOSSARY.md#compound-data) [`primitive procedure`](GLOSSARY.md#primitive-procedure) [`procedure`](GLOSSARY.md#procedure) [`compound procedure`](GLOSSARY.md#compound-procedure) [`message passing`](GLOSSARY.md#message-passing) [`Church numerals`](GLOSSARY.md#church-numerals)
