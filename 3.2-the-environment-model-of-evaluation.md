## 3.2 Mô hình môi trường của sự thực thi

Khi _quy trình tổng hợp_ lần đầu xuất hiện ở Chương 1, _mô hình sự thay thế_ định nghĩa việc áp dụng quy trình như sau: "Để áp dụng một quy trình tổng hợp lên các đối số, hãy thực thi thân quy trình với mỗi tham số hình thức được thay thế bằng đối số tương ứng."

Tuy nhiên, ngay khi phép gán được đưa vào ngôn ngữ lập trình, định nghĩa đó trở nên không còn đủ nữa. Như mục 3.1.3 đã lập luận, khi có phép gán, "một **biến** không còn có thể được xem đơn thuần là tên gọi của một giá trị. Thay vào đó, biến phải đóng vai trò chỉ định một 'chỗ' có thể lưu trữ giá trị." Trong mô hình thực thi mới, các chỗ đó được duy trì trong các cấu trúc gọi là **môi trường**.

Một **môi trường** là một chuỗi các _khung_ (frame). Mỗi _khung_ là một bảng (có thể rỗng) gồm các _ràng buộc_ (binding), liên kết tên **biến** với giá trị tương ứng. Một khung chứa tối đa một ràng buộc cho mỗi biến. Mỗi khung còn có một con trỏ trỏ đến _môi trường bao ngoài_ của nó, trừ khi đó là khung được coi là khung toàn cục. _Giá trị của một biến_ so với một môi trường là giá trị được ghi trong ràng buộc của biến đó tại khung đầu tiên trong chuỗi môi trường có chứa ràng buộc cho biến đó. Nếu không có khung nào trong chuỗi ràng buộc biến đó, thì biến được gọi là _không bị ràng buộc_ (unbound) trong môi trường đó.

Hình 3.1 minh họa một cấu trúc môi trường đơn giản gồm ba khung, ký hiệu là I, II và III. Trong sơ đồ, A, B, C và D là các con trỏ đến các môi trường. C và D trỏ đến cùng một môi trường. Các biến `z` và `x` bị ràng buộc trong khung II, còn `y` và `x` bị ràng buộc trong khung I. Giá trị của `x` trong môi trường D là `3`. Giá trị của `x` trong môi trường B cũng là `3` — ta tìm được bằng cách đi từ khung III (không có ràng buộc nào cho `x`) lên môi trường bao ngoài D rồi tìm thấy ràng buộc trong khung I. Ngược lại, giá trị của `x` trong môi trường A là `7`, vì khung đầu tiên trong chuỗi đó (khung II) đã ràng buộc `x` với `7`. Ta nói rằng ràng buộc `x → 7` trong khung II _che khuất_ (shadow) ràng buộc `x → 3` trong khung I.

![Hình 3.1: Một cấu trúc môi trường đơn giản.](./figures/figure23.png)

**Môi trường** đóng vai trò cốt lõi trong quá trình **thực thi**, vì nó xác định ngữ cảnh mà trong đó một **biểu thức** được đánh giá. Thực ra, ta có thể nói rằng "các biểu thức trong ngôn ngữ lập trình tự chúng không có ý nghĩa gì cả. Một biểu thức chỉ có ý nghĩa khi được xét trong một môi trường cụ thể." Ngay cả một biểu thức đơn giản như `(+ 1 1)` cũng đòi hỏi ta hiểu rằng `+` là ký hiệu của phép cộng. Vì vậy, trong mô hình thực thi mới này, ta luôn nói đến việc thực thi một biểu thức _trong ngữ cảnh của một môi trường_ nào đó. Để mô tả các tương tác với trình phiên dịch, ta giả định có một **môi trường toàn cục** duy nhất, bao gồm một khung đơn (không có môi trường bao ngoài) chứa các giá trị cho các ký hiệu gắn với các quy trình nguồn. Chẳng hạn, ý tưởng "`+` là ký hiệu của phép cộng" được thể hiện qua việc ký hiệu `+` được ràng buộc trong môi trường toàn cục với quy trình nguồn thực hiện phép cộng.

---

[`environment`](GLOSSARY.md#environment) [`global environment`](GLOSSARY.md#global-environment) [`variable`](GLOSSARY.md#variable) [`expression`](GLOSSARY.md#expression) [`evaluation`](GLOSSARY.md#evaluation) [`compound procedure`](GLOSSARY.md#compound-procedure) [`substitution model`](GLOSSARY.md#substitution-model) [`procedure`](GLOSSARY.md#procedure) [`primitive procedure`](GLOSSARY.md#primitive-procedure)
