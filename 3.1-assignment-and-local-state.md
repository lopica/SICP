## 3.1 Gán giá trị và trạng thái cục bộ

Chúng ta thường mô hình hoá thế giới như một tập hợp các đối tượng riêng biệt, mỗi đối tượng có trạng thái thay đổi theo thời gian. Một đối tượng được gọi là _có trạng thái_ nếu hành vi của nó bị ảnh hưởng bởi lịch sử của nó. Ví dụ, tài khoản ngân hàng có trạng thái: câu hỏi "tôi có thể rút 100 đồng không?" phụ thuộc vào lịch sử các lần nạp và rút tiền trước đó.

Ta có thể mô tả trạng thái của một đối tượng bằng một hay vài **biến** _trạng thái_, giữ đủ thông tin về lịch sử của đối tượng để xác định hành vi hiện tại của nó. Trong một mô hình đơn giản về tài khoản ngân hàng, trạng thái được xác định bởi **biến** `balance` (số dư tài khoản). Trong một mô hình phức tạp hơn, ta có thể cần thêm các **biến** như hạn mức tín dụng, lịch sử giao dịch, v.v.

Để mô hình hóa trạng thái thay đổi bằng cách dùng các **biến** trong ngôn ngữ lập trình, ta cần một phép tính mới: **phép gán**. Trong Lisp, phép gán được thực hiện qua dạng đặc biệt `set!`. Một khi ta cho phép **phép gán**, **môi trường** không còn có thể bỏ qua, và mô hình tính toán trở nên phức tạp hơn đáng kể — nhưng đổi lại, ta có được khả năng mô phỏng những hệ thống thực sự phong phú hơn.

---

[`variable`](GLOSSARY.md#variable) [`state variables`](GLOSSARY.md#state-variables) [`local`](GLOSSARY.md#local) [`environment`](GLOSSARY.md#environment) [`special form`](GLOSSARY.md#special-form)
